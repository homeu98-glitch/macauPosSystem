# 2026-08-19 工作記錄

## Salon 快速開單：走進/電話 toggle 改為有意義（方案 A）

用戶指出快速開單右上角「走進 / 電話」toggle 無實際分別（兩個 mode 都強制填 8 位電話），困惑點在於「走進都要電話」。解釋咗根因：電話係會員/客戶檔案系統主 key（ensureSalonCustomer(name, phone) 必定 run，用嚟開/連 customerId），所以舊規則兩 mode 都 mandatory；source 欄本意係記錄 intake channel 畀統計用，純顯示維度。

用戶揀方案 A：**走進 → 電話 optional；電話 → 電話 mandatory**。

落點 `src/components/salon/booking-form.tsx`：
- validate（~150）：電話 requirement 改 `source === "phone" && !/^\d{8}$/...`；deps 加 `source`（避免 stale closure）。
- handleSubmit（~191）：走進無電話時 `customer = null`，唔 call `ensureSalonCustomer`（避免 `phone:""` 孤兒檔案）；有揀會員仍照連 selectedCustomerId。
- 電話 label（~536）：走進顯「（選填）」灰字、電話顯紅 `*`。
- 電話 placeholder（~551）：走進「8 位數字（選填）」、電話「8 位數字」。
- 保留：電話 mode 電話 input sky-blue 底（強化「電話 mode 電話重要」視覺提示）。

驗收：`tsc --noEmit` 零非 layout 錯；div 平衡 40/40。

設計慣例備忘：salon `source` 欄取值 walk_in / phone / online_ledger（線上預約經 Ledger flow，快速開單 form 只露 walk_in+phone 兩個店內 intake）。本輪只改快速開單 form 嘅電話必填規則，source 欄本身語義不變（仍純統計維度）。

用戶 dev box 待辦：`npm run lint && npm run build` + push。

## Salon 四項需求（線上訂單 tab / Ledger 對接文檔 / 充值入口 / 套餐產品 Ledger 為主）

用戶提出 4 點：(1) 開「線上訂單」tab + 商家確認/安排師傅流程；(2) 出一份 .md 給 Ledger 團隊講清楚要佢哋做咩 integration；(3) 會員直接充值入口（佢以為自己冇睇到，實情確實冇）；(4) 套餐/產品應以 Ledger 為主（商家手機端 set，兩邊互通，似餐飲 menu）。

落點：
- **線上訂單 tab（#1）**：`salon-sidebar.tsx` 加 `{href:"/salon/online",label:"線上訂單",short:"線"}`（擺快速開單之後）；新建 `src/app/salon/online/page.tsx`：列 `source==="online_ledger"` 預約，filter 待處理/已確認/已到店-服務中/已完成-取消；每卡 workflow：pending→[確認接單]/[拒絕]；confirmed→[安排師傅 modal 設 booking.staffId + 每項 service staffId + stationId]/[標記到店]/[取消]；checked_in→[開始服務]；in_service→[完成]。用 `updateMockBooking` + `loadBookings` + `MOCK_REALTIME_EVENT` 即時更新；加 5s 輪詢安全網（留畀未來 Ledger realtime）。資料層零改，realtime seam 保留。
- **demo seed（#1）**：`mock-realtime.ts` seed 改 1 筆 online_ledger pending（林小姐）+ 加 1 筆 pending（何太，定金 200 未付）；demo fallback 先種（真店唔種，符合舊規則）。
- **充值入口（#3）**：`customer-profile.tsx` 加「替會員充值（Ledger）」鈕 → modal 內嵌現有 `MemberTopupPanel`（餐飲 SSO iframe `POST /api/topup/owner-embed`）。salon 原本完全冇充值入口，依家 reuse 餐飲機制，符合「充值留 Ledger」。
- **Ledger 對接文檔（#2/#4）**：新建 `docs/32-salon-ledger-integration-requirements.md`（跟 docs/NN 編號 + meta block 風格）。列 7 項需求：R1 批量會員讀取、R2 積分/忠誠度、R3 預約 API（booking 非 order）、R4 預約 realtime 推送、R5 充值通道確認、R6 套餐/產品 Ledger 為主雙向同步、R7 餘額/等級確認。#4 套餐/產品 Ledger 為主 寫成 R6（P2，未實作遷移，只定需求）。附 salon 端 migration seam 表 + 優先級（P0=R3+R4+R5；P1=R1+R2；P2=R6）。

驗收：`tsc --noEmit` 零非 layout 錯；div 平衡（sidebar 8/8、online/page 32/32、customer-profile 81/81）。用戶 dev box 待辦：`npm run lint && npm run build` + push。

注意：#4 套餐/產品 Ledger 為主係**架構方向**，本輪只寫入需求文檔（R6），實際遷移（settings CRUD → 鏡像 Ledger）未做，留待 Ledger 實作 R6 後。充值入口依賴 Ledger SSO iframe，沙盒環境（CSP/eval）未必能實際彈出，但代碼路徑正確。

**決策（用戶 11:41）**：R6 salon 端遷移骨架 **等 Ledger 團隊按 docs/32 回覆先做**，唔提前動手。咁線上訂單 tab、充值入口、對接文檔 3 項已交付；R6 實作封存，等 Ledger 回應。用戶需將 docs/32 發給 Ledger 團隊並跟進（P0=R3+R4+R5 最關鍵）。
