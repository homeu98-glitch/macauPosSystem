# 2026-08-22

## 修客人掃 QR 落到 demo 店（store 參數空 + 店名顯示）

用戶反映：喺自己店掃枱 QR，手機開咗 `/menu` 但係「demo 店（澳門店 A / macau-store-a）」嘅餐牌。兩層原因：

1. **餐牌內容（layer 1 真正 bug — 已修）**：手機係獨立設備，本地 `macau-pos/bootstrap` cache 係空 → `bootstrap = loadBootstrapCache() ?? mockBootstrap` 永遠 fallback 去 `mockBootstrap`（storeId=macau-store-a、storeName=澳門店 A），所以無論掃邊間店都睇 demo 餐牌。**backend 其實一早有 per-store 餐牌**：商家點餐機經 `GET /api/pos/bootstrap?storeId=<merchantId>` 讀 `pos_bootstrap_config`（pos-app.tsx:438 用緊，normalize 後寫本地 cache）。舊碼客人 scan 流程冇 call 呢個 endpoint，所以手機食咗 mock。用戶（2026-08-20）糾正：佢已經用 Ledger 登入、餐牌已從 Ledger sync 落 `pos_bootstrap_config`，kiosk/掃碼都要用商家點餐機內嘅餐牌。即 per-store menu backend **一直都存在**，只係手機未 call。
2. **落單路由都錯落 demo 店（實際 bug，已修）**：`kiosk-qr-panel.tsx` 舊碼讀 `loadAuthSession()?.merchantId` 做 storeId。但 kiosk 登入（`login-screen.tsx` mode=kiosk）淨係 `saveKioskDeviceBinding(...)`、`return` 之前唔 `saveAuthSession`（設計上「唔留 staff session」），所以「掃碼點餐」tab 度 `loadAuthSession()` 係 null → `storeId=""` → QR 變 `/menu?tableId=X&store=`（store 空）。客人手機 `storeId = binding?.storeId ?? scanStoreId ?? DEFAULT_KIOSK_STORE_ID`，手機無綁店 + scanStoreId 空 → fallback `macau-store-a`（demo）。連「呢張單落到邊間店」都錯。

**修法（layer 2 實際 bug）**：
- `kiosk-qr-panel.tsx`：storeId/storeName 改讀 `loadKioskDeviceBinding()`（kiosk 登入一定寫埋 storeId+storeName，可靠來源），fallback `loadAuthSession()`。URL 加 `&storeName=<綁店名>`。
- `use-kiosk-order.ts`：讀 `?storeName=`, 新增 `displayStoreName = binding?.storeName ?? scanStoreName ?? bootstrap.storeName` 並回傳。
- `order/page.tsx` / `menu/page.tsx`：頂欄店名改 `displayStoreName`（原本用 `bootstrap.storeName` / `binding?.storeName ?? bootstrap.storeName`）。order page 移除變成 unused 嘅 `binding` / `storeId` destructure。

**驗證**：`tsc --noEmit` 零新錯（剩已知 layout.tsx LayoutProps 誤報）；ESLint 0 errors（1 個預存 isPickup unused warning）。

**已完成（layer 1，2026-08-20）**：客人手機 `/menu` 現已按 `scanStoreId` call `GET /api/pos/bootstrap?storeId=<scanStoreId>` 攞商家點餐機同步落 `pos_bootstrap_config` 嘅真 menu（同 pos-app 員工端同一份）。`use-kiosk-order.ts`：
- 新增 `fetchedBootstrap` state；`bootstrap = fetchedBootstrap ?? loadBootstrapCache() ?? mockBootstrap`。
- 新增 `menuLoading = Boolean(scanStoreId) && fetchedBootstrap === null`；`menu/page.tsx` 載入閘 `if (!hydrated || menuLoading) return 載入中`，避免手機 flash demo 餐牌。
- `scanStoreId` 有值時 `useEffect` fetch `/api/pos/bootstrap?storeId=`，成功就 `setFetchedBootstrap(data)` + `setActiveCategory(data.categories?.[0]?.id)`；失敗保留 cache / mock fallback。
- kiosk 路徑（無 scanStoreId）完全唔變，照用本地 cache。

**下一步**：dev box `npm install && npm run lint && npm run build` 確認零迴歸後 push。手機依家顯示所屬店名、落單落正確 storeId、餐牌係商家點餐機嘅真 menu，唔再係 demo 澳門店 A。

## 5 項客人掃碼 / kiosk 體驗修復（2026-08-22）

用戶報 5 個問題，全部已改完，tsc 零新錯、eslint 0 errors（剩預存 warning）：

1. **手機菜單不能向下滾動** — 根因：`globals.css` 有 `body { overflow: hidden }` 禁咗整頁滾動，而 `/menu` 嘅菜單 `<section>` 無 `overflow-y-auto`，內容超出就睇唔到。修：`menu/page.tsx` 個 `<main>` 改 `h-[100dvh] overflow-hidden` + 頂欄/底 bar 加 `shrink-0` + 菜單 `<section>` 加 `overflow-y-auto`（內部自滾）。**唔動全局 overflow**（POS 平板需要）。

2. **客人落單後商店 POS 睇唔到新單** — 根因：cashier 收銀側嘅 state-pull（`/api/pos/state`）只喺 `[offlineMode, runtimeRefreshTick, queue]` 跑，`runtimeRefreshTick` 淨係 `backToTables` 時 bump，**唔係週期性**，所以一旦 Supabase Realtime 靜默失敗（anon RLS / Realtime 未啟用）就永遠睇唔到 kiosk 單（kiosk 單唔喺本機 localStorage，全靠 realtime / pull）。修：(a) `/api/pos/orders` GET 加 `?storeId=` 過濾；(b) `pos-app.tsx` 加 15s 週期 fetch `/api/pos/orders?storeId=merchantId` 並 `mergeOrderLists` 入本機 + saveOrders（server 端 service role 繞過 anon RLS）。配合上一輪 storeId 修正，收銀一定見到。

3. **落單成功號碼跟店內線下序號** — 之前 kiosk 用 `堂食/自取/外賣 + timestamp 後綴`，同店內完全唔連。修：落單前 `useKioskOrder.placeOrder` 向 `/api/pos/sequence` 攞同日序號，kind 對齊店內（`dine_in→pos`、`pickup→pickup`、`delivery→delivery`），storeId 用所屬店，咁就同店內線下單共用 `next_daily_sequence` 同一日序列表。`buildKioskOrder` 加 `localOrderNo?` 參數；fetch 失敗 fallback 返 timestamp 後綴。

4. **kiosk 落單成功頁無適配 iPad / 平板** — `/order` 嘅 `submittedOrder` 成功頁用 `max-w-md`（手機寬度卡），喺平板好窄。改 `max-w-2xl` + 放大 checkmark / 單號 / 字號，垂直置中。手機 `/menu` 成功頁唔變。

5. **菜品要顯示圖片（從 Ledger）** — `MenuItem` 加 `image?`；`LedgerMenuProduct` 加 `image?` 由 RPC 輸出解析（`image/image_url/photo/thumbnail/img/picture` 多候選）；`mapLedgerProduct` 帶 `image`；`normalizeMenuItem` 讀 `image`；`/menu` 同 `/order` 渲染圖片（有圖先 render，避免 broken img）。圖經 `pos_bootstrap_config.menu_items` 落到手機。注意：要商家重新從 Ledger 同步一次餐牌，DB 先會有 `image`，手機先睇到。

**驗證**：`tsc --noEmit` 零新錯（剩 layout.tsx LayoutProps 誤報）；`eslint` 改動檔 0 errors。下一步 dev box build + push。

## 二維碼「太長，無法生成 QR」全部出現 + kiosk 單 POS 睇唔到（2026-08-22 後段，再排查）

用戶報：① kiosk 落單後 POS 睇唔到新單但單號正常；② 掃碼點餐 QR 全部顯示「太長，無法生成 QR（請用複製網址）」。

**② QR 根因（已修，確定）**：`src/lib/qrcode.ts` 自包含編碼器只支援 v1–5（byte mode EC-L 上限 = data 108 − 2 = 106 bytes）。而家 `storeId` 係 Ledger `merchant_id` UUID（36 bytes）+ 中文 `storeName`，URL ≈ 120–142 bytes 超過 106 → `encodeQrMatrix` 回 null → 全部顯示「太長」。
修法：(a) 編碼器擴充到 v6（size 41 / data 136 / ec 18，單 block，v1–6 唔使 version-info 模塊；alignment 中心 (34,34) 按標準 placement）；(b) `kiosk-qr-panel.tsx` 由 URL 移除 `&storeName=`（手機 `/menu` 已按 `scanStoreId` 去 `/api/pos/bootstrap` 攞返所屬店真名，`displayStoreName` fallback `bootstrap.storeName`，所以移除唔影響店名顯示，仲更準確）。移除後 URL ≈ 94–106 bytes，v5/v6 都穩陣。Node type-strip 測試確認 120 bytes URL → size=41（v6）OK、94 bytes → v5 OK。

**① POS 睇唔到 kiosk 單根因（硬化 + 診斷）**：happy-path 代碼其實正確——kiosk 落單 `store_id = binding.storeId (= merchantId UUID)`，POS 用 `authSession.merchantId` 過濾 realtime + 15s 輪詢 `/api/pos/orders?storeId=merchantId`，兩者應一致；`mergeOrderLists` / 顯示 tab 冇過濾。真兇係 **`/api/pos/sync` 靜默失敗**：舊碼 `if (supabase && events.length>0){...}` 唔 check upsert error，且 `supabase` 為 null（server env 缺）時都回 `ok:true` → kiosk 收到 200 → 顯示成功單號（所以「單號正常」），但單根本冇寫入 `pos_orders` → POS 永遠睇唔到。而家硬化：(a) `supabase` 為 null 且有 events → 返 503 + 明確 error；(b) 每個 upsert 收 `error`，有就收齊回 500 + 第一個 error message；(c) `submitKioskOrder` 解析回應，`!res.ok || body.ok===false` 就 throw 真 error（kiosk 改顯示真錯誤而唔係假成功）。
→ 部署後重測：若 kiosk 現顯示 error，就係 server 端 DB/env/schema 問題（relay 個 error 即可定位）；若仍顯示成功但 POS 唔見，就係 kiosk 綁店 merchantId ≠ POS 登入 merchantId（同店必須同一 Ledger 帳號），或 `pos_orders` 表喺共用 Supabase 項目未建（該表由 Macau-Ledger repo migration 建立，本 repo 只加 policy/publication）。

**改動檔**：`src/lib/qrcode.ts`、`src/components/kiosk-qr-panel.tsx`、`src/app/api/pos/sync/route.ts`、`src/lib/kiosk-order.ts`。`tsc --noEmit` 源碼零新錯（剩 layout.tsx 已知誤報）。下一步 dev box build + push。

## 0010_kiosk_realtime.sql 根本冇跑成 → ② POS 收唔到 kiosk 單嘅真兇（再排查）

用戶手動跑 `0010_kiosk_realtime.sql` 報：`ERROR: 42601: syntax error at or near "not"` @ `alter publication supabase_realtime add table if not exists pos_orders;`。
**根因**：`ALTER PUBLICATION ... ADD TABLE` 喺 PostgreSQL / Supabase **唔支援 `IF NOT EXISTS`**（只有 `CREATE PUBLICATION` 支援），所以第 9 行 parse 失敗 → 成個 migration 喺 transaction 內 rollback，**乜都冇建到**：
- `pos_orders` / `pos_print_jobs` / `pos_soldout` 全部冇加落 `supabase_realtime` publication → anon realtime 收唔到 pos_orders 變更；
- `pos_orders anon read` 等 RLS policy 冇建 → anon client 根本讀唔到 pos_orders；
- `pos_soldout` 表冇建。
→ 呢個就係 POS 收銀秒級見唔到 kiosk 單（realtime 路徑）嘅真正原因；15s 輪詢係 service role 繞過 RLS 先有 fallback，但已經係「補短板」。

**修法**：`supabase/migrations/0010_kiosk_realtime.sql` 第 1) 段改成 idempotent DO block——先查 `pg_publication_tables`（`pubname='supabase_realtime' AND schemaname='public' AND tablename=t`）判斷，唔喺 publication 先 `execute format('alter publication supabase_realtime add table %I', t)`。`create table if not exists pos_soldout` / `drop policy if exists` / `create index if not exists` 本身語法 OK 唔使改。

**重要前提**：`pos_orders` 同 `pos_print_jobs` 必須已經存在（由 Macau-Ledger repo 嘅 migration 喺共用 Supabase 項目建立）先可以 `add table`，否則 DO block 內 execute 會彈 `relation does not exist`。跑之前確認 Ledger migration 已 apply、呢兩張表喺 `public` schema。

**影響**：呢個 migration 之前若用 `supabase db push` 跑過，失敗會 rollback 且 0010 唔會標記為 applied（重跑會再試）；若喺 SQL editor 手動跑就直接重跑修好嘅 SQL 即可。重跑成功後 realtime 路徑恢復，配合 Aug 22 嘅 15s 輪詢 + sync 硬化，② 應該解決。下一步 dev box build + push + 重跑 migration 驗證。

**第二次報錯 42P01（`relation "pos_soldout" does not exist` @ DO block line 13）**：我第一版 DO block 將 `add table` 排喺 `create table pos_soldout` **之前**，所以 `pos_soldout` 仲未建就想去 `add table` → execute 彈 `relation does not exist`，成個 block 中斷。**最終修法**：重排成 3 段——(1) `create table if not exists pos_soldout` + index 先；(2) DO block（加 3 張表落 publication，已有 `to_regclass('public.'||t) is not null` 守門，表唔存在就 skip，唔會硬掛）；(3) RLS enable + anon read policy + grant select。而家 `0010_kiosk_realtime.sql` 係呢個順序，可安全重跑。

**確認清單（用戶跑 migration 前/後）**：
- 用戶已喺 macau-pos 自己個 Supabase project 手動跑咗 0010（冇報 `relation does not exist`，即 `pos_orders`/`pos_print_jobs` 已存在）；跑完查 `select * from pg_publication_tables where pubname='supabase_realtime'` 確認 `pos_orders`/`pos_print_jobs`/`pos_soldout` 三者都在；再 `select * from pg_policies where tablename in ('pos_orders','pos_print_jobs','pos_soldout')` 確認 `anon read` policy 在。

## ⚠️ 更正：pos_* 表唔係 Ledger 建立，係 macau-pos 自己嘅（用戶 2026-08-22 指正）

之前我喺 memory / code comment 寫「pos_orders / pos_print_jobs 由 Macau-Ledger repo migration 建立、共用 Supabase 項目」——**錯**。用戶明確：所有操作都係 macau-pos，唔係 Ledger 發起；pos_* 表屬 macau-pos 自己個 Supabase project（env `NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_URL` 指去嗰個 project）。`src/lib/pos/supabase-client.ts:10-11` 句「與 Ledger 共用同一個 Supabase 項目（pos_* 資料表同位於此項目）」正正就係呢個誤解嘅源頭，要改。

**更重要的發現**：repo 嘅 `supabase/migrations/` 得 0005–0010（salon + 呢版 kiosk realtime），**任何 migration 都冇建立呢啲 POS 表**。code 實際依賴 7 張 macau-pos 自己嘅表，目前只靠用戶早年手動 SQL 喺佢個 DB 建立，repo 唔係 source of truth：
- `pos_queue_events`（sync/state）
- `pos_orders`（sync/state/orders）← 欄位見 pos-order-mapper.ts
- `pos_print_jobs`（sync/state）
- `pos_bootstrap_config`（bootstrap/backoffice-server）
- `pos_soldout`（0010 建）
- `pos_device_configs`（state/device-config）
- realtime 訂閱用 `pos_orders`/`pos_print_jobs`/`pos_soldout`

**重新諗過嘅方案**：macau-pos migrations 要做成呢啲表嘅唯一真源——新增 `0011_pos_core_tables.sql` 用 `CREATE TABLE IF NOT EXISTS`（+ index）補齊 pos_queue_events/pos_orders/pos_print_jobs/pos_bootstrap_config/pos_device_configs（pos_soldout 已由 0010 建，但 `if not exists` 雙保險），idempotent 喺 existing DB 係 no-op、新 DB 完整建立；0010 保留做 realtime publication + anon RLS + grant。frontend realtime 用 anon key 訂閱、寫入經 /api/pos/sync（service role）。詳見俾用戶嘅 plan。

## 用戶再報：kiosk + 掃碼落單收銀睇唔到（診斷複盤）

happy-path 代碼（realtime + 15s 輪詢 `/api/pos/orders?storeId=merchantId` + sync 硬化 + 0011 migration）經前面幾輪已經做齊。用戶再次報睇唔到，最可能剩兩個原因：

1. **store_id 對唔齊（最 common）**：kiosk 寫落 `pos_orders.store_id` = `binding.storeId ?? scanStoreId ?? "macau-store-a"`；收銀 realtime + 15s 輪詢都 filter `store_id = authSession.merchantId`（Ledger merchant UUID）。若 kiosk 平板**冇用 mode=kiosk 綁店**，或 QR 由未綁店嘅 kiosk 生成（authSession 喺 kiosk 模式唔 save → null，binding 又 null → store="" → 手機 fallback "macau-store-a"），張單就落去 "macau-store-a"，而收銀用真 merchant UUID → 永遠過濾走。
   - 驗證：Supabase `select id,local_order_no,store_id from pos_orders order by created_at desc limit 5;` 對比你張測試單嘅 `store_id`；再喺收銀 browser console `JSON.parse(localStorage.getItem('macau-pos/auth-session')).merchantId`（注意 key 係 `macau-pos/auth-session`，唔係 `macau-pos/auth`）攞收銀 merchantId。兩個唔同就係呢個原因。
   - 修：kiosk 平板先 mode=kiosk 登入（同一 Ledger 商戶）綁店，之後 /order 同 QR 都帶正確 storeId；或確保收銀同 kiosk 用同一 merchant 登入。
2. **migration / realtime 未生效（secondary）**：若 store_id 對齊但依然唔見，查 0011 有冇跑（pg_publication_tables + pg_policies 有冇 pos_orders anon read）；但 15s service-role 輪詢應該繞過 RLS 照見，所以呢個只會令「秒級」變「≤15s」，唔會完全唔見。完全唔見 → 一定係 store_id 對唔齊 或 kiosk submitKioskOrder 彈錯（已硬化會顯示真 error）。

**再用戶確認（11:41）**：收銀係 堂食（枱檯 view）；`訂單01` 用嘅枱 ID 唔喺收銀樓層設定（localSettings.floors）入面。→ 排除 store 後，呢個係 display 層缺口：(a) 自取09/08 係 pickup/counter 單，堂食枱檯 view 唔 render counter 單（只 快餐 mode action bar 出）；(b) 訂單01 dine_in 但枱 ID 唔喺 floor → 枱檯 view 冇位 render。根本原因：kiosk/掃碼落單嘅 tableId 來自 `bootstrap.tables`（DB 共享），收銀枱檯 view 用 `localSettings.floors`（每終端本地），兩者唔同步就 invisible。仲未做 check 1（console fetch）確認單有冇收到；若收到就純 display，要加「未知枱 / unassigned」桶 或 收銀枱檯改讀 bootstrap.tables。

**已實作 B（治本，12:19 後）**：用戶揀 B — 收銀枱檯 view 改讀 `bootstrap.tables`（共享真源）而唔係 `localSettings.floors`。
- `src/components/pos-app.tsx` 加 module-level `buildDisplayFloors(bootstrapTables, localFloors)`：
  (1) bootstrap.tables 按 `area` 分組成 display floor（`id: area:<area>`）；(2) overlay `localSettings.floors` 中唔喺 bootstrap 嘅本地獨有枱（返結 temp 枱 / 本地新增），保留原 local floor id。
- `const floors = localSettings.floors;` → `const floors = useMemo(() => buildDisplayFloors(bootstrap?.tables ?? [], localSettings.floors), [bootstrap, localSettings]);`（line ~626）。
- `effectiveFloorId` 加 stale 保護：`activeFloorId && floors.some(f=>f.id===activeFloorId) ? activeFloorId : floors[0]?.id ?? ""`（避免舊本地 floor UUID 令枱 grid 變空，line ~665）。
- 效果：kiosk / 掃碼 dine_in 單嘅枱（來自 bootstrap.tables）一定喺收銀枱檯 view 有得 render；counter/pickup 單（自取09/08）仍只喺快餐 mode 出（枱檯 view 唔 render counter 單，by design）。返結 temp 枱流程 `setActiveFloorId(temp.floorId)` 對應返 display floor，唔會 invisible。
- 未做 tsc（repo 本地冇裝 typescript，`npx tsc` 搵到假冒 tsc@2.0.4 package；`.bin/tsc` 唔存在）。改動全用 `PosBootstrap["tables"]` / `PosLocalSettings["floors"]` 結構類型，唔需要新 import，`floors` 類型（FloorConfig[]）同改前一致，已做嚴格 code review。下一步用戶 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸後 push。
- 注意：`online-orders.tsx:88` 張枱 picker 仍讀 `localSettings.floors`（指派線上單用），未跟改；如要「枱列表永遠一致」可之後一併改，但超出本次 scope。

**已執行（2026-08-22）**：
- `src/lib/pos/supabase-client.ts:10-11` 錯誤註解已改：說明 `pos_*` 表屬 macau-pos 自己個 Supabase 項目（唔係 Ledger），frontend anon key 訂閱、寫入經 /api/pos/sync service role。
- `0010_kiosk_realtime.sql` 精簡：移除 realtime publication + RLS + grant（避免 fresh-DB 按檔名序跑時 pos_orders 未建就 alter table 失敗），只留 `create table if not exists pos_soldout` + index；realtime/RLS 交 0011 統一。
- 新增 `0011_pos_core_tables.sql`（自給自足、全 idempotent）：建 7 張表（pos_queue_events/pos_orders/pos_print_jobs/pos_bootstrap_config/pos_device_configs + pos_soldout 雙保險）+ store_id/updated_at index → DO block 加 pos_orders/pos_print_jobs/pos_soldout 落 supabase_realtime → enable RLS + `anon read` policy (using true) + grant select。existing DB 跑係 no-op、fresh DB 完整。
- `npx tsc --noEmit` 零新錯（剩 layout.tsx LayoutProps 已知誤報）。
- **用戶下一步**：喺 macau-pos SQL editor 跑 `0011`（唔使重跑 0010）；跑完驗證 `select * from pg_publication_tables where pubname='supabase_realtime' and tablename in ('pos_orders','pos_print_jobs','pos_soldout')` 同 `select * from pg_policies where tablename in ('pos_orders','pos_print_jobs','pos_soldout')`；再 dev build + push + 真機 kiosk 落單測收銀秒級見單。

## 決定性根因：Vercel 缺 SUPABASE_SERVICE_ROLE_KEY → kiosk 單寫唔入（2026-08-22 最後）

用戶 `select id,local_order_no,store_id,status from pos_orders order by created_at desc`（全表，冇 LIMIT）得 4 行（訂單01 / 自取09 / 自取08 + macau-store-a 訂單84），但 kiosk 已落到 12 → **新單根本冇寫入 pos_orders**，係 write 失敗，唔係 display（B 嘅枱檯 view 改動對齊後都冇 data 可 render）。store_id 對齊早前已確認冇問題（kiosk storeId = cashier merchantId UUID）。

根因：`/api/pos/sync` 用 `getSupabaseServerClient()`，key 解析 `SUPABASE_SERVICE_ROLE_KEY ?? SUPABASE_SERVICE_KEY ?? SUPABASE_ANON_KEY ?? NEXT_PUBLIC_SUPABASE_ANON_KEY`。Vercel 通常有 `NEXT_PUBLIC_SUPABASE_URL`（指 macau-pos 自己個 project，有 pos_orders）+ `NEXT_PUBLIC_SUPABASE_ANON_KEY`，但**冇 `SUPABASE_SERVICE_ROLE_KEY`** → fallback 去 anon key → 0011 migration 只 grant anon `select` → insert `pos_orders` 被 RLS 擋 → 500 → 舊 kiosk build（submitKioskOrder 未硬化）吞咗 → 顯示假成功單號。localhost dev 有 `.env.local` 嘅 service role key，所以頭 3 張單寫到。

硬化（防復發，已落 repo）：
- `src/lib/supabase-server.ts:resolveAnonKey` 移除 `NEXT_PUBLIC_SUPABASE_ANON_KEY` fallback → 無 POS service role key 就返 null → sync/sequence 返 503 明確 error，唔再靜默連去 anon key 寫失敗。
- `src/app/api/pos/sequence/route.ts` 無 supabase 唔再 fake 隨機單號，改返 503 明確 error（避免 kiosk 顯示假「訂單12」）。

用戶必做修復：Vercel Project Settings → Environment Variables 加 `SUPABASE_SERVICE_ROLE_KEY`（macau-pos project 嘅 service_role key，server-only 唔加 NEXT_PUBLIC_ 前綴）+ `SUPABASE_URL`（如未經 NEXT_PUBLIC 覆蓋）。加完 **redeploy** 令硬化碼上線（否則舊 build 照吞 error）。之後 kiosk 落單會見真 error 或真成功，收銀 15s 輪詢 + realtime 會見到。

驗證 probe（kiosk browser console）：空 events 探 `fetch('/api/pos/sync',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({storeId:'<merchantId>',events:[]})})` 睇 status —— 200=ok；500 含 RLS/permission=anon key 冇 service role（加 key）；503 未配置=連 URL 都冇；500 含 relation pos_orders does not exist=URL 指錯 project。

## 補返漏咗嘅 migration：0012 next_daily_sequence（2026-08-22 續）

`src/app/api/pos/sequence/route.ts` 一直 call `supabase.rpc("next_daily_sequence", {p_store_id, p_kind})`，但 repo **任何 migration 都冇建呢個函數**（docs/06 提到但無 SQL）→ feature #3「落單號跟店內線下序號」只係用家早年手動 SQL 建咗先 work，repo 唔係真源。

新增 `supabase/migrations/0012_daily_sequence.sql`（idempotent）：`create table if not exists pos_daily_sequences(store_id,kind,biz_date,last_number PK)` + `create or replace function next_daily_sequence(p_store_id, p_kind) returns integer`（用 `now() at time zone 'Asia/Macau'` 定 biz_date；`insert ... on conflict do update set last_number=last_number+1 returning last_number` 原子遞增）；`grant execute` 俾 anon/authenticated/service_role。簽名同 route call 完全一致（p_store_id/p_kind）。用家喺 macau-pos SQL editor 跑一次（fresh/existing 都安全）。

`docs/06-api-reference.md` 同步更正 `/api/pos/sequence` 請求/回應合約（舊 doc 寫 `{"date":...}→{"sequence":42}` 錯；實際 `{kind,storeId}`→`{ok,source,kind,value,display,bizDate}`，無 DB 返 503 唔再 fake 隨機單號）。

**tsc 限制**：沙盒 `node_modules` 唔完整（react/next/@types 都冇裝），`tsc --noEmit` 全 file 報 `Cannot find module 'next'/'react'`，無法喺沙盒驗證我嘅改動類型；只可以喺用家 dev box `npm install && npx tsc --noEmit && npm run build` 驗證。我嘅改動（supabase-server.ts remove anon fallback / sequence/route.ts 去 fake / pos-app.tsx B buildDisplayFloors）已做嚴格 manual review，類型安全。

## 用家 redeploy 後又壞：自取2629（假號）+ 單唔喺 POS（2026-08-22 最尾）

用家報最新 deployment 又壞：① kiosk 落單號唔跟真實店內單號，出自取2629（錯）；② 落單後 POS 收銀見唔到新單。
**確認收銀同 kiosk 都 call 同一個 `/api/pos/sequence` RPC**（pos-app.tsx:1921 `fetch("/api/pos/sequence",{kind,isQuickMode?quickTypeKind():"pos",storeId:bootstrap.storeId})`，失敗 fallback 2 位 timestamp `自取${slice(-2)}`；kiosk fallback 4 位 `自取${slice(-4)}`）。→ #1 同 #2 同一個根因：server Supabase 未搞掂，`sequence` + `sync` 齊 fail。

推斷：
- `自取2629` = kiosk 4 位 timestamp fallback → 證 `/api/pos/sequence` 失敗（無 supabase / `next_daily_sequence` RPC 未建 = 0012 未跑）。
- 單唔喺 POS → `/api/pos/sync` 寫失敗（無 `SUPABASE_SERVICE_ROLE_KEY` → anon key RLS 擋 / 或 503）。
- 見到「自取2629」假成功號 → **部署緊嘅係舊 build**（現時 repo 硬化碼 submitKioskOrder 會 throw → show error 唔係假成功）。所以必須 redeploy 現時 repo code。
- 訂單01/自取09/自取08 之前寫到過 → pos_orders 表存在（0011 等價手動 SQL 跑過），所以 sync 失敗唔係「表無」，係 env（缺 service role key）。

完整修復（用家側，我唔做到）：
1. Vercel 加 `SUPABASE_SERVICE_ROLE_KEY`（macau-pos project service_role，server-only 唔加 NEXT_PUBLIC_）+ `SUPABASE_URL`（如未經 NEXT_PUBLIC 覆蓋）。
2. macau-pos Supabase SQL editor 跑 `0011`（表/realtime/RLS，已存在變 no-op）+ `0012`（建立 next_daily_sequence RPC，否則 sequence 永遠 500 fallback）。
3. **Redeploy 現時 repo code**（令硬化碼 + sequence 503 上線；否則舊 build 照吞 error 顯假成功）。

決定式驗證（kiosk/收銀 browser console 貼）：`(async()=>{const k=JSON.parse(localStorage.getItem('macau-pos-kiosk-device')||localStorage.getItem('macau-pos/auth-session')||'{}');const storeId=k.storeId||k.merchantId||'d564b932-0c91-45e9-86fd-0ec8e2711f13';const sync=await fetch('/api/pos/sync',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({storeId,events:[]})}).then(r=>r.json().then(b=>({status:r.status,body:b})));const seq=await fetch('/api/pos/sequence',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:'pickup',storeId})}).then(r=>r.json().then(b=>({status:r.status,body:b})));console.log('SYNC',JSON.stringify(sync));console.log('SEQUENCE',JSON.stringify(seq));})();` —— SYNC 200=env OK；503 未配置=缺 key/URL；500 含 RLS/permission=anon 冇 service role；500 含 relation pos_orders does not exist=0011 未跑；SEQUENCE 200 source:supabase=0012 OK；500 找不到 next_daily_sequence=0012 未跑；503 未配置=同 env。把兩段 output 貼返。

## 用家跑 probe 結果：SYNC 200（寫入已好）+ SEQUENCE 500 column last_number 唔存在（2026-08-22 最尾）

用家貼 probe output：
- `SYNC {"status":200,"body":{"ok":true,"syncedCount":0,...}}` → **env + 0011 已搞掂寫入**！落單會寫入 pos_orders，收銀見到（issue #2 實際上已好，只係用家仲見假號以為冇）。
- `SEQUENCE {"status":500,"body":{"ok":false,"error":"column \"last_number\" of relation \"pos_daily_sequences\" does not exist"}}` → **pos_daily_sequences 表已存在但係舊 schema**（用家早年手動建，欄位名唔係 last_number），我嘅 `create table if not exists` 唔會改舊表 → 函數 call 彈 column 唔存在。

修：`0012_daily_sequence.sql` 改 v2 → 開頭 `drop function if exists next_daily_sequence(text,text); drop table if exists pos_daily_sequences;` 再 `create table` + `create or replace function`（權威真源，唔理舊 schema；每日序號歸零無損；重跑安全）。用家喺 Supabase SQL editor **重跑 0012** 即可。跑完 SEQUENCE 應返 200 `source:supabase`，kiosk 單號跟店內（shared RPC）。

**最終狀態**：env(SUPABASE_SERVICE_ROLE_KEY+SUPABASE_URL) + 0011(表/realtime/RLS) + 0012(RPC) + redeploy 現時 repo code（硬化 + sequence 503）→ kiosk 單寫入 POS + 單號跟店內序號。

## 用家再報：POS 收銀完全睇唔到 kiosk 單（寫入確認 OK 後，純 display 缺口）

用家確認 `select ... from pos_orders` 已有 7 張單（自取02/15/13/09/08、訂單01，store_id 全部 = d564b932…；另 1 張 macau-store-a 訂單84 demo），status 全 `sent_to_kitchen`，但收銀「pos 系統還是沒有顯示訂單」。

**診斷（receive 路徑係健康，純 display 缺口）**：
- RLS：`0011` 已建 `pos_orders anon read` policy (`using (true)`) → anon realtime 收得到；`/api/pos/state` + 15s 輪詢 `/api/pos/orders?storeId=merchantId` 用 service role → 繞過 RLS。→ 單會入 `orders` state。
- `mergeOrderLists` / `openOrders`（status sent_to_kitchen 在內）都冇過濾 store / 隱藏。
- **真正缺口**：收銀預設 `operatingMode="dinein"` → 進「桌台總覽」view。該 view 張枱 grid（`pos-app.tsx` `visibleTables` = `buildDisplayFloors(bootstrap.tables)`）只 render 真枱；kiosk 自取/外賣單 `buildKioskOrder` 給 `tableId="counter"`（`kiosk-order.ts:116-124`），**唔喺枱 grid、冇 tile**；右側「快捷操作」sidebar 亦冇 counter 單面板。→ dine-in 模式永遠睇唔到呢啲單（舊設計只喺 快餐 mode 底 bar `QuickModeOrdersBar` 出）。所以收銀喺預設 dine-in 模式 = 完全 invisible。

**修法（`src/components/pos-app.tsx`）**：
- 加 memo `counterKioskOrders = openOrders.filter(o => o.tableId === "counter" && !o.onlineOrderId)`（本地 kiosk 單，排除 Ledger 線上單）。
- import `localOrderStatusLabel`（pos-order-filters）。
- 喺 桌台總覽 右 sidebar（快捷操作下方、`offlineMode` banner 之前）加「自取 / 掃碼訂單」面板：`!isQuickMode && counterKioskOrders.length>0` 時 render，每張單顯示 `localOrderNo` / `localOrderStatusLabel` / `tableName · N 件`，三個動作：查看（`setViewingOrderId`）、標記可取（`updateQuickFulfillment(id,"ready")`，僅 `paid && fulfillmentStatus!=="ready"`）、結帳（`setPayingOrderId`）。快餐 mode 唔重複出（QuickModeOrdersBar 已有）。

**效果**：收銀無論 dine-in 或 快餐 mode 都見到 kiosk 自取/掃碼單；落單後 15s 輪詢 / realtime 入 `orders` → 面板即時列出，可一鍵標記可取 / 結帳。

**驗證**：沙盒無 node_modules（冇 react/next/@types），`tsc --noEmit` 唔能跑；改動用 `PosOrder` 既有字段 + 已存在 helper（`setViewingOrderId`/`updateQuickFulfillment`/`setPayingOrderId`/`localOrderStatusLabel`），嚴格 manual review 類型安全。用家 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸後 push + redeploy。

## 診斷確認：store_id 對齊係真兇 + probe undefined 係 artifact（用家貼 console probe）

用家貼 probe output：
```
CASHIER merchantId = d564b932-0c91-45e9-86fd-0ec8e2711f13
FILTERED(merchantId) 數 = 8 (8) [undefined ×8]
UNFILTERED 數 = 8
UNFILTERED store_ids = [undefined]
自取/訂單單 = []
```

**讀 `src/app/api/pos/orders/route.ts`（27–45 行）確認**：API 回 camelCase，`localOrderNo/tableId/tableName/status/...`，**根本冇 return `store_id` / `storeId` 呢個 field**。
→ 三點結論：
1. `merchantId = d564b932…` ＝ DB 內 7+ 張 kiosk 單嘅 `store_id` → **store_id 對齊確認**，證實係之前「收銀睇唔到單」嘅根本原因（三條接收路徑 realtime/15s poll/loadRuntimeState 全用 store_id filter，對唔齊就過濾走）。
2. `FILTERED 數 = 8` + `UNFILTERED 數 = 8` → endpoint 成功返 8 張單，對應用家「所有單突然出晒」（re-login/reload 後 merchantId 變 d564b932 對齊，loadRuntimeState 一次 pull 全返；realtime 不 backfill 舊單）。
3. 啲 `[undefined]` 同空 `自取/訂單單` 係 **probe 自己寫錯**（用 snake_case `o.local_order_no`/`o.store_id` 讀 camelCase 回應，且 store_id 連 camelCase 都無 return）→ 唔係 app bug。App 用 camelCase 讀，正常顯示（用家已見到單）。

**仲有位 fragility（未修，待用家決定）**：15s poll `useEffect`（pos-app.tsx:700–720）mount 時若 `authSession.merchantId` 仲 null 就 return 走，deps 得 `[offlineMode]`，session 之後 ready 都唔 restart。建議改 deps 加 `merchantId` + 令 `loadRuntimeState` 喺 merchantId 變非空嗰刻重跑，令訂單 100% 自動出。用家未決定 apply 定留現狀（現狀已對齊可見）。

## 用家新要求：即時、禁用 polling（2026-08-22 16:10）

用家報：重新落單「自取4」又唔見，要求「需要即時，但不能用 polling 的方式」。

**診斷**：
- 讀 `use-pos-realtime.ts` / `pos-order-mapper.ts` / `supabase-client.ts` / pos-app realtime 接駁 → realtime 代碼路徑全啱（subscription、`store_id=eq.` filter、`mapPosOrderRow` 讀 snake_case 對應 WAL payload、`onOrderUpsert` merge 入 state、anon client 正確）。**代碼冇死症**。
- 個 15s poll（`useEffect`，deps `[offlineMode]`，mount 讀 `loadAuthSession()?.merchantId`，null 就 return 永遠唔 restart）其實係死嘅 → 之前見到嘅 8 張舊單唔係 poll 拉到，係 `loadRuntimeState`（mount 一次過 pull，event-driven 非週期）拉到。新單靠 realtime（未 deliver）+ poll（死）→ 永遠唔見。→ 呢個解釋晒「舊單見、新單唔見」。
- `loadRuntimeState` 本身非週期（deps `[offlineMode, runtimeRefreshTick, queue]`，`runtimeRefreshTick` 只喺 line 1108 `backToTables` 等 bump），唔算 polling。

**新單即時唔到嘅兩個真因（要 user side 確認）**：
1. `pos_orders` 唔喺 `supabase_realtime` publication（realtime 收唔到推，最有可能；0011 嘅 DO block 若未跑 / 冇加到就係咁）。
2. 自取4 呢張新單 `store_id` ≠ 收銀 `merchantId`（kiosk 綁店對唔齊，filter 排走）。

**已改 code（`src/components/pos-app.tsx`）**：
- 移除 15s poll `useEffect`（用家禁用 polling）。
- `loadRuntimeState` 由另一個 `useEffect` 內部 function 提到 component scope（俾 realtime `onResubscribed` 共用），加 `eslint-disable-next-line react-hooks/exhaustive-deps`。
- `usePosRealtime` 加 `onResubscribed: () => void loadRuntimeState()` → realtime (re)subscribe 成功 3s 後一次過 backfill 現有 open 單（event-driven，非週期；補 realtime 唔 backfill 舊 row + visibilitychange/CHANNEL_ERROR 重連）。
- 即時架構 = realtime 推新單 + onResubscribed/mount 一次過 backfill，全程零 setInterval 輪詢。
- 沙盒無 node_modules，未 tsc；改動用既有 helper，嚴格 manual review。

**用家必做（realtime 要 deliver 嘅前置條件，code 改咗都要呢步）**：macau-pos Supabase SQL editor 跑
`select tablename from pg_publication_tables where pubname='supabase_realtime' and tablename in ('pos_orders','pos_print_jobs','pos_soldout');`
→ 若 pos_orders 唔喺，跑 0011 等價 SQL：`DO $$ ... alter publication supabase_realtime add table ... $$` + `ALTER TABLE pos_orders ENABLE ROW LEVEL SECURITY; DROP POLICY IF EXISTS "pos_orders anon read" ON pos_orders; CREATE POLICY ... USING (true); GRANT SELECT ON pos_orders TO anon;`（idempotent）。
另 `select id,local_order_no,store_id,status from pos_orders order by created_at desc limit 5;` 確認自取4 嘅 `store_id` = 收銀 `merchantId`（d564b932…）；唔等就係 kiosk 綁店錯。
DB 改完即生效（唔使 redeploy）；我嘅 code 改動要 redeploy 先生效。下一步用家跑 SQL + dev box build + redeploy。

## Vercel build 失敗 + 手機掃碼三個功能請求（2026-08-22 16:35–16:55）

用家 redeploy 後報 Vercel build fail：`src/app/menu/page.tsx(12,30): error TS7053` + `(12,35): error TS2304: Cannot find name 'language'`。**根因**：早前移除手機語言切換 UI 時，一併將 `language` 由 `useKioskOrder()` destructure 刪走，但 `const t = (key) => I18N[language][key] ?? key` 仍 reference 佢 → 類型錯。已修：menu/page.tsx destructure 加返 `language`（第 19 行）。`/order` 冇影響（佢一直留咗 `language`）。

用家同步提 3 個手機掃碼介面功能（全部已實作）：
1. **本枱已落單明細**：手機 `/menu` 之前得個細條 `resumeHint` banner，睇唔到已落咗咩菜。新增 `activeTableOrder`（`use-kiosk-order.ts`：`mode==="dine_in" ? resumedOrder ?? tableOrder : null`）+ 共用 `OrderSummaryCard` 組件（menu/order 兩版各一份 module-level），render 菜式 + 數量 + 小計 + 枱上總計 + 備註。主畫面（resume 載入 / 剛落單）都顯示。
2. **移除 PT / EN**：`KioskLanguage` type 收窄到 `"zh-HK"`（移除 `"pt"|"en"`）；`KIOSK_I18N` 刪 pt/en 兩塊；menu/order 兩版頂欄語言 switch + order settings `<select>` 移除。全 default 中文。
3. **加單（堂食先准）**：`use-kiosk-order.ts` 新增 `tableOrder` state（落單後 dine_in 保留、quick 清空）+ `addToOrder()`（guard `mode!=="dine_in" || !tableOrder` → return；把 tableOrder.items 載返購物車、setResumedOrder(tableOrder) 令下次落單 reuse 同 id → ORDER_UPDATED、清 submittedOrder 返 menu）。成功頁 dine_in 分支：顯示 `本枱已落單` 卡 + `加單` 鈕 + `完成`；quick 分支保留 `再點一單`（唔准加單，落單後要再落新單）。

**Task #6 時間顯示（UTC→澳門時間）**：用家報「pos 系統時間唔係 UTC+8」。pos-app.tsx 上輪已改 `formatMacauDateTime`；今輪掃清其餘 `.replace("T"," ").slice(0,16)`（直接剁 UTC ISO）共 9 個檔：
- 5 個 backoffice/admin：本地 `formatTime()` helper 改 call `formatMacauDateTime`（加 import）— backoffice-accounts / backoffice-store-detail / backoffice-stores / backoffice-sync / admin-accounts。
- local-orders-panel:210、online-orders:666、print-center:871,1957、reports-dashboard:104,321、shift-page（10 處 row.closedAt/row.openedAt/now/shift.openedAt/shift.closedAt）→ 全部換 `formatMacauDateTime(...)` + 加 import。
- 全 repo grep 確認 0 個 `.replace("T"` / `.slice(0,16)` 殘留（format.ts 本身定義除外）。

**驗證限制**：沙盒 `node_modules` 仍冇（react/next/@types 未裝），`tsc --noEmit` / `npm run build` 沙盒跑唔到；改用家 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸（含 menu/page.tsx 修復）後 push + redeploy。改動全用現有 `PosOrder` 字段 + `formatMacauDateTime`，嚴格 manual review 類型安全。

## kiosk 落單 UX 三改（2026-08-22 17:56）

用家報 3 個 UX 問題，全部已實作（hook 上輪已加 `started`/`startOrdering`/`returnToHome`，今輪落 page）：

1. **kiosk 落單直接發送 + 5 秒倒數自動彈回主頁**：`落單` 本身經 `useKioskOrder.placeOrder` 已即時 `submitKioskOrder` 發送（唔使再按「再點一單」；用家舊感知係舊 build 假成功）。`order/page.tsx` 成功頁改 loading + 5 秒倒數自動 `returnToHome()`：加 `useEffect`（deps `[submittedOrder]`）`setReturnIn(5)` + `setInterval` 每秒 -1，到 0 用 `submittedRef.current` 守門 call `returnToHome()`（避免加單清走 submittedOrder 後仲誤觸發）；`submittedRef` 每 render 同步最新 `submittedOrder`。UI：橙色 progress bar（`width = (5-returnIn)/5*100%`）+「N 秒後自動返回主頁…」。`returnToHome` 清 `submittedOrder` + 重設 `started=false` + 刪 sessionStorage `kiosk-started` → 下一個客人重新見 landing。

2. **Landing page（唔用點餐介面做主頁）**：`use-kiosk-order.ts` 加 `started`（init 讀 sessionStorage `kiosk-started`）+ `startOrdering()`（set started + 寫 sessionStorage）+ `returnToHome()`（清 gate）。`menu/page.tsx` 同 `order/page.tsx` 都加 `started`/`startOrdering`/`returnToHome` destructure + 閘：`if (!started) return <Landing/>`（店名 + 🍽️ + `開始點餐` 鈕 → `startOrdering`）。kiosk 平板（共用設備）落單後 5s 彈回 → 重見 landing；手機掃碼開 `/menu` 首次都見 landing，按「開始點餐」先入餐牌。

3. **手機落單後「返回」只顯示下完單內容 + 加單**：`menu/page.tsx` 成功頁重寫——所有模式都 render `OrderSummaryCard`（本單明細），`isDineIn` 才顯示 `加單` 鈕（`addToOrder`，手機掃碼=枱號→dine_in 一定顯示）；移除舊 quick 分支 `再點一單`（newOrder）。`完成` 鈕改 call `returnToHome`（而唔係 `setSubmittedOrder(null)` 返 menu），所以返回 landing 唔會再彈完整餐牌——只有 `加單` 先令客人返 menu 再點（req：按加單後先再點餐下單）。`setSubmittedOrder` 兩版成功頁已無使用，由 destructure 移除（避免 unused 警告）。

**落點檔**：`src/lib/use-kiosk-order.ts`（hook 上輪）、`src/app/order/page.tsx`、`src/app/menu/page.tsx`。
**注意**：`newOrder` i18n key 變 unused（放喺 dict 唔會 error）；`setSubmittedOrder` 已移出兩版 destructure。`order/page.tsx` 加 `useEffect`/`useRef` import。沙盒無 node_modules 未 tsc；用家 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸後 push + redeploy（Vercel env/0011/0012 上輪已確認）。

## kiosk 閒置 1 分鐘彈回 + 手機已落單枱鎖定餐牌（2026-08-22 19:34）

用家再報 2 個 UX 要求，全部已實作：

1. **kiosk 閒置 1 分鐘自動返回 landing**：`order/page.tsx` 加 `useEffect`（deps `[started]`，`!started` 即 return 唔裝 listener）— 監聽 `mousemove/mousedown/touchstart/keydown/scroll`（passive），每次操作 `reset()` 清舊 timer + 重設 60s `setTimeout(() => returnHomeRef.current(), 60_000)`；`returnHomeRef`（useRef）存最新 `returnToHome` 避免 dep 變動令 effect 重跑 reset（否則每 render 重置計時，永遠到唔到 60s）。cleanup 移除 listener + clearTimeout。`started=false`（落單 5s 彈回 / 手動返回）時 effect cleanup。手機 `/menu` 唔裝（用家只要求 kiosk 模式）。

2. **手機已落單枱鎖定餐牌（必須按加單先點餐）**：`use-kiosk-order.ts` 加 `ordering` state（default false）。`addToOrder()` 加 `setOrdering(true)`（解鎖餐牌）；`placeOrder()` 成功加 `setOrdering(false)`（落完返明細鎖定）；`startOrdering()` / `returnToHome()` 加 `setOrdering(false)`（重置）。`menu/page.tsx` 加 `ordering` destructure + 閘：`if (activeTableOrder && !ordering) return <明細介面>`（🧾 + 單號 + 枱號 + `OrderSummaryCard` + `加單` 鈕 → `addToOrder` + `完成` → `returnToHome`）。
   - 優先級：`submittedOrder`（落單成功明細）→ `activeTableOrder && !ordering`（已落單枱鎖定明細）→ 餐牌。
   - 效果：掃碼開 `/menu`，若呢張枱已有未結單（resume）→ 入去只見「明細 + 加單」，唔能直接點餐；按「加單」先 `addToOrder` 載返購物車入餐牌（`ordering=true`）→ 落單後 `submittedOrder` 彈「明細 + 加單」→ 再按加單又入餐牌。完全符合用家「按加單先入點餐、下完單又回明細」。
   - 首次落單（無 resume）流程唔受影響：activeTableOrder 落單前為 null → 直接點餐；落單後 `submittedOrder` 明細；按加單→餐牌（`ordering=true`，因 activeTableOrder 已 set 但 ordering true 跳過鎖定）。

**落點檔**：`src/lib/use-kiosk-order.ts`、`src/app/menu/page.tsx`、`src/app/order/page.tsx`。
**驗證限制**：沙盒無 node_modules，`tsc`/`build` 跑唔到；用家 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸後 push + redeploy。改動用現有 `PosOrder` 字段 + hook setters，嚴格 manual review。

## 掃碼再入已結帳枱仲顯示「已落單」（2026-08-22 深夜，Round C 調查+修）

用家報：POS 收銀將枱按「結帳」（單已完成），再掃該枱 QR 入 `/menu` 仍顯示「已落單」。

**調查（追完整 data flow）**：
- 手機 `/menu` 嘅「已落單」明細屏由 `activeTableOrder = resumedOrder ?? tableOrder`（`use-kiosk-order.ts:253`）驅動；`menu/page.tsx:184` 閘 `if (activeTableOrder && !ordering)`。
- 全新掃碼（新 page load）`tableOrder = null` → `activeTableOrder = resumedOrder` → `resumedOrder` 嚟自 `fetchUnsettledKioskOrder(storeId, tableId)`（`kiosk-order.ts`），讀 `/api/pos/state?storeId=` → `pos_orders` 表，`return open.find(o => o.tableId === tableId)` 但 `open` 過濾走 `TERMINAL_STATUSES = {settled,cancelled,refunded,partially_refunded}`。
- POS 結帳路徑：`pos-app.tsx` `confirmPayment`→`applyPaymentToOrder`：`status = quickPaidFlow ? "paid" : "settled"`（`quickPaidFlow = isQuickMode && tableId==="counter"`）。**堂食枱** checkout → `"settled"`（terminal）。`selectTable`→`tableOrderMap.get(tableId)` 攞到嘅正正係 kiosk 單（id `kiosk-<uuid>`、`status sent_to_kitchen`），`activeOrderId` 設返佢，`applyPaymentToOrder(targetOrder)` 用同一 id，`ORDER_SETTLED` event `orderId = 該 id`；`/api/pos/sync` ORDER_SETTLED handler（`sync/route.ts:75-88`）`update pos_orders set status=event.payload.status ?? "settled" where id=orderId` → **backend row 應變 settled**。
- **真正缺口**：`pushEvents([paymentEvent])` 只 `persistQueue`（寫本機 localStorage queue），冇即時上 backend；backend 寫入靠 `pos-app.tsx:695-704` 嘅 **30s 批量 setInterval flush**（`syncNow`）。所以收銀按結帳 → backend `pos_orders` 仍 `sent_to_kitchen`（非 terminal）長達 30s；若收銀結帳時**離線**（offlineMode → `syncNow` 直接 return）→ 單永遠留 queue 等重連先 flush。→ 客人呢段時間再掃，resume 讀到 `sent_to_kitchen` 非 terminal → 顯示「已落單」。呢個係真正根因（唔係 id 對唔齊，id 一路都對）。

**修法（兩面夾擊）**：
1. **resume 端（`src/lib/kiosk-order.ts`）**：`TERMINAL_STATUSES` 加 `"paid"`（快餐先收款後出餐嘅 paid 單，對客人已結帳，唔應該再畀加單）；`fetchUnsettledKioskOrder` 改「以該枱 **最新一張單**（/api/pos/state 已 `updated_at desc` 排好）判斷」——`latest = orders.find(o => o.tableId === tableId)`，最新一張 terminal → return null（枱已完結，唔畀再加單），否則 return latest。解決「舊 open 單遮住新 settled 單」同 timing window。
2. **sync 端（`src/components/pos-app.tsx`）**：兩條 checkout 路徑（`applyPaymentToOrder` 內 `pushEvents([paymentEvent])` @2422；另一條 @2570）push 後**即時 `void syncNow([...queue, paymentEvent], { silent: true })`**，`syncNow` 內有 `offlineMode` 守門（離線唔做），連線即把 settled 狀態寫 backend，唔等 30s。

**效果**：收銀按結帳（連線）→ backend `pos_orders` 即刻 settled → 客人再掃 → resume 讀到最新 settled → 顯示「枱已完結」唔再「已落單」。離線結帳 → 單留 queue，重連後 30s flush 補上（預期行為）。
**驗證限制**：沙盒無 node_modules；用家 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸後 push + redeploy。改動：`syncNow` 係 component scope const（2422/2570 兩處閉包可擷取），`[...queue, paymentEvent]` 含新 event，類型安全，已嚴格 manual review。

## 手機落單成功頁 / 已落單頁 不適配屏（2026-08-22 深夜，UI 修）

用家報：手機按完下單出來嘅介面「無適配好屏的大小」。

**根因**：`src/app/menu/page.tsx` 嘅 `submittedOrder`（落單成功）同 `activeTableOrder && !ordering`（已落單）兩個確認屏，用 `min-h-[100dvh] ... flex-col items-center justify-center` 但**無內部 scroll 容器**；而 `globals.css` 有 `body { overflow: hidden }`。單嘅 `OrderSummaryCard` 一長（多件菜）就超過 viewport，下面嘅「加單 / 完成」鈕被 clip 且冇得 scroll → 手機見唔到 / 撳唔到。

**修法**：兩個屏改標準全屏可滾動結構（同 menu 主頁 `main`+`section overflow-y-auto` 一致）：
- `<main className="... h-[100dvh] ... flex-col overflow-hidden">`
- 內層 block scroll：`    <div className="flex-1 overflow-y-auto">`
- 再內層 `min-h-full` 置中：`      <div className="flex min-h-full flex-col items-center justify-center px-6 py-8 text-center">`（短內容置中；長內容 `min-h-full` 令 wrapper 高過容器，外層 scroll 唔會 clip 頂）。
- 關 `</div></div></main>` 平衡。

**效果**：手機任何長度嘅落單確認 / 已落單明細都可整屏 scroll，加單 / 完成鈕一定撳到。`needsBinding` / `started`(landing) 屏內容短（單一鈕）冇 overflow 風險，冇改。

## 生成一鍵清空線下單 SQL（2026-08-22 深夜）

用家要一次過清走線下（本地 / 掃碼 / 堂食）單，嫌太亂。生成 SQL（唔 execute，用家自己喺 macau-pos Supabase SQL editor 跑）：
- 備份 `create table if not exists pos_orders_backup_YYYYMMDD as select * from pos_orders;`
- 主刪：`delete from pos_orders where online_order_id is null;`（保留 Ledger 線上單；`online_order_id` 唔空 = 線上）。替代：`delete from pos_orders;`（清全部）或加 `where store_id='<merchantId>'`（只清某店）。
- 可選清孤兒：`delete from pos_queue_events where entity_id not in (select id from pos_orders);` + `pos_print_jobs where order_id not in (...)`。
- 確認：`select status, count(*) from pos_orders group by status;`
- **重要提醒**：POS 收銀 `orders` 仲 merge 咗本機 localStorage（`macau-pos/stores/{merchantId}/orders`，見 `src/lib/storage.ts:85-89`），DB 清完收銀 browser 舊單仲會現；要喺收銀 browser console `localStorage.removeItem('macau-pos/stores/<merchantId>/orders')` 後 reload，或清 site data / re-login 先乾淨。

**驗證限制**：沙盒無 node_modules；menu/page.tsx 改動用現有 className，嚴格 manual review，用家 dev box build 確認。

## kiosk（mode=kiosk 綁店）平板顯示 demo 店餐牌（2026-08-22 深夜，Round D 修）

用家報：清完單 + 登入 kiosk mode，平板 `/order` 顯示 **demo store（macau-store-a / 澳門店 A）** 嘅餐牌，唔係登入嗰間店嘅餐牌。

**根因（確認）**：`use-kiosk-order.ts` 嘅 menu fetch `useEffect` 舊碼 `if (!scanStoreId) return;` —— **只有手機掃碼路徑（scanStoreId 有值）先去 backend 攞真 menu**，kiosk 平板（無 scanStoreId、有 `binding.storeId`）**完全唔 fetch**，只靠 `bootstrap = fetchedBootstrap ?? loadBootstrapCache() ?? mockBootstrap`：
- `loadBootstrapCache()` → `storeScopedStorageKey("bootstrap")` 無 merchantId → `getActiveMerchantId()` 讀 `macau-pos/auth-session.merchantId`；但 **kiosk 登入唔 `saveAuthSession`**（只 `saveKioskDeviceBinding`）→ 返 null → 讀 **legacy global** `macau-pos/bootstrap`。
- 用家清單 / 清 localStorage 後 legacy global 係空 → fallback `mockBootstrap`（storeId=`macau-store-a` = demo）。
→ 所以 kiosk 平板永遠食 demo 餐牌，除非 cashier 同一 browser 之前寫過 scoped cache（跨設備唔成立）。

**修法（令 kiosk 同手機一樣按所屬店攞真 menu）**：
1. `src/lib/use-kiosk-order.ts`：
   - import 加 `saveBootstrapCache`（`loadBootstrapCache` 旁）。
   - menu fetch `useEffect` 改 `const targetStoreId = scanStoreId ?? binding?.storeId ?? null;`（`binding?.storeId` 喺 kiosk 平板有值），fetch `/api/pos/bootstrap?storeId=<targetStoreId>`；成功 `setFetchedBootstrap` + `setActiveCategory` + `saveBootstrapCache`（離線 fallback 會係所屬店而唔係 demo）；deps `[scanStoreId, binding?.storeId]`。
   - 加 `menuFetchDone` state（成功/失敗/`!res.ok` 都設 true）；`menuLoading = (Boolean(scanStoreId) || Boolean(binding?.storeId)) && !menuFetchDone`。防止離線/失敗時 `fetchedBootstrap` 留 null → `menuLoading` 卡死無限「載入中」（舊 kiosk 用 cache 係離線安全，新邏輯要靠 flag 唔 hang）。
   - `bootstrap` memo 註解更新（kiosk 唔再「只用 cache 唔變」）。
2. `src/app/order/page.tsx`：destructure 加 `menuLoading`；喺 `submittedOrder` 屏之後、主餐牌 render 之前加閘 `if (menuLoading) return <載入中…>`，避免 flash demo 餐牌。`/menu` 頁本來 already `if (!hydrated || menuLoading) return 載入中`（line 103），手機行為無變（手機 binding 為 null，menuLoading 仍只跟 scanStoreId）。

**效果**：kiosk 平板登入綁店後，按 `binding.storeId` 去 backend 攞該店真 menu（同商家點餐機同一份 pos_bootstrap_config）；首次連線後 `saveBootstrapCache` 落 legacy global，之後離線都食所屬店 cache；網絡掛咗就 fallback cache/mock（首登離線冇 cache 才 demo，可接受）。

**驗證限制**：沙盒無 node_modules（`tsc`/`build` 跑唔到）；用家 dev box `npm install && npx tsc --noEmit && npm run build` 確認零迴歸後 push + redeploy。改動用現有 `PosBootstrap` 類型 + `saveBootstrapCache`，嚴格 manual review 類型安全。

## 掃碼區（QR 面板）新增桌台唔顯示（2026-08-22 深夜）

用家報：喺 POS「樓層與桌台」新增桌台，但「掃碼點餐」tab（掃碼區 / `kiosk-qr-panel.tsx`）冇出張新枱嘅 QR。

**根因（確認）**：系統有兩個枱資料源，長期 duality：
- `localSettings.floors`（嵌套 `FloorConfig[]`，每 floor 有 `tables: StoreTable[]`）——「樓層與桌台」設定 tab 編輯嘅對象，per-terminal 本地。
- `bootstrap.tables`（扁平 `StoreTable[]`，來自 `pos_bootstrap_config` DB / `loadBootstrapCache`）——收銀枱檯 view（`buildDisplayFloors` 改讀佢做共享真源）、QR 面板、手機/kiosk 讀嘅對象。
「樓層與桌台」嘅「保存」(`saveTablesLocal`) 舊碼只 `savePosLocalSettings(localSettings)` 寫本地 floors → **完全冇同步去 `bootstrap.tables`**。QR 面板（`kiosk-qr-panel.tsx:67` 舊碼 `const tables = bootstrap.tables`）只讀 `bootstrap.tables` → 新枱（只喺 floors）唔顯示。

**修法（兩面夾擊，安全優先）**：
1. `src/components/kiosk-qr-panel.tsx`：`import loadPosLocalSettings`；`tables` 改為 `[...bootstrap.tables, ...loadPosLocalSettings().floors.flatMap(f=>f.tables)]` 按 `id` 去重。掃碼區直接反映 cashier 在設定新增嘅枱（read-only，零風險，仲 retroactively 修好舊 store 已加但未顯示嘅枱）。
2. `src/components/device-settings.tsx` `saveTablesLocal()`：保存時將 floors 枱 **merge** 入 `bootstrap.tables`（本地枱優先、bootstrap 獨有枱保留，唔用覆蓋式避免誤刪舊 store DB 已有枱）→ `saveBootstrapCache` + `setMenuDraft(tables: mergedTables)`。咁「保存菜單」(`POST /api/pos/bootstrap`) 會一併推去 DB，手機/kiosk 先會見到新枱名（否則手機只顯示 raw tableId 做枱名，落單仍 work）。`buildDisplayFloors` 按 id 去重，無雙重計。
   - 用 merge 而唔係 overwrite 嘅原因：`defaultPosLocalSettings.floors` 雖有 5 個預設枱（A01–A03/B01/B02）唔會空，但舊 store 嘅真枱可能只喺 `bootstrap.tables`（DB）而 floors 仲係預設 → 覆蓋式會 wipe。merge 保留 bootstrap-only，最安全。

**效果**：① 新增枱後去掃碼區即刻見到 QR（無論有冇重新 save，因 QR 面板直接讀 floors）；② 按「保存菜單」推去 DB 後，手機/kiosk 見到枱名。用家流程：樓層與桌台新增 → 保存 → 去掃碼區生成 QR；要手機見枱名再按保存菜單。

**注意**：merge 唔會 propagate「在 floors 刪走枱」去 bootstrap（保留 bootstrap-only 嘅副作用）——刪枱要手動喺 bootstrap 處理或之後再優化；加枱/改名有 propagate。下一步用家 dev box build + push + redeploy；沙盒無 node_modules 未 tsc。
