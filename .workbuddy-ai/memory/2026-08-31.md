# 2026-08-31 · 自助點餐 v2（docs/87）實作

## 已完成

### 1. 共用 confirmSelfOrder() + rejectSelfOrder()（src/lib/pos-orders.ts）
- confirmSelfOrder(orderId)：draft 自助單 → sent_to_kitchen + 建廚房單 + 標籤單 +（scan 補顧客小票）+ 推 ORDER_UPDATED
- rejectSelfOrder(orderId, reason?)：draft 自助單 → cancelled + 推 ORDER_UPDATED
- 兩者都會檢查 isSelfOrder() 同 status === "draft"，並 dispatch pos-orders-changed

### 2. 收銀端 realtime 自動建廚房單（src/components/pos-app.tsx）
- onOrderUpsert：新收到嘅 kiosk/scan sent_to_kitchen 單 → 自動建廚房單 + 標籤單（scan 補顧客小票）
- 去重防護：用 loadPrintJobs() 檢查是否已經有該單嘅 kitchen job（normal + 非 receipt + items.length > 0），有則 skip
- loadRuntimeState() backfill 後亦會補建（收銀端恢復在線時）
- draft 自助單 → toast「待確認」

### 3. 三處顯示「確認 / 拒絕」按鈕（規格 6）
- local-orders-panel.tsx 訂單卡片 + 查看彈窗：draft 自助單顯示「確認出單 / 拒絕」
- pos-app.tsx dine-in 模式「自取 / 掃碼訂單」面板：同樣顯示確認 / 拒絕

### 4. 放寬「可取餐」閘門（docs/87 §6.3）
- quick-order-fulfillment.ts：updateQuickFulfillmentInStore 閘門由 status === "paid" 放寬為 draft / sent_to_kitchen / paid
- local-orders-panel.tsx QuickOrderActions：同樣放寬
- quick-local-orders-strip.tsx：同樣放寬

### 6. 移除「返回製作」掣（用戶回報：三掣擠兩行，應改為一行、移除「返回製作」）
- quick-local-orders-strip.tsx OrderCard（截圖嗰張）：waiting mode 移除「返回製作」掣，剩「查看」+「已取餐」；wrapper 改 `flex flex-nowrap gap-1.5` 明確一行。
- local-orders-panel.tsx QuickOrderActions（訂單頁）：paid + ready 分支移除「返回製作」掣，剩單一「已取餐」掣（wrapper 唔再需要）。
- 死碼清理：`onReturnPreparing` prop chain（pos-app.tsx → quick-mode-orders-bar.tsx → quick-local-orders-strip.tsx）全部移除；pos-app.tsx `updateQuickFulfillment(orderId, nextStatus)` 收窄為 `updateQuickFulfillment(orderId)`，toast `已返回製作中` / action `back_to_preparing` 死分支一齊清走；lib/quick-order-fulfillment.ts `updateQuickFulfillmentInStore` 同樣收窄為只接受 `"ready"`。
- 驗證：`grep` 全項目 0 個 `返回製作` / `back_to_preparing` / `onReturnPreparing` hit。

### 5. 其他補充
- upsertCurrentOrder() 新建單時顯式寫入 source: "pos"
- device-settings.tsx syncConfig() 剝走 autoAcceptSelfOrder，避免寫落 pos_device_configs（同 onlineOrderSettings.autoAccept 同款 bug）
- /order/page.tsx（kiosk 平板）隱藏「自取 / 外賣」切換，鎖死「自取」（docs/87 §5.1）

## 已存在（無需本次修改）
- pos_kiosk_settings 表（0015 migration）+ /api/pos/kiosk-settings GET/POST
- SelfOrderAutoAcceptToggle 已取代「刪除全部訂單」掣位
- OrderSourceBadge 已在訂單頁 / 快餐單卡片 / 結帳畫面顯示
- PrintTemplates.kiosk 第四槽位 + print-center.tsx 第四分頁 + DEFAULT_KIOSK_TEMPLATE
- buildKioskReceiptPrintJobs()（kind="receipt" + kiosk template + copies:1）
- KioskModeGate 開機 redirect /order

## 佈局修正（用戶回報「🖥️自助點餐機 block 咗訂單號」）

- 根因：quick-local-orders-strip.tsx OrderCard 原本將 OrderSourceBadge 同 localOrderNo 擺喺同一行（flex gap-1 + truncate），窄卡 240px 下 emoji 標記擠住號碼。
- 第一版修正（過時）：將 badge 移到第二行貼住 tableName —— 用戶回報「應該放喺製作中下面」。
- **最終約定（規格 7）**：來源標記統一放喺**狀態藥丸下面、右對齊**（`flex flex-col items-end gap-1.5` 包裹藥丸 + badge）。左邊只留訂單號 + 枱號。
- 套用範圍（三個顯示位全部一致）：
  - quick-local-orders-strip.tsx OrderCard（快餐單條，240px 窄卡）
  - local-orders-panel.tsx 訂單卡片（訂單頁，grid 卡）
  - pos-app.tsx counterKioskOrders 面板（dine-in 模式「自取/掃碼訂單」）
- 結帳畫面（pos-app.tsx:4430）保留 header 同行（modal header 用 flex flex-wrap，唔會 block）。
- tsc --noEmit 只餘 layout.tsx LayoutProps 假陽性（standalone tsc 見唔到 .next/types，唔影響 Vercel build）。

### 7. strip 卡排版全面優化（用戶回報：品項名稱與價格同 row、固定卡 size、整體優化）
- 改動：`quick-local-orders-strip.tsx` `OrderCard`。
- 卡高度固定 `h-[180px] overflow-hidden`：`flex flex-col` + 按鈕 `mt-auto`，無論有冇 badge / 1 件 vs 2 件品項，所有卡 100% 一致高，按鈕永遠對齊底部。
- 品項摘要 + 訂單總額**合併同一行**（`flex items-baseline justify-between gap-2` + `border-t border-slate-100 pt-2` 分隔）—— 用戶「品項名稱與價格同一行」要求。
- 總額 `text-sm font-bold tabular-nums text-slate-900`（搶眼，數字 tabular 對齊）；品項摘要 `truncate flex-1`（長名唔擠總額）。
- 訂單號 / 枱號區 `leading-tight` + `mt-1` 微調間距；按鈕加 `shrink-0` 防止 flex 空間唔夠時被擠壓。
- 影響範圍：只有 strip 卡。kiosk 客人面對嘅 `/order`、`/menu` 用戶確認唔改。

### 8. 三種下單方式統一「icon + 文字標籤」格式（用戶新增需求）
- 改動：`src/lib/pos/order-source.ts` + `src/components/order-source-badge.tsx`。
- 舊：`orderSourceLabel("pos")` 回傳空字串、`OrderSourceBadge` 對 `pos` 直接 `return null`（收銀台落單唔顯示標記）。
- 新：三種都顯示統一格式 —— `pos` → 👤 商家下單（slate）/ `kiosk` → 🖥️ 自助點餐機（indigo）/ `scan` → 📱 掃碼下單（cyan）。
- `orderSourceLabel`：pos→"商家下單"、kiosk→"自助點餐機"、scan→"掃碼下單"（用戶指定文字；scan 由舊 "掃碼落單" 改 "掃碼下單"）。
- `OrderSourceBadge`：抽出 `SOURCE_STYLE` map（icon + ring 顏色），三款都係 `inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[11px] font-semibold`，排版/間距/字型一致；icon 包 `aria-hidden`。
- 影響：① 訂單頁 ② 收銀台 strip 卡 ③ 結帳畫面 ④ dine-in counterKioskOrders 面板 —— 全部經同一 `OrderSourceBadge` 自動統一，加 `pos` 標記後收銀台單都會見到「👤 商家下單」。
- tsc --noEmit 只餘 layout.tsx LayoutProps 假陽性。

### 9. 客人掃碼誤判「已落單」（用戶掃 A01 枱面「空間 / 已坐 0/10」卻被擋）
- **根因**：`fetchUnsettledKioskOrder` 舊邏輯用「該 tableId 有任何 server-side open 單」＝「有人坐」；`reopened` / `paid` counter / 其他 terminal `draft` 撞到 tableId 就會誤擋客人。
- **修正（src/lib/kiosk-order.ts）**：resume 嘅單一真源改為「客人自己嘅單」—— 用 `sessionStorage["kiosk-last-order"]` 配 server-side 對應 ID，**且只認 `source === "scan"`**。`tableId` fallback 永遠 return null（保留參數做 call site 簽名穩定，但唔再用佢做判定）。
- 新增 `isCustomerScanOrder()` predicate，文件化「resume 對象」嘅定義。
- 影響：
  - ✅ 客人第一次掃（sessionStorage 空）→ 正常落單
  - ✅ 重複掃 + 單未 terminal → resume 顯示明細
  - ✅ 重複掃 + 單已 settled → null，客人可落新單
  - ✅ 掃到 stale reopened / 商戶單枱 → null，唔再被誤擋 ← bug fix
- tsc --noEmit 只餘 layout.tsx LayoutProps 假陽性。

### 11. 「快捷入口」section 拿掉（用戶「堂食不需要了, 拿走吧」+ follow-up「快捷入口那些按鈕,全部都不需要了」」）
- 改動：刪 `pos-app.tsx` 行 3073–3170 嘅「快捷入口」section wrapper + section 標題 + 8 粒掣（去結帳 / 查看本單 / 送廚房 / 查看打印 / 線上訂單 / 交班核對 / 設置 / 返回桌台）。
- 連帶 dead code 一齊清（用戶確認「UI + dead code 全刪」）：
  - `pos-app.tsx:327` — `draggingQuickAction` state（連 drag handlers 喺刪 UI時一齊清走）
  - `types.ts:341-343` — `PosLocalSettings.dineInQuickActionOrder` field
  - `storage.ts:293-295` — `normalizePosLocalSettings` fallback default
  - `mock-data.ts:497` — `defaultPosLocalSettings` 嘅 default array
- 保留：頂部 4 粒資訊卡（當前桌台 / 待結帳 / 待同步 / 打印設備）、「快捷操作」標題、`posMode === "tables"` 堂食枱面 mode 同 entry path、`backToTables` 同 `sendToKitchen` function（其他 call site 仲用緊：1927/2675/2731/2811/3300/3777）。
- 驗證：`grep` 全項目 0 個 `dineInQuickActionOrder` / `draggingQuickAction` / `setDraggingQuickAction` hit。`tsc --noEmit` 只餘已知 layout.tsx LayoutProps 假陽性。

### 12. 「快捷操作」頂部 4 粒資訊卡拿掉（用戶 follow-up：「拿走那張圖內的那4個按鈕」）
- 改動：刪 `pos-app.tsx` 行 3042–3071 — 整個 `<div className="grid gap-3">` 包住 4 粒資訊卡 grid（當前桌台 A01 / 待結帳 自取04 / 待同步 0 / 打印設備 1）+ 外層 grid wrapper。
- 連帶：冇 dead code 要清 —— `activeTable` / `currentSettlementOrder` / `pendingQueue` / `printJobs` / `selectedTableStatus` / `deviceConfig` 全部仲有其他 call site（結帳 flow / dine-in 邏輯 / sync 計時 / print queue / printer config），只係刪咗佢哋喺 info cards 嘅 display call site。
- 保留：「快捷操作」標題 + 副標題（行 3038–3041）—— 用戶只講「那4個按鈕」，冇講標題。
- 驗證：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。

### 13. 會員搜尋區冗文清走（用戶 follow-up：「全部字拿走」+ 確認「保留第一行教學、拿走三段冗文」）
- 改動：`src/components/members-page.tsx`
  - 刪行 436–439 — 搜尋掣下面細字「契約 v3.2 §5.7：唔可以無輸入就列出全店會員；要睇全店名單請用會員通 Web /merchant/reports/users」（搜尋結果區已 inline 重述「只係今次搜尋筆數，唔係全店總數」，完全冗餘）。
  - 刪行 521–530 — 虛線 box 入面「POS 支援已註冊會員現場充值 (merchant_apply_pos_txn)…建檔並充值 (ensure-customer)…契約 v3.2：唔支援亦唔容許…」兩段技術 / 契約說明。
- 保留：虛線 box 第一行「輸入完整 8 位手機號碼（查單一會員），或用上方搜尋框以姓名／電話搜尋」—— 實用教學，唔係冗文。
- 驗證：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。

### 14. 客人線上取消請求有 POS 對應處理掣（用戶「動手改」）
- **根因回顧**：客人取消唔係改 `status`，而係寫 `change_request_type="cancel"` + `change_request_status="pending"|"requested"`。舊 POS 用 `hasPendingCancelRequest()` 擋走接受/拒絕掣，淨顯示玫瑰色「客人申請取消」badge → 收銀無法確認/拒絕，單卡死。
- **改動（4 個 file）**：
  1. `src/lib/ledger/online-order-actions.ts` — `OnlineOrderActionKey` 加 `confirm_cancel` / `decline_cancel`；新增 `getCancelRequestActions(order)`：當 `hasPendingCancelRequest` 時回 `[同意取消(slate,nextStatus=cancelled), 拒絕取消(violet)]`。
  2. `src/lib/ledger/order-actions.ts` — 新增 `respondToCancelRequest(orderId, "declined"|"approved")` 包 `update_change_request_status` RPC（參數 `p_order_id` / `p_status`）。⚠️ **RPC 名需同 Ledger 後端確認**：若實際叫 `clear_change_request` / `respond_change_request` 或參數名唔同，只改呢個函式。
  3. `src/components/quick-online-orders-panel.tsx` — 抽取 `renderCancelRequestActions()`；`renderModalActions` 同 strip 卡 action row 喺 `hasPendingCancelRequest` 時改顯示「同意取消 / 拒絕取消」。`runAction` 加 `confirm_cancel`（window.confirm → updateOrderStatus cancelled + void print，沿用 existing cancelled 分支）同 `decline_cancel`（→ respondToCancelRequest declined）。
  4. `src/components/online-orders.tsx`（/orders 頁）— 原本靜態「（請至 Ledger Web 確認）」玫瑰標籤 → 換成「同意取消」(confirmCustomerCancel → pushStatus cancelled) +「拒絕取消」(declineCustomerCancel → respondToCancelRequest declined) 兩粒掣。
- **行為**：同意取消 → 訂單 cancelled（已 terminal，自動離開 active list + cancel sound + void print）；拒絕取消 → change_request 清走，訂單恢復正常流程（接受/拒絕掣重新出現）。
- ⚠️ **待用戶確認**：`update_change_request_status` RPC 喺 Ledger 後端嘅真實名同參數。若唔存在，拒絕取消掣會 runtime error（已經 mapRpcErrorMessage 包住，唔會 crash，但功能唔到）。
- 驗證：`tsc --noEmit` 4 file 全過（只剩已知 layout.tsx LayoutProps 假陽性）。

### 15. 收據六項改進（下單時間／結帳時間／單價／總金額前綴／加購價／字型）
- **根因**：用戶截圖見收據 (1) 冇時間 (2) 加購冇價 (3) 總金額冇「總金額:」前綴 (4) 店名「澳門示範店」render 變形（mono 字型冇 CJK fallback）。
- **改動（6 個 file）**：
  1. `src/lib/types.ts` — `ReceiptSectionId` 加 `order_time` / `checkout_time`。
  2. `src/lib/escpos-template.ts` — `RECEIPT_SECTION_META` 加兩條 meta、`RECEIPT_BLOCK_DEFAULTS` 加兩條 default block、`DEFAULT_RECEIPT_TEMPLATE.order` 插入兩條；新增 `checkoutTimeLabel(order)`（`originalSettledAt` → `servedAt` → 空字串）；`buildReceiptContent` 寫 `order_time: formatMacauDateTime(createdAt)` + `checkout_time: checkoutTimeLabel(order)`，並將 `total` 改為 `\`總金額: ${formatMoney(order.total, currency)}\``。
  3. `src/lib/escpos-render.ts` — 加 `formatSpecLine({groupName, optionLabel, priceDelta})`：delta > 0 → `加購:加麵 $5`；delta < 0 → `加購:加麵 -$3`；0 → `加購:加麵`（唔加符號，避免冇加購嘅規格被誤會有收費）。`PrintItemLine` 加 optional `price?: number` 欄位（companion / android 收到會自動加印，未支援時唔影響主行）。
  4. `src/lib/print-jobs.ts` — receipt builder（line 67-74）改用 `formatSpecLine`；**廚房 builder（line 152）保持原樣**（廚房單唔印價，避免噪音）。
  5. `src/lib/mock-data.ts` — default receipt / kiosk templates 加 `order_time` (visible:true) / `checkout_time` (visible:false，因為大部份單結帳時先印，自動 populated) 兩個 block，order array 插入對應位置。
  6. `src/components/escpos-preview.tsx` — (a) 換 `font-mono` 為 explicit `PREVIEW_FONT_STACK`：`ui-monospace, ..., monospace, "PingFang TC", "Microsoft JhengHei", "Noto Sans CJK TC", "Source Han Sans TC", system-ui, sans-serif` 解決 CJK 變形；(b) `splitSpecLine()` 將 `formatSpecLine` 拼接嘅 `"加購:加麵 $5"` 拆返 (label, price)，spec line 用 `flex items-baseline justify-between` 將價錢右對齊；(c) 品項主行右側 `x{qty}` 後面追加 `{item.price ? $X : null}`（每個菜品小計）。
  7. `src/components/receipt-ticket-preview.tsx` — 用 `formatSpecLine` 渲染，加 `price` 欄位（quantity × unit price）。
  8. `src/components/print-center.tsx` — designer 預覽亦用 `formatSpecLine`（receipt 預覽）。
- **設計師改動**：商家喺「打印」頁 receipt / kiosk 模版而家可以開關「下單時間」/「結帳時間」兩個區塊；「總金額:」前綴直接烘喺 total 內容，商家改 alignment 都唔影響前綴。
- **驗證**：`tsc --noEmit` 通過（只剩已知 layout.tsx LayoutProps 假陽性）。`grep -c formatSpecLine` receipt builder 2 處（import + line 74），kitchen builder 0 處（保持原樣）。

### 16. 線下訂單卡片加「下單時間 HH:MM」（用戶「在自取的下面, 自助點餐機同一行」）
- **根因**：`quick-local-orders-strip.tsx` `OrderCard` 右下角只顯示來源 chip（🖥️ 自助點餐機），冇下單時間；繁忙時間商家要靠記憶判斷邊張單排前面。
- **改動**：單一 file `src/components/quick-local-orders-strip.tsx`
  - 新增 import `formatMacauTime` 從 `@/lib/format`（沿用專案 zh-HK + Asia/Macau + HH:MM 24h 慣例，見 escpos-template.ts / shift-page.tsx / print-center.tsx）。
  - `OrderCard` 開頭 `const orderTime = formatMacauTime(order.createdAt);`
  - 包住 `OrderSourceBadge` 嘅 div 改為 `flex items-center gap-1.5`，左側加 `<span aria-label="下單時間" className="...tabular-nums text-[11px] font-semibold text-slate-500">{orderTime}</span>`（同 badge 高度、字型大小一致）。
- **結果**：`[ 製作中 ]` 之下變 `[HH:MM 🖥️ 自助點餐機]` 一行並排；時間永遠同來源 chip 對齊，唔擠訂單號。
- **唔做**：① 唔動 `isSelfOrder` import（未用但沒影響，out-of-scope 移除）；② 唔動其他兩處 OrderSourceBadge（local-orders-panel / pos-app.tsx counterKioskOrders）—— 用戶只指 strip 卡。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。

### 17. 收據主行印「菜品原價」唔印 final 價（用戶截圖：招牌牛三寶 $105 應係 $95 + 燙青菜 +$10）
- **根因**：`print-jobs.ts` line 73 + `receipt-ticket-preview.tsx` line 25 + `print-center.tsx` line 327 三處用 `Math.round(it.price * it.quantity)`，但 `OrderItem.price` 已經係 `finalPrice = item.price + Σ spec.priceDelta`（`pos-app.tsx:1407 priceWithSpecs()`），所以主行印嘅係「原價 + 加購」、下面 spec row 又再個別加 `+$X`，**重複收費**：招牌牛三寶（基價 95 + 燙青菜 +10）→ 主行 `$105`、spec `加購:燙青菜 $10`，合計 `$115`，但 total 係 `$105`。
- **改動（4 個 file）**：
  1. `src/lib/escpos-render.ts` — 新增 `unitBasePrice(it)` helper：`it.price - Σ selectedSpecs.priceDelta`，clamp `Math.max(0)`。文件化「收據主行用基價、spec row 個別加價」嘅口徑，標明 kitchen builder 唔影響、companion / android 收到 `price` 欄位會自動加印（實紙未支援 `price` 欄位前都唔影響熱敏紙出單）。
  2. `src/lib/print-jobs.ts` — receipt builder（`buildTemplateReceiptJobs`）line 73 改 `Math.round(unitBasePrice(it) * it.quantity)`；廚房 builder 唔改（unchanged：kitchen 不印價）。
  3. `src/components/receipt-ticket-preview.tsx` — line 25 同改。
  4. `src/components/print-center.tsx` — designer receipt 預覽 line 327 同改。
- **效果**：
  - 招牌牛三寶（95 + 燙青菜 +10）：主行 `$95` + spec `加購:燙青菜 $10` = $105（== total，唔重複）。
  - 冇 specs：`unitBasePrice` = `it.price`，行為不變。
  - 全 0 delta specs（甜度 / 冰量 / 杯型）：`Σ delta = 0`，基價 = final 價，行為不變；`SYNTHETIC_SAMPLE_ORDER` designer preview 全部 0 delta，唔受影響。
- **唔做**：① 唔動 OrderItem / PrintJob 持久資料；② 唔動 `itemIdentity()`（price 變 part of identity 會引爆 orderNote-lock 嘅全套鎖定邏輯，out-of-scope）；③ 唔動 kitchen / label / kiosk template 預覽（kitchen builder 唔印價；label 用 `sampleOrder.items[0]` 唔入價 field；kiosk receipt 已經走 `buildTemplateReceiptJobs` 同一 helper）。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。

### 17. 自助單「確認出單 / 拒絕」按鈕組統一（用戶截圖指出兩粒掣視覺權重不一致）
- **根因**：3 個 call site（local-orders-panel.tsx 卡片+彈窗、pos-app.tsx 收銀端 strip）各自重複 inline 寫掣樣式 —— 確認 = emerald-600 實心、拒絕 = 白底 ring-1 slate（明顯唔同視覺權重），無 icon、無 disabled 防連點、無 size 統一（卡片 px-3 py-2、strip px-2 py-1.5）。
- **改動**：
  1. **新元件 `src/components/self-order-action-buttons.tsx`**：單一真源。確認=emerald-600 實心、拒絕=rose-600 實心（破壞性操作紅色警示）、兩粒都帶 check / x SVG icon、padding 統一 `rounded-xl` + `text-xs font-semibold`、有 `queueMicrotask` pending state 防快速雙擊、支援 `size="sm"|"md"` 兩種 padding。
  2. **`local-orders-panel.tsx`**：卡片底部（舊行 352-385）+ 查看彈窗（舊行 448-483）兩處 inline 按鈕都換成 `<SelfOrderActionButtons>`。
  3. **`pos-app.tsx`**：dine-in 模式 `counterKioskOrders` strip（舊行 「自助單 draft → 顯示確認 / 拒絕」block）換成 `<SelfOrderActionButtons size="sm" />`。
- **統一規則**：① 視覺權重對等（兩粒都實心，但顏色語義清楚：綠=接受、紅=拒絕）② icon 區分（✓ / ✗）③ pending 時兩粒都 disabled 防 race ④ 寬度 `flex-1` 平分；`size` 變體收銀端 strip 用 `sm`（px-2 py-1.5），訂單頁用 `md`（px-3 py-2）。
- **驗證**：`tsc --noEmit` 通過（只剩已知 layout.tsx LayoutProps 假陽性）。

### 16. Vercel build 修復（commit 12276dd 冇 `@/components/icons` → build fail）
- **根因**：`self-order-action-buttons.tsx` 舊版 import `{ Check, X } from "@/components/icons"`，但 `src/components/icons.tsx` 一直唔存在 → `next build` type-check 報 `TS2307: Cannot find module '@/components/icons'`，build exit 1。本地 `tsc --noEmit` 因為 `.next/types` 唔喺 local 見，所以冇預先 catch 到（Vercel build 先嚴格 check）。
- **修法**：① 新建 `src/components/icons.tsx` 匯出 `Check`（✓ path `"M5 12l4.5 4.5L19 7"`）+ `X`（✗ path `"M6 6l12 12M6 18L18 6"`），props `{ size?, strokeWidth? }` 用 `currentColor`；② 工作樹 `self-order-action-buttons.tsx` 移除本地 `Icon` 元件，改 import `{ Check, X }` from `@/components/icons`（同 committed 版一致，避免 icons.tsx 變孤兒 file）。
- **CSS warning（非致命）**：Vercel build log 有 `@media (min-width:某寬)` invalid media query warning —— 來源係 Vercel build cache（"Restored build cache from previous deployment"），唔喺我哋 source tree（`grep 某寬` 全 repo 0 hit）。warning 唔 block build，唔使改。
- **經驗**：Vercel `next build` 會跑 `tsc` type-check（唔似 local `tsc --noEmit` 有 `.next/types` 假陽性遮蔽）；任何新 import 嘅 module 一定要真係存在先可以 deploy。
- **驗證**：`tsc --noEmit` 通過（只剩 layout.tsx LayoutProps 假陽性）。

### 17. 自助單「可取餐 + 結帳」獨立並存 + 灰掉機制（kiosk / scan 快餐）
- **用戶需求**：快餐模式 kiosk / scan 單嘅「可取餐」同「去結帳」獨立並存，先做嗰個掣灰掉（disabled），兩個都做齊先出現「已取餐」按下 settled。
- **根因（舊 bug）**：用戶截圖：「按咗可取餐後，訂單內只剩查看」。原因：pos-app `quickPreparingOrders` filter 只包 `sent_to_kitchen`（任何 fulfillmentStatus）+ `paid + preparing`，但 strip UI gate 喺「`sent_to_kitchen + ready` 唔再 show 可取餐掣」 → 單跌入「兩 list 都唔包」嘅盲區，淨低「查看」掣。
- **改動（3 個 file）**：
  1. `src/components/quick-local-orders-strip.tsx` — `OrderCard` 加 `onCheckout?` prop；當 `onCheckout && isQuickCounterOrder(order) && isSelfOrder(order)`（kiosk/scan 快餐）→ 顯示 3-button 獨立模式：① 查看（永遠 active）② 去結帳（`disabled={status==="paid"}`、slate-700）③ 可取餐（`disabled={fulfillmentStatus==="ready"}`、orange-500）；`paid && ready` 時追加已取餐（emerald-600）按下 `markOrderCompleted` → settled。否則（pos 來源）保留舊單鏈。draft 自助單只顯示「查看」+ 查看彈窗嘅「確認出單 / 拒絕」（docs/87 §6）。
  2. `src/components/quick-mode-orders-bar.tsx` — 加 `onCheckout?` prop 同 pass-through。
  3. `src/components/pos-app.tsx` — `onCheckout={(orderId) => setPayingOrderId(orderId)}` 接到 strip（直接開付款 modal → `confirmPayment` → status="paid"）；`quickPreparingOrders` filter 補註釋解釋點解要包埋 `sent_to_kitchen` 任何 fulfillmentStatus（唔好將 `ready` 嘅自助單 filter 走）。
- **範圍**：限**快餐 + 自助來源**（kiosk / scan）。收銀台落單（source="pos"）嘅快餐單保留舊單鏈邏輯唔變。堂食模式嘅 `counterKioskOrders` 側欄原本已有「標記可可取」+「結帳」兩個掣，唔動。`/orders` 頁嘅 `LocalOrdersPanel` 查看彈窗**冇動**（用戶截圖顯示係收銀台 strip；`/orders` 頁冇付款 modal，要 checkout 需另外 dispatch event，唔喺今次範圍）。
- **狀態機（kiosk / scan 快餐）**：

  | status | fulfillment | strip list | mode | 按鈕 |
  |---|---|---|---|---|
  | draft | - | preparing | preparing | 查看 + 確認/拒絕（彈窗內） |
  | sent_to_kitchen | preparing | preparing | preparing | 查看 + 去結帳(active) + 可取餐(active) |
  | sent_to_kitchen | ready | preparing | preparing | 查看 + 去結帳(active) + 可取餐(grey) |
  | paid | preparing | preparing | preparing | 查看 + 去結帳(grey) + 可取餐(active) |
  | paid | ready | waiting | waiting | 查看 + 去結帳(grey) + 可取餐(grey) + 已取餐(active) |
  | settled | * | (none) | (none) | — |

- **驗證**：`tsc --noEmit` 通過（只剩已知 layout.tsx LayoutProps 假陽性）。3 粒掣 + 查看 ~172px 闊，card 240px 內無 wrap；4 粒掣（paid+ready waiting mode）會 flex-wrap 到第二行（234px > 216px 內容區），但卡高 180px 有 60px buffer 可容納。

### 18. 自助單掣從「灰掉」改為「消失」（用戶截圖 feedback：待取餐狀態太擠）
- **根因**：上版（§17）用戶體驗後反映：截圖「待取餐」（paid+ready）狀態出現「去結帳(grey)+可取餐(grey)+已取餐(active)」三粒掣擠埋一齊，視覺雜亂。用戶原先文字「先做完嘅動作會灰掉」嘅意思被誤解成「disabled 半透明」；截圖表達嘅真正意圖係「**直接消失，唔再 render**」。
- **修法（1 個 file, `quick-local-orders-strip.tsx`）**：將 `disabled={isPaid}` / `disabled={isReady}` 由「按鈕仲喺度、opacity-50」改為「成個 `!isPaid && ... ? <button> : null` 包住」—— 條件 false 就唔 render 任何 button element。`completeText` 按鈕邏輯（`isBothDone` 時出現）唔變。
- **狀態機重寫（kiosk / scan 快餐）**：

  | status | fulfillment | render 嘅掣 |
  |---|---|---|
  | draft | – | 查看 + 確認/拒絕（彈窗內） |
  | sent_to_kitchen | preparing | 查看 + 去結帳 + 可取餐 |
  | sent_to_kitchen | ready | 查看 + 去結帳 |
  | paid | preparing | 查看 + 可取餐 |
  | paid | ready | 查看 + 已取餐 |
  | settled | * | (離開 strip) |

- **「消失後無法再次顯示」嘅保證（單向閘）**：
  1. `status` paid 之後唔會回到 sent_to_kitchen —— `confirmPayment` 只寫 paid/settled；reopen 會去到 reopened（唔再落 quick strip）；`updateQuickFulfillmentInStore` 唔動 `status`。
  2. `fulfillmentStatus` 一旦寫成 ready 唔會被 reset —— `updateQuickFulfillmentInStore` 永遠寫 ready；`markQuickOrderCompletedInStore` 寫 ready+settled；join 操作唔 reset fulfillmentStatus。
  3. 所以 `isPaid` / `isReady` 一變 true 就永久 true，掣一消失就永久唔再 render。
- **畫面更新時機**：`useState` in pos-app 持有 `orders`；當 `updateQuickFulfillmentInStore` / `confirmPayment` / `markQuickOrderCompletedInStore` 完成後，佢哋 dispatch `pos-orders-changed` event（`saveOrders` 入面包咗）→ pos-app 嘅 listener `refreshOrders()` 重新讀 `loadOrders()` → 更新 React state → `quickPreparingOrders` / `quickWaitingOrders` `useMemo` 重算 → 重新 pass 落 strip → 掣按 `isPaid`/`isReady` 自動 render/唔 render。**唔需要任何 useEffect 追蹤狀態**——純粹 derive from `order.status` + `order.fulfillmentStatus`。
- **驗證**：`tsc --noEmit` 通過（只剩已知 layout.tsx LayoutProps 假陽性）。

### 18. 收據模板三處改良（下單/結帳時間前綴 + 實體單據菜價 schema + 預覽拉高問題保留）
- **根因（3 個獨立問題）**：
  1. `escpos-template.ts` line 202-203 直接食 `formatMacauDateTime(...)` 冇前綴，顧客睇唔出時間軸（截圖顯示兩行只係 `31/08/2026 23:52`/`31/08/2026 23:52`，冇標明邊個係下單、邊個係結帳）。
  2. 實體單據冇印菜價：`types.ts:497-502` 嘅 `PrintJob.items` schema 缺 `price?: number` field。`escpos-render.ts` 嘅 `PrintItemLine` type 有 `price`，但**序列化去 companion / android agent 嘅 JSON schema 由 `PrintJob.items` 決定**（TypeScript type 只係編譯時，runtime JSON schema 唔包 price）。所以即使 `print-jobs.ts` line 73 已寫 `price: unitBasePrice * quantity`，agent 收到 JSON 都冇 price field render。
  3. 預覽「菜品拉高」：`EscPosPreview` wrapper `lineHeight: 1.4` 對 m (14px) row = 19.6px linebox，比 ESC/POS 真實 m 行高（GS ! 0x11 雙寬單高 ≈ 同行高）**睇起嚟偏大**。但實際熱敏紙 dot-feed 唔受 CSS line-height 影響，**純 preview 視覺問題**。
- **改動（2 個 file）**：
  1. `src/lib/escpos-template.ts` — `buildReceiptContent` `order_time` 包 `下單時間: ` 前綴；新增 `checkoutTimeLabelWithPrefix(order)` helper 統一包 `結帳時間: ` 前綴（未結帳空字串 → 區塊自動隱藏）。保留原有 `checkoutTimeLabel` raw helper（其他地方或會用）。
  2. `src/lib/types.ts` — `PrintJob.items` schema 加 `price?: number`（optional，舊 renderer 唔識會忽略，forward-compatible）。文件化「companion / android 識嘅就會印，唔識就忽略（out-of-scope 唔影響主行）；POS preview EscPosPreview 已 render」。
- **唔做（明確保留）**：
  - **唔動 EscPosPreview line-height**：design == output 原則（docs/74）；m (14px) × 1.4 = 19.6px linebox 係 SIZE_PX 表嘅 heuristic，影響範圍太大（所有 size 嘅 preview row），動到可能破壞「整張單垂直節奏」嘅設計。用戶若想改 preview spacing 必須明確指示要乜效果。
  - **唔動 ESC/POS 真實出紙 side**：companion / android agent repo 不喺呢個 project，schema 加 `price?: number` 後佢哋需要 update renderer 嚟 render price（TODO 標記俾用戶跟進）。
  - **唔動 print-jobs.ts / receipt-ticket-preview.tsx / print-center.tsx**：上一輪 (§17) 已將 `price` 寫入 `PrintItemLine`；types.ts schema 加返 field 後自動透傳到 companion / android 收到嘅 JSON，**唔需要再改呢啲 file**。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。
- **🟡 Sandbox incident（已恢復）**：`git stash` 操作時 git 內部 throw `'2864b3fd...' is not a stash-like commit` 後 sandbox 出現 git corruption（HEAD ref 指向不存在嘅 object `2d73fb78b...`，reflog 全部 invalid，tsc 報 18 個 pos-app.tsx JSX 錯其實係 git fsck artifact）。**`git fetch origin`** 重新同步 refs 即刻恢復，working tree 嘅改動（device-settings / pos-app / escpos-template / mock-data / storage / types）全部健在、tsc 重新乾淨。**結論**：未來 sandbox 唔好跑 `git stash`（呢個 project 有 untracked `src/lib/pos/discount.ts`，stash 入面 conflict 觸發 corruption）；如有需要用 `git stash -u`（帶 untracked）或單純 `cp -r` backup 文件。

### 19. 折扣系統（設置 tab + 全單折扣 + 單品折扣）
- **需求（用戶 3 部）**：① 設置頁新增「折扣」tab，商家增刪折扣項目（名稱 + 百分比，介面唔顯 `%`）；② 結帳頁原「折扣金額」免填 input → 改「全單折扣」下拉（title 都係「全單折扣」），選折扣套用全單；③ 每個菜品加「折扣」掣 → 彈窗（同全單折扣下拉）→ 只套用該單品，顯示原價刪除線 + 折後價（`~~$100~~ $80`）。
- **數據模型**：
  - `DiscountPreset = { id, label, rate }`（`rate` 係百分比數字：80 = 8 折 = 收 80 元/原價 100；見 `types.ts`）。
  - `PosLocalSettings.discounts: DiscountPreset[]`（新增 field）；`OrderItem.discountRate?: number`（單品折扣，唔係 itemIdentity 一部分 → 已下單菜品都改得、唔會爆 order-note-lock）。
- **改動 file**：
  1. `src/lib/types.ts` — `DiscountPreset` interface + `PosLocalSettings.discounts` + `OrderItem.discountRate`。
  2. `src/lib/mock-data.ts` — `defaultPosLocalSettings.discounts` 預置 9折/85折/8折/5折。
  3. `src/lib/storage.ts` — `normalizePosLocalSettings` 加 `discounts` fallback（filter 掉壞 entry）。
  4. `src/lib/pos/discount.ts`（新）— `discountAmountFromRate(total, rate)` / `discountedUnitPrice(unit, rate?)` / `itemDiscountSaving` / `findDiscountPreset` / `newDiscountId`。`rate` 語義 = 百分比（0-100）。
  5. `src/components/device-settings.tsx` — 新增「折扣」tab（union + tab button + 增刪 UI：名稱 input + 百分比 input + 加入/刪除，rate 校驗 0-100）。`syncConfig()` 經 `serverSettings = {...localRest, floors:...}` 自動包埋 `discounts` 推上 server；`savePosLocalSettings` 落本機。
  6. `src/components/pos-app.tsx`（結帳頁）：
     - `discountValue` state 由「免填金額字串」改為「已選折扣 preset id」（"" = 冇折扣）。
     - `discountAmount` useMemo 改由 `localSettings.discounts.find(id)→discountAmountFromRate(paymentBase.total, rate)` 計（移到 `paymentBase` 之後宣告，避免 used-before-declaration）。
     - 原 折扣金額 input → `<select>` 全單折扣（options = 冇折扣 + 各 preset.label），title 改「全單折扣」。
     - 每個 cart item 動作列加「折扣」掣（amber 高亮當有折扣）→ 開 `itemDiscountEditor` modal（同款下拉）→ `applyItemDiscount(itemKey, rate|undefined)` 寫 `item.discountRate`。
     - 菜品價錢行：有 `discountRate` 時顯示 `原價(line-through) + 折後價(amber)`。
     - `orderTotals` subtotal 摺入單品折扣：`unit = price * rate/100`，tax/service 跟減（正確口徑）。
     - 載入已存單：`matchDiscountId()` 由 `order.total + discountAmount`（pre-discount 總額）反向配對 preset id，搵唔到（preset 被刪）就 ""。
  7. `src/lib/print-jobs.ts` — receipt builder 用 `discountedUnitPrice(unitBasePrice(it), it.discountRate)` 令收據主行反映單品折後價。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。
- **同步到 server 嘅口徑**：`PosOrder.discountAmount`（money）照舊只儲全單折扣部分；單品折扣經 `items` JSONB（`discountRate` field）自動透傳，唔使 migration。`/api/pos/sync` 接收 `discount_amount` + raw items，冇改。
