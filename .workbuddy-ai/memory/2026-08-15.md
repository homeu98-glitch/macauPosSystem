# 2026-08-15

## P2 美容院 Package 玩法（結帳抵扣 + 購買贈送 Ledger）

承接 P1（套票模板 CRUD + 客戶套票卡）。SQL 0006 用戶已執行。

落實 doc 28 §3.4 P2：

- `checkout.tsx`：結帳「套票抵扣」一步。依 `booking.customerId` 取 active 套票卡（status=active 且未過期），`computePlan()` 自動對每個 booking 服務項扣 1 次（先購先扣）；`deductedAmount` 從 `grandTotal` 減除；`handleSettle` 訂單存妥後才寫回套票卡次數（`applyCustomerPackageDeductions`，used_up 置狀態），經 sync 上雲。訂單 `SalonOrderItem.note` 標「套票抵扣」。
- `SalonPosOrder` 加 `packageDeduction?: number`；收據 `print.ts` 加「套票抵扣 -X」行 + 項目注記。
- `mock-ledger.ts` 加 `applyMockLedgerBonus(identifier, {points, balance})`；`customer-profile.tsx` `buyPackage` 購買當下寫入贈送積分/儲值（mock 本地；真 RPC 只換此函式）。
- `tsc --noEmit` 僅餘 `src/app/layout.tsx` 的 `LayoutProps` 誤報（next build 生成全局型別，與本輪無關）。

待用戶 dev box `npm run lint && npm run build` + push Vercel（P1+P2+上輪 build 修復均未 push）。

P3（報表 + 即將到期提醒）未做。

## P-積分兌換（per-item 積分價 + mix）

用戶要每個服務項目各自設「積分價」（如 項目1 $200 = 5000 分），結帳可逐項用積分兌換、支援部分積分+現金 mix；設置與結帳/客戶檔案都顯示。AskUserQuestion 確認：每項目個別積分價（非全局匯率）、mix、結帳+客戶檔案。

- `types.ts`：`SalonServiceItem.pointsPrice?`；`SalonPosOrder.pointsDeduction?` / `pointsRedeemed?`。
- `settings.tsx`：服務項目編輯器加 `pointsPrice` 欄位 + `emptyItem` 加 `pointsPrice:0`。
- `checkout.tsx`：新增「積分兌換」區塊（套票抵扣與小費間）。逐項 checkbox + 數字輸入（mix）；`pointsDeduction` 從 `grandTotal` 減除；`canSettle` 加 `!pointsOverflow`；`handleSettle` 先 `applyMockLedgerPointsPayment` 扣積分再存單；項目 note 串「積分兌換 N分」；收據/摘要加行。
- `mock-ledger.ts`：加 `applyMockLedgerPointsPayment(identifier, points)`（本地扣 ledgerPoints，不足回傳 ok:false）。
- `customer-profile.tsx`：加「積分兌換力」區塊（列 pointsPrice>0 服務 + 可兌 N 次 = floor(ledgerPoints/pointsPrice)）。
- `print.ts`：收據加「積分兌換 -X（N分）」行。
- `tsc --noEmit` 零錯誤（僅 layout.tsx LayoutProps 已知誤報）。doc 28 補 §5.10 + §5.11 待 push。

設計關鍵：每分等值 = price/pointsPrice，隱含於 per-item 雙價，**不做全局匯率設定**。

全部改動（P1+P2+P3+積分兌換+build 修復）仍未 push；待用戶 dev box lint/build + push Vercel。
