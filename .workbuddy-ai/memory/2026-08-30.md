# 2026-08-30

## Companion 列印變形修正 — P0-A/P0-B/P1-B 落碼（docs/81 執行）

用家批准 docs/81 計劃後，執行 Companion source 修正。

### 背景
第三方研究（SUMMARY.md + round-research-preview-vs-paper.md）揭穿 docs/80 嘅 B1（APK 丟 template）唔係出事嗰張紙嘅主因——用家行緊 Windows Companion → USB RAW ESC/POS → POS-80，唔經 native.ts。真主因係 `companion-server.mjs` 嘅 `setStyle` 發 `ESC!0x30` + `textLine` 喺 CJK 行再發 `GS!0x30` → 相乘（ASCII 4×4、中文 2×2）= 版面撕裂。

### 改咗乜

**`C:\dev\desktop-companion\companion-server.mjs`**：
- **P0-A 互斥放大**：`setStyle` 加第三參數 `isCjkLine`。`kanjiEnlarge=="GS!"` 時 CJK 行 `ESC!` 降為 `0x00`（唔帶放大），放大全靠 `GS!`；純 ASCII 行照常 `ESC!SIZE_BYTE[size]`。`FS!` 路線用 `ESC!`+`FS!`（作用對象唔同唔相乘）。
- **P0-B 標準 nibble**：新增 `GS_SIZE_BYTE = {s:0x00, m:0x01, l:0x11}`（標準 Epson nibble `(h-1)<<4|(w-1)`）；`FS_SIZE_BYTE = {s:0x00, m:0x04, l:0x0C}`；舊 `KANJI_SIZE_BYTE` 改名 `KANJI_SIZE_BYTE_LEGACY` 保留俾 `printer.gsNibble=false` fallback。預設走標準 nibble `0x11`。
- **P1-B resetMagnify**：模板路徑 + 舊 fallback 路徑結尾都加 `resetMagnify()`（`GS!0x00` + `ESC!0x00` + `ESC 3 30`），防放大狀態殘留到下一張。
- 註解區改為三套指令對照表（ESC!/FS!/GS! 作用對象 + 語意 + 值表）。

**`C:\dev\desktop-companion\test-kanji-size.mjs`**：
- 舊版 V1-V6 每行都帶 `ESC!0x30`+`GS!/FS!`（冇分離 → 冇校準 nibble）。重寫為 4 個 A/B 變體：
  - V-A 只 `GS!0x11`（標準 nibble，唔發 ESC! 放大）→ 預期 ASCII=2×（相乘修好）
  - V-B 只 `GS!0x30`（舊 ESC bit 語意）→ 判定 0x30 喺真 Epson = 寬×4
  - V-C 只 `ESC!0x30`（管 ASCII，唔發 GS!）→ ASCII=2× 中文唔變大
  - V-D `ESC!0x30`+`GS!0x30`（重現舊 bug）→ ASCII 應 4×4
- 對照行：R-1（12×A）、R-2（純中文）、R-3（日期時間）、R-4（規格兩行重疊）

**`C:\dev\desktop-companion\package.json`**：版號 `0.1.14` → `0.1.15`

### 驗證
`node --check` 兩檔都 0 error。`tsc --noEmit` 唔適用（.mjs 唔係 TS）。

### 待用家做
1. `npm run dist` 打包 Companion 0.1.15 exe → 報版號
2. `node test-kanji-size.mjs <ip>:9100` 印 A/B 測試紙
3. 對照 V-A（應正常 2×）vs V-D（應巨怪 4×4）→ 坐實相乘 bug + 修正確認
4. 若 V-A 中文唔變大 → 設 `printer.gsNibble=false`（韌體非標準，走舊 0x30）
5. 真紙驗收：廚房單桌台/時間 size=l → 中英文放大一致、唔重疊、唔爆行

APK 側同邏輯另案，唔阻塞 Companion 驗收。Web 預覽危險提示（P2）+ docs/80 修訂（P3）最後做。

## A/B 實機結果 + post-fix 確認（22:30 ✅）

用家印咗 A/B 測試紙，結果：
- V-A（只 GS!0x11）→ ✅ PASS
- V-B（只 GS!0x30）→ ❌ FAIL
- V-C（只 ESC!0x30）→ ✅ PASS
- V-D（ESC!0x30+GS!0x30）→ ❌ FAIL（同 V-B）

**V-B/V-D 共通原因**：兩者都用 `GS!0x30`。V-A 用 `GS!0x11` 成功 → POS-80 食標準 Epson nibble 語意。`0x30` 喺 nibble = 寬×4 高×1，唔係 2×2。即 `0x30` 唔係「非標準韌體偏好」而係純粹寫錯（docs/71 V5 遺留）。

### Post-fix 改動（Companion 0.1.16）
- 移除 `KANJI_SIZE_BYTE_LEGACY` 常數 + `gsNibble` flag（0x30 確認係錯嘅，唔使保留 fallback）
- `gsByte()` 簡化為 `GS_SIZE_BYTE[size] || 0x00`（永遠 0x11）
- `test-kanji-size.mjs` v3：F-1~F-5 確認測試（F-2 = renderEscPos 修正後嘅實際 CJK 路徑輸出，最關鍵）
- 版號 0.1.15 → **0.1.16**

### 待用家
1. `npm run dist` 打包 0.1.16 → 報版號
2. 印確認測試紙：`node test-kanji-size.mjs <ip>:9100`
3. 預期：F-1 ✅、F-2 ✅、F-3 ❌（證明互斥必要）、F-4 ❌（舊 bug 對照）、F-5 ✅
4. F-2 PASS = 修正生效
5. 真紙驗收：廚房單桌台/時間 size=l → 中英文 2× 一致、唔重疊、唔爆行

## 美團式打印機設置重設方案（23:00，方案已定稿未實作）

用家提供 10 張美團打印機添加流程截圖，要求重新設計打印機設置功能：取代現有所有設置方法，全部改為手動選擇、步驟引導，隱藏所有技術性設置。

方案 `docs/82-meituan-style-printer-setup-redesign.md` 核心：

**三步 wizard**：
- Step 1：選用途（廚房機/小票機/標籤機）+ 連接方式（LAN/USB）
- Step 2：選型號（LAN=型號列表, USB=Companion 偵測設備列表）→ 選定後鎖定變灰
- Step 3：LAN=手動輸入 IP 測試連接；USB=自動偵測設備列表從中選擇

**隱藏嘅技術欄位**：VID、PID、偵測機型、OS 打印端口、charset、kanjiEnlarge、lineSpacing——全部由型號表 `USB_PRINTER_DB` 自動帶入，商家完全唔見。

**鎖定規則**：用途、連接方式、型號一經選定不可更改（變灰）；唯一可改=IP 地址（LAN）；要改=刪除重新添加。

**唔改**：`DevicePrinterConfig` 介面唔改（數據結構唔變，只 UI 改）、列印路徑唔改、Companion renderer 唔改。

**新增組件**：`printer-wizard-modal.tsx`（三步 wizard）、`printer-card-v2.tsx`（簡化卡片）。

**與 docs/81 關係**：互補——81 修正引擎（GS!0x11 + 互斥），82 簡化駕駛介面（自動帶入 kanjiEnlarge）。商家揀「商頌 POS-80」→ 自動 `kanjiEnlarge=GS!` → Companion 行 0x11 互斥 → 出紙正確。

待用家批准先實作。

## 美團式打印機設置 — 實作完成（17:30）

用家批准「好, 請開始」後，全量實作 docs/82 三步 wizard + 簡化卡片。

### 新建檔案

1. **`src/components/printer-wizard-modal.tsx`** — 三步 wizard：
   - Step 1：用途卡片（廚房機/小票機/標籤機）+ 連接方式卡片（LAN/USB）+ 分區選擇（如需要）
   - Step 2：LAN=型號列表（`getLanModelOptions()` from `printer-models.ts`）；USB=Companion 偵測 `enumerateCompanionUsbPrinters()` 自動掃描
   - Step 3：LAN=IP 輸入 + `probeLan()` 測試按鈕；USB=已連接確認
   - 型號/連接方式一經選定 🔒 鎖定，底部摘要帶鎖標
   - `complete()` 建構完整 `DevicePrinterConfig`，技術欄位（charset/paperSize/kanjiEnlarge）全部自動帶入

2. **`src/components/printer-card-v2.tsx`** — 簡化打印機卡片：
   - 只顯示：狀態燈 + 名稱 + 用途/連接方式/型號（🔒）+ IP（LAN 可改）+ 分區（zone/label 可改）+ USB 已連接確認 + 啟用開關 + 刪除 + 測試打印
   - 所有技術欄位（VID/PID/charset/kanjiEnlarge/lineSpacing/usbPort/copies）完全隱藏
   - `PrinterEmptyState` 空狀態引導添加

### 修改檔案

3. **`src/components/device-settings.tsx`**：
   - import `PrinterWizardModal` + `PrinterCardV2` + `PrinterEmptyState`
   - 加 `printerWizardOpen` state
   - 刪舊 L801-1084 打印機表單（全技術欄位暴露），改為 `PrinterCardV2` 列表 + 「添加打印機」按鈕 + `PrinterWizardModal` 觸發
   - 刪 `handleRoleChange`（wizard 鎖定 role，唔再改）
   - 刪 `printerReadiness`（`PrinterCardV2` 自帶 `readinessDot`）
   - 刪 `resolveUsbMeta` import（不再用）

4. **`src/lib/print-bridge/printer-models.ts`**：
   - 新增 `LanModelOption` interface + `getLanModelOptions()` 函式（扁平化 `USB_PRINTER_DB` + 通用兜底 + 商頌 POS-80）

5. **`src/lib/print-bridge/companion.ts`**：
   - 新增 `probeLan(ip, port=9100)` → `POST /api/probe-lan`，回 `{ ok, error? }`
   - Companion 離線時 fallback 做 IP 格式驗證

6. **`C:\dev\desktop-companion\companion-server.mjs`**：
   - 新增 `POST /api/probe-lan` endpoint：收 `{ip, port}` → `net.Socket` TCP connect 3s timeout → 回 `{ok:true}` / `{ok:false, error}`
   - 註解區更新 endpoint 列表

### 驗證
- `tsc --noEmit`：唯一 `layout.tsx(37) LayoutProps` 已知誤報（standalone tsc 見唔到 `.next/types`），唔影響 Vercel build
- `eslint`：全部 0 errors 0 warnings（printer-models 修正 `vid`/`pid` unused → 改 `[, vendor]` / `[, model]`）

### 待用家
1. `npm run dev` 試跑 → 設置頁 → 打印機列表 tab → 添加打印機 wizard 流程
2. `npm run dist` 打包 Companion 0.1.16 含 `POST /api/probe-lan` → 測試 LAN 連接按鈕
3. F-1~F-5 確認測試紙仍未跑（需 Companion 0.1.16 打包）
