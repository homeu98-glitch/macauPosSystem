# 2026-08-26 工作記錄

## 廚房單打印格式殘缺修復（companion renderEscPos，已修，要 rebuild exe）

用家報：落單經廚房打印機出單，但紙上只有「廚房打印機」字樣 + 菜名，數量/備註/桌號/單號全部遺失；螢幕 KitchenTicketPreview 有完整內容。

### 根因（companion-server.mjs）
- `renderEscPos(job, printer)` 係個極簡 stub：只印 `job.storeName || printer.name` 做抬頭（storeName 冇傳入 → fallback 去 `printer.name`＝「廚房打印機」），再 `for` 每 item 只印 `it.name`（有 note 才補 `  ${note}`）。**完全冇用 `quantity` / `specs` / `tableName` / `orderNo` / `createdAt`**。
- 雙重失誤：`/api/print` handler 原 `const { job, printer } = parsed` **冇收 `storeName`**（POS 經 payload.storeName 傳門店名，PrintJob 本身無 storeName 欄）→ storeName 被 drop → 抬頭錯。

### 修法（companion-server.mjs，非 git，要 rebuild）
1. `renderEscPos` 重寫對齊螢幕 `kitchen-ticket-preview.tsx`：門店名（bold center）→ 類型標題（廚房/收據/標籤，按 `printer.role`）→ 分隔線 → 枱號 / 單號 / 類型 / 時間 → 分隔線 → 每項 `name  x數量`(bold) + 規格(縮排 ·) + 備註(縮排 注：) → 分隔線 → 留底 footer。
2. `/api/print` handler 改 `const { job, printer, storeName } = parsed` + `if (storeName) job.storeName = job.storeName || String(storeName);` 注入門店名。
- `node --check companion-server.mjs` 過。receipt / salon 單共用同一 renderEscPos（泛型處理缺欄位，唔會 crash）。

### 部署
- 純桌面 companion 改動，**唔使改 POS / 唔使 push Vercel**。但 **必須喺 Windows dev box `npm run dist` 重 build exe** 先生效。
- 桌面版本升 **`0.1.7` → `0.1.8`**（按用家 standing instruction 主動報版本號；0.1.7 含 printLan 300ms drain + 版本號讀取，0.1.8 再加完整格式）。rebuild 後 Companion 視窗 / `/api/health` 顯 0.1.8，可核對 exe 含最新修正。
- 注意：Android APK 有自己嘅 EscPosRenderer.renderKitchenTicket（kitchen-ticket-preview.tsx 註解提過），今次只修桌面 companion 路徑；APK 側如要一致要另查 native.ts / EscPosRenderer。

## 62 · 「立即同步」vs「刪除全部訂單」行為釐清（2026-08-26，只分析未實作 · 待用家 confirm）

用家問兩點：①「立即同步」係咪直接從 DB 撈舊訂單落嚟？②「刪除全部訂單」係咪一次性連 DB 一齊刪？

### ① 立即同步 = 只上傳（push），唔撈 DB
- `pos-app.tsx:3054-3060`「立即同步」btn → `syncNow(queue)`（1835-1860）。`syncNow` 只 `POST /api/pos/sync` 推本地 queue 事件（ORDER_CREATED/UPDATED/DELETED、PRINT_JOB_CREATED、付款事件）去伺服器，標 `synced`；**完全無 GET / 無撈 orders**。
- 所以「舊訂單全部同步下來」並唔係呢個 btn 做嘅。真正「落」來自 `loadRuntimeState`（574-637）→ `GET /api/pos/state?storeId=X`（route.ts:24-26 讀 `pos_orders` Supabase DB，store 隔離）。觸發時機：mount / realtime `(re)subscribe`（`onResubscribed` 691-695，斷線重連都會）/ `backToTables`。
- 結論：用家見到嘅「舊訂單」確實係直接從 DB `pos_orders` 撈出嚟，但係經 backfill（reconnect/返枱面）落嚟，唔係「立即同步」btn 本身。網上 Supabase 模式 = 真 DB；mock 模式 route 返 `orders:[]`。

### ② 刪除全部訂單 = 設計上連 DB 一齊刪（一次性）
- `local-orders-panel.tsx:155-184` `handleDeleteAllOrders`：①`DELETE /api/pos/orders?storeId=X` → 伺服器 `pos_orders DELETE WHERE store_id=X AND online_order_id IS NULL`（route.ts:65-69）；②`saveOrders([])` 清本地；③filter 走 ORDER_* queue events 免重推；④dispatch `pos-orders-changed`。
- **範圍限制**：只刪 `online_order_id IS NULL`（店內線下訂單）；Ledger 線上單（online_order_id 唔空）按設計**保留**（免同會員餘額/線上單脫鉤）。btn 文案「刪除全部訂單」實際 = 「全部線下訂單」（toast 都寫「已刪除全部線下訂單」）。
- **落差 / 復活風險**：
  - A) Ledger 線上單被 exclude → 下次 backfill 照樣撈返嚟，用家以為「刪咗又返」。
  - B) `handleDeleteAllOrders` **冇寫 tombstone**（`deletedOrderIds`）——單筆刪除 `deleteOrderPermanently` 有，但「全部刪」靠 DB DELETE 完整。若 mock 模式（無 supabase）DELETE 返 `{deleted:0}` 當成功、DB 實際冇嘢刪（route.ts:58-61），本地清咗但 DB 仲有 → 下次 backfill 復活。
  - C) 其他終端唔會自動清（confirm dialog 已講明），佢哋 backfill 仍見到。

### 待 confirm（用家要「先出方案」）
- 方向：①只釐清（改 UI 文案/confirm 寫明「只刪線下單、不含 Ledger」+ 寫 docs）；②硬化（delete-all 加 tombstone + 顯示 `data.deleted` 筆數 + 確保 backfill 唔復活線下單）；③擴範圍（加開關「一併刪 Ledger 線上單」，危險，默認關）。
- 暫未改碼。

## 63 · 刪除範圍確認 + 「立即同步」必要性評估（2026-08-26，只出 plan · 待用家 confirm）

用家追加：Ledger 單無異議（非佢哋系統）；需求＝「立即刪除」要刪 **本地線下 DB + macau-pos DB 中登入店所有訂單**；並評估「立即同步」btn 喺現架構仲有冇必要。

### 刪除邏輯確認（範圍 vs 需求）
- 本地線下 DB = localStorage（`saveOrders([])`，local-orders-panel.tsx:172 清晒本機 orders）。
- macau-pos DB = Supabase `pos_orders`：`DELETE /api/pos/orders?storeId=X` → `route.ts:65-69` `DELETE WHERE store_id=X AND online_order_id IS NULL`。
- 登入店隔離：`storeId = loadAuthSession()?.merchantId`（local-orders-panel.tsx:156-157）；null 時擋「請重新登入」。
- **結論：符合需求**——登入店嘅線下單本地+DB 雙刪；Ledger 單（online_order_id 唔空）按用家無異議保留。
- 冇復活漏洞：delete-all 清走 ORDER_* queue events（174），唔會重推；DB DELETE 係直刪非經 queue。
- **待硬化（可選）**：①冇 tombstone → 若 DB DELETE 失效/RLS 擋/無 supabase（mock 返 `{deleted:0}` 當成功，`route.ts:58`）→ backfill 復活線下單，違「DB 全刪」要求；②handler 冇睇 `data.deleted` 筆數，0 刪都當成功。建議 toast 顯 `deleted` 筆數 + 0 時警告。

### 「立即同步」必要性評估（current arch）
- 該 btn（pos-app.tsx:3054）= `syncNow(queue)` = **只 push 本地 queue events 去 `/api/pos/sync`**；`syncNow` 本身唔撈 DB（落單係 `loadRuntimeState` backfill，`route.ts:25` 讀 `pos_orders`）。
- `/api/pos/sync`（route.ts）會將 ORDER_CREATED/UPDATED/SETTLED upsert 入 `pos_orders`、ORDER_DELETED 真刪 → **寫入本就經 syncNow 自動推**（每次動作 799/2139/2478/2625 都 call）。所以線上時寫入已自動上傳。
- 「落」嚟（pull）係 realtime `(re)subscribe` / `backToTables` 自動 `loadRuntimeState`，唔使撳 btn。
- 手動 btn 剩餘價值：①離線累積事件 reconnect 後補傳；②realtime/網絡卡住時手動 retry flush。
- 問題：label「立即同步」誤導（似雙向，實際只 push；用家以為佢「撈線下單返嚟」其實唔係）。
- 選項：A) 保留 + 改名「補傳離線資料」（push-only 誠實 label，低風險）；B) 保留 + 改真雙向（onClick 同時 syncNow(queue)+loadRuntimeState()，即時上傳+即時撈 DB，最符用家期望）；C) 移除（依賴自動 realtime，失去離線補傳 fallback，唔推薦）。

### 待 confirm（用家要 plan）
- 刪除：確認現狀 OK / 加 tombstone + 顯 `deleted` 筆數硬化。
- 立即同步：A 改名 / B 改雙向 / C 移除。
- 暫未改碼。

## 63b · 刪除硬化 + 移除「立即同步」已實作（2026-08-26）

用家 confirm：刪除邏輯加 tombstone + 顯筆數硬化；「立即同步」btn 移除。

### 刪除硬化（local-orders-panel.tsx handleDeleteAllOrders）
- import `addDeletedOrderIds`（storage.ts:433）。
- 刪 DB 前先 `loadOrders().filter(o => !o.onlineOrderId).map(o=>o.id)` 拎本機線下單 id → `addDeletedOrderIds(localOfflineIds)` 記 tombstone。**只記線下單**（onlineOrderId 空＝DB 真刪嗰批）；Ledger 線上單（onlineOrderId 唔空）唔記，DB 冇刪、用家無異議保留，下次 backfill 照常顯示。
- toast 改回報 DB 實際刪除筆數 `data.deleted`：>0 → `已刪除 N 筆線下訂單（本地 + DB）`；0 → warning `已清除本機訂單，但 DB 未刪除任何單（可能離線 / mock 模式）`。`setToast` 喺呢檔係 string setter，warning 用純字串（唔用 {tone,message} 物件，否則 tsc TS2353）。
- 效果：即使 DB DELETE 失效（RLS/mock 返 `{deleted:0}`），tombstone 令本機 backfill 唔會復活線下單；toast 0 筆即時提示用家 DB 冇真刪。

### 移除「立即同步」btn（pos-app.tsx:3049）
- 移除 `{offlineMode ? banner : <button>立即同步</button>}` 嘅 button 分支，只留 offline banner（文案改「恢復網絡後會自動補傳資料」）。
- 安全：移除前已確認 `pos-app.tsx:794-803` 有 30s 自動批量 `syncNow(pendingQueue)`（offline 時 return、online 每 30s flush pending）→ 離線累積事件 reconnect 後 30s 內自動推 DB，移除 btn **無 regression**。`syncNow` 仍被 799/2139/2478/2625 用，唔會 unused。
- `tsc --noEmit` 零新 error（僅 layout.tsx 已知誤報）。web-only，Vercel push 即生效，唔使 rebuild exe/apk。

### 部署
- 兩項都係 web-only；待用家 commit + push。
- 範圍不變：Ledger 單仍 exclude（用家無異議）。

## 64 · 打印模板 spec/備註 字型大小不可調（analysis · 2026-08-26）

用家問：模板內 spec/備註 內容點解冇得調字型大小 / 物件尺寸。結論＝架構性寫死，唔係 bug、唔係個別設定鎖死。

- **樣式模型（資料綁定約束）**：`EscPosBlockStyle = {visible, size, bold, align}`（types.ts:217）只有 block-level `size`（s/m/l）。specs / note 唔係獨立 block，只係 `items` block 嘅 sub-line → model 根本冇 `subSize` / `specSize` 欄位，設計介面無嘢可 bind。
- **render 演算法**：`renderEscPosLines`（escpos-render.ts:35）將 items 成塊 emit 出一條 `{kind:"items", size:b.size}` — size 只 apply 到「菜名 + x數量」主行，sub-line size 冇傳落去。
- **三條 renderer 全部寫死 s**：
  - web 預覽 `escpos-preview.tsx:33,38` → spec/note sub-line `fontSize: SIZE_PX.s`（固定）。
  - 桌面 Companion `companion-server.mjs:267-268` → `textLine(spec, "s", false, align)`（固定）。
  - Android（未做）按設計會 mirror 同一寫法。
- **設計介面鎖定**：`print-center.tsx:443-444` designer 只提供 per-block `size` select（s/m/l），冇 spec size 控制項 → 商家改 items block size 只放大「菜名+x數量」，spec 仍係細字。
- **列印引擎限制**：ESC/POS thermal 只有 3 檔字型（`ESC ! n`，SIZE_BYTE s=0x00/m=0x20/l=0x60）；冇任意物件尺寸 / px 概念。「物件尺寸」對熱敏機無意義，只係字型大小。sub-line 寫死 s 係刻意（spec 係次要細節、細字易讀；避免每行 spec mid-line toggle 字型，慳指令+防錯）。
- **要啟用可調**：擴 `EscPosBlockStyle` 加 `subSize`（或 items block 專屬 subSize）+ 三 renderer 改讀 subSize + designer 加控制項（四處同步）。屬新功能，待用家決定。

## 65 · 規格/備註 可獨立調大小 已實作（2026-08-26）

用家 confirm：規格(spec) 太細睇唔到，要可獨立調。採 **獨立 subSize** 做法（非跟菜名size）。

- `types.ts`：`EscPosBlockStyle.subSize?` + `EscPosTemplateSnapshot.blocks.subSize?` 改 **optional**（避免 mock-data.ts 40 個 inline literal 全改；所有 reader 用 `?? "s"` / `|| "s"` fallback）。
- `escpos-template.ts`：`block()` helper 加 `subSize = "s"` 第五參（所有 call site 唔使改，舊行為保留）；`buildSnapshot` 已 spread 全 block → 自動帶 subSize。
- `escpos-render.ts`：`EscPosLine` items variant 加 `subSize`；`renderEscPosLines` items 分支傳 `b.subSize ?? "s"`。
- `escpos-preview.tsx`：spec / note sub-line 由寫死 `SIZE_PX.s` 改 `SIZE_PX[line.subSize ?? "s"]`。
- `companion-server.mjs`：spec / note `textLine(..., b.subSize || "s", false, ...)`；**version 0.1.9 → 0.1.10**（renderer 改，要 Windows dev box `npm run dist` rebuild）。
- `print-center.tsx`：`renderDesigner` 當 `sel === "items"` 加「規格 / 備註大小」select（細/中/大）→ `patchBlock(kind, "items", { subSize })`；只 items block 顯示。
- `storage.ts`：`mergeTemplateBlocks` 已 merge over DEFAULT（含 subSize:"s"）→ 舊店自動補 "s"，零遷移。
- `tsc --noEmit` 零新 error（layout.tsx 誤報照避）；`node --check companion-server.mjs` OK。
- 範圍：Label `specs` 本身獨立 block（可調）唔受影響；`KitchenTicketPreview`（無 template fallback 預覽）未改（用家經 template 唔經佢）。
- web-only（Vercel push 即生效）+ Companion 0.1.10 rebuild。待用家 commit+push。

## 67 · 用家報「規格/備註大小」搵唔到 → 根因＝選項隱藏 + Label 無 items（2026-08-26）

用家喺模板設置搵唔到「規格 / 備註大小」，3 個模板都好似冇。

### 根因（code 實證，非缺失）
- 控制項確實喺 `print-center.tsx:472-487`，但 `sel === "items"` 先 render → **只喺選中「菜品明細」區塊時出現**。用家大機會喺第啲區塊（門店名/單號/總計…）搵，所以睇唔到。
- **Receipt / Kitchen**：`*_SECTION_META` 都有 `items`("菜品明細") → 揀佢就見到控制項，subSize 經 `renderEscPosLines` 喺 `EscPosPreview` 即時反映（設計器預覽 line 520 統一用 EscPosPreview，三模板都 honour subSize）。
- **Label**：`LABEL_SECTION_META` **無 `items` block**（label 係 per-item）；「規格」=`specs`、「單品備註」=`item_note` 係 top-level section，各自有 字型大小/粗體/對齊 可直接調 → label 根本唔使 subSize 控制。

### 修法（發現性，低風險）
- `print-center.tsx` 設計器加 amber hint：當 `sel !== "items"` 且 `kind==="receipt"||isKitchen` 時，顯「想調整每道菜的規格/備註字體？請點選『菜品明細』區塊」提示。
- `tsc --noEmit` 零新 error（layout.tsx 誤報照避）。web-only，Vercel push 即生效。
- 重要提示用家：呢個控制項 + subSize 係 §65 嗰輪加嘅，**未有 push 就 live 見唔到**；確認 live = HEAD 含 §65 再查。

## 67b · 打印內容 list view 方案 B（分層卡片）已落碼（2026-08-26）

用家喺 A/B/C 三方案中揀咗 **B 分層卡片清單**：序號前綴 `1. 2. 3.` + 品名行（加粗）+ 名下虛線規則線 + 規格/備註成組縮排（無 bullet）+ 菜間留白。取代舊式 `flex justify-between` 左右排列。

落點：
- `types.ts`：加 `EscPosItemsLayout = "inline"|"card"|"stacked"`；`EscPosBlockStyle.layout?` + `EscPosTemplateSnapshot.blocks.layout?`。
- `escpos-template.ts`：`block()` helper 加第 6 參 `layout`；`DEFAULT_RECEIPT/KITCHEN_TEMPLATE.items` 設 `"card"`（新店預設 card）。
- `escpos-render.ts`：`EscPosLine` items variant 加 `layout`；`renderEscPosLines` 推 `layout: b.layout ?? "card"`（舊店模板缺 layout → 當 card，即全店即時變 B）。
- `escpos-preview.tsx`：items 分支 `layout === "card"` → 序號前綴 + 虛線 + 成組縮排(pl-4,無 bullet)；否則 inline 舊式。preview 與 Companion 一致（只有 "card" 當 card，其餘 inline）。
- `companion-server.mjs`：`renderEscPos` items 分支 `b.layout || "card"` → `${i+1}. ${name}  x${qty}` + `divider()` + 規格(`  規格`,無 bullet) + note(`  注：`) + 菜間 `textLine("")` 留白；否則 inline。**version 0.1.10 → 0.1.11**（desktop 改動要 Windows dev box `npm run dist` rebuild）。
- `docs/55-android-template-driven.md`：補 `layout` 合約 + APK items 分支要 mirror card（P7 待做）。
- 新 `docs/67-list-view-card-layout.md`：決定 + 對比表 + 實作落點 + 切換方法（inline/card/stacked）+ 部署。

`tsc --noEmit` 零新 error（layout.tsx 誤報照避）；`node --check companion-server.mjs` OK。
部署：web 側 Vercel push 即生效；**Desktop Companion 0.1.11 要 rebuild exe**；Android APK 待 P7。
待用家 commit + push（§65/§66/§67/§67b 一批過 push）。

## 68 · 結帳後空枱被舊單 occupy 修復 已落碼（Option A · 2026-08-26）

用家報：完成一張單收單（settled）後，原本空閒嘅幾張枱突然顯示舊訂單記錄。

### 根因（數據同步 / 唔係結帳邏輯錯）
- 收單 `applyPaymentToOrder`（pos-app.tsx ~2478）set `status:"settled"` 後 call `syncNow([...queue, paymentEvent], {silent:true})` → queue 清空 → effect(564-570) 觸發 `loadRuntimeState()` → `GET /api/pos/state?storeId=X` **撈齊全店 orders（無日期/狀態 filter，`limit(200)`，route.ts:24-26）**。
- 合併 `mergeOrderLists` + `filterResurrectedOrders`（589-597）。舊版 `filterResurrectedOrders` 只擋：①tombstoned 刪除 id；②server 單邊終態單（cancelled/refunded/partially_refunded/settled）。**冇擋 server 單邊嘅「舊 open 單」**（draft/sent_to_kitchen/paid/reopened）。
- 個別卡住未結帳 / 落單冇成功 / 落咗冇落單嘅舊 open 單（updatedAt 舊到昨日或更早）經 backfill 復活 → `openOrders` 包含佢哋 → `tableOrderMap` 將 idle 枱指向舊單 → 枱面顯示「有單」。
- 面板 vs 枱面錯覺：`local-orders-panel.tsx:140-145` 用 `orderMatchesLocalDateFilter(order,"today")` filter → 舊單唔喺面板出，但枱面用全 `orders` → 「枱面見單、面板搵唔到」。

### 修法（Option A · client 擴 filterResurrectedOrders）
`pos-order-filters.ts`：
- 加 `STALE_OPEN_ORDER_MAX_AGE_MS = 24*60*60*1000`（1 日，可調 2 日）。
- `filterResurrectedOrders` 加第三擋：**server 單邊 open 單 + 本機無（`!localIds.has`） + `updatedAt` 超過 1 日 → drop**。
  - 今日 open 單（含 kiosk realtime 新單）updatedAt 夠新 → 照常通過；
  - 本機已有嘅單（無論幾舊）永遠保留（localIds 優先）；
  - 終態單（docs/52）原有規則不變。
- 判定 `orderTimestamp(o) = Date.parse(o.updatedAt || o.createdAt)`；`ts > 0` 先防 0 值誤殺。

`tsc --noEmit` 零新 error（layout.tsx 誤報照避）。web-only，Vercel push 即生效，**唔使 rebuild exe / apk**（Companion/APK 唔用呢個 filter）。
新 `docs/68-stale-open-order-occupy-table.md`：根因鏈 + 代碼表 + `pos_orders` SQL 排查 + 三選項 A/B/C + 落碼。

### 部署 + 待辦
- §68 改動 web-only；連埋 §65/§66/§67/§67b 一批過 commit + push 去 Vercel。
- 用家如需確認「邊啲舊單企咗枱」，跑 `docs/68` 入面嘅 SQL：`SELECT id, table_id, status, updated_at FROM pos_orders WHERE store_id='<店>' AND status NOT IN ('settled','cancelled','refunded','partially_refunded') AND updated_at < now() - interval '1 day' ORDER BY updated_at;` → 改成 `settled` / 清走即正本店數據。
- Companion 0.1.11 rebuild 仍 pending（§67b）；Android APK P7 仍待做。

## 69 · 觸控 UX 全面審計 已出清單（2026-08-26）

用家要求全面檢查 touch 不友善問題（熱區過細 / 點擊難操作 / 輸入唔適合觸控）。先出 Plan → confirm → 執行。

### 方法
- grep 全 `src` 掃 anti-patterns：`<input>` 冇 `inputMode`、原生 `<select>`、`type=checkbox/radio/number`、細熱區 class（`px-1`/`h-6`/`text-xs`/`size-4`/`title=`）。
- 讀取 3 題關鍵檔（`pos-app.tsx` 開桌/結帳、`print-center.tsx` 菜品打印設置、`salon/checkout.tsx`）+ 現有 keypad 基建（`numeric-keypad`/`input-pad-modal`/`fixed-number-pad`）。
- 2 個 Explore agent 並行廣度掃描：salon 模組 + 餐飲二線/共用/kiosk/登入/後台。

### 關鍵發現
- **系統已有優秀觸控數字鍵盤基建，但分佈唔均**：salon 結帳實收(`FixedNumberPad`)/折扣(`InputPadModal`)/會員電話(`NumericKeypad`) 已用；**餐飲 `pos-app.tsx` 結帳折扣/實收只係 `inputMode="decimal"` 純 input**，靠 OS 鍵盤（POS WebView 未必彈出）→ 題③核心。
- **已知 3 題全確認**：①開桌入座人數 `pos-app.tsx:2849-2857` 純 `type=number` 冇加減/快捷；②print-center `:401-428,463-468` checkbox 原生細 + ↑↓ `px-1` 細箭頭；③結帳折扣/實收 `:4262-4279` 冇大鍵盤。
- **跨模組共性（一次做 component 修一大片）**：A 數字 input 冇大鍵盤(~15)、B 原生 `<select>`(~22)、C checkbox/radio/stepper/icon <44px(~20，含 device-settings `h-3.5 w-3.5`=14px!)、D `title=` hover 冇觸控等效(~12)、E modal 關閉鈕細、F 細文字鈕/list 行距。
- 共約 90 項（HIGH~35 / MID~40 / LOW~15）。Kiosk/登入無問題；salon checkout 已良好。

### 交付
- 新 `docs/69-touch-ux-audit.md`：摘要 + 已知 3 題詳細 + 跨模組共性表 + 按模組清單（file:line/問題/不友善/嚴重/建議）+ 修復路線（階段1起 TouchSelect/TouchCheckbox/NumberField 共用 component；階段2 已知3題+device-settings；階段3 其餘）。
- **本階段只列清單唔改碼**，待用家 confirm 修復範圍再落碼。

## 70b · 模板「大」字型 fix #1/#2/#3 全部落碼（2026-08-26）

- 用家貼 localStorage `printTemplates.kitchen.blocks` 確認 `size` 持久化正確 → 假設①「未保存」排除；授權改 #1，再講「一起做」→ #2+#3 一齊做。
- #1 Companion `companion-server.mjs:227` `SIZE_BYTE={s:0x00,m:0x20,l:0x30}`（舊 l:0x60=雙寬+保留位→當雙寬同 m；新 0x30=雙高雙寬真最大）。待 rebuild exe + 講新版本號。
- #2 Android：`PrintDtos.kt` 加 `content`/`template`(Block/TemplateDto) 解析；`EscPosRenderer.kt` 加 `SIZE_BYTE`+`Buf.style/align/reset`+`renderTemplateTicket`（同源 Companion renderEscPos）；`MainActivity.printJob` 優先 call `renderTemplateTicket`。修復 Android 之前完全 ignore 字型設定。沙盒無 SDK 未編譯，待 dev box `./gradlew assembleDebug`。
- #3 web `escpos-render.ts:47` `SIZE_PX.l` 18→22。`tsc --noEmit` 零新 error（僅 layout.tsx 誤報）。web 重 build+push。
- 詳見 `docs/70-template-font-size-mismatch.md` §7–§8。三項都改碼但未打包生效（Companion exe / APK / web Vercel 各自 rebuild）。

## 71 · 模板設定未生效 / 儲存失敗 排查計劃（2026-08-26，只出 Plan · 待用家 confirm）

用家報兩新症 + 要加 save 鈕：P1 設「大」按打印彈返預設；P2 設「大」但顯示冇變大；要加明確「儲存」動作（auto-save 感覺冇確實儲）。

### 查 source 定位頭號嫌疑（實證）
- `storage.ts:77-87` `writeJson` 對 `localStorage.setItem` 拋錯 `catch` 後完全 ignore → 靜默寫失敗（P1-A 高可能性，亦係「感覺冇儲存」主因）。
- `storage.ts:153-156` `getActiveMerchantId()` = `authSession.merchantId ?? null` → store/local key 依賴當下 session → save/load 可能落唔同 key（P1-B scope 翻轉，中可能性）。
- `mergeTemplateBlocks` 保留 size（唔掉）；`normalizePosLocalSettings` 當結構畸形落 defaults（P1-C 低）。
- P2 高可能 = 未 rebuild（web SIZE_PX.l 18→22 / Companion l:0x30 / Android SIZE_BYTE 都只 source 改，未打包）所致。

### 交付
- 新 `docs/71-template-save-revert-troubleshoot-plan.md`：假設排序（P1-A/B/C、P2-A/B/C）+ 瀏覽器 Console 可驗證步驟（hook setItem 捉 FAIL、鎖 key 改前後讀 size、打印後再讀、global vs store key 比對）+ 修復方案（writeJson 返 boolean + savePosLocalSettings 返 boolean + 加「💾 儲存模板」鈕 read-back 驗證 toast；鎖 bootstrap.storeId scope）+ 落碼順序。
- **本輪只出 Plan 未落碼**，待用家 confirm 先實作（storage.ts writeJson/savePosLocalSettings boolean + print-center.tsx 鈕 + 鎖 storeId）。

## 71b · docs/71 修復方案已落碼（2026-08-26，web-only）

用家 confirm「全部 web 改動」+ 報「已 rebuild 但仍冇變大」。

### 已落碼（storage.ts + print-center.tsx）
- `storage.ts` `writeJson` → 返 `boolean`（setItem 失敗 `console.error`+`false`，唔再靜默 ignore）；`writeStoreJson` 跟返 boolean。
- 加 `resolveSettingsStoreScope()`（優先 `authSession.merchantId`，無 session 用 `loadBootstrapCache()?.storeId`）+ `getLocalSettingsKey()`（UI read-back 用）。`load/savePosLocalSettings` 改用 → **鎖 scope 防 P1-B**；`savePosLocalSettings` 返 boolean。
- `print-center.tsx` 設計介面 header（撤銷/重做旁）加「💾 儲存模板」鈕 → `saveTemplateNow()`：`savePosLocalSettings` 收 boolean + `getLocalSettingsKey()` read-back 驗證 → toast ✅/❌；auto-save 保留。
- `tsc --noEmit` 零新 error（layout.tsx 誤報照避）。詳見 `docs/71` §7。

### P2 實證結論（查 source）
- 網頁預覽 render 路徑正確（`patchBlock`→`setLocalSettings`→`buildPreviewLines`→`EscPosPreview` `fontSize:SIZE_PX[line.size]`，**無 useMemo 緩存**；`buildSnapshot` 帶 size）。現 source 設「大」(l=22px) 預覽必即時變大 → **P2 唔係 web render bug**。
- 用家 live「冇變大」推論：①live build 早過 #3（SIZE_PX.l 舊 18）；②設定冇 persist（P1-A）remount 彈預設；③**出紙紙張**冇變大＝Companion #1 `l:0x60`/Android #2 未 rebuild。本次 web 改動令 ①② 可被 save 鈕偵測；③ 要 Companion rebuild exe + Android rebuild APK（P7）。
- 部署：web `next build`+push 即生效；Companion 0.1.x rebuild 報版本號；APK P7。

## 71c · save 鈕「無 toast + 冇儲存」根因 + 健康檢查確認（2026-08-26 晚）

用家報：撳💾儲存模板 → 無 toast（成功/失敗都無）+ 切換回網頁模板冇儲存；另持續見 `127.0.0.1:9311/api/health` GET。

### 根因（查 source 確證）
- **無 toast**：`print-center.tsx` `toast` state 喺 6 處 `setToast`，但 main return（554–760）**完全無 `{toast && …}` 渲染元素** → 永遠唔顯示（pre-existing gap，連重打單 toast 都冇顯過）。唔係事件冇綁定。
- **冇儲存**：`saveTemplateNow`→`savePosLocalSettings`→`writeJson`→`setItem` 路徑正確；若 setItem 成功必寫入。冇儲存＝**setItem 拋錯（ok=false）**：Safari 私隱模式 / 沙箱 iframe / quota 滿 / kiosk WebView 禁 storage。舊 `writeJson` 靜默 ignore → 零回饋；今返 boolean 但 toast 冇渲染 → 兩樣都唔知。scope 錯配（P1-B）已鎖定，唔係今次成因。
- **健康檢查**：100% 本地。`127.0.0.1`=loopback，Companion 代理係同機 exe/APK 本地 server，**唔 call Vercel**。來源＝`PrintFlushWorker`（root layout 全域）每 **2.5s**（`FLUSH_INTERVAL_MS=2500`）`tryAutoPairCompanion()` 探 localhost（現 source `/api/config`，舊 wiring `/api/health`）；`CompanionStatusCard` mount 一次 `/api/health`；「測試連線」btn `/api/health`。無遠端憂慮，可選 throttling。

### 修法
- `print-center.tsx` main return 加 fixed toast 元素（success=emerald / error=rose），令所有 setToast 有可視回饋。`tsc` 零新 error。
- live 後：撳 save 彈 ✅/❌；彈 ❌ → 確證 P1-A（照 docs/71 §4 步驟 0 捉 `[setItem FAIL]`）；彈 ✅ 但 reload 仍預設 → 先查 scope。詳 `docs/71` §8。
- 部署：web `next build`+push 即生效。
