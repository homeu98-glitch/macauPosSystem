# 2026-09-02 工作日誌

## 00:0x · Sunmi 打印中繼機方案（docs/96）— 只出方案，未動 code

承接 2026-09-01 23:00 嘅 iPad 打印可行性評估。

**用戶拍板**：① iPad 係其中一個終端選項（唔係唯一）② 中繼機用 **Sunmi**，仲要打去 **LAN 打印機** ③ 要 result 回傳，現有 POS 打印功能全部要過渡到 ④ 行雲端中繼方案 B。
**重要更正**：Sunmi **唔係**用嚟包住 macau-pos（唔係 WebView 殼），要**另外開發一隻 APK** 做純中繼。

**產出**：`docs/96-sunmi-print-relay-plan.md`（新文檔，未完成落地）。

### 關鍵發現：核心基建已經喺度，唔使由零寫

- `pos_print_jobs` 表**已存在**（0011 建、0012/0015 加欄位），有 `store_id` / `items` jsonb / `template` / `content` / `printer_id`，**已入 `supabase_realtime` publication**。
- RLS 已有（0016）：anon SELECT（14 日窗）、service_role 全權。
- **上傳路已經通**：`pos-app.tsx` 有 9 處 enqueue `PRINT_JOB_CREATED` → sync queue → `/api/pos/sync:282` → `pos_print_jobs` upsert（`onConflict: id`，冪等）。
- **訂閱路已經通**：`src/lib/pos/use-pos-realtime.ts` 用 anon client 訂閱 `pos_print_jobs`，filter `store_id=eq.<storeId>`。
- Android 端：`EscPosRenderer.renderTemplateTicket(job, cfg)` 就係主入口；`SdkPrinter.print()` 經 `net.posprinter` AAR 支援 **ETHERNET（info = "ip,port"）/ USB / BT**；`EscPosPrinter.printRaw()` 係裸 socket 後備。

⇒ 「另外一隻 APK」嘅低風險做法 = **喺 `C:\dev\print-agent-android` 開新 app module（:relay），抽 :core 復用 `net/`+`model/`+`PrinterHub`**，只換輸入層（WebView bridge → Supabase Realtime + claim RPC）同 UI。**由零寫會變第四個 renderer，重新踩 docs/95 三倉合約嗰堆坑。**

### Supabase Realtime 限制（查證 supabase.com/docs/guides/realtime/limits）

- **postgres_changes payload 上限 1,024 KB；超標時 `new`/`old` 只帶每個 ≤64 bytes 嘅欄位** → `template`/`items`/`content` 會被剝走。
- Broadcast payload：Free 256 KB / Pro 3,000 KB。Concurrent connections：Free 200 / Pro 500。Messages/s：Free 100 / Pro 500。Channels per connection 100。
- **⇒ 關鍵設計：Realtime INSERT 事件只當「叫醒訊號」，權威拎單一定要經 `pos_claim_print_jobs()` RPC 由 DB 直接攞完整 row。** 順便一併解決 event 遺失同多機爭奪。

### 方案要點（詳見 docs/96）

- 原子拎單：`pos_claim_print_jobs()` PL/pgSQL，`for update skip locked` + `attempts < 5` + `ttl > now()` → **物理上唔可能重複打印**。
- migration 0020：`pos_print_jobs` 加 `printer/kind/store_name/payment_method/total/qr/qr_url/copies/ttl/updated_at/claimed_by/claimed_at/attempts/last_error/finished_at`；新增 `pos_print_agents`（只存 `token_hash`）。
- 4 條 Vercel route（service_role）：`pair` / `claim` / `result` / `heartbeat`。
- 配對：APK 顯示 `agentId` QR → iPad 掃（復用 `loadJsQr()`）→ server 簽 token → 經 **broadcast 頻道 `pair:<agentId>`** 推畀 APK（一次性、唔落 anon 可讀表）→ APK 存 EncryptedSharedPreferences。**Sunmi 唔使有相機。**
- 狀態機：`pending → printing → sent|failed`，`failed` 且 `attempts<5` 自動退回 `pending`。

### 截斷風險嘅緩解

Realtime 只做叫醒 + **60s 對賬 tick** 兜底 → payload 截斷 / event 遺失都唔會漏單。

### ⚠️ 待用戶拍板（docs/96 §12.2）

1. Sunmi 型號 / Android 版本 —— 現有 `minSdk = 26`，部分 Sunmi 係 Android 7.1（API 25）→ 可能要降 minSdk。
2. 要唔要用 Sunmi 內置打印機（要 → 引入商米 AIDL SDK，`ISunmiPrinterService.sendRawData(bytes)`；唔要 → 淨用 LAN:9100，零新依賴）。
3. 一間舖幾部 Sunmi（單點故障）。
4. Relay vs native bridge 優先級。
5. token 有效期。

### 已知大風險

**依賴互聯網**：Supabase / 店內網絡斷 → 印唔到。建議 Sunmi 插網線；長遠保留「終端係 Android 就走 native bridge」做雙路徑（docs/43）。

---

## 00:3x · 收據第四輪：結帳時服務費被丟棄（收少錢）— 已修

承接 ROUND 3（優惠合計 -81）完結時拋出嘅 §12.6 未修 bug，用戶拍板「落」。

**根因**：`pos-app.tsx` `paymentBase` 兩個 branch 硬寫 `serviceChargeAmount: 0` 且
`total = subtotal + taxAmount`，但落單（`upsertCurrentOrder` → `orderTotals()`）係
`total = subtotal + service + tax − discount`。只要 `rules.serviceChargeRate > 0`：
落單收據有服務費、結帳冇 → **收銀實收少咗**。連帶 `discountAmount = discountAmountFromRate(paymentBase.total, rate)`
全單折扣基數都縮水（落單計喺含服務費嘅數、結帳計喺唔含嘅數）。

**改法**（`src/components/pos-app.tsx`）：
- 加 `const sumOrderBaseTotal = (order: PosOrder) => order.subtotal + (order.serviceChargeAmount ?? 0) + order.taxAmount;`
- `paymentBase` 兩 branch：`serviceChargeAmount: <order>.serviceChargeAmount ?? 0`、`total: sumOrderBaseTotal(<order>)`
  （cartItems 落 `totals` 嗰 branch 本來就啱，`orderTotals()` 有計服務費）
- `paymentSummary.serviceChargeAmount` 由硬寫 `0` 改 `paymentBase.serviceChargeAmount`

**核過冇雙重計**：
- 自助點餐單 `kiosk-order.ts:135` 本身 `total = subtotal + tax + service`，同口徑。
- **線上 Ledger 單唔會經 `paymentBase`**：`bridgeLedgerOrderToPos()` 只寫落 `bridgedOrders`
  in-memory Map（契約 M3/M8「線上單唔 mirror 入 POS DB」），而 `currentSettlementOrder`
  只從 `orders` 搵 → 永遠搵唔到 bridged 單。`ledger-pos-bridge.ts:322` 本地計嘅
  `serviceChargeAmount` 只影響廚房單／收據顯示，**唔影響結帳收錢**。
- `service_charge_amount` 落 DB 同讀返（`pos-order-mapper.ts:59` / `api/pos/state:61` /
  `api/pos/orders:37` / `api/pos/sync:211`）全通。

**⚠️ 歷史資料**：fix 前已結帳落 DB 嘅單 `total` 冇服務費，**唔做 migration**（改歷史金額＝改歷史營收）。
收據層靠 §12 `resolveTotalDiscount()` 取細值頂住 → 舊單收據對唔平但**唔會印錯折讓**。

**驗證**：`tools/verify-receipt-totals.mjs` Case F 拆做 F1（修好後 total 110 ✅ 平衡）/
F2（舊單 total 100 ⚠️ 已知失衡、折讓 0）。A–G 共 8 case 全綠。
`npx tsc --noEmit` 只剩已知誤報 `layout.tsx(38,50) LayoutProps`。
文檔 `docs/95-receipt-print-fix.md` §12.6 改標「已喺 §14 修好」，新增 §14。

**仍未修（已知，未拍板）**：`pos-app.tsx:1737` 退菜路徑
`total: Math.max(0, nextTotals.total - activeOrder.discountAmount)` —— 重計 `subtotal` 但沿用
**stale `discountAmount`**，即 ROUND 3 嘅根因 #1。收據層已被雙軌對帳頂住，但資料本身仲係錯。
