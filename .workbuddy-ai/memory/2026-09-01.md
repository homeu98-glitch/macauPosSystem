# 2026-09-01

## 訂單詳情 modal 「刪除訂單」掣刪除（用戶截圖）
- **改動**：1 個 file，`src/components/pos-app.tsx` 行 4215-4225 — 移除 viewing modal footer 嘅紅色「刪除訂單」掣（`bg-red-50 ring-red-200 text-red-700` 樣式 + `window.confirm` → `deleteOrderPermanently(viewingOrder.id)`）。
- **保留**：`deleteOrderPermanently()` function（行 2319-2352）保留喺度，唔刪。理由：① 用戶只指明刪掣，冇講刪 function；② docs/52 同 docs/56 都將呢個 function 描述為 ORDER_DELETED 真刪同步嘅核心（tombstone + push event + syncNow POST /api/pos/sync），保留讓日後 re-wire 或代碼考古時有跡可循；③ dead function 唔影響 runtime / bundle / typecheck。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。
- **⚠️ 副作用**：真要刪單時需要靠其他途徑（DB 直接 / 後台 sync 補傳），但訂單詳情 modal 暫時無「刪」入口。如需復活，請告知。

## 訂單詳情 modal actions 與外面 quick strip 完全一致（用戶截圖：「為什麼外面顯示可取餐，按查看後就有已完成？」）
- **根因**：pos-app.tsx viewing modal footer 用咗舊邏輯（paid 顯示「已完成」 + 「去結帳」 + 「取消結帳」），但 strip 嘅可取餐掣已經由 docs/87 §6.3 改成「按完消失」。結果：pos counter 單 paid+preparing 狀態，外面顯示「可取餐」、modal 顯示「已完成」+「去結帳」——完全唔對。
- **修法（1 個 file，`src/components/pos-app.tsx` viewing modal actions）**：用 IIFE 分流，mirror `quick-local-orders-strip.tsx` OrderCard 嘅按鈕邏輯：
  1. 加 `isQuickCounterOrder` import from `@/lib/pos-order-filters`（原本只 import `filterQuickActionBarOrders` / `localOrderStatusLabel` / `mergeOrderLists`）。
  2. **自助單 split（kiosk/scan counter）**：mirror strip split 雙掣邏輯
     - `!isPaid && status !== draft` → `去結帳`（slate-700）
     - `!isReady && status !== draft` → `可取餐`（orange-500）
     - `isPaid && isReady` → `{quickCompleteLabel(v)}` 動態文字（已取餐 / 已交付 / 已完成，emerald-600）
  3. **收銀台快餐單（pos counter，舊單鏈）**：mirror strip 舊邏輯
     - `inPreparing && !isReady` → `可取餐`（orange-500）
     - `inWaiting（paid && ready）` → `{quickCompleteLabel(v)}`（emerald-600）
  4. **堂食單 / 其他**：fallback 舊邏輯（取消結帳 + 去結帳），admin-only
  5. **保留**：modal 通用嘅 `關閉` / `重打單`；settled / partially_refunded 嘅 `部分退款` + `整單退款`（strip 冇，modal-only 管理員操作）。
- **掣樣式對齊 strip 視覺語言**：`去結帳` 改 slate-700（原本 slate-900 太重）、`可取餐` orange-500、`已取餐/已交付/已完成` emerald-600，hover 色加深一級。
- **唔做**：① 唔抽 `<QuickOrderActions>` 共用元件（strip 用 `rounded-xl px-2.5 py-1.5 text-[11px]`，modal 用 `rounded-2xl px-4 py-2 text-sm`，樣式差異太大，硬抽共用會兩邊都唔啱）；② 唔動堂食單 fallback（cancel_order + checkout 掣依然係 modal-only 管理員入口）。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。

## Vercel build 失敗 → 修復（online-orders.tsx 折扣欄位未提交）
- **現象**：Vercel 部署 `4845489`（`main`）`next build` 失敗，`src/components/online-orders.tsx(966-977)` 報 `Property 'discountRate'/'discountAvos' does not exist on type '{ name: string; qty: number }'`。
- **根因**：`online-orders.tsx:79` 嘅 `detailItems` state 原本型別係 `Array<{ name: string; qty: number }>`，後來本地加了 `discountRate?`/`discountAvos?` 同埋 `openOrderDetail` 入面 map 埋兩個 field（用嚟喺訂單明細位顯示「折扣多少」）—— **但呢個 fix 一直未 commit**，Vercel 用舊 `HEAD` build 就爆 type error。
- **修復**：`git add` + commit `1748b5d`（3 file：`online-orders.tsx` + `quick-online-orders-panel.tsx` + `src/lib/pos/sync-flush.ts`，全部本地已改未提交、type-check 乾淨）；`git push origin main` → `4845489..1748b5d`。Vercel 會自動用新 commit rebuild。
- **教訓**：本地改完會令 `tsc --noEmit` 過嘅 code，如果未 commit/push，Vercel 永遠用舊 `HEAD` build → 一樣爆。每次修完 type error 一定要 push 先算數。
- **CSS warning（唔致命）**：`globals.css` 有條 `@media (min-width:某寬)` 無效 media query（Tailwind 生成嘅 placeholder class `min-[某寬]:flex-nowrap` 殘留），只係 warning 唔阻 build；同用戶確認過唔理。

## 訂單頁兩卡對齊（orders-hub：左 線上訂單 / 右 店內線下訂單）
- **用戶回報**：截圖見兩卡高度唔一致，兩粒「自動接單」toggle 樣式都唔同。
- **根因**：
  1. 右卡（`local-orders-panel.tsx:238`）有條冗長副標題「快餐走製作中 → 待取餐 → 完成；堂食維持送廚結帳流程」，左卡只係「今天 · 全部 · 共 0張 · 新單 0 張」一行 → 右卡 header 高過左卡。
  2. 兩粒 toggle 風格唔同：左（online-orders.tsx）係 pill button（label + `rounded-full px-3 py-1` emerald/白 ring button 文字「開/關」），右（self-order-auto-accept-toggle.tsx）係 iOS-style switch（`h-6 w-12` + 滑動白圓）→ 視覺唔統一。
- **改動（2 file）**：
  1. `src/components/local-orders-panel.tsx` — 移除行 238 嘅冗長副標題，留低標題「店內線下訂單」。底部「今天 · 共 X 張」meta 行保留（資訊密度同左卡底部「今天 · 全部 · 共 0張 · 新單 0 張」匹配）。
  2. `src/components/self-order-auto-accept-toggle.tsx` — 整個 toggle component 換成同左「自動接單」一樣嘅 pill 樣式：
     - 外層：`flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1.5`
     - label span：`text-xs font-semibold text-slate-600`，文字「自動接自助單」（保留原義，比左邊「自動接單」多「自助」兩字因係唔同設定）
     - 掣：`rounded-full px-3 py-1 text-xs font-semibold`，開=emerald-600 實心白字「開」/ 關=白底 ring slate-200 灰字「關」
     - loading/saving 文字保留喺 label 後；error 保留喺外面
     - 移除原 iOS switch 同底部「自助點餐／掃碼落單後直接出單」helper（一係 toggle 樣式統一，一係 helper 同左「自動接單」唔對稱，留喺度更顯眼唔一致）
- **效果**：兩卡 header 同高（標題 + 同一樣式 pill），底部「狀態 filter row」起始位置相同。toggle 兩邊視覺一致（emerald pill + 開/關文字）。
- **唔做**：① 唔抽 `<AutoAcceptPill>` 共用元件（兩處邏輯差異大：左即時寫 localSettings.autoAccept → POST /api/online-order-settings；右係 optimistic + fetchKioskSettings/saveKioskSettings + storeId 真源），強抽會將兩套狀態管理 mix 埋；② 唔動左卡「自動接單」toggle（已經係目標樣式）。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。Commit `ef98b9f`，push 上 `main`。

## 訂單頁兩卡 header 完全對齊（線下卡補齊 4 段摘要）
- **用戶回報**：截圖顯示左卡 header 摘要 `今天 · 全部 · 共 0 張 · 新單 0 張`（4 段），右卡只有 `今天 · 共 11 張`（2 段）。要求右卡補齊成同樣 4 段、字眼／分隔／排列一致；同時兩卡 header 高度同欄位間距完全對齊。
- **改動（1 file，`src/components/local-orders-panel.tsx`）**：
  1. 新增派生：
     - `activeTabLabel = STATUS_TABS.find(t => t.key === statusTab)?.label ?? "全部"`（對齊左卡嘅 `tabLabel(activeTab)`，由 `STATUS_TABS` 即場搵）
     - `draftCount = useMemo(() => filteredOrders.filter(o => o.status === "draft").length, [filteredOrders])`（對齊左卡嘅 `stats.pending` 語意：當前 tab+dateFilter 範圍內未處理嘅新單；左卡 pending = `rawLedgerStatus === "pending"`，右卡 draft = 未送廚房）
  2. Header 結構跟足左卡：
     - row1：`flex items-center justify-between gap-2`（舊係 `items-start justify-between gap-3`，對齊改成 `items-center gap-2`）
     - 標題下 `mt-1 text-xs text-slate-500 sm:text-sm` 副標題，文字 `{dateFilterLabel(dateFilter)} · {activeTabLabel} · 共 {filteredOrders.length} 張 · 新單 {draftCount} 張`
     - 移除原本獨立 row3 嘅 `mt-2 text-xs` 摘要行（已合併入標題下）
  3. STATUS_TABS 行 `mt-2 flex flex-wrap gap-2`（舊係 `gap-1`），掣樣式 `rounded-full px-3 py-1.5 text-xs font-semibold`（舊係 `px-3 py-1 text-[11px]`，對齊左卡 `text-xs` + `py-1.5`）
- **效果**：兩卡 header 結構同間距完全一致：
  ```
  [標題]                            [auto-accept pill]
  日期 · tab · 共 X 張 · 新單 X 張
  [tab1] [tab2] [tab3] ...
  ```
- **唔做**：① 唔動左卡 toggle（已對齊）；② 唔抽共用 `tabLabel`，因為左用 `LedgerOrderTab`、右用 `LocalOrderPanelTab`，type 唔同，強抽會引 generic 噪音。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。Commit `f3dbb54`，push 上 `main`。

## 收銀台移除診斷 info card（用戶截圖：「待同步/打印任務/打印設備/訂單時間線/廚房單預覽 全部拿走」）
- **用戶回報**：快餐模式右側「收銀與支付」下面三個 panel 完全冇用 — info card（待同步 0 / 打印任務 88 / 打印設備 2）+ 訂單時間線 + 廚房單預覽，指示「全部拿走」。
- **改動（1 file，`src/components/pos-app.tsx`，206 行 diff / 8 增 198 刪）**：
  1. **UI**：刪走三個 `<div className="mt-5">` 區塊（舊行 3812-3908，info card + 訂單時間線 + 廚房單預覽），留返「收銀與支付」底下嘅結帳摘要 + 「去結帳」掣 + 離線 banner。
  2. **Dead code 一齊清**：
     - `timelineOrderId` const、`orderTimeline` useMemo、`previewPrintJob` useMemo（3 個派生都只係畀呢 3 個 panel 用）
     - `describeTimelineEvent(event)` function（60 行，舊行 1987-2045，只畀訂單時間線 render label）
     - `ticketTypeLabel(ticketType)` 本地 helper（舊行 102-106，return「加單/VOID 退菜/正常下單」，只畀 describeTimelineEvent 用；`escpos-template.ts` 嗰個版本「落單/退菜」唔同、依然喺度）
  3. **保留**：state var `pendingQueue` / `printJobs` / `deviceConfig` / `queue` 全部仲喺 sync / void / refund / persistPrintJobs 等業務邏輯用（9+ 處），唔係 dead。`formatMacauDateTime` import 仲喺 activeOrder workspaceOrder original settled 顯示用（3309/3336/4539）。
- **唔做**：① 唔動收銀 / 結帳 UI / `QuickModeOrdersBar`（業務掣）；② 唔刪 `pendingQueue` / `printJobs` / `deviceConfig` state（sync / print / settings 真源）；③ 唔抽 PrintJob 真源到 global（已有 storage.ts，out-of-scope）。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性；`grep -n "orderTimeline\\|previewPrintJob\\|describeTimelineEvent\\|timelineOrderId"` 0 hit。Commit `50700c4`，push 上 `main`。

## 所有訂單明細位加折扣顯示（用戶截圖：線下/線上/查看 全部要見折扣）
- **根因**：§19 折扣系統只做咗「設定頁新增折扣 tab」+「結帳頁全單折扣 dropdown」+「單品折扣 modal」，但**顯示層完全冇接**——所有訂單卡片、查看 modal、線上訂單明細都唔見折扣。外加 Ledger 線上單 pull 落嚟時 `getOrderDetail` 直接 drop 晒 `discount_*` 欄位，`bridgeLedgerOrderToPos` 仲 hardcode `discountAmount: 0`，所以菜單拉過嚟嘅線上單完全無折扣內容。
- **新檔 `src/components/order-discount-display.tsx`**（可重用折扣顯示元件）：
  - `OrderItemDiscountLine({item, currency, variant?})` — 單品折後價 + 原價 line-through + 「優惠 X」。
  - `OrderDiscountRow({items, currency, wholeOrderDiscountAmount?, variant?})` — 訂單級明細：單品折扣 / 全單折扣 / 合計優惠；total <= 0 返 null。
  - 兩者用 `formatMoney`(@/lib/format) + `discountedUnitPrice`/`itemDiscountSaving`/`orderItemDiscountTotal`(@/lib/pos/discount)。
- **修 9 個 file / 11 個顯示位**：
  1. `pos-app.tsx` viewing modal items loop → 每 item 加 `<OrderItemDiscountLine>` + 右側對比價；total 區加 `<OrderDiscountRow>`；付款 summary + 結帳 modal 換 `<OrderDiscountRow>`。
  2. `quick-local-orders-strip.tsx` OrderCard → 金額變 amber 折後 + slate line-through 原價；左下角加「單品 -$/全單 -$」；加 `<OrderDiscountRow variant="compact">`。
  3. `local-orders-panel.tsx`(/orders 線下) → 卡片「已優惠 -$X」+「原 $X」；view modal item 加 `XX% off` 徽章 + `<OrderDiscountRow compact>`。
  4. `online-orders.tsx`(/orders 線上) → 卡片「已優惠 -$X」；view modal item `XX% off`/「已優惠」徽章 + 「折扣 -$X」row；`detailItems` 型態加 `discountRate?`/`discountAvos?`。
  5. `quick-online-orders-panel.tsx` → strip/stack/view modal 同上「已優惠 -$X」；`detailItems` 型態擴展。
  6. `print-center.tsx` + `receipt-ticket-preview.tsx` → item price 改 `discountedUnitPrice(unitBasePrice(it), it.discountRate) * qty`。
  7. `src/lib/escpos-template.ts` 已經有 `subtotal_before_discount` + `discount_amount` block（computeSubtotalBeforeDiscount/computeTotalDiscount），確認無需改。
- **Ledger 折扣 defensive 接（用戶揀「Defensive 寫法 + 一次過修齊本地」）**：
  - `src/lib/ledger/order-mapper.ts` → `LedgerOrderRow` 加 `discount_avos`/`coupon_avos`/`promotion_avos`/`subtotal_avos`；`mapLedgerOrderRow` 防禦式讀 + 加總 3 個折扣 avos → 計 `discountAmount` + `subtotalBeforeDiscount`。`LedgerOnlineOrder` 加 `discountAmount?`/`subtotalBeforeDiscount?`。
  - `src/lib/ledger/orders.ts` → `getOrderDetail` payload 讀 `discount_avos`/`discount_rate`/`line_discount_avos`(item) + `discount_avos`/`coupon_avos`/`promotion_avos`/`subtotal_avos`(order)；加 `mapDiscountAvos()` helper；`LedgerOrderDetailItem`/`LedgerOrderDetail` 加欄位。
  - `src/lib/ledger/ledger-pos-bridge.ts` → `mapDetailToOrderItems` 單品折扣：`discountRate` 優先，冇就用 `discountAvos/qty ÷ unitOriginal` 倒推 rate；`bridgeLedgerOrderToPos` `discountAmount` 由 hardcode `0` 改為 `(detail.discountAvos/100) || options.ledgerOrder.discountAmount || 0`。
- **⚠️ Ledger TODO**：rpc `list_merchant_orders` / `get_order_detail` 嘅實際折扣欄位名仲未同 Ledger 方確認（defensive 用咗 `discount_avos`/`coupon_avos`/`promotion_avos`）。一旦 Ledger 回傳欄位名唔同，mapping 要對應調；但一旦啱欄位名，線上單折扣會自動顯示，唔使再改 code。
- **驗證**：`tsc --noEmit` 通過（只剩已知 layout.tsx LayoutProps 假陽性）。

## 小票優化（仿 `Desktop/57小票餐厅.doc` + 修 3 個 bug）
- **問題**：用戶截圖（人氣半筋半肉麵 $72 + 糖心蛋 $5，總額 77）出現 ①「優惠合計 MOP -72」神秘負數；② 菜品右邊格式唔夠 user-friendly；③ 單品折扣冇顯示。
- **修法（10 個 file）**：
  - `src/lib/types.ts`：`PosBootstrap.storeTel?`、`ReceiptSectionId` 加 7 個 (`store_tel/server/discount_breakdown/service_charge_amount/tax_amount/cash_tendered/change_amount`)、`PrintJob.items[]` 加 4 個 optional (`discountRate/originalUnitPrice/discountedUnitPrice/savingAmount`)。
  - `src/lib/escpos-render.ts`：`PrintItemLine` 加上述 4 個 fields + 加 comment 教訓（加新 field 必須 audit native.ts payload map，docs/82 §20）。
  - `src/lib/escpos-template.ts`：`DEFAULT_RECEIPT_TEMPLATE` 排版仿 57.doc（store_name→store_tel→order_no→table_name→order_time→checkout_time→server→items→discount_breakdown→subtotal→service_charge→tax→rounding→discount→total→cash_tendered→change→payment→order_note→footer）；`RECEIPT_BLOCK_DEFAULTS` 全 section defaults。`computeTotalDiscount` 加 sanitization（cap 到 subtotalBefore）— 原 bug-1：discountAmount = 72 入到 rawDiscount 會直出 -72。`computeItemSavings` 拆出嚟（item-only savings） + `discount_breakdown` section 逐件菜印「折扣率 X% 折讓 $Y」。
  - `src/components/escpos-preview.tsx`：主菜行 + sub-line（detect `hasItemDiscount`）— 有 discount 時印 `折扣率 80%（原價 $72）` `折讓 $14`，主行只印折後價。
  - `src/components/receipt-ticket-preview.tsx` + `src/lib/print-jobs.ts`：`buildTemplateReceiptJobs` 帶全 fields + 由 `loadAuthSession().name` derive `serverName`。
  - `src/components/print-center.tsx`：sample order 帶新 fields + `storeTel: "(853) 2888-0000"` + `serverName: "示範收銀員"`。`SECTION_META` 已自動 render 新 sections，唔使改 UI。
  - `src/lib/print-bridge/native.ts`：APK payload map 加 4 個 `...(typeof X === "number" ? { X } : {})` forward-compatible spread（舊 APK/Companion 自動忽略）。
  - `src/lib/mock-data.ts`：`mockBootstrap.storeTel`、receipt/kiosk template blocks 加新 sections、`order` 排版仿 57.doc。
  - `docs/90-receipt-optimization-57doc-style.md`：紀錄呢次改動 + bug 根因分析 + 7 個新 section 對應表。
- **驗證**：`tsc --noEmit` clean（只剩已知 layout.tsx LayoutProps 假陽性）。
- **限制**：店家電話只係 mock-bootstrp 帶；實際要 db migration 加 `pos_stores.tel`（如要 full integration 落 admin UI，下 round 再做）。

## 快餐模式頂部精簡（用戶要求）
- **改動**：1 個 file，`src/components/pos-app.tsx` 行 3184-3200 — 原本左側欄頂部第二行係 `{!isQuickMode ? 返回桌台掣 : 可直接點餐並結帳}` + `狀態：XX`，現改為整行包 `{!isQuickMode ? (...) : null}`。
- **結果**：快餐模式頂部淨係剩三樣 — `displayStoreName`（老饕牛肉麵）/ `displayStoreSubtitle`（[示範] 60000002）/ `快餐模式` pill。堂食模式完全不變（返回桌台掣 + 桌台狀態都保留）。
- **寫法**：因為成個 block 只喺 `!isQuickMode` 先 render，入面嗰個 `!isQuickMode ? A : B` 三元可以簡化掉，直接寫「返回桌台」掣。
- **驗證**：`tsc --noEmit` clean（剩已知 LayoutProps 假陽性）；`npx eslint src/components/pos-app.tsx` 0 errors / 9 warnings 全係改動前已有（`selectedTableStatus` 無 warning → 確認仲用緊，無變 dead code）。
- **注意**：呢個只係頂部 header。下面嘅「訂單明細」+ 快餐單類型掣（堂食/外賣/自取）屬功能性，冇拎走。

## 免單功能（comp order，docs/91）
- **需求**：結帳時新增「免單」掣，必須揀／填備註；備註清單放 設置 > 備註 > 免單備註。
- **⚠️ 最重要決策：免單備註唔可以寫入 `orderNote`** —— `orderNote` 係廚房備註，受 docs/84 鎖定（`isOrderNoteLocked()`：sent_to_kitchen 起鎖死），而免單一定喺 sent_to_kitchen 之後發生。所以開獨立稽核欄 `compNote` / `compedAt`，跟 `cancelledReason` / `reopenReason` / `voidedReason` 同一 pattern。
- **語義**：免單 = 照出收據、照計營業額、但實收 0；`total: 0`、`discountAmount: 免單前金額`、`paymentMethod: "免單"`、`roundingAmount/cashTendered/changeAmount: 0`、`memberDeductionAvos: 0`（唔扣會員餘額）。同退菜（item.voided）唔同 —— 退菜係單品層、減營業額；同折扣唔同 —— 折扣係部分減。
- **改動 6 個 file**：
  1. `src/lib/types.ts` — `PosLocalSettings.compNotePresets: string[]`（唔 reuse `notePresets`／`cancelNotePresets`，因為用途唔同要分開維護）；`PosOrder.compNote?` / `compedAt?`。
  2. `src/lib/mock-data.ts` — 預設 5 條：「老闆請客 / 員工餐 / 客人投訴補償 / 試食推廣 / 熟客優惠」。
  3. `src/lib/storage.ts` — `normalizePosLocalSettings` 加 fallback（舊設定冇呢個 key 唔會崩）。
  4. `src/components/device-settings.tsx` — 備註區加第三張卡「免單備註」（新增／刪除，跟現有卡片 style）；`syncConfig` payload 加 `compNotePresets`。
  5. `src/components/pos-app.tsx` — `compModalOpen` / `compNote` state；`settleCompOrder(targetOrder, reason, now)`（mirror `confirmPayment` 尾段：setOrders+merge → `ORDER_SETTLED` event 帶 compNote → `syncNow` → printReceipt → backToTables）；`confirmComp(note)` 處理快餐購物車（`sendToKitchen({silent, forceNewOrder})`）＋ 由 `payingOrderId`/`activeOrder`/`unsettledOrder` 解析目標單；「去結帳」掣下面加「免單」掣；modal `z-[70]` 帶 preset chips + 自由輸入，未填備註 disable 確認；viewing modal 加 `免單備註` slate box + `compedAt` 時間。
  6. `src/components/local-orders-panel.tsx` — `/orders` 單據明細顯示 `免單備註`。
- **雲端持久化限制（已知，未做）**：`compNote` 搭 `queue_events.payload`（JSONB，唔使 migration）可以上到雲；但 `pos_orders` 冇 `comp_note` column，所以換機／清 cache 由 server state reload 之後備註會冇咗。migration SQL 已經寫好喺 `docs/91-comp-order.md`（`0018_pos_comp_note.sql`），等用戶決定幾時落。
- **🐛 發現既有 bug（未修，待用戶決定）**：`device-settings.tsx` 嘅 `syncConfig` payload 有 `cancelNotePresets` 但**冇 `notePresets`** → 「常用備註」從來冇 sync 上 server。今次跟既有 pattern 加咗 `compNotePresets`，但冇順手補 `notePresets`（避免擴大改動範圍）。
- **驗證**：`tsc --noEmit` clean（剩已知 layout.tsx LayoutProps 假陽性）；`npx eslint` 0 errors / 10 warnings 全係既有（含 `device-settings.tsx:475` `autoAcceptSelfOrder: _`）。
- **文檔**：`docs/91-comp-order.md`（語義對比表 + 點解唔用 orderNote + 改動清單 + migration SQL）。

## 0018 migration：免單備註上雲（用戶拍板「落」）
- **五個落點**（次序重要，第 3 項最易漏）：
  0. `supabase/migrations/0018_pos_comp_note.sql` — `comp_note text` + `comped_at timestamptz`（nullable）+ partial index `pos_orders_comp_note_idx`（`where comp_note is not null`）+ DO block 建 `report_ro.v_pos_comp_orders`（**純新增，刻意唔改 `v_pos_orders`** —— `create or replace view` 唔可以淨加一欄，要成個重貼，偏差會靜默改錯）。
  1. `src/app/api/pos/sync/route.ts` — upsert 加 `comp_note` / `comped_at`；`ORDER_SETTLED` patch **唯有 payload 有帶先寫**（同 party_size 一樣，避免一般結帳抹走）。
  2. `src/lib/pos/pos-order-mapper.ts` — `PosOrderRow` + `mapPosOrderRow`（Realtime 訂閱用）。
  3. **`src/app/api/pos/state/route.ts`** ← 最易漏。`mergeOrderLists()`（`src/lib/pos-order-filters.ts:77`）係「timestamp 新嘅**成個 object** 取代舊嘅」，唔係逐欄 merge → 呢份 inline mapping 少一欄就等如 reload 時清走本機值。**落 DB migration 但唔改呢度 = 白做**。
  4. `src/components/pos-app.tsx` — `settleCompOrder()` 嘅 `ORDER_SETTLED` payload 加 `compedAt: now`。
- **🆕 `isoOrNull()` helper（sync route）**：`timestamptz` 欄位收到非法字串 Postgres 會**直接報錯令成個 upsert 失敗**（張單寫唔入雲），唔似 `text()` 靜默截斷 → 必須 `Date.parse` 驗過先寫。
- **未喺 Supabase 上跑**：本機冇 `.env.local`／DB 連線，要人手去 Dashboard SQL Editor 貼，或 `supabase db push`。全部 `if not exists` 可重複執行；**可以先 deploy code 後跑 migration**（缺欄只係 fallback undefined，唔會崩）。
- **🔴 順便查出嘅大坑（未修，已寫入 docs/91 §5.4）**：查過 0011/0012，`pos_orders` **從來冇** `roundingAmount`、`cashTendered`、`changeAmount`、`cancelledAt/Reason`、`refunded*`、`reopen*`、`originalSettledAt`、`ledgerMemberPhone`、`memberDeductionAvos` → 呢堆 `PosOrder` field 全部本機 only，server reload 一樣會被 `mergeOrderLists` 清走。即係「抹零同退款記錄換機之後會冇咗」。要根治要再開 0019 加一批直欄。

## 收據電話 = 商家登入號碼（用戶要求）
- **新真源 `src/lib/pos/store-tel.ts` `resolveStoreTel(bootstrapTel?)`**：`bootstrap.storeTel` → **`AuthSession.account`（商家登入號碼，例如 `60000000`）** → undefined（`store_tel` 區段自動收起）。
- **⚠️ 關鍵：`mock-data.ts` 嘅 `mockBootstrap.storeTel` 要留空**。以前填咗 `(853) 2888-0000`，而 mock bootstrap 係 fallback → 一有值就永遠壓住登入號碼 fallback，所有舖印同一個假電話。
- 三個消費點全改用 resolver：`src/lib/print-jobs.ts`（實際打印，收據 + kiosk 共用 `buildTemplateReceiptJobs`）、`src/components/receipt-ticket-preview.tsx`、`src/components/print-center.tsx`（以前硬編 `(853) 2888-0000`）。

## 補 `notePresets` 上 sync payload
- `device-settings.tsx` `syncConfig` 嘅 `DEVICE_CONFIG_UPDATED` explicit payload list 加 `notePresets`（以前淨係有 `cancelNotePresets`）。
- **修正上次嘅判斷**：呢個**唔係功能性 bug** —— `DEVICE_CONFIG_UPDATED` payload 暫時冇下游消費（`grep` 過全 repo）；真正生效嘅路徑係 `POST /api/pos/device-config` 帶 `localSettings: serverSettings`（全份 spread），`notePresets` 本來就喺入面。**改佢只係為咗 explicit list 一致性**：一旦日後變成離線補傳／跨 terminal 廣播嘅真源，缺一欄就靜默唔同步。

## 🚨 環境事故：`.git/refs` 消失 + 未 commit 改動被 rollback
- **現象 1（12:00）**：我啱啱寫完嘅改動（0018 migration + sync/mapper/state/pos-app 五處）全部蒸發，`git status` 報 clean。reflog 顯示 12:00 有外部 `reset: moving to origin/main` → `reset: moving to 2ffed6e`。**要重新寫一次。**
- **現象 2（12:09）**：我用 `git stash push -- <file>` 想驗證 lint error 係咪既有 → 個 command 俾 SIGTERM 截斷 → **`.git/refs/` 目錄成個消失**，git 變「not a git repository」。
- **修復**：由 `.git/logs/refs/**` reflog 攞返 SHA，重建 `.git/refs/heads/main` = `2ffed6e`、`.git/refs/remotes/origin/main` = `2ffed6e`、`.git/refs/remotes/origin/HEAD` = `ref: refs/remotes/origin/main`（加空 `refs/tags`）。objects 完好 → git 立即正常，所有改動原封不動。
- **教訓（重要）**：
  1. 呢個 workspace 有外部 process 會做 `git reset`／干擾 FS（亦試過 `ls` 只顯示 4 個 entry）。**改完 file 要即刻 `grep` verify 落盤**，唔好假設 Write 成功就永久。
  2. **絕對唔好喺呢個 repo 用 `git stash` 做「對比 HEAD」**，太危險。改用 `git show HEAD:<path> > <temp>.tsx` → lint → 即刪（今次靠呢招證實 print-center 嘅 4 個 `react-hooks/refs` error 係既有）。
  3. 要長期改動安全 → 早啲 commit。

## 線下訂單排序改「單號由小到大」（用戶反映：改狀態後單會移位）
- **根因**：兩處排序都用 `orderTimestamp()`（`updatedAt`）做 key：
  - `src/lib/pos-order-filters.ts` `filterQuickActionBarOrders()` → 收銀「線下訂單」strip
  - `src/components/local-orders-panel.tsx` `filteredOrders` → /orders 線下訂單卡
  每次改狀態（出餐 / 可取餐 / 結帳）都 refresh `updatedAt` → 張單彈去最前 = 「按狀態排」。
  另 `pos-app.tsx` `counterKioskOrders`（堂食模式「自取 / 掃碼訂單」面板）本身冇 sort，直接繼承 `openOrders`（`mergeOrderLists` = updatedAt 新→舊）→ 一樣會移位。
- **新真源 `compareOrderByLocalNo()`（`src/lib/pos-order-filters.ts`）**：
  1. `localOrderNoSortKey()` 用 `/^(\D*?)(\d+)$/` 拆 `(prefix, numeric)`。prefix 只認純非數字 → fallback 單號（`自取-a1b2c3`）當「冇號碼」，避免誤抽尾數。
  2. 排序 key 順序：**numeric ascending → `createdAt` ascending → prefix localeCompare → id**。
  3. 有號碼排先過冇號碼（舊單 / fallback 靠 createdAt 排尾）。
  - 點解要 createdAt 做副 key：序號係 per `(store_id, kind, biz_date)` 各自遞增（migration 0012），跨 prefix 號碼會撞（`自取01` 同 `外賣01` 同一日並存）→ 撞號碼時按下單時間分先後（呼應用家「跨 prefix 按下單時間」）。
- **改 3 個顯示位 + 1 個 strip 結構**：
  1. `filterQuickActionBarOrders()` → `.sort(compareOrderByLocalNo)`
  2. `local-orders-panel.tsx` `filteredOrders` → `.sort(compareOrderByLocalNo)`（順便由 import 移除已唔用嘅 `orderTimestamp`）
  3. `pos-app.tsx` `counterKioskOrders` → 加 `.sort(compareOrderByLocalNo)`
  4. **`quick-local-orders-strip.tsx` 改單一列**：原本 render 兩段（`preparingOrders.map` → `waitingOrders.map`），單一撳「可取餐」就由左段彈去右段 —— 呢個本身就係「按狀態分段」。現改成 `cards = [...preparing, ...waiting].sort(compareOrderByLocalNo)` 單一列 render，每張卡嘅 `mode`（藥丸配色 + 掣）由單自己嘅狀態決定，同分段時完全一致。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性；另外用 temp script 跑 comparator sanity：`自取01(12:05) | 外賣01(12:10) | 外賣02 | 取餐09 | 取餐10 | 訂單100 | (冇號碼) | 自取-a1b2c3`，**全部 updatedAt 改成 23:59 之後排序完全唔變**（即改狀態唔移位）。temp script 已刪。
- **注意**：測試時踩過坑 —— 自己砌嘅 ISO timestamp 寫成 `T12:10:00:00+08:00`（多咗 `:00`）→ `Date.parse` 返 NaN → createdAt 全部失效、跌咗去 prefix compare。寫 fixture 要小心。

## 線下訂單排序再改：純 `createdAt`（用戶反映單號會跨 prefix 跳邊）
- **用戶截圖**：`自取01 10:32` 排得比 `取餐09 01:25` 仲前。即係「先下單先做」嘅工作流被破壞。
- **第二輪問題**：第一輪用「單號 numeric asc 為主 + createdAt 輔助」解決「改狀態移位」嘅問題，但暴露咗第二個 bug —— 跨 prefix 嘅同日單號會撞（`自取01` 同 `外賣01` 兩邊都係 `01`，序號係 per `(store_id, kind, biz_date)`，見 migration 0012），純單號排會跨 prefix 跳邊，唔符收銀「先下單先做」嘅心模。
- **改法（1 個 file，`src/lib/pos-order-filters.ts`）**：
  1. `compareOrderByLocalNo()` 由「單號 + createdAt tiebreak」改成**純 `createdAt` ascending + id tiebreak**。
  2. `localOrderNoSortKey()` helper 加 `// eslint-disable-next-line @typescript-eslint/no-unused-vars` 並保留做 documenting reference（之後如要改回單號排，復用即可）。docstring 解釋咗點解棄用單號。
- **不變嘅部分**（call site / strip 結構）：
  - `filterQuickActionBarOrders()` / `local-orders-panel.tsx filteredOrders` / `pos-app.tsx counterKioskOrders` 全部沿用同一個 `compareOrderByLocalNo()` → 自動跟住改。
  - 收銀 strip 仍係單一列、唔按狀態分段（first commit 已改）。
- **驗證**：`tsc --noEmit` + `eslint` clean。Sanity check（用戶截圖情境）：`取餐09@01:25 | 外賣02@09:00 | 自取01@10:32` ✓ —— 同用戶期望（先下單排前）一致。`updatedAt` 改 23:59 之後排序完全唔變。

## 菜品層折扣 sync — 用戶澄清：線上才顯示折扣，POS 不改 logic
- **用戶回報**（截圖 3 張）：
  1. **截圖 1 線上菜單（Ledger 客人端）**：「大麥克牛肉麵」客人見到「**MOP 68.40**（紅字粗體，9折限1份徽章）+ ~~MOP 76.00~~（灰色刪除線）」= Ledger 線上平台有 9 折（`promo_rate_permille = 900`）。
  2. **截圖 2 POS 收銀台揀菜 cart**：「大麥克牛肉麵 MOP 76」= 店家揀菜價 = **原價（無菜品層折扣）**。
  3. **截圖 3 POS 菜單設定**：「價格 = 76」= 店家設定原價。
- **用戶要求**：
  - 「**當客人線上下單的時候, 才需要顯示出來**」原價 + 折後價（即線上菜單 client UI）— 線上已正確，唔使改。
  - 「**商家很有可能是在pos 上面不想要打折**」→ POS 揀菜 cart / 菜單設定 / POS 自家落單都唔顯示菜品折扣，揀菜價 = 店家原價。
  - 「**我們Pos 現在logic 可以不用改**」→ POS 揀菜 cart 邏輯保持（用 `MenuItem.price`，唔加菜品層折扣）。
- **差異確認**（用戶查詢要點）：
  - **POS 收銀員自己揀菜**（堂食 / 自取）→ POS 揀菜 cart = `MenuItem.price` = 76 → POS 結帳收 76。
  - **Ledger 客人線上下單** → bridgedOrder（in-memory only，見 ledger-pos-bridge.ts 契約 M3/M8）= 從 Ledger `get_order_detail` 拉 `unit_price_avos`（即客人實際畀嘅價）→ POS 揀菜 cart = 68.40 → POS 結帳收 68.40。
  - 兩條獨立路徑：**POS 自家揀菜 vs Ledger bridgedOrder**。同一個菜喺 POS 揀菜 cart 內可能見到兩個唔同價（76 / 68.40），但呢個係「線上優惠 ≠ POS 自家價」嘅雙軌定價，**差異係預期嘅，唔係 bug**。
  - POS 揀菜 cart 內兩條路徑會 merge 顯示（同 `OrderDiscountRow` / `OrderItemDiscountLine` 已有 display），客人睇到嘅 OrderItem 來源 metadata（`order.source` kiosk / scan / pos）由 badge 顯示。
- **代碼已經對齊（無需改）**：
  - `src/lib/ledger/orders.ts:115-120`：`LedgerOrderDetailItem.unitPrice` 從 `unit_price_avos` / `price_avos` 讀取，兩者都是「客人實際畀嘅價」（已含 `promo_rate_permille` 折後）。
  - `src/lib/ledger/ledger-pos-bridge.ts:137`：`mapDetailToOrderItems` 寫 `price: item.unitPrice ?? menu?.price ?? 0` → 客人實際價優先，否則 fallback 到 MenuItem 原價。
  - `src/lib/ledger/menu.ts:resolveProductPriceMop` 嘅 import 邏輯：`price_avos` 已經是「客人實際畀嘅價」（已含折扣），所以客人落單嘅 `unit_price_avos` 同 `price_avos` 應都係折後價（推斷，未經 Ledger 方回信確認；見 `docs/integration/ledger-client-api-v2-source.md` §5.3 列 `promo_rate_permille` 屬性，但 `get_order_detail` 嘅 `unit_price_avos` 語義**仲未明文**）。
- **菜品折扣 UI 改動（被撤銷）**：本 session 開頭曾喺 `device-settings.tsx` / `pos-app.tsx` / `app/order/page.tsx` / `app/menu/page.tsx` 加「菜品折扣 dropdown / 揀菜 card 顯示~~原價~~折後價 + 8折徽章」嘅 UI 改動，但用戶澄清「POS 不打折」後無需要——本 session 末段發現呢啲改動已被某個 cleanup step 撤銷（git diff HEAD 無 uncommitted 改動）。**`MenuItem.discountRate` / `originalPrice` 嘅 type 仍保留喺 `2ffed6e` commit**（菜單 import sync 仍帶返 Ledger 嘅折扣資料），但**POS UI 唔用** — 純粹保留做未來可能要喺「線上單 cart」顯示嘅「~~原價~~ 折後價」標籤嘅備用 field。

## 訂單排序監察頁方向翻轉（用戶截圖反饋）
- **用戶回報**：「在訂單內的店內線下訂單，排序係反過來嘅，最新嘅放最上面，最舊嘅放下面」——截圖顯示 外賣01(11:09) → 自取01(11:09) → 取餐11(12:24)，即「舊→新」asc。
- **根因回顧**：
  - 之前 round `2ffed6e` 將排序由「`localOrderNo` asc」改為「`createdAt` asc only」—— 因為同日 `自取01` 同 `外賣01` 嘅 numeric suffix 撞 (都係 01)，純單號排會跳邊。
  - 但 `createdAt` asc 仍係「舊→新」——用戶明確想要「newest-first」。
- **改動（1 file，`src/components/local-orders-panel.tsx`）**：
  - 行 148 嘅 `.sort(compareOrderByLocalNo)` → 內聯 `(a, b) => { const ca = Date.parse(a.createdAt || "") || 0; const cb = ...; if (ca !== cb) return cb - ca; return String(b.id).localeCompare(String(a.id)); }`
  - 同步刪 `compareOrderByLocalNo` import（行 19，呢個 file 已經冇 caller）
  - 加 JSDoc-style 註解解釋：監察頁 vs 收銀條 strip 嘅語意分工
- **設計判斷：點解唔改 `compareOrderByLocalNo` 函式本身？**
  - `compareOrderByLocalNo` 仍有 3 個 caller 全部係**「下一張要做嘅單」** 嘅處理 flow：`filterQuickActionBarOrders` (pos-order-filters:171)、`pos-app.tsx:977` (dine-in 工作枱 openOrders)、`quick-local-orders-strip.tsx:231` (底條 strip)。呢啲 surface 嘅 mental model 係 FIFO（舊→新）——「處理咗單 1 先做單 2」，**asc 喺度係正確嘅**。
  - 用戶嘅「newest-first」係監察頁嘅需求（睇住啱啱落咗咩單），唔係處理 flow 嘅需求。兩種語意唔應該共用同一個 helper。
  - 註解入 `pos-order-filters.ts` 嘅「⚠️ 2026-09-01 第二輪」已經寫住「Cashier workflow is first in, first served」嘅 rationale，呢度繼續保留。
- **唔做**：① 唔動 `pos-order-filters.ts` 嘅 `compareOrderByLocalNo`（FIFO 用嘅 surface 仲要 asc）；② 唔動 strip / workspace 嘅排序（處理流程 asc 啱）；③ 唔抽新嘅 `compareOrderByCreatedAtDesc` helper（單一 caller 內聯更清晰）。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。Commit `214facd`，push 上 `main` (`881ccf0..214facd`)。
- **驗證**：`tsc --noEmit` 通過（只剩已知 layout.tsx LayoutProps 假陽性）。

## 點餐搜尋欄精簡（用戶截圖要求）
- **改動 1 個 file**：`src/components/pos-app.tsx:3780-3801` — 點餐頁右上方嘅搜尋區：
  - **拎走旁邊嗰粒「清除」掣**（用戶截圖紅圈強調）。
  - input 容器由 `flex w-full xl:w-56`（XL 56 = 224px）改做 `flex xl:w-28`（28 = 112px），整體縮短到大約一半。
- **驗證**：`tsc --noEmit` 通過（剩已知 LayoutProps 假陽性）；`grep "清除"` 喺 pos-app.tsx 淨返一個 code comment（tombstone 解釋），唔係掣。
- **替代方案**：用戶而家可以① 撳 input 鍵盤 Esc ② 直接打字覆蓋舊值 ③ 撳類別 chip 切換（會自動清空搜尋，因為類別切換時 reset）。

## 線下訂單卡：按鈕永遠在底（用戶截圖要求）
- **問題**：截圖顯示「取餐12」卡有極大折扣數值（`MOP 500,000,000`）時，`OrderDiscountRow` 撐高咗中間區，`mt-auto` 推掣嘅按鈕被推落卡外 / 被遮住。
- **改動 1 個 file**：`src/components/quick-local-orders-strip.tsx` 嘅 `OrderCard`：
  - **中段（品項摘要 + 總額 + 折扣明細）改用 `flex flex-col` + `flex-1 min-h-0` 包住**，而折扣明細單獨放喺 `flex-1 overflow-y-auto` 容器入面（h-[180px] 卡嘅高度係固定嘅，中段需要內部 scroll 而唔係撐高成個卡）。
  - **掣區由 `mt-auto`（被動）改做 `mt-2 shrink-0 border-t pt-2`**（主動）：永遠喺卡最下層；外加 `shrink-0` 防止 flex 壓縮掣高度；上方 `border-t` 同折扣區清楚分界。
  - 設計原理：掣係**第一優先**（主要功能：可取餐 / 已取餐），折扣明細係**資訊**。資訊要跟內容大小就 scroll，掣要固定位置。兩者用 flex 高度分配切割開，互不影響。
- **驗證**：`tsc --noEmit` 通過（剩已知 LayoutProps 假陽性）；`eslint` 0 errors。

## 快餐模式兩粒「自動接單」掣改 contained size=md 對稱顯眼（用戶截圖）
- **用戶回報**：截圖顯示「自動接單」+ 綠色「開」樣式（emerald pill），但只見一邊。問：「**在快餐模式, 線下訂單, 也需要加一個自動接單的按鈕**」+「**跟線上訂單一樣**」+「**他們按了, 我也要顯示按了...他們那邊也需要按掉**」（後者指線上嗰粒）。
- **澄清 1 確認**：線下訂單嗰粒掣**已存在**（`QuickSelfOrderAutoAcceptPill`，`quick-mode-orders-bar.tsx:33-50`），同線上訂單嗰粒**並排對稱**喺 QuickModeOrdersBar 嘅 section header —— 用戶只係睇唔到右邊嗰粒（細字 plain sm 視覺唔顯眼）。
- **澄清 2 確認**：用戶所講嘅「他們按了，我也要顯示按了」**係指線上訂單嗰粒**（autoAccept），呢個**已經做齊**（docs/92）：POS → Ledger 出站（`auto-accept-sync.ts`）+ Ledger → POS 入站 webhook（`/api/integration/ledger/auto-accept`）+ Supabase Realtime 全店共用 + `useOnlineOrderSettings()` singleton store 跨 call site。**線下嗰粒唔對接 Ledger**（docs/87 §11 明確標明範圍外：自助落單係 POS 內部設定，Ledger 冇對應掣）。
- **改動 2 個 file**：
  1. `src/components/quick-mode-orders-bar.tsx`：左邊（線上單）+ 右邊（線下單，QuickSelfOrderAutoAcceptPill）兩粒掣由 `variant="plain" size="sm"` 全部改做 `variant="contained" size="md"` —— 對稱顯眼、同訂單頁線上單嗰粒樣式一致（slate-100 pill底 + 較大字）。QuickSelfOrderAutoAcceptPill 上面 docstring 同步更新。
  2. `src/components/auto-accept-pill.tsx`：頂部 docstring「四個 call site」清單由 `plain sm × 2` 改為 `contained md × 4`；「兩粒掣嘅真源」段落擴充，寫明線上單係 `pos_online_order_settings.auto_accept`（per-store，server 權威 → 同步 Ledger，docs/92）+ 自助單係 `pos_kiosk_settings.selfOrderAutoAccept`（per-store，全店共用 → **唔對接 Ledger**，docs/87 §11 範圍外）。
- **唔做**：① 唔將兩粒掣合併成一粒（真源唔同，線上單要 push Ledger，自助單唔可以）；② 唔動「線上單自動接單」嘅 sync 邏輯（已 OK）；③ 唔為「線下單自動接單」加 Ledger sync（業務上 Ledger 冇對應掣，做咗反而係假 sync）。
- **驗證**：`tsc --noEmit` 通過（剩已知 layout.tsx LayoutProps 假陽性）；`AutoAcceptPill` 喺 desktop `lg:grid-cols-2` 兩 section 並排 ok，唔會爆位。

## 🚨 線上訂單「自動接單」完全冇反應 —— 根因：3 個斷點（docs/93）
- **用戶回報**：「現在線上訂單的自動按鈕，沒反應，我在 Ledger 那邊改了，也是一樣沒反應。就兩邊完全沒有交往。」
- **查證方法（重要，可重用）**：本機冇 `.env.local`／DB 連線，**直接 `curl` 打 production API 探測**就驗到，唔使連 DB：
  - `GET /api/online-order-settings` → **500** `Could not find the table 'public.pos_online_order_settings' in the schema cache`
  - `POST /api/integration/ledger/auto-accept`（無簽名）→ **500** `伺服器未設定 webhook secret。`
  - production Supabase project = **`https://zymdemjflsckicwcinxl.supabase.co`**（由 `/_next/static/immutable/chunks/2-chh_bv193on.js` 度 grep `https://*.supabase.co` 搵到；`NEXT_PUBLIC_` 會 inline 落 bundle）
- **三個斷點**：
  1. **表唔存在**：`pos_online_order_settings`（migration 0019，2026-08-31 寫）**從來未喺 Supabase 跑過** → 讀寫兩個方向都 500。Realtime publication 亦未加。
  2. **`LEDGER_WEBHOOK_SECRET` 未設定**：入站 webhook 一律 500（route.ts:55-58 `missing-secret` → 刻意唔放行）。就算表建好，Ledger 推過嚟都係死。
  3. **server error 被靜默吞**（最誤導）：`use-online-order-settings.ts:242` 舊寫法 `throw new Error(\`HTTP ${status}\`)` 丟咗 server 嘅 error message；`catch` 一律顯示「儲存失敗，請稍後再試。」；而且 `online-orders.tsx:711` 同 `quick-mode-orders-bar.tsx:73` 兩個 `<AutoAcceptPill>` call site **都冇傳 `error` prop** → 連嗰句都冇顯示。結果：撳掣 → 樂觀變「開」→ POST 500 → rollback 變返「關」→ 零提示 = 「冇反應」。
- **已做嘅 code 改動（可觀測性，4 file）**：
  1. `src/lib/pos/use-online-order-settings.ts` — POST 失敗攞 server `error` message；新增 `describeServerError(detail, status)` 將 Supabase 低層錯誤翻譯成有行動指向嘅中文（「後台尚未建立自動接單設定表（需執行 migration 0019）。」／RLS／未配置／401 等）；`ledgerSynced === false` 由「淨係 dev console.warn」改為**顯示錯誤**畀用家（否則「POS 改咗但 Ledger 冇反應」又係靜默失敗）。
  2. `src/components/online-orders.tsx` — 解構 `loading`/`error` → 傳 `<AutoAcceptPill busy error>`。
  3. `src/components/pos-app.tsx` — 同上，經新 props 傳落 `QuickModeOrdersBar`。
  4. `src/components/quick-mode-orders-bar.tsx` — 新增 `autoAcceptOnlineLoading` / `autoAcceptOnlineError` props → 傳落 `<AutoAcceptPill>`。
- **新文檔**：`docs/93-online-auto-accept-dead-silence-runbook.md`（根因 + Step 1/2 修復 + 三條 curl 驗收 + Ledger 配合清單 + 4 條教訓）；`docs/sql/93-fix-online-auto-accept.sql`（即貼版 migration，含 4 段驗收 query）。
- **⚠️ 仲要用戶人手做（我冇 DB 權限，做唔到）**：
  1. Supabase Dashboard → project `zymdemjflsckicwcinxl` → SQL Editor → 貼 `docs/sql/93-fix-online-auto-accept.sql` → Run（**特別要驗 4.4 Realtime publication**）。
  2. Vercel → macau-pos-system → Settings → Environment Variables → 加 `LEDGER_WEBHOOK_SECRET`（要**同 Ledger 方夾同一支值**）→ Redeploy。
  3. Ledger 方要開 `POST {LEDGER}/api/integration/pos/auto-accept`（出站嗰條，`auto-accept-sync.ts` 已經 call 緊但佢哋未開）。
- **驗證**：`tsc --noEmit` 通過（剩已知 layout.tsx LayoutProps 假陽性）。
- **教訓（重要）**：① **寫咗 migration ≠ 跑咗 migration** —— 呢個 repo 本機冇 DB 連線，0018 踩過一次，0019 再踩；寫完一定要 `curl` 打 production API 驗證。② **雙向同步要分開驗 3 個方向**：POS→Ledger（出站）、Ledger→POS（入站）、POS 機↔POS 機（Realtime）。

---

## 16:05 — 報表對外接口方案評估（用戶問：打包成一條 API 畀 Ledger call，得唔得）

- **用戶提議**：把而家報表內容打包成一條 API，Ledger 每次要睇就 call 一次，顯示喺佢哋手機端。
- **評估結論**：方向可行，但**唔建議喺 Vercel 用 TS 自己計**，建議改成「一個 Postgres function 返回 jsonb，A/B 兩個通道共用」。
- **🔴 關鍵技術限制（新發現，要記住）**：server 端只有 `@supabase/supabase-js`（PostgREST over HTTPS），**冇 `pg` driver**；**PostgREST 冇 `GROUP BY`**。所以「菜品排行／桌台排行／尖峰時段」呢類聚合**用 Supabase JS client 砌唔出**。要嘛寫成 Postgres function 再 `.rpc()` call，要嘛加 `pg` 依賴（違反「唔引入新依賴」）。
- **建議方案**：`report_ro.build_full_report(p_store_id, p_from, p_to) returns jsonb`，內部由現有 22 個 View 砌 JSON。
  - 通道 A：Ledger 用現有 `ledger_report_ro` 唯讀連線 `select report_ro.build_full_report(...)`（唔使新 secret、唔使理 Vercel timeout、沿用 `connection limit 3`）
  - 通道 B（可後加）：Vercel route `GET /api/v1/report/full` → `supabase.rpc('build_full_report')` → 同一個 JSON，唔使加依賴、唔使重寫邏輯
- **口徑鐵律**：絕對唔可以出現第三套計法（前端 localStorage `aggregate()` / SQL views / API 各一套）→ 數字一定分叉。
- **已知缺口（一條 API 都變唔出嚟，docs/83 §6）**：覆蓋人數、食材消耗 BOM、人流、沽清、會員充值（喺 Ledger 自己 DB）、毛利（expenseRecorder）、退款金額、salon 返結稽核。payload 要有 `meta.gaps` 列明，等對方 UI 顯示「—」而唔係當 0。
- **若做 HTTP 版要注意**：獨立一條 key（**唔好共用 `LEDGER_WEBHOOK_SECRET`**，一個係寫入一個係讀取）；`store_id` 必須驗權防跨店；反 polling 照 §7（rate limit + `Cache-Control` + ETag）；只讀 GET；`/v1/` 版本號。
- **狀態**：等用戶揀 A / B / 兩樣都要，同埋「第一版淨係餐飲定包埋 salon」。未寫文檔、未改 code。

### 16:30 — 用戶拍板 → 已交付 docs/94

- **用戶決定**：① 淨係 A（Postgres function，暫不加 HTTP route）② 淨係餐飲，唔包 salon ③ 區間上限 90 日。
- **交付**：
  - `docs/sql/94-ledger-report-api.sql` — `report_ro.build_full_report(text, date, date, int, int) returns jsonb`
  - `docs/94-ledger-report-api.md` — 正式文檔（用法 / payload 欄位表 / 口徑對照 / gaps / 約束 / 驗收）
  - `tools/check-94-sql.py` — 用 pglast（libpg_query，PostgreSQL 真 grammar）驗 SQL 語法，改動後可重跑
- **🔴 新發現嘅技術限制（要長期記住）**：server 得 `@supabase/supabase-js`（PostgREST），**冇 `pg` driver**，而 **PostgREST 冇 `GROUP BY`** → 任何 group-by 聚合（菜品／桌台／尖峰排行）**一定要寫成 Postgres function**，喺 Vercel 用 TS 砌唔出。
- **function 設計要點**：
  - `security invoker`（唔用 definer，唔開後門）+ `stable` + `set search_path = public, report_ro`
  - 全部聚合由 83 號 22 個 View 砌，**冇重新計數** → 口徑唯一
  - P95 **刻意唔用** `percentile_cont(0.95)`（會插值），改用離散 `arr[ceil(0.95*n)]` 對齊前端 `p95Of()`；中位數用 `percentile_cont(0.5)`（同前端 `medianOf()` 一致）
  - covers 用 **dynamic SQL** + `information_schema` 守門（`pos_orders.party_size` 係 0017，未跑則 static SQL 會 plan 失敗）→ 未跑時回 `null` 並加落 `meta.gaps`
  - 超出上限**唔報錯**，倒推截斷 + `meta.clamped = true` + 保留 `meta.requestedRange`
- **已知口徑微差（已寫入文檔 §5）**：① `pos_orders` **冇 `original_settled_at` 欄**（前端 serving fallback 用佢）→ DB 版退到 `updated_at` ② 「跌 20%」規則改用「本期日均 vs 前 7 日日均」，且基線窗口剔走本期（前端包埋今日，佔 1/7 會稀釋跌幅）
- **Part D 效能坑**：0017 嘅 index 用 `timezone(...)` 寫法，View 嘅 `biz_date` 用 `at time zone` 寫法 —— 語義一樣但 **textual 唔同 → PG 當唔同 expression，index 幫唔到手**。目前靠 `pos_orders_store_idx` 頂住；90 日慢先加 Part D 個 index。
- **⚠️ 仲要用戶人手做**：去 Supabase Dashboard（project `zymdemjflsckicwcinxl`）→ SQL Editor → 貼 `docs/sql/94-ledger-report-api.sql` → Run → 跑 Part C 驗收（**C7 對數最關鍵**：`kpi.revenue` 要等於 `v_pos_daily_summary` 同一日加總）。