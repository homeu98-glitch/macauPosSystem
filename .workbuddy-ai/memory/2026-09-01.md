# 2026-09-01

## 訂單詳情 modal 「刪除訂單」掣刪除（用戶截圖）
- **改動**：1 個 file，`src/components/pos-app.tsx` 行 4215-4225 — 移除 viewing modal footer 嘅紅色「刪除訂單」掣（`bg-red-50 ring-red-200 text-red-700` 樣式 + `window.confirm` → `deleteOrderPermanently(viewingOrder.id)`）。
- **保留**：`deleteOrderPermanently()` function（行 2319-2352）保留喺度，唔刪。理由：① 用戶只指明刪掣，冇講刪 function；② docs/52 同 docs/56 都將呢個 function 描述為 ORDER_DELETED 真刪同步嘅核心（tombstone + push event + syncNow POST /api/pos/sync），保留讓日後 re-wire 或代碼考古時有跡可循；③ dead function 唔影響 runtime / bundle / typecheck。
- **驗證**：`tsc --noEmit` 只剩已知 layout.tsx LayoutProps 假陽性。
- **⚠️ 副作用**：真要刪單時需要靠其他途徑（DB 直接 / 後台 sync 補傳），但訂單詳情 modal 暫時無「刪」入口。如需復活，請告知。