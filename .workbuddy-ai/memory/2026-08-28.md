# 2026-08-28

## 餐飲店每日營運總結報表 — 設計方案

用戶要求設計一份餐飲店每日報表總結方案（讓老闆一眼掌握營運並做優化決策），整合庫存＋營運數據，含 9 模塊＋補充項＋自動優化建議，篩選順序統一「今天/昨天/7天/30天/全部」置右上、與現有風格一致。

交付：
- `docs/76-restaurant-daily-report-design.md` — 完整設計方案（目標、篩選、版面層級、9 模塊規格、補充項、優化建議規則表、資料源對照、schema 缺口、設計語言、實施階段、待確認）。
- `docs/77-restaurant-daily-report-mockup.html` — 高保真視覺原型（KPI 帶＋模塊佈局＋優化建議面板）。

關鍵發現（對接真實代碼）：
- 篩選順序已存在常數 `REPORT_RANGE_OPTIONS`（`src/lib/ledger/report-period.ts`），順序正正係用戶指定；建議 salon 報表也升級到此五鍵。
- 餐飲報表視覺主色 = orange-500（沿用 `reports-dashboard.tsx`），唔係 salon 嘅 rose。
- 資料缺口（影響精確度，已於方案誠實標出）：① 食材消耗需 BOM 配方表（現只有買貨成本 `fetchPurchaseSummary`）；② 出餐時間無 per-transition 時間戳（`fulfillmentStatus` 有但無 `sentToKitchenAt`/`servedAt`）；③ 人流無直接計數（只有 `partySize` 覆蓋數）；④ 會員數需 Ledger RPC；⑤ 低庫存需 par level。
- 方案分三階段：A MVP（估算/降級即上）→ B 儀器化補精確 → C 硬化（每日快照/導出/規則 UI）。

狀態：設計文檔已完成，待用戶確認方向後實作。

## Phase A 實作（同日）

用戶回「按 Phase A」→ 直接實作 MVP。

交付（代碼）：
- `src/components/restaurant-daily-report.tsx` — 新增每日總結組件。KPI 帶（營業額/毛利估=營收−買貨已付/訂單數/客單價/覆蓋人數/會員充值 topupMop）、模塊3 菜品銷售線上線下合併＋渠道色標（線下=冇 onlineOrderId、線上=有 onlineOrderId，澳門時區 byHour 熱力）、模塊6 會員充值＋線上佔比（會員數留「待接 RPC」）、模塊7 沽清（loadSoldOutState remainingQty<=0）、模塊8 桌台排行、補充（尖峰時段/退菜率/折扣佔比/同環比 vs 7日均值）、模塊9 自動化優化建議（R4/R5/R6/R7/R8/R9/R10，紅/橙/灰優先級）。篩選五鍵置右上（orange-500 高亮），沿用 REPORT_RANGE_OPTIONS 順序，CSV 導出。
- `src/app/reports/page.tsx` — 改渲染 RestaurantDailyReport（AuthGuard 包住）。
- 刪除舊 `reports-dashboard.tsx`（已無引用，被新組件取代）。

驗證：`npx tsc --noEmit` 零錯（除已知 layout.tsx LayoutProps 誤報）。

注意：`loadAuthSession()?.merchantName` 唔存在（AuthSession 冇此欄），店名改由 `loadBootstrapCache()?.storeName` 取。毛利/會員數/出餐時間仍係估算或留位（Phase B）。

## Phase B 實作（同日）

用戶回「Phase B」→ 儀器化補精確。

交付：
- **出餐時間（模塊4）**：`PosOrder` 加 `sentToKitchenAt?`/`servedAt?`（types.ts）；`PosOrderRow`+`mapPosOrderRow` 同步（pos-order-mapper.ts）；`api/pos/sync/route.ts` 寫 `sent_to_kitchen_at`/`served_at`（upsert + ORDER_SETTLED payload）；四過渡點寫入——`upsertCurrentOrder`（送廚房）、`markOrderCompleted`（堂食 settled）、`applyPaymentToOrder`（堂食結帳）、`updateQuickFulfillment`+`markQuickOrderCompletedInStore`（快餐 ready）。報表計 avg/中位數/P95（分），舊單缺戳以落單→結帳估算標「含估算」。migration `0014_pos_serving_times.sql`。
- **食材消耗（模塊1/2）BOM**：新增 `src/lib/restaurant-bom.ts`（localStorage per-store 配方＋`computeIngredientConsumption` 按 BOM×已售份數展開）；報表加「食材消耗（本月）」＋「食材使用量排行」兩卡；新增配方管理頁 `src/app/reports/bom/page.tsx`（每菜品填食材/用量/單位/單位成本）。未設配方顯示空白提示＋連去管理頁。
- **會員數（模塊6）**：`ledger/reports.ts` 加 `getMerchantMemberSummary`（依賴 RPC `get_merchant_member_summary`，未部署降級「—」）；報表 pill 顯示真數或「—（待接 RPC）」。

未做（留待確認）：人流計數（現以 partySize 覆蓋人數代理）、低庫存 par 預警（需 inventory_items.par_level）。

驗證：`npx tsc --noEmit` 零錯（除已知 layout.tsx 誤報）；eslint 剩 2 個 Phase A 既有 warning（setOrders 未用、soldOut useMemo dep）。設計文檔 `docs/76` 已補 Phase B 實施狀態。

## Phase B 收尾（同日）

用戶回「Phase B the rest of the way」→ 補完剩低兩項（模塊5 人流 + 低庫存 par）。

- **人流（模塊5）**：新增 `src/lib/restaurant-footfall.ts`（本機 localStorage 按澳門日存「入店人次」手動記錄；`macauDateKeysInRange` / `footfallTotalInRange` / `footfallFocusKey`）。報表加「當日人流（入店人次）」卡：選取範圍累加 + 堂食轉化率（覆蓋人數÷入店人次）+ 焦點日可編輯儲存。無門口計數硬件，呢係最務實嘅精確化。
- **低庫存預警**：讀本店 `inv_products`（`0013_inv_products.sql` 已有 `current_qty`/`reorder_level`，reorder_level 即 par）；`current_qty <= reorder_level` 列低庫存。報表加「低庫存預警」卡，經 `/api/inventory/products?store=` 讀取，失敗降級「未能讀取庫存」。

Phase B 全數完成（A→B）。`tsc --noEmit` 零錯（除 layout.tsx 誤報）；eslint 剩 2 個 Phase A 既有 warning。`docs/76` 已更新 Phase B 狀態。

備註：人流係手動記錄（非硬件），真門口計數器到位後將 `loadFootfallAll` 換做讀硬件/Ledger RPC 即可；低庫存依賴 macau-pos Supabase（`SUPABASE_URL` admin client），未部署/未連線時降級顯示。

## Ledger v3.2 對接計劃（同日，計劃已定稿未實作）

用戶俾咗兩份契約檔（`pos-ledger-client-api.md` 權威 + `pos-v3.2-partner-handover.md` 實作清單），要我「先唔做、先畀計劃」。我讀兩份檔＋audit 現有 code，出咗定稿計劃 `docs/77-ledger-v3.2-integration-plan.md`。

5 個 open question 夥伴已答（閉環，寫入 77）：
1. 會員總數：RPC `get_merchant_member_summary` **唔存在** → 用 `get_merchant_report_summary.member_count`（wallets 列數，省略 p_merchant_id）。`list_merchant_customers.total` 係搜尋筆數唔係全店總數。
2. 線上營收摘要直接用 summary（`order_paid_avos`/`order_balance_paid_avos`/`order_in_store_paid_avos`/`topup_avos`/`deduct_avos`）；**唔好**用 `list_merchant_orders` 加總；菜品排行 summary 無欄且禁 loop `get_order_detail` → 日報菜品排行只計 POS 堂食本地單。
3. 返結/退款 POS **唔好動 Ledger 餘額**；`p_type=add` 唔開放；`reopenPosOrder` 要移除 `applyPosAdd`（members.ts）改唔 call Ledger。
4. `/api/topup/pending-count` **唔係** Ledger 路徑（POS 內部轉帳截圖 queue）→ 現有 setInterval 可保留，但要 audit 確認唔 proxy Ledger。
5. 正式 migration `20260828120000` 已 2026-08-28 上正式庫；聯測先 UAT `membership-uat.macau-tech.com`。

現狀完成度：login/PIN 簽章(`.server.ts`)、session、supabase-client、member lookup/deduct、orders realtime 已喺；未做＝`ensure-customer` 路由、`list_merchant_customers` 封裝、移除 `applyPosAdd`、日報 KPI 分源、products realtime。驗收閘＝契約 §9 第 20–24 項。

→ 用戶態度：先計劃唔實作；5 題答完仍未叫我落手。下一步等用戶 approve 先開始 M1–M8。

## Ledger v3.2 實作（續，同日）

用戶回「新增會員我會再問 Ledger 之後拿，先把已知和確定的需求做了吧」→ 落手做已知確認項（M5③ ensure-customer 路由 DEFER）。

已做（tsc 零錯，除 layout.tsx LayoutProps 誤報）：
- `members.ts`：刪 `applyPosAdd`（`p_type=add` 禁項）；新增 `applyPosTopup`（`p_type=topup`，帶 idempotency key，同 deduct 返回形狀）。
- `pos-orders.ts`：`reopenPosOrder` 返結只切本機狀態，註解明確唔 call Ledger（Q3 規則）。
- `reports.ts`：`LedgerReportSummary` 加 `memberCount`（來自 `member_count`）；移除 `getMerchantMemberSummary`；加 `reportMemberCount()` helper。
- `restaurant-daily-report.tsx`：會員 pill 讀 `ledger.sel?.memberCount`（移除 phantom RPC）。
- `members-page.tsx`：加充值區（`applyPosTopup` + MOP→avos + `topup-<id>-<ts>` key + 成功刷餘額/券）；註明新增會員待補。
- **M3 mirror 修**（重要）：`ledger-pos-bridge.ts:bridgeLedgerOrderToPos` 移除 `saveOrders(nextOrders)`（線上單唔 mirror 入 POS DB），改用 in-memory `bridgedOrders` Map；`print-jobs.ts:findPosOrderForLedger` 先查 in-memory registry 再 fallback `loadOrders()`，void/receipt 打印唔會斷。副作用：日報 `loadOrders()` 現純 POS 堂食單，線上單唔再污染菜品排行 scope（順帶修好 M4 scope）。
- **M7 products realtime**：新 `use-ledger-products-realtime.ts`（訂 `public.products` filter `merchant_id`，**唔** subscribe `wallets`）；`menu-import.ts:patchMenuFromRealtimeRecord` 單筆 patch/upsert bootstrap 餐牌 cache + 售罄狀態，dispatch `pos-bootstrap-changed`；`pos-app.tsx` 接 hook + 聽事件即時重讀餐牌。重用 `parseLedgerOrderMenu`/`mapLedgerProduct`/`toLedgerMenuItemId`，守衛「未匯入 Ledger 餐牌就跳過單筆 patch」。

DEFER：M5③ `ensure-customer` 路由（等用家向 Ledger 確認接口）。未動：M1 staff_role UI 權限、M8 pending-count/`wallets` 再 audit。

計劃文檔 `docs/77` 已更新狀態 + 新增 §8 實作進度（含完成表 / DEFER / 驗收）。下一步：用家向 Ledger 拿到 ensure-customer 接口後補 M5③；建議用家 dev box 跑 `next build` 確認。
