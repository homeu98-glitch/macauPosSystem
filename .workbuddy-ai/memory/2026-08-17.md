# 2026-08-17

## P3 美容院 Package 玩法 — 報表 + 即將到期提醒

承接 P1（模板 CRUD + 套票卡）、P2（結帳抵扣 + 購買贈送 Ledger）。

`src/app/salon/reports/page.tsx` + sidebar「報表」入口已存在；本輪擴充 `src/components/salon/reports.tsx`（既有訂單營業報表不動）：

- 載入 `loadSalonCustomerPackages` / `loadSalonPackageTemplates` / `loadCustomers`。
- 總覽加「套票銷售額」（依範圍按 purchasedAt 篩選 Σ price）。
- 新增「套票使用率」區塊：per 模板進度條（已用/總次數），依購買範圍聚合。
- 新增「即將到期套票（催銷）」區塊：status=active 且 expiresAt 在 30 日內，依剩餘天數升冪；≤7 天玫瑰紅高亮。不套用範圍篩選。

`tsc --noEmit` 僅餘 `src/app/layout.tsx` 的 `LayoutProps` 誤報（與本輪無關）。

整個 salon package 玩法（P1→P2→P3）已全數落地。仍待用戶 dev box `npm run lint && npm run build` + push Vercel（含上輪 build 修復、SQL 0006 已執行）。

範圍外：報表未接 POS DB 聚合；到期催銷僅清單未做主動通知（SMS/push）。

## 餐飲打印 — WebUSB 直印通道完成（用戶選 B：零設定 browser 直印）

用戶回報：筆電 + browser + USB 打印機 local 打印，按測試打印無反應；想唔使 set 名、自動偵測設備。

Root cause：browser 基於安全唔能直接連 USB/LAN 打印機，中間必須有 `print-bridge` 中轉；用戶冇 set `NEXT_PUBLIC_PRINT_BRIDGE_URL` 所以 testPrint 行雲端排隊分支冇印到。用戶揀 B = WebUSB 真·零設定。

本輪完成設定頁 WebUSB UI（`src/components/device-settings.tsx`）— 之前輪已落 escpos.ts / print-webusb.ts / types / dispatch / worker 接管：
- 連接方式 dropdown 加 `WebUSB（browser 直印）` 選項。
- 打印機卡片：connectionType=webusb 時收起 LAN IP/端口/USB名稱欄，改顯示「偵測 / 綁定 WebUSB 打印機」掣（call `requestWebUsbDevice()` 授權 → 存 `printer.webusbSerial` + label）。webusb-only 用戶 worker 照常 flush。
- mount 時 `listWebUsbDevices()` 按 serial 填 label，已綁定 printer 唔使再撳掣就顯示設備名。
- 唔支援 WebUSB（非 Chromium）顯示提示用 Chrome/Edge + https/localhost。

`tsc --noEmit` 零錯誤（僅 layout.tsx LayoutProps 已知誤報）。

未完成/待用戶：改動仲未 build+push，用戶睇緊舊線上版先會見到新選項；且要先「新增打印機」揀 WebUSB 先有卡。WebUSB 限制：部機要 ESC/POS 且 OS 冇 bind 死 driver（可能要 Zadig 換 WinUSB）；中文 UTF-8 傳輸模式支援視機型。

注意：按 global 規則未主動新建 docs/*.md；如需 WebUSB 實施文檔可另開。

## 餐飲 WebUSB 實測確診：Windows driver 霸住 → Access denied

用戶實測測試打印：彈揀機對話框、揀完、冇紙出，status 報 `Failed to execute 'open' on 'USBDevice': Access denied.`

確診：Windows 將 USB 打印機 interface 綁咗 `usbprint.sys`，WebUSB `requestDevice` 揀機得但 `device.open()` 被 OS 擋 → Access denied。呢係 OS 層 block，唔係 code bug，ESC/POS / transferOut 根本未執到。

已改 `src/lib/print-webusb.ts`：`printToWebUsb` 嘅 catch 改用 `describeWebUsbError()`，detect "Access denied"/"open" 返中文引導（Zadig 換 WinUSB 或改用 print-bridge + USB）。`tsc --noEmit` 零錯誤。

**架構結論（重要）**：對 Windows + USB 收銀機，WebUSB 直印天生打不過 OS printer driver；**推薦路徑 = print-bridge + USB（連接方式 USB、填 Windows 印表機名、OS driver 行 RAW print）**。WebUSB 直印只適合 claimable 設備（部分型號 / Linux / Mac / 或 Zadig 換 WinUSB 後）。用戶係 Windows USB 機 → 應導佢行 print-bridge USB。

## 用戶追問：能否網頁打包、零安裝打印？

用戶測試機 = **WL-R80A**（USB 熱敏收銀機），已搵到 `WL-R80A-win` Windows driver，確認 USB 機要裝電腦驅動。用戶「唔一定全用 PC」（可能 Android 平板等）。

確實答案：
- **網頁唔能打包/安裝 USB 打印機 driver**：browser sandbox 冇任何 API 裝 kernel driver，呢個係 OS 層安裝，每部 PC 裝一次，唔係 per app / per web page。所以 USB-on-Windows「零安裝」喺 OS 層根本唔可能。
- **關鍵互斥**：裝咗 vendor driver（`WL-R80A-win`，正常打印必需）就等於 WebUSB 搶唔到 interface → Access denied。即 **vendor driver 同 WebUSB 喺 Windows 上互斥**。裝咗 driver 就唔好諗 WebUSB（除非 Zadig 換 WinUSB 拆走 driver）。
- **真正「web app 零額外安裝」嘅路（driver 裝好後）**：browser 原生 `window.print()` → 用 OS 已裝 driver 經打印對話框出紙。web app 唔裝嘢。代價：HTML/CSS 收據、無 ESC/POS 切紙/錢箱/廚房單控制，質量睇 driver + CSS。可加 `browser` connectionType 做呢模式。
- **全 ESC/POS（切紙/錢箱/廚房）喺 Windows** = print-bridge + USB（一次性本地服務）+ driver。
- **用戶「唔一定 PC」呢點係重點**：Android / Chromebook / Mac / Linux 通常無 driver 霸 interface → **WebUSB 可以真·零安裝**（Android Chrome 彈 USB 授權就 claim 到）。所以現有 LAN/USB/webusb + bridge 多模設計對多客端係啱嘅：Windows 行 bridge、Android 等行 WebUSB。
- 建議：Windows 測試機 → 裝 WL-R80A-win + print-bridge USB；同時可加 `window.print()` 模式做「app 零安裝」fallback。WebUSB 留畀非 Windows claimable 客端。

## 落實：window.print() browser 打印模式（用戶揀 3 = 兩樣都做）

新增 `browser` connectionType（零額外安裝 fallback，driver 仍係 OS 裝）：
- `src/lib/types.ts`：ConnectionType 加 `"browser"`。
- `src/lib/print-browser.ts`（NEW）：`renderTicketHtml`（內聯 CSS 熱敏收據，58≈220px / 80≈300px）、`printHtmlViaIframe`（隱藏 iframe 叫 `window.print()` 再 remove）、`printBrowserJob`（按 role 揀收據/廚房 HTML）、`printBrowserTestPage`。
- `src/lib/print-bridge/dispatch.ts`：flush 加 `browser` 分支 call `printBrowserJob`，唔使 bridge / webusb。
- `src/components/device-settings.tsx`：dropdown 加「瀏覽器打印（window.print）」；browser 時收起 LAN/USB/WebUSB 欄、顯示提示；testPrint 加 browser 分支 call `printBrowserTestPage`。
- `tsc --noEmit` 零錯誤（僅 LayoutProps 誤報）。

限制：無 ESC/POS 切紙/錢箱；auto-print 由 worker flush 觸發 window.print()，部分瀏覽器對非手勢 print 對話框可能彈提示（正常）。WL-R80A-win driver 仍要 Windows 裝一次。

## print-bridge Windows 啟動步驟（用戶要嘅，已口頭畀）

1. 裝 Node.js（LTS）。
2. `cd print-bridge` → `npm install`（裝 `printer` + `iconv-lite`；`printer` 喺 Windows 要 build tools/python，裝唔到就 `npm install --global windows-build-tools` 或預編 node-printer）。
3. 裝 WL-R80A-win driver，睇「設定→印表機」個名（例如 `WL-R80A`）。
4. `npm start`（監聽 127.0.0.1:9222）。
5. POS 設置頁：連接方式=USB，USB 系統印表機名稱=上面個名；橋接 URL=`http://127.0.0.1:9222`（或 .env.local 設 NEXT_PUBLIC_PRINT_BRIDGE_URL）。
6. 撳測試打印 → 出紙即通。
開機自啟：Windows 工作排程器登入時跑 `node ...\print-bridge\src\server.mjs`。
