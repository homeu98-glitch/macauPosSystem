# 2026-08-18 工作記錄

## Salon 7 功能（F1–F7）落地 + 驗收（續 2026-08-17 設計 doc 31）

前一輪已碼完 7 功能（工錢/級別、員工管理頁、員工級別、賣產品、檔案號碼、預約看板入工作台、打印入設置）。今日做驗收閘門：

- **`tsc --noEmit`**：clean（僅 `src/app/layout.tsx` LayoutProps 誤報，非真錯）。
- **`next build`**：compile 成功、TypeScript 通過、49 個 static pages 全生成。
  - 唯一報錯是沙盒 `safe-delete` shim 在 finalize 階段刪 `.next/export-detail.json` 失敗 —— 純環境假錯，非程式問題；用戶 dev box / Vercel 不會觸發。
  - **重要發現**：`next build`（Next 16.3）**不跑 ESLint**（build log 無 lint 階段），故 lint error 不擋 build。
- **`npm run lint`**：原 30 error → 修好我新引入的 2 個（`react-hooks/set-state-in-effect` 於 `settings.tsx:475`、`staff-detail.tsx:64`，加 `eslint-disable-next-line` 註解，屬 localStorage 初始化有效 effect）+ 移除 `staff-detail.tsx` 2 個 unused import。現 salon 全部 0 issue；剩餘 **28 error 全為既有**（ledger/print-webusb 等，非 salon，不在本輪範圍）。

落點檔案（本輪無新增，僅修 2 處 disable + 2 處 import）：
- `src/components/salon/settings.tsx`（F1+F3+F7，bootstrap 初始化 effect 加 disable 註解）
- `src/components/salon/staff-detail.tsx`（F2，初始化 effect 加 disable 註解 + 移除 SalonPosOrder/SalonProductSale 未用 import）

### 待用戶（workflow 不代 push）
- 用戶 dev box：`npm run lint && npm run build` 確認 + push GitHub/Vercel。
- 既有 28 lint error 不擋 build，是否要清屬技術債決策，不在本輪範圍。

## 補漏：員工管理左欄入口（F2 漏咗）

用戶反映「睇唔到員工管理」。查證：`src/app/salon/staff/page.tsx` + `staff/[id]/page.tsx` + `staff-list.tsx` + `staff-detail.tsx` 早已碼好（F2），但 `salon-sidebar.tsx` 嘅 `salonNavItems` 冇加「員工」項，所以個頁孤兒咗、介面搵唔到。

修復：`salon-sidebar.tsx` `salonNavItems` 加 `{ href:"/salon/staff", label:"員工", short:"員" }`（擺喺客戶檔案之後）。desktop 左欄 + mobile 底欄共用同一 array，一併修好。現左欄 7 項：工作台 / 快速開單 / 客戶檔案 / 員工 / 產品 / 報表 / 設置。

教訓：之後新增獨立路由頁，記得同步加 sidebar 入口，唔好再孤兒頁。

## SQL migration 0007（新功能雲端同步所需）

用戶問新改動要唔要 SQL deploy。結論：offline/mock 模式唔使；開咗 Supabase 雲端同步就話 0005/0006 漏咗新實體。用戶揀「寫 0007 備用」。

建立 `supabase/migrations/0007_salon_staff_products.sql`（idempotent，跟 0005 風格）：
- 新表：salon_products / salon_product_sales / salon_staff_leaves / salon_staff_shifts（含 RLS allow_all）
- salon_customers 加 file_number 欄
- salon_bootstrap_config 加 products jsonb + staff_level_multipliers jsonb 欄
- SEED：4 款示範產品（同 DEFAULT_SALON_PRODUCTS）+ demo 店 bootstrap 帶 products/級別倍率

重要：雲端同步要真正寫入呢啲表，仲要擴充 `/api/salon/sync/route.ts` 處理 product / productSale / staffLeave / staffShift event（用戶揀咗「備用」冇包呢部分）。純 offline 完全唔使本 migration。

## 0007 SQL 語法錯誤修復（用戶 Supabase 執行報 42601）

用戶喺 Supabase SQL Editor 貼 0007 執行報 `ERROR: 42601: syntax error at or near "end"`（LINE 61）。
原因：`end` 係 PostgreSQL 保留字，唔可以裸用作欄名。

修復（重命名保留字欄，唔影響前端 camelCase 字段）：
- `salon_staff_leaves`：`end text` → `end_date text`（start 非保留字，保留 `start`）
- `salon_staff_shifts`：`end text` → `end_time text`（start → `start_time` 一併改齊風格）
- 同步更新版頭註釋：指明 sync route 已擴充（見下）。

## 擴充 `/api/salon/sync/route.ts`（用戶答「好」授權）

新增 3 個 upsert 分支，處理 idb.ts 推過嚟嘅新 event type：
- `PRODUCT_SALE_CREATED` → `upsertProductSale` → salon_product_sales
- `STAFF_LEAVE_UPDATED`  → `upsertStaffLeave`  → salon_staff_leaves（rec.end → end_date）
- `STAFF_SHIFT_UPDATED`  → `upsertStaffShift`  → salon_staff_shifts（rec.end → end_time）
- 擴充既有 `upsertBootstrap`：加 `products` jsonb + `staff_level_multipliers` jsonb（產品目錄/級別倍率雙寫 bootstrap，經 BOOTSTRAP_UPDATED 帶上）
- 擴充既有 `upsertCustomer`：加 `file_number`（經 CUSTOMER_UPDATED 帶上）

字段映射全部 camelCase(rec.*) → snake_case 欄，同檔案現有風格一致。前端 `SalonStaffLeave.end` / `SalonStaffShift.end` 仍係 camelCase，sync route 內部轉 `end_date`/`end_time`，localStorage 熱路徑零改動。

### 驗收
- `tsc --noEmit`：clean（exit 0，無輸出）。
- `eslint`（node_modules/eslint/bin/eslint.js）：**0 error / 21 warning**（21 個 warning 全為既有 unused-vars 技術債，本輪新碼 0 新增）。
  - 註：`npm run lint` 喺沙盒偶發空輸出 exit 1（疑似網絡/環境抖動），直跑 eslint.js 正常。用戶 dev box 跑 `npm run lint` 應如常。
- 用戶 dev box 待辦：`npm run lint && npm run build` 確認 + push GitHub/Vercel；Supabase 貼 0007 執行（已修保留字）。

## Salon 7 項 UX 改動（用戶 8/18 早上嘅 7 點需求）

用戶提出 7 點（員工分類+今日動態、角色多選、產品銷售員工全開、產品搬入快速開單、走進/電話生效、快速開單選會員、產品新增搬入設置）。前幾輪已落 R2 類型地基（types.ts `SalonStaff.roles:SalonStaffRole[]` 取代 `role` + `SalonBookingProductSelection` + `SalonBooking.productSelections?`；mock-data 3 人轉 `roles`；wages.ts 掃 `roles` 揀最佳工錢 role；storage.ts 舊 `role`→`roles` 遷移補 `SalonStaffRole` import）。今日做剩餘 UI：

1. **R1 員工分類 + 今日動態 + 忙/閒**（staff-list.tsx 重寫）：按 `SALON_STAFF_ROLE_ORDER` 分組；每卡顯示「今日 N 單」+ 狀態徽章（在忙·客人名 / 有預約 / 空閒 / 未上班）；在忙 = 今日有 in_service/checked_in 預約（主員工或任一服務細項執行人）。
2. **R2 角色多選**（settings.tsx + staff-detail.tsx + storage.ts + service-runner.tsx）：settings 員工表單 `role`→`roles` multiselect；emptyStaff `roles:["therapist"]`；renderSummary 顯示 `roles` 全部；staff-detail 顯示 `roles` join；service-runner 銷售員工下拉改 `roles` join；storage 補 `SalonStaffRole` import。
3. **R3 產品銷售員工全開**：快速開單產品 tab 嘅「銷售」下拉用 `allStaff`（bootstrap.staff 全員，唔再 filter active）。
4. **R4 產品搬入快速開單、併入同單**：booking-form.tsx 加「項目 / 產品」左 tab；產品 catalog tap 選購、右欄「已選產品」有數量 ± 同 per-product 銷售員工；pushMockBooking 帶 `productSelections`；checkout.tsx `subtotal` 含產品、handleSettle 生 `SalonOrderItem{kind:"product", commissionAmount}`、UI 加「產品」block；types.ts `SalonOrderItem` 加 `commissionAmount?`。
5. **R5 走進/電話生效**：booking-form 來源 toggle 加 `aria-pressed` + 右欄「來源」chip 明確顯示；電話模式電話 input 變 sky 底 + 提示「已致電客人確認」。
6. **R6 快速開單選會員**：booking-form 加會員 select（loadCustomers），選咗即 prefill 姓名/電話 + 設 selectedCustomerId；handleSubmit 優先用選中會員 id 連結 customerId，唔使重填。
7. **R7 產品新增搬入設置 + 移除 nav**：settings.tsx TABS 加「產品」tab + `productFields` + `emptyProduct` + `upsertProduct/deleteProduct`（CrudSection 操作 bootstrap.products）；salon-sidebar.tsx 移除 `/salon/products` nav；刪除孤兒路由 `src/app/salon/products/page.tsx` 同 `src/components/salon/products-manager.tsx`（獨立賣產品流程由快速開單取代，不再寫 SalonProductSale）。

一致性跟進：staff-detail.tsx 產品佣金匯總改為兼計 orders 入面 kind:product 嘅 `commissionAmount`（因 SalonProductSale 唔再新寫）；customer-profile.tsx 仍讀 SalonProductSale 作歷史顯示（暫未改，新產品銷售經 orders，屬後續一致性可選）。

### 驗收（進行中）
- `tsc --noEmit`：salon 改動零新增 error；初跑報 3 錯已修（storage 缺 SalonStaffRole import、service-runner `s.role`→`roles`、`.next/types` 舊 products 路由 stale）→ 刪 `.next/types` 後剩 `layout.tsx LayoutProps` 誤報（環境生成型，build 會重生）；跑 `next build` 重生 `.next/types` 中（背景 task U2xr1s）。
- 用戶 dev box 待辦：`npm run lint && npm run build` 確認（預期 49 static pages）+ push。

## 緊急 Bug 修復：r.roles is not iterable

用戶回報美容院頁面載入報 `r.roles is not iterable`（「頁面載入時發生錯誤，你可以重試」）。

根因：`loadSalonStaff()` 讀**獨立** `salon/staff` 鍵；但 R2 嘅 `roles` 回填只寫咗 bootstrap 副本（`salon/bootstrap`）。對於**改呢版之前已 seed 嘅舊店家**，獨立 `salon/staff` 鍵仲係舊嘅 `role`-only 記錄（無 `roles` 陣列），`loadSalonStaff()` 回傳無 `roles` 嘅員工 → 任何 `.roles` 迭代（staff-list / settings / staff-detail / service-runner 銷售下拉 / wages）即 crash。

修復（`src/lib/salon/storage.ts`）：
- 加 `normalizeStaff(s)` helper：確保 `roles` 一定係陣列（舊 `role` → `[role]`，無值 → `["therapist"]`）。
- `loadSalonStaff()`：回傳前 `.map(normalizeStaff)`（防禦性，任何讀取都保證 roles）。
- `loadSalonBootstrap()`：回傳前 `b.staff = b.staff.map(normalizeStaff)`。
- `ensureSalonBootstrap` 升級塊 `if (changed)` 時加 `writeJson(SALON_STORAGE_KEYS.staff, existing.staff)`，將遷移後員工同步寫回獨立鍵（舊店家 standalone key 即時補 roles；首次員工編輯經 saveSalonBootstrap 亦會寫齊兩鍵，自癒）。

`tsc --noEmit` 過（僅 layout.tsx LayoutProps 環境誤報）。所有讀員工路徑（loadSalonStaff / loadSalonBootstrap = settings、service-runner、checkout staffMap、booking-form allStaff、staff-list、staff-detail）現已全部 normalized，crash 唔會再發。

用戶 dev box 待辦：`npm run build` 確認 + push。本地資料唔會遺失（normalize 只補 roles，唔改其他欄）。

## 商家自定「角色類型 / 級別類型」（用戶 8/18：角色同級別都要自己 set）

用戶澄清：「商家需要自己新增不同的角色，和不同的級別類型」——即角色（原硬編碼 5 個 stylist/colorist/therapist/assistant/receptionist）與級別（原硬編碼 junior/senior/master，含工錢倍率）改為**商家於設置自定可增刪**。

設計：角色/級別不再係硬編碼 union，改由 `SalonBootstrap` 持有一份清單（預設種子同舊 5+3），顯示層全部經新 helper 由 bootstrap 衍生，取唔到先回退預設、再回退 id 本身（避免空白）。

落點：
- `types.ts`：`SalonStaffRole` / `SalonStaffLevel` 由 union 改 `string`；新增 `SalonConfigRoleType{id,label}` / `SalonConfigLevelType{id,label,multiplier}`；`SalonBootstrap` 加 `staffRoleTypes?` / `staffLevelTypes?`，**移除** `staffLevelMultipliers`（倍率改由 `staffLevelTypes[].multiplier` 單一來源）。`SalonServiceItem.wages?: Partial<Record<string,number>>`（string key）。
- `salon-labels.ts`：保留預設靜態映射作回退；新增 `DEFAULT_SALON_STAFF_ROLE_TYPES` / `DEFAULT_SALON_STAFF_LEVEL_TYPES` 同 `getSalonStaffRoleTypes` / `getSalonStaffLevelTypes` / `getSalonStaffRoleLabels` / `getSalonStaffLevelLabels` / `salonRoleLabel` / `salonLevelLabel` / `getStaffLevelMultipliers` helper。
- `wages.ts`：`computeStaffWage` 改用 `getStaffLevelMultipliers(bootstrap)`（由 `staffLevelTypes` 衍生），移除 `DEFAULT_STAFF_LEVEL_MULTIPLIERS` 對 `staffLevelMultipliers` 欄依賴。
- `mock-data.ts`：`defaultSalonBootstrap` 改種 `staffRoleTypes` / `staffLevelTypes`（移除 `staffLevelMultipliers`）。
- `storage.ts` `ensureSalonBootstrap` 遷移：舊店缺 `staffRoleTypes`→補預設；缺 `staffLevelTypes`→由舊 `staffLevelMultipliers`（如有）重建（保留倍率，id 對得上就用預設 label），否則補預設。
- `sync/route.ts`：`upsertBootstrap` 嘅 `staff_level_multipliers` 改由 `staffLevelTypes` 衍生寫入（DB 欄位保留兼容）。
- `settings.tsx`：TABS 加「角色與級別」tab；兩個 CrudSection（角色類型 `{label}` / 級別類型 `{label,multiplier}`）；員工表單 roles/level 選項改由 `getSalonStaffRoleTypes/LevelTypes(bootstrap)` 動態；服務項目工錢編輯器按可配置角色列出；舊「級別工錢倍率」section 移除（搬入級別類型 CRUD 嘅 multiplier 欄）。**內部 id 自動產生 `genId("role"/"lvl")`、唔開放編輯**——避免改名 id 引致 CrudSection upsert 按 id 配對失敗而產生重複條目（label 全開放自定）。
- `staff-list.tsx` / `staff-detail.tsx` / `service-runner.tsx`：載入 bootstrap，角色/級別顯示改用 `salonRoleLabel/salonLevelLabel`（可配置標籤）；staff-list 分組順序依 `getSalonStaffRoleTypes` 衍生 order。

驗收：`tsc --noEmit` 零真錯（僅 `layout.tsx LayoutProps` 環境誤報）；`eslint` 改動檔 0 error（剩 4 個 warning 全為既有 unused-vars / exhaustive-deps，非本輪新增）。用戶 dev box 待辦：`npm run lint && npm run build` + push。

設計決策備忘：角色/級別 id 係內部鍵（員工 roles、服務工錢 wages、員工 level 都參照），前端絕不顯示；顯示一律用 label。商家自定 = 自定 label（+ 級別 multiplier），唔係自定 id——簡單且避 rename bug。

## Salon 產品 SQL：store_id = demo-salon-001（唔係 Ledger 8 位登入號）

用戶要生成 30 產品 SQL，並以為自己店 ID 係 Ledger 8 位登入號 60000002。澄清咗一個關鍵架構事實：

- **salon 模組嘅 `store_id` 而家寫死 `demo-salon-001`**（`DEFAULT_SALON_STORE_ID`，`mock-data.ts:20`；`workbench.tsx:52` `ensureSalonBootstrap(DEFAULT_SALON_STORE_ID)` 永遠傳呢個值）。
- Ledger 8 位登入號（`resolve-shop-id.ts` 叫「topUp / Ledger 對外嘅 8 位店舖編號，非店員登入帳號」）暫時只係充值/會員用，**未接去 salon store_id**；`setActiveSalonStore` 全 codebase 冇人 call，localStorage `macau-pos-salon/active-store` 永遠係預設 `demo-salon-001`。
- 所以用戶雖然用 Ledger 60000002 登入，salon app 查嘅 store_id 都係 `demo-salon-001`。產品要見到就必須種落 `demo-salon-001`，種落 60000002 係睇唔到。

產出（supabase/migrations/，未 commit）：
- `0008_seed_salon_products_30.sql` — 30 項 `salon_products`，store_id = `demo-salon-001`，`on conflict (id) do nothing`。
- `0009_seed_store_demo_salon_001.sql`（原 `0009_seed_store_60000002.sql`，已 rename）— 將同一 30 項補入 `salon_bootstrap_config.products` jsonb + `staff_level_multipliers`，`on conflict (store_id) do update`；頂部註明 store_id 事實 + 將來真 Ledger 綁定時 replace `demo-salon-001`→新店號即可。

用戶待辦：Supabase 貼 0005→0006→0007→0008→0009 順序執行；`demo-salon-001` 係目前唯一 salon store（單租戶 mock）。將來要做「salon store_id = Ledger 8 位號」先算 Phase 2+ 綁定（另開 task）。

## Salon 產品目錄 mirror 入 salon_products 獨立表（用戶要求「所有內容上雲」）

用戶確認：產品要同時寫入 `salon_products` 獨立表（之前只喺 `salon_bootstrap_config.products` jsonb）；其餘設定型數據（員工/服務/類目/房型）保持 jsonb，唔拆獨立表。

落點：`src/app/api/salon/sync/route.ts` — `upsertBootstrap` 寫完 bootstrap 後，新增 `upsertSalonProducts(supabase, storeId, rec.products)` 逐條 upsert 入 `salon_products`（欄位 map：name/category/price/cost/commission_rate/active/sort_order/updated_at；store_id = rec.storeId ?? "demo-salon-001"；onConflict id）。**唔做 cascading delete**（避免清走 seed / 其他來源 product）；失敗 console.error 但唔阻流程。前端零改動（產品本來經 `BOOTSTRAP_UPDATED` 帶成個 array 過嚟）。

驗收：`tsc --noEmit` 零新增錯（唯一報錯仍係 `layout.tsx` LayoutProps 環境誤報）。用戶 dev box 待辦：
1. Supabase 跑 `0007_salon_staff_products.sql`（建 `salon_products` 表 + RLS；已 commit）。0008/0009 係 seed（可選）。
2. `npm run lint && npm run build` + push Vercel（sync route 係 server-side，要部署生效）。
3. 部署後，任意設定變更會觸發 `BOOTSTRAP_UPDATED` 將現有產品 backfill 入 `salon_products`；之後每次產品新增/編輯自動上表。已加嘅產品如想即刻入表，喺設置隨便改一項再儲存即可觸發。

## 修 sync「加咗嘢但上唔到雲」嘅真因 —— flush 機制缺位

用戶回報「加咗產品但 Supabase 冇嘢」。查證：產品經 `upsertProduct`→`patchBootstrap`→`saveSalonBootstrap`→`idbEnqueue({entity:"bootstrap"})` 確實入咗 queue。但 `flushSalonSyncQueue()` 舊有且只喺 **`online` 事件 / `visibilitychange` / app init** 觸發；用家一路 online 又冇切 tab，queue 個 item 永遠冇人 push → 所以連 jsonb（salon_bootstrap_config）都冇 sync。

修法（兩個檔）：
- `src/lib/salon/idb.ts` `idbEnqueue`：await tx commit 後 `void flushSalonSyncQueue()`（排完即 flush；flush 內已檢查 onLine + 按 entity dedupe，零成本）。
- `src/lib/salon/storage.ts` 啟動 block：加 `salonFlushTimer` 守衛嘅 `setInterval(triggerFlush, 20000)` 安全網，retry 失敗/遺留 item。
- `src/app/api/salon/sync/route.ts` POST：當 `!supabase && events.length>0` 時 `console.warn` 提示（診斷 env 冇 POS Supabase）。

驗收：`tsc --noEmit` 零新增錯（只餘 layout.tsx LayoutProps 誤報）。

**重要診斷點（用戶 dev box 驗證用）**：升級後加產品，開 DevTools→Network 睇 `POST /api/salon/sync` 有冇 fire + response `syncedCount`。若 fire 咗但 DB 空 → 查 server log 有冇「Supabase server client is null」警告，即係**部署 env 冇 POS Supabase**（要 `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`，呢個同 Ledger 嘅 `NEXT_PUBLIC_SUPABASE_URL` 係兩套）。若完全冇 fire → flush 仲未生效（應該唔會）。另確認 `salon_products` 表存在（跑 0007）。

### 再查：用戶確認「加產品完全冇 call 任何 API」→ 真因係 `!navigator.onLine` 硬擋

用戶反饋加產品時 Network 完全冇 `POST /api/salon/sync`。進一步查證：`flushSalonSyncQueue()` 開頭有
`if (... && !navigator.onLine) return;` —— 若瀏覽器/環境誤報 `navigator.onLine === false`（常見於 webview / 網絡探測飄忽），個 flush 喺 fetch 之前直接 return，所以**永遠冇 API call**，連 jsonb 都唔會 sync。呢個先係「冇 call API」嘅根。

追加修法（`src/lib/salon/idb.ts`）：
- 移除 `!navigator.onLine` 硬性 early-return；改為直接嘗試 fetch，離線 fetch 失敗會自然標 failed 等 retry。
- `openDb()` 開啟失敗時 `dbPromise = null`（唔快取 rejected promise，下次 call 再試）；`idbEnqueue` 用 `dbOrNull == null ? null : await dbOrNull.catch(()=>null)` 避免 null 上 call `.catch` 報 type error。
- flush 過濾改為 `pending || failed`（舊 code 只撈 pending，failed 會永遠卡住唔 retry）。
- 加 `console.debug`：`flush: N pending` / `pushed <entity> -> ok|http N` / `push failed (offline?)` —— 用家可以喺瀏覽器 Console 直接睇到 flush 有冇 fire、push 成功定失敗。

**關鍵提醒用戶**：改完要 **重啟 `next dev`**（唔好靠 HMR），因為 `storage.ts` 個 `if (typeof window !== "undefined") { setInterval + flush }` 係 module load 時跑一次，舊 module 唔會重跑。重啟後加產品，Console 應見到 `[salon-sync] flush: 1...` 同 `[salon-sync] pushed bootstrap -> ok`，Network 見 `POST /api/salon/sync`。若 `pushed ... -> ok` 但 DB 空 → 查 server log 有冇 `Supabase server client is null`（env 冇 POS Supabase）；若 `push failed (offline?)` → 真係網絡/離線。

### 環境性 CSP 擋 eval → Next.js dev client JS 跑唔到（根本性坑，記低）

用戶報「加產品 console 冇 log、network 冇 request，無痕都一樣」。查證：
- repo 完全冇 set CSP（`next.config.ts` 空白、`public/sw.js`、`src/` grep 冇、`middleware.ts` 唔存在）。
- `node_modules/next/dist/server/app-render/app-render.js:209`：`const csp = headers['content-security-policy'] || headers['content-security-policy-report-only']` —— **Next.js 唔會 send CSP，只係讀取上游已有嘅 CSP 嚟計 nonce**。
- 結論：個嚴格 CSP（block `eval`）係由**上游層 inject**（WorkBuddy desktop 網絡層 / 公司 proxy / VPN / 防毒 web-shield 喺 localhost 流量插 CSP）。
- 影響：Next.js **dev 模式靠 eval 做 HMR**，CSP 擋 eval → client JS bundle 執行唔到 → click handler 唔 fire → 表面「冇 log 冇 network」。我哋 sync code 係對嘅（tsc 過），純粹被環境卡死。

**解決路徑（用戶側，唔係 code fix）**：A) 拎走 CSP source（斷 VPN / 暫停防毒 / 試 `127.0.0.1` 代替 `localhost`）; B) 行 production build `npm run build && npm run start`（prod 唔使 eval，通常嚴格 CSP 下都 run 到，且可 end-to-end 驗 sync）; C) 喺唔經 CSP 層嘅乾淨瀏覽器開 localhost。加 `next.config.ts` 加 permissive CSP header 冇用（多 CSP 係 intersection，鬆唔到）。

**重要**：之後喺呢個 dev 環境 debug「前端冇反應」類問題，第一時間諗 CSP/eval，唔好又兜一大圈去改 sync code。

## Salon 綁定真實 Ledger 商戶（唔再用 demo-salon-001）

用戶確認 sync 已 fire，但 (1) DB 冇新產品；(2) 佢用嘅係**真實 Ledger 帳號/店**，唔係 `demo-salon-001`。根因：salon `store_id` 一直 hardcode `demo-salon-001`，而 `active-store` 呢個 key 冇人 set 過（login 冇寫、workbench 直接傳 `DEFAULT_SALON_STORE_ID`）。

改動（全部 tsc 過，只餘 layout.tsx 誤報）：
- `login-screen.tsx`：mode=salon 登入成功後 `saveActiveSalonStore(session.merchantId)` —— 真正把 salon 綁去登入嘅 Ledger 商戶。
- `workbench.tsx`：`ensureSalonBootstrap(loadActiveSalonStore() ?? DEFAULT_SALON_STORE_ID)` —— 真店就用真店 id，無 active store 先 fallback demo（開發/未登入）。
- `mock-data.ts`：加 `buildEmptySalonBootstrap(storeId)`（全空 bootstrap，唔種 demo）。
- `storage.ts` `ensureSalonBootstrap`：真實非 demo store 首次進入 → 種全空（該店從未開過 salon 係正常）；若本地本係 demo 資料而切去真店 → 重置全空（唔帶 demo 去真店）。
- `api/salon/bootstrap/route.ts`：指定 `storeId` 但無記錄 → 返 `source:"empty"` 全空（唔再 fallback demo），令從未開 salon 嘅店保持空；只有「無 storeId 嘅通用查詢」先 fallback demo（開發用）。
- `api/salon/sync/route.ts`：storeId 優先取 client 傳入（active-store=真 merchantId），缺省退取 BOOTSTRAP_UPDATED payload 內嵌 storeId，最後先 fallback demo。client 個 `pushSalonMutation` 已經會帶 `readActiveSalonStore()`。

驗收：用真實 Ledger 帳號登入（選「美容」）→ 該店應全空 → 加產品/服務 → Network 見 `POST /api/salon/sync` → `salon_products` / `salon_bootstrap_config` 嘅 `store_id` 應係該 Ledger merchantId（唔係 demo-salon-001）。
