# 2026-09-11

## 報表 KPI 帶「5 格一行 + 5 格全寬堆疊」根因診斷 + 修正（已修）
- 用戶回報：報表頁最上方 KPI 只有頭 5 格正常一行；尾 5 格（未結帳訂單／餘額總額／會員充值／會員扣點／毛利（估））各自佔滿一行、**緊貼無 gap**。與 2026-09-10 交付時描述嘅「10 格 → 5-5」不符。
- 根因：`src/components/restaurant-daily-report.tsx`
  - L2199 開 `<div className="mb-4 grid grid-cols-5 gap-3">`
  - L2243 **提早出現 `</div>`** → 個 grid 喺第 5 格之後就閂咗
  - L2250–2337 嘅其餘 5 個 `<Kpi>` 掉出 grid，成為 L2115 `div.block.p-4` 嘅直接子元素 → 全寬堆疊，`gap-3` / `mb-4` 亦隨 grid 消失
- 成因（2026-09-10 合併 grid 嘅操作失誤）：合併「兩個 5 格 grid」成「一個 10 格 grid」時，刪走咗第二個 grid 嘅 `<div>` 開頭同結尾，但**漏刪第一個 grid 嘅 `</div>`**。
- 為何驗證全綠卻漏咗：JSX 仍然完全平衡（`<div>` 有閂、`</>` 有閂）→ typecheck / eslint / next build 全部 0 error，`npm run test` 亦冇 DOM 測試。**只有肉眼睇畫面才捉到。**
- 建議修正（待用戶確認）：刪 L2243 嗰個 `</div>`，令 10 個 `<Kpi>` 同處一個 `grid grid-cols-5`。同時修好 L2245–2249 嗰段「同上面係同一個 grid」嘅誤導註解。
- 已寫入 `docs/113-agent-gotchas.md` 新章節「JSX 合併 grid 漏刪 `</div>`：build 全綠但版面散」。
- 用戶偏好提醒：呢個頁面嘅版面改動，用戶要求「**畫給我看，我說動手才動手**」→ 先出模擬圖 + 診斷，**唔好直接改**。

## 修正已套用
- 做法：**唔係單純刪 `</div>`，而係「搬」** —— 刪走 L2243 嗰個提早嘅 `</div>`，喺第 10 個 Kpi（`毛利（估）`）之後、`</>` fragment 之前補返 `</div>`。只刪唔補會令 L2199 嗰個 `<div>` 冇閂 → JSX 報錯。
- 同時改寫 L2244–2250 嗰段註解，明確寫「中間**絕對唔可以有 `</div>`**」+ 記低 2026-09-11 中過呢個坑。
- 結構驗證（人手核對，因為 build 捉唔到）：
  - L2199 `<div className="mb-4 grid grid-cols-5 gap-3">` → L2339 `</div>`，之間**冇其他同層 `</div>`**
  - 之間嘅 `<Kpi` 恰好 10 個（L2200/2207/2213/2219/2225/2253/2263/2268/2274/2280）
  - skeleton 分支（L2344）本來就正確（一個 grid、10 格）
- 驗證：`npm run typecheck` 0 error；`npm run test` 61 pass / 0 fail；`npx eslint <該檔>` 0 error（4 個既有 warning：`PosLocalSettings` / `debugOpen` / `setDebugOpen` / `Pill`）。
- 仲未做（已向用戶提過，等指示）：加最小 DOM 測試（驗 KPI grid 容器 `children.length === 10`）去防止同類錯再靠肉眼把關。

## 快餐線下訂單「接受 / 拒絕」按鈕 + 操作欄一行對齊（2026-09-11 已實作）
- 用戶需求：① **自動接單關掉後**，點餐頁「線下訂單」卡片亦要顯示「接受 / 拒絕」；② 訂單列表操作欄嘅掣改為**純文字「接受 / 拒絕」**（唔要 icon）、**一行過**、對齊。
- 現況證據鏈：`filterQuickActionBarOrders(openOrders)` → `isActionableQuickOrder()`（`pos-order-filters.ts` L213 **已包含 draft**）→ `quickPreparingOrders`（`pos-app.tsx` L1794 **已包含 draft**）→ `OrderCard` 有 render，但 `quick-local-orders-strip.tsx` L177 / L190 兩個掣都被 `order.status !== "draft"` 擋住 → **卡片只剩「查看」**（同用戶截圖一致）。
- 列表頁掣換行根因：`local-orders-panel.tsx` L496 操作欄係 `flex flex-wrap`，而 `SelfOrderActionButtons` 用 `flex-1` + `確認出單`（4 字）+ Check/X icon → 3 粒掣（查看／確認出單／拒絕）合計 ~224px 逼爆 `w-[19%]` 欄 → 換行。
- 計劃改動：
  1. `src/components/self-order-action-buttons.tsx`：`確認出單` → **`接受`**；移除 `Check` / `X` icon（純文字）；加 `whitespace-nowrap`；寬度改固定內距（唔再用 `flex-1` 撐爆欄）。
  2. `src/components/local-orders-panel.tsx`：L496 操作欄 `flex-wrap` → 一行；L534（行內）同 L648（查看彈窗）兩個 call site 跟住自動生效。
  3. `src/components/quick-local-orders-strip.tsx`：`OrderCard` 為 draft 自助單加「接受 / 拒絕」（新增 `onConfirmSelfOrder` / `onRejectSelfOrder` props）。**唔需要**傳 `selfOrderAutoAccept`：`status === "draft" && isSelfOrder(order)` 本身就等於「自動接單關掉、等人手接受」。
  4. `src/components/quick-mode-orders-bar.tsx`：把兩個回呼由 `pos-app.tsx` 透傳落 strip。
  5. `src/components/pos-app.tsx`：`QuickModeOrdersBar` call site（L5178）補回呼（`confirmSelfOrder` / `rejectSelfOrder` 已喺 L151 import）。
- 狀態：**已實作**（用戶 2026-09-11 10:47 確認「動手」）。
- 實作內容：
  1. `self-order-action-buttons.tsx`：標籤 `確認出單` → **`接受`**；**移除 Check/X icon**；加 `whitespace-nowrap`；**pending 期間刻意唔換文字**（只 `disabled` + `opacity-60` + `aria-busy`）——換「接受中…」會令掣闊多 24px，喺剛剛好放得落嘅欄位即刻逼出換行。保留 `flex-1` 令同層各掣等闊（卡片／彈窗／表格通用）。
  2. `local-orders-panel.tsx`：操作欄 `w-[19%]` → **`w-[22%]`**（`min-w-[860px]` 下 ~189px、扣內距 ~165px > 需求 156px；多出 3% 由「菜品」欄吸收）；「查看」「返結帳」加 `whitespace-nowrap`；4 處 toast 文案 `已確認自助單` → `已接受自助單`。
  3. `quick-local-orders-strip.tsx`：新增 `onConfirmSelfOrder` / `onRejectSelfOrder` props；`OrderCard` 加 `isDraftSelfOrder` 分支 → draft 自助單出「接受 / 拒絕」（三粒掣 `flex-1` 等闊）；狀態藥丸 draft 由硬寫死「製作中」改為 **「點單中」**（slate-100/slate-600 + slate-400 dot）。
  4. `quick-mode-orders-bar.tsx` + `pos-app.tsx`：補兩個回呼（pos-app L5178 call site），toast 文案同步。
- 驗證：`npm run typecheck` 0 error；`npm run test` **88 pass / 0 fail**；`eslint`（5 個改動檔）**0 error**（9 warning 全部既有，另修走一個 `self-order-action-buttons.tsx` 嘅 unused eslint-disable directive）；`next build`（首次撞到「Another next build process is already running」→ 重跑）。
- 根因一句話：**icon + 4 字標籤 令每粒掣 min-content 得 ~58px，而操作欄最窄只有 ~139px 可用** → 三粒掣 176px 放唔落 → `flex-wrap` 換行。修法要「縮 min-content」＋「擴欄寬」兩邊一齊做，缺一都會復發。
- 已寫入 `docs/113-agent-gotchas.md` 新章節「訂單操作欄『按鈕換行』」。

## POS「訂單詳情」彈窗補「接受 / 拒絕」（2026-09-11 第二輪）
- 用戶要求：撳卡片「查看」開嘅彈窗，要同外面一樣有「接受 / 拒絕」，文字 / 樣式 / 尺寸 / 間距 / 對齊都要同主要介面統一。
- 根因（同 strip 同一個病）：`pos-app.tsx` 彈窗 `actions` IIFE 內，draft 自助單三個掣全部被 `v.status !== "draft"` 擋走 → 彈窗只剩「關閉 / 補打帳單 / 重打單」，**冇任何接單入口**。呢個 IIFE 嘅註解本身就寫明「查看內嘅掣要同外面 quick strip 完全一致」——即係當初 mirror 嘅時候漏咗 draft 呢個 case。
- 改動：
  1. `self-order-action-buttons.tsx`：`size` 加第三級 **`lg`**（`rounded-2xl px-4 py-2 text-sm`，同彈窗隔離「關閉 / 重打單」同一尺寸）；新增 `fill?: boolean`（預設 `true`），因為 `ResponsiveModal` 嘅 action 列係 `flex flex-wrap justify-end gap-2`，唔關掉 `flex-1` 會令兩粒掣拉長霸滿整行。
  2. `pos-app.tsx`：IIFE 內 `if (v.status === "draft" && isSelf)` 早於 `showSplit` 分支 return `SelfOrderActionButtons`（`size="lg"` `fill={false}`）；成功後 `setViewingOrderId(null)` 關窗（同訂單頁彈窗行為一致）+ toast。
- 驗證：`typecheck` 0 error；`test` 88 pass / 0 fail；eslint 0 error；`next build` 成功。
- 待用戶實機確認：彈窗用 `lg`（14px）而卡片用 `sm`（11px）係刻意嘅分級（同一元件、同一語意）；若果佢要連尺寸都同卡片一模一樣，改成 `size="sm"` 即可。

## ✅ 第十輪：Realtime 根因「確診」+ 自檢工具 + 錯 key 誤判修正（2026-09-11 10:20）

**用戶證據**：
1. 收銀台警示條顯示 `連線目標：zymdemjflsckicwcinxl.supabase.co（ledger-fallback）` → **完全坐實**第九輪推斷：瀏覽器 Realtime 訂緊 **Ledger 專案**（`NEXT_PUBLIC_SUPABASE_URL` 舊變數），而 server 寫單去 POS 專案。
2. `information_schema.role_table_grants`（`pos_orders`）：`anon` **只有 SELECT**、`service_role` 全權 → 證明 **0016 已跑**（revoke all + grant select 生效），POS 專案本身 realtime-ready。唯一未確認項剩：「`pos_orders` 有冇入 `supabase_realtime` publication」（0011 動態 add）。

**本輪新增**：`tools/2026-09-11-check-pos-realtime.mjs`
- 用法：`npx vercel env pull .env.local` → `node --env-file=.env.local tools/2026-09-11-check-pos-realtime.mjs [--store <id>] [--watch 20]`（預設讀 `SUPABASE_URL`/`SUPABASE_ANON_KEY`，即 POS 專案）。
- 做三件事：① rest 探測 `pos_orders`/`pos_print_jobs`/`pos_soldout`/`pos_online_order_settings` 存在 + anon 可讀；② `@supabase/supabase-js` 訂 `pos_orders` 睇 channel status；③ `--watch N` 邊聽邊試（期間用手機落測試單）＝**唯一可信嘅 end-to-end 驗證**。
- ⚠️ 腳本明講「SUBSCRIBED ≠ 推送正常」（訂錯專案一樣 SUBSCRIBED）→ 唔可以當健康證明。

**🔴 新坑（實測撞到，已修）**：**錯 anon key 唔可以報成「表存在但 anon 被拒」**。
- 實測：錯 key → 401 `{"message":"Invalid API key"}`；完全冇 key → 401 `{"message":"No API key found in request"}`。
- PostgREST **未認證就拒絕，根本冇查表** → 錯 key 時**判斷唔到表存在與否**，報成「表存在但被拒」會帶錯排查方向（以為係 RLS/grant 問題）。
- 修法：`PosRealtimeProbeStatus` 新增 **`bad_key`**（`isBadApiKeyBody()`：`/invalid api key|no api key|invalid jwt|jwt expired|invalid signature/i`），次序必須**先判 bad_key** 再判 401/403/42501；`unauthorized` 只保留「key 有效但 anon 冇 select」。腳本同 `realtime-target.ts` 兩邊同步，且腳本撞到 bad_key 即刻 `exit(1)` 唔再刷版。

**驗證**：`typecheck` ✅ 0 error；`test` ✅ **88/88**（+5）；`eslint`（3 檔）✅ 0 error（pos-app 8 warning 全既有）；腳本 3 條分支實跑（錯主機 / 錯 key / 缺參數）全部正確。

**仍然等用戶做**（我冇 Vercel 權限，亦冇 any key）：Vercel 加 `NEXT_PUBLIC_POS_SUPABASE_URL` + `NEXT_PUBLIC_POS_SUPABASE_ANON_KEY`（值 = POS 專案）→ **重新部署** → 收銀台強制 reload → 警示條消失 + 落測試單驗即時彈出。

## ✅ 第十一輪：撳「掃碼新單」提示改為「留在點餐頁面顯示」（2026-09-11 10:40）

**用戶需求（附 2 張截圖）**：「我想改一下彈窗按後, 在點餐頁面內顯示即可, 不需要去到訂單的頁面內顯示查看。」
- 截圖 1 = 右上角持續提示「自取03 已下單／請查看」；截圖 2 = 撳完之後彈出嘅「訂單詳情」modal（自取01，背景係快餐點餐頁 + 底部自取卡片 strip）。

**舊行為（被移除）**：`openSelfOrderNotice()` 對 `tableId === "counter"`（快餐 / 自助機自取）嘅單會 `router.push("/orders?orderId=")` → `OrdersHub` 讀 query → 開「查看」彈窗。堂食單枱面 map 未更新時 fallback `setViewingOrderId()` 亦會開同一個 modal。

**新行為**：
- **冇枱（counter）**：唔跳頁、唔開 modal → `setNoticeFocus({ orderId, seq: Date.now() })`，喺**當前點餐頁面**高亮 + `scrollIntoView` 該張訂單卡，`NOTICE_FOCUS_MS = 2400` 自動熄。快餐模式 = 底部「線下訂單」strip；堂食模式 = 右欄「自取 / 掃碼訂單」面板。
- **有真枱**：維持 `selectTable()`（本身就係點餐頁面工作台）；枱面 map 未更新嘅 fallback 由「開詳情 modal」改為 `setActiveTableId()` + toast。
- **兜底**：`quickListOrderIdSet`（由 `actionBarLocalOrders` 建）判斷該單係咪真係喺列表；唔中就出 toast 指引，保證唔會撳完零反應。

**改動檔案（5 個）**：
1. **新增 `src/components/notice-focus-card.tsx`** — `NoticeFocusCard({ focusKey, className, children })`：`focusKey !== null` → 加 `ring-2 ring-orange-500 ring-offset-2` + `scrollIntoView({ block:"nearest", inline:"center", behavior:"smooth" })`。
2. `src/components/quick-local-orders-strip.tsx` — `OrderCard` 加 `focusKey`（`<article ref>` 上 ring + effect scroll）；`QuickLocalOrdersStrip` 加 `noticeFocus` prop。
3. `src/components/quick-mode-orders-bar.tsx` — 透傳 `noticeFocus`。
4. `src/components/pos-app.tsx` — `NOTICE_FOCUS_MS` 常數；`noticeFocus` state + 自動熄 timer effect；`quickListOrderIdSet` memo；重寫 `openSelfOrderNotice()`；右欄 counter 面板卡片改用 `<NoticeFocusCard>`；`<QuickModeOrdersBar noticeFocus>`；更新註解。
5. `src/components/self-order-notice-stack.tsx` — 更新規則 2 嘅註解（唔再跳 `/orders`）。

**🔴 新坑（已寫入 docs/113）**：`focusKey` **唔可以用 boolean**。`useEffect` 依賴比較係 `Object.is` → 同一張卡連撳兩次 `true → true` 唔會重跑 effect → 第二次「撳完冇反應」。必須傳**每次遞增嘅序號**（`seq: Date.now()`）。

**驗證**：`npm run typecheck` ✅ 0 error；`npm run test` ✅ **88/88**；`eslint`（5 檔）✅ 0 error（pos-app 8 個 warning 全部既有）；`next build` ✅ `Compiled successfully in 64s`。⚠️ 提醒：呢類 JSX 結構改動 build 捉唔到（見 docs/113 合併 grid 嗰條）→ 已人工核對 `<NoticeFocusCard>` / `</NoticeFocusCard>` 配對與內層 `</div>` 層數。

## Kiosk 模式全面審查（唯讀，未改任何 code）
- 用戶三問：① kiosk setting 嘅打印機 matching 做完未？② 係咪 real time、同掃碼一致？③ kiosk 係咪只做快餐？
- **① 未做（唔存在）**：`loadKioskPrinterConfig` / `saveKioskPrinterConfig` / key `macau-pos-kiosk-printer` **grep 零命中**；`docs/87 §6.2` + Phase 7 有規格但未落地。`/order` 裝置設定彈窗（`order/page.tsx:458-497`）只有「已綁定店鋪 / 重新綁定 / 退出 / 取消」；`kiosk-mode-panel.tsx:48-98` 只有開關；`pos_kiosk_settings` 表只有 `store_id / self_order_auto_accept / scan_mode`（`api/pos/kiosk-settings/route.ts:63-67`）。
- **現時 kiosk 出紙靠邊部機**：`buildKioskReceiptPrintJobs`（`print-jobs.ts:197-201`）→ `buildTemplateReceiptJobs`（:133-180）只揀 **`loadDeviceConfig().printers.filter(role==="receipt" && enabled)`**，空 → `return []`（:141）。而 `defaultDeviceConfig.printers = []`（`mock-data.ts:320`）→ 一部**專用 kiosk 平板**（從未喺 /settings 配過打印機）會**靜默唔出紙**（`use-kiosk-order.ts:619-625` 用 try/catch 吞掉）。
- **② 即時性唔一致（設計本就唔同，但兜底唔對稱）**：kiosk = 本機 `appendPrintJobs` + `PrintFlushWorker` 2.5s flush（`print-flush-worker.ts:8,49-51`）**唔上雲**；掃碼 = 由收銀端 realtime 補印，但**只認 `source === "scan"`**（`pos-app.tsx:1511-1513`、`:1095-1096`、`pos-orders.ts:271-272`）→ **kiosk 單冇任何兜底**，本機印唔到就完全冇紙。
- **③ 只做快餐＝冇硬閘，只有隱含**：`login-screen.tsx:173-174` 只寫 store-scoped `operatingMode="quick"`（唔係執行時閘）；`use-kiosk-order.ts:430` `mode = tableId ? "dine_in" : "quick"`，而 `/order` 冇台號 UI → 永遠 quick；但 `buildKioskOrder`（`kiosk-order.ts:260-286`）**支援 `dine_in`、冇 throw** → `docs/87 §5.1` 要「硬寫 quick」**未做**；邊緣 `/order?tableId=A01&store=x` 會被 `isScanLink`（`use-kiosk-order.ts:203`）判成掃碼堂食。
- **其他**：`kiosk-outbox.ts:82-85` 上限 20 **丟最舊**（可能丟未上雲舊單）；`:123` attempts>10 亦丟棄。會員自助扣款（Phase 9）**未做** —— `/order` 完全冇 member/phone 邏輯（grep 零命中），`login-screen.tsx:88` 只保留 session 備用。
- 已核對**已做**嘅 Phase：1（source / copies / print job 三欄 mapper `pos-order-mapper.ts:112-114`）、2（`DEFAULT_KIOSK_TEMPLATE` + print-center 第四分頁）、3（kiosk-mode-gate + 設定頁 + redirect）、4 一半（source + badge；鎖快餐未做）、5（kiosk 端唔建廚房單 + 收銀端 realtime 建單 + `confirmSelfOrder`）、6、8（`copies:1`；可取餐閘門已放寬 `quick-order-fulfillment.ts:36-38`）。**Phase 7 只做一半**（小票 builder 有，打印機設定 UI + `resolveJobPrinter` 擴充冇）。

## ✅ 第十二輪：Kiosk 專屬打印機（server 側）＋ 鎖快餐 ＋ 隊列修正（2026-09-11 下午）
**用戶定案**：① kiosk 要**另一台**打印機出紙畀客人（需要）；② 要 real time；③ kiosk **只做快餐**；④ 打印機設定**放 server 側 + 本機快取**；⑤ 入口已分開（我 confirm 過：`LoginMode` 四粒獨立按鈕、kiosk → `/order`、唔寫 `scan_mode`、登入即 `saveKioskMode(true)`，唯一 remark 係四粒掣同一版登入頁）。
> 過程：我先誤把「server 側」理解成 4 個選項去問，用戶回「這是你提出的，我也不太明白」，我用白話再解釋一次（本機 vs server 側嘅分別），並**更正自己**：原建議 #4「kiosk-outbox 轉 server 側」係我講錯（隊列本質就係離線暫存，搬去 server 等於廢咗個功能）。
**實作（13 檔改、3 檔新增）**：
1. **migration `0032_pos_kiosk_printers.sql`**：`pos_kiosk_settings.printers jsonb NOT NULL DEFAULT '[]'` + `jsonb_typeof = 'array'` CHECK。
2. **`lib/pos/kiosk-settings.ts`**：`KioskSettings` 加 `printers` + **`fromServer`**（區分「server 話冇」vs「攞唔到」，後者唔可以清快取）；新增 `normalizeKioskPrinters()` **白名單**過濾（缺 id/name、role/connection 唔合法 → 剔走）；`fetchKioskSettings(storeId, { fallbackPrinters })`。
3. **`lib/storage.ts`**：新 `kioskPrinters` store-scope key + `loadKioskPrinters()` / `saveKioskPrinters()`（快取，讀寫都過 normalize）。
4. **`print-bridge/hub.ts`**：`resolveJobPrinter()` = `[...deviceConfig.printers, ...loadKioskPrinters()]`。⚠️ 唔合併 → by-printerId 搵唔到 → 跌 by-role → **靜靜印去收銀台部機**。
5. **`print-jobs.ts`**：`buildTemplateReceiptJobs(..., receiptPrintersOverride?)`；`buildKioskReceiptPrintJobs()` 有 kiosk 機就用佢，空清單先 fallback deviceConfig（單機行為不變）。
6. **`print-bridge/dispatch.ts`**：修 `copies` —— 舊版只讀 `printer.copies`，`job.copies` 係死欄位（types.ts 明文話 job 優先）→ kiosk 小票「固定 1 張」形同虛設。改 `job.copies ?? printer.copies ?? 1`。
7. **`app/api/pos/kiosk-settings/route.ts`**：GET/POST 支援 `printers`；**42703 多級降級**（42703 唔講係邊個欄位 → 逐級試：全欄位 → 冇 printers → 連 scan_mode 都冇）；降級時 **omit** `printers` key（唔回 `[]`）。
8. **`lib/use-kiosk-order.ts`**：① **鎖快餐** —— `mode = variant === "scan" && tableId ? "dine_in" : "quick"`（舊版 `tableId ? dine_in : quick` = 只係隱含鎖，`/order?tableId=` 會變堂食）；② 落單時 `fetchKioskSettings(storeId, { fallbackPrinters: loadKioskPrinters() })`，`fromServer` 才 `saveKioskPrinters()`（**必須喺 `printKioskReceiptForOrder()` 之前**）；③ 隊列滿唔再扮成功（見 #10）。
9. **`components/printer-wizard-modal.tsx`**：加 `lockRole?: PrinterRole` → 略過用途選擇（kiosk 鎖 `receipt`）。
10. **`lib/pos/kiosk-outbox.ts`**：上限 20 → **50**；`enqueuePendingKioskOrder()` 改回 `{ count, enqueued }`，**滿咗唔再 `shift()` 丟最舊**（舊版丟走未上雲舊單 + 照當成功 = 假成功、廚房漏單）→ caller 報錯「請通知職員」。
11. **新增 `components/kiosk-printer-panel.tsx`**：`/order` 設定彈窗內嘅「自助點餐機打印機」（reuse `PrinterWizardModal` + `PrinterCardV2`）；本機即時寫、雲端防抖 800ms（IP input 逐字 onChange）；`/order` 設定彈窗改 `max-w-2xl` + 可滾動。
12. **新增 `lib/print-bridge/printer-test-print.ts`**：由 `device-settings.tsx` 抽出「三條通道逐條試」測試打印（抽走 device-settings 104 行 + 6 個 import）。
13. **測試**：`kiosk-settings.test.ts` 加 6 個 `normalizeKioskPrinters` case。
**驗證**：`typecheck` ✅ 0 error；`test` ✅ **94/94**（88+6）；`eslint`（13 檔）✅ 0 error（2 個 warning 全既有：`order/page.tsx:103`、`hub.ts:92`）；`next build` ✅ Compiled in 32.0s。
**🔴 新坑（已寫入 docs/113）**：`node --test`（無參數）會將 `**/test-*` 當測試檔 → 新模組原名 `test-print.ts` 被當測試跑，`ERR_MODULE_NOT_FOUND: @/lib` → 改名 `printer-test-print.ts`。
**仍未做**：0032 migration 未跑（需用戶／部署執行）；未實機驗證 kiosk 打印機出紙；`PrinterCardV2` 對 receipt role 顯示嘅標籤寫死「收銀台收據打印機」（kiosk 場景字眼未改，功能無影響）。

## ✅ 第十三輪：用戶四項回覆處理（2026-09-11 下午）
**用戶回覆**：① 0032 migration **佢自己跑**（我唔使落）；② 彈窗按鈕文字「**接受**」可以，維持唔改；③ 按鈕尺寸**統一**；④ 佢**已經 commit 咗**（`0f71e3e up`，18 檔）。
**本輪唯一改動 —— 按鈕尺寸統一**（`SelfOrderActionButtons`，3 檔改 + 1 份 docs）：
- 刪除 `size?: "sm" | "md" | "lg"` prop，`sizeClass` 寫死 **`rounded-xl px-3 py-2 text-xs`**；三個 call site（`quick-local-orders-strip.tsx`、`pos-app.tsx` 快餐條、`pos-app.tsx` 訂單詳情彈窗）移除 `size=`。
- **揀呢個尺寸嘅理由**：`local-orders-panel` 「操作」欄係最窄容器（`w-[22%]` + `min-w-[860px]` → ~165px 可用），三粒掣每粒 min-content 48px → **156px，啱啱好**。大一級（`text-sm`/`px-4` = 60px）即逼換行；細一級（11px）會同列表「查看」掣唔一致。
- ⚠️ **已知代價**：POS 詳情彈窗會出現 12px（接受/拒絕）vs 14px（關閉/補打帳單/重打單）。因為 `ResponsiveModal` action 列係 `flex flex-wrap justify-end`（**預設 `align-items: stretch`**）→ 高度會自動拉齊，只係字級／圓角唔同，唔會對唔齊。用戶反饋本身就係要「彈窗內嘅掣同外面一致」，所以**統一向下**係正確方向。若日後想連「關閉/重打單」都縮到 12px，再統一次。
- docs/113 舊條目「尺寸分三級」已改寫為「尺寸全局一種，唔可以再分級」。
**驗證**：`typecheck` ✅ 0 error；`test` ✅ **94/94**；`eslint`（4 檔）✅ 0 error（8 個 warning 全屬 `pos-app.tsx` 既有）；`next build` 見下。

## ✅ 第十四輪：模版「即時預覽」改為跟實際設定（2026-09-11 下午）
**用戶問題**：「我在模版內已經拿走了門店名，但即時預覽（真實熱敏樣式）沒有更改，為什麼？」
**根因**：`print-center.tsx` `buildPreviewLines()` 舊版**刻意** clone 一份 `visible` 全 `true` 嘅快照（只有 `divider` 例外）—— 理由係「等商家一眼見到完整版面」，但代價係左邊「區塊順序」checklist **對預覽零效應**（`escpos-render.ts:263` 嘅 `if (!b.visible) continue` 永遠唔觸發）。而個預覽叫「**即時**預覽（**真實**熱敏樣式）」，字面同行為直接矛盾。
**用戶決定**（AskUserQuestion）：「跟實際設定（推薦）」。
**改動**（`src/components/print-center.tsx` 一個檔）：
1. 刪走 `previewSnapshot` clone → 4 個分支直接用 `snapshot` → 預覽 == 真實出紙（同一套 `!b.visible` / `!text` 跳過規則）。
2. 橙色提示改寫：由「預覽**一律顯示全部欄位**（包括你喺左邊熄咗嘅區塊）」→「預覽**同實際出紙一致**：左邊冇打勾嘅區塊，呢度唔會顯示（想返嚟就打勾）」。
3. 「區塊順序」標題下加一句：「打勾 = 會印，熄 = 唔會印（預覽亦會即刻消失）」。
**取捨（已同用戶講清）**：收據模板有 8 個區塊**預設熄**（`checkout_time`/`server`/`service_charge_amount`/`tax_amount`/`rounding_amount`/`discount_amount`/`cash_tendered`/`change_amount`）→ 改完之後唔會再自動出現喺預覽（要打勾先見）。廚房 / 標籤 / 交班模板全部預設開，零影響。
**驗證**：`typecheck` ✅ 0 error；`test` ✅ 94/94；`eslint print-center.tsx` **16 error 全部既有**（`react-hooks/refs` / `immutability`，喺 L378 / L1102 / L1139 / L1246，唔關今次改動）；`next build` 見下。

## 🔴🔴 重大事故：環境把 `.git` 移入回收筒（第三次，2026-09-11 13:51）
- **發現**：跑 `git stash push`（**冇**設 `CODEBUDDY_SAFE_DELETE_ENABLED=0`）之後所有 git 指令報 `fatal: not a git repository`。
- **缺失**：`.git/refs/**` 整個目錄、`.git/objects/pack/*.pack` ×3（只剩 `.idx`）、大量 loose objects。原因：`is_git_directory()` 要求 `refs/` 存在，冇就直接話你唔係 repo。
- **根因（高度可疑）**：環境有「安全刪除」層會將 `unlink()` 變成**移入回收筒**（回收筒塞滿 `.git/index.lock`、`.git/index.stash.<pid>` 等 git 即棄檔）。git 跑 `gc --auto` / `pack-refs`（會刪舊 pack、刪 loose ref）時就被搬走 → repo 即刻散。⚠️ **對策：所有 git 指令一律 `CODEBUDDY_SAFE_DELETE_ENABLED=0 git …`**（出事嗰次冇設；同日 `next build` 有設，完全無事）。
- **還原**：寫 Python 解 `C:/$Recycle.Bin/<SID>/$I*`（v2: 0=ver, 8=size, 16=FILETIME, 24=nlen, 28=UTF-16LE 路徑）→ 撈出原路徑屬本 repo `.git` 嘅 1215 項 → 只複製目標唔存在嘅、跳過 `*.lock`/`index.stash.*` → **還原 1064 檔 + 56 目錄，0 失敗**。全部刪除時間 = `2026-09-11 13:51:16`（同一事件）。`refs/heads/main` = 797c5c9 復原，`git status`/`branch` 全部正常。
- ⚠️ **回收筒有 59,370 項**：`ls -la` / `find` 會被 sandbox SIGTERM 殺；只可以用 `ls` 導出去檔案分析。Python 掃描（16 秒）可靠。
- ⚠️ **仍有殘留損壞（未能還原）**：`git fsck` → **6 missing commit + 40 tree + 49 blob**；`git log` 由 HEAD 只行到 1 個 commit。缺失嘅係 9 個近期 local commit 物件：`322231b`、`579b78c`、`814acb4`、`68d554b`、`353cd0f2`、`dab0ec97`、`81bd01a3`、`67e9a02d`、`634aeca8`（回收筒冇、舊備份冇、origin 亦冇 —— 本機領先 origin 133 個 commit，origin tip = 366260d 只到 Sep 1）。
- ✅ **未受影響**：**工作區所有檔案完好**（code 冇損失）；`git log 366260d` 可完整行 **296 個 commit**；index / HEAD tree 可讀，可以照 `add` / `commit` 前進。

## 第十五輪：後廚屏 / 出餐台屏（KDS）方案（2026-09-11 14:00，純方案未實作）
- **用戶起點**：見到麥當勞廚房有部 iPad 顯示訂單、可確認出餐、唔用打印機 → 問係咩（答：KVS/KDS）→ 再問「我哋都做，可行嗎？」，要方案 + 介面／流程圖。
- **交付**：`docs/116-kds-kitchen-display-plan.md`（完整方案）＋ `docs/117-kds-mockup.html`（可互動原型，雙頁籤：後廚屏／出餐台屏，可撳 ✓ / 長按整單完成 / 撤銷 / 召後 / 模擬新單 / 模擬斷網）。**未改任何 src 代碼**。
- **可行性結論**：地基已有 8 成 —— `OrderItem.printerGroup`（分區真源）、`PosOrder.fulfillmentStatus` / `sentToKitchenAt` / `servedAt`、`usePosRealtime()`、`saveKioskDeviceBinding()` 模式、`posDeviceAuthHeadersFresh()`、`public/sounds/new-order.mp3`。
- **🔴 兩個關鍵設計決定（日後實作唔可以改）**：
  1. **單品完成表唔可以合併入 `pos_orders.items` JSONB**。必須獨立表（`pos_kds_item_state`，PK `(store_id, order_id, item_key)`）。原因：JSONB 逐行更新要整行覆寫 → 後廚屏同收銀台會互相冚走對方改動（lost update）。獨立表 = 兩個寫入者寫唔同嘅行，天然無衝突。
  2. **完成狀態用 `done_qty`（已出份數）整數，唔可以用 `done: boolean`**。客人加單（quantity 1→3）時 `done_qty=1 < 3` → 該行自動重新亮起「仲欠 2」；用打勾就會靜默漏單。呢個係 `diffAddedItems()` 之外嘅第二個加單盲區。
- **KDS 唔行 outbox**：後廚屏係另一部機、冇本機 order → 直接打專用端點（`/api/pos/kds/board|items|orders`）。**唔可以**叫佢走 `/api/pos/sync`（會產生半張單事件污染收銀台隊列）。
- **登入**：`LoginMode` 由 4 → 6（加 `kitchen` / `expo`）。⚠️ `scanModeForLoginMode()` 對呢兩個模式**必須回 `null`**（同 kiosk/salon 一樣），否則會同收銀台嘅堂食登入互相覆蓋店級 `scan_mode`（docs/115 §12 硬性口徑）。
- **最大兩個實作風險**（方案 §9）：**R1** Realtime 訂錯專案（`NEXT_PUBLIC_POS_SUPABASE_URL` 未配 → 照 `SUBSCRIBED` 但冇推送 → 「reload 先見到新單」）；**R2** iPad 分頁唔會自動換 JS → 屏開足一個月都係舊版。
- **等用戶拍板**（方案 §11）：屏係替代打印定並存（建議 `both` 過渡）、工位用 `printerGroup` 定另開一套、出餐台屏要唔要兼做叫號屏、後廚屏帳號用店長 8 位電話綁定（建議）定廚房師傅各有 PIN。
- **原型實作坑（已修）**：卡片「全部完成 → 8 秒後滑走」嘅 `setTimeout` 一定要有 **token 守門**，否則用戶撳「撤銷」之後舊 timeout 照樣剷走張單 → 靜默漏單。已用 `bumpToken()` / `scheduleRemove()` 解決。

## 🔍 第十二輪：排查「線上訂單接單後冇打印廚房單」（2026-09-11 14:20）· **只讀，零改動**

**用戶要求**：查明為何線上訂單接了之後冇廚房單；**明確要求唔做任何改動**（故本輪只讀檔 + grep，冇改任何 code／docs）。

**完整鏈路（已確認）**：接單 → `acceptLedgerOrder()` → ①`bridgeLedgerOrderToPos()`（dine-in 路徑；建 in-memory PosOrder + 出廚房/標籤 PrintJob）或 ②`printKitchenForLedgerOrder()`（quick 面板路徑，只印唔建 PosOrder）→ 兩者都行 `buildPrintJobsForItems()`（`src/lib/ledger/ledger-pos-bridge.ts:175`）→ `savePrintJobs()` + dispatch `pos-print-jobs-changed` → `PrintFlushWorker`（掛喺 `src/app/layout.tsx:56`，**全域**每 2.5s flush）→ 派去打印機。

**`buildPrintJobsForItems` 嘅閘門（決定會唔會出紙）**：
1. `!isPrintContentEnabled("kitchen") && !isPrintContentEnabled("label")` → **靜默 `return []`**（L183-185）。
2. 已啟用打印機中冇 `role === "zone" | "label"` → **throw**（L197-206，訊息會經 catch 顯示，但 quick 面板顯示成籠統「廚房單送出失敗，可稍後重打」）。
3. `items.length === 0` → **靜默 `return []`**（L207）。
→ ⚠️ 三條路徑**上層一律當成功**：`runAcceptAndBridge` / `runAccept` 都照彈「已接單並已送廚」success toast，即使 `printJobs = []`。**「已送廚」唔等於真係出咗紙。**

**🔴 最大結構性發現：只有「訂單頁」有『已接單但冇 local job』嘅補印機制**。
- `ensureKitchenPrintForAccepted()` 只存在於 `src/components/online-orders.tsx`（L227 定義；L350/404 realtime handler；L423-433 初次載入／resubscribe 掃一輪）。
- **`quick-online-orders-panel.tsx` 完全冇**（已 grep：只有 `runAccept` 自己撳嗰下會印，冇 `loadPrintJobs` 比對、冇補印）。→ 快餐模式下若張單係「喺別處接／自動接／面板重掛前已 accepted」→ **永遠唔會出廚房單**。
- 另外 `pos-app.tsx` 主畫面（dine-in 桌台總覽）**完全冇掛載任何線上訂單面板**（只有快餐模式嗰條 bar）→ 唔會自動接單、亦唔會補印，一定要開 `/orders`。

**其他已查清嘅非病因**：`printContentToggles` 有喺 `normalizePosLocalSettings` 白名單（`storage.ts:399-406`，預設 true）→ 唔會被 normalize 剷走；`autoPrint` **唔係**線上單閘門（ledger bridge 唔讀佢）；`resolvePrintJobStatus` 永遠回 `pending`；flush worker 全域、冇過濾 `ledger-` 單。

**⚠️ 高危組合（要交用戶核對）**：結帳區「自動打印」掣（`pos-app.tsx:3135 setAutoPrint`）會**一次過**寫 `kitchen + label + receipt` 三個細粒度開關 → 商家為咗唔想自動出收據而關掉「自動打印」，會**連廚房單一齊關**，而線上單照樣彈「已接單並已送廚」（零錯誤提示）。

**建議嘅 30 秒判定**：① 去「打印中心」睇有冇 `orderId = ledger-<id>` 嘅 job（冇 = 未建 job；有 pending = 建咗但送唔到機）；② 睇底部左邊有冇「列印失敗 N 張」；③ 睇設備設置 → 打印開關（kitchen/label 係咪 true）+ 打印機綁定（有冇 enabled 嘅 zone 機）；④ 睇係喺邊個畫面接單。

### 🔧 git 重建結果（用戶選「重建歷史」）
- 備份：`C:/dev/macauPos/macauPosSystem-git-damaged-20260911`（184MB，整個 `.git` 完整複製）。
- 標記分支：`pre-incident-20260911` → `797c5c9`（保留舊 SHA 參照；佢本身引用唔存在嘅物件，所以 `git fsck` 仍報 73 missing。滿意之後可以 `git branch -D pre-incident-20260911`）。
- 步驟：`git reset --soft 366260d` → `rm .git/index` → `git add -A`（由工作區重建 index，強制重新寫入所有 blob）→ `git commit`。
- ⚠️ **一定要 `rm .git/index` 再 `add -A`**：直接 `git add` 對 stat 未變嘅檔案會跳過 hash，唔會補寫已遺失嘅 blob（我早前中過，要用 `git hash-object -w` 逐個補）。
- 結果：新 commit `cb0b4b3`（parent = `366260d` = 2026-09-01）。`git log` 可行 **297 個 commit**、`git status` 乾淨、`git diff` / `git diff --cached` 正常。
- ✅ **`main` 可達 4,477 個物件 = 0 missing**（`git rev-list --objects main | git cat-file --batch-check | grep -c missing`）→ **可以正常 push**。
- 失去嘅：2026-09-01 → 09-11 之間嘅逐次 commit 記錄（247 個檔、+44301/-4843 行壓成 1 個 commit）。**檔案內容零損失**。

## 🔴🔍 第十三輪：線上單廚房單 —— 根因鎖定（2026-09-11 14:32）· **只讀，零改動**

**用戶補充**：打印中心只見到 `001`、狀態「有問題」；底部**冇**「列印失敗」紅標；打印機綁定**有**；用**堂食**；線上單 = Ledger 來源。

### 根因（代碼級硬傷）：Ledger 接單路徑**冇入 `PRINT_JOB_CREATED` 隊列** → 雲端 `pos_print_jobs` 永遠冇呢行 → relay APK claim 唔到 → 零出紙，但 UI 樂觀顯示「已發送」。

- 出紙真通道＝雲端中繼：`relay-transport.ts:1-8`（web「發單」= 確保已寫落雲端 `pos_print_jobs`）＋ `print-jobs.ts:91-108`（明文寫「淨行 `appendPrintJobs` 嘅建單路徑 → 張 job 永遠留喺本機 localStorage …一張紙都唔會出」）。
- 雲端寫入唯一入口＝`PRINT_JOB_CREATED` 經 `/api/pos/sync`（`src/app/api/pos/sync/route.ts:849`）。
- ❌ **`src/lib/ledger/ledger-pos-bridge.ts:305`（`printKitchenForLedgerOrder`）同 `:419`（`bridgeLedgerOrderToPos`）只做 `savePrintJobs()` + dispatch DOM event，冇入隊。**
- ✅ 全 repo 其他 **9 處** `savePrintJobs` 全部有配對 `PRINT_JOB_CREATED`（pos-app 2609/2745/2838/2988/3302/3538/3642、print-center 1021、shift-page 511/793）→ **呢兩處係唯二漏配**（`grep -rn "savePrintJobs(" src/` 已核）。
- 失敗鏈：job 淨喺 localStorage → 2.5s 後 `PrintFlushWorker` 見 `pending` → `dispatchOneJob()` relay 分支 → `RelayTransport.send()` **本身係 no-op**（只 `flushPosSyncQueue`，`relay-transport.ts:25-34`）→ 回 `{ok:true}` → `dispatch.ts:75` 寫 `status:"sent"` → 打印中心綠色「**已發送**」但冇紙。
- 因此**冇紅色「列印失敗」**（唔係 `failed`）→ 完全對得上用戶四點觀察。
- 二次傷害：`print-center.tsx:948-983 syncCloudPrintOutcomes()` 靠雲端覆寫本地，雲端冇呢行 → `cloudById.get(job.id)` = undefined → 永遠 `return job` → **就算 APK 真係印到，本地都永遠唔會變「打印成功」**，卡死喺「已發送」。

### `001` 之謎（已解）
- 打印中心「訂單號」欄 = `job.orderNo`（`print-center.tsx:1626`）。
- 線上單 `orderNo` = `resolveLocalOrderNo()`（`ledger-pos-bridge.ts:275`）：**優先 `pickupCode`**，冇先 fallback `自取-`/`外送-`/`線上-` + id 前 6 碼。
- → 該 Ledger 單 `pickup_code = "001"` → 打印中心寫 `001`（訂單頁寫「取餐碼 001」，`order-mapper.ts:188-189`）。**唔係見唔到線上單，係單號用咗取餐碼格式。**
- 判定法：睇嗰行「餐台」＝`堂食`（`resolveTableMeta` 冇指派台號）或 `堂食取餐`（`resolveQuickPickupTableName`）；票種＝「落單」（`ticketTypeLabel("normal")`，`escpos-template.ts:1016-1020`）。

### 已排除（非病因）
- 打印中心篩選隱藏：預設 `filter="all"` / `dateFilter="today"`（`print-center.tsx:216-218`）→ 除非係昨日單，否則唔會隱藏。
- `reprintOrder()`（`print-center.tsx:1018-1027`）同 `printReceiptForPosOrder()`（`print-jobs.ts:457-466`）**兩步做齊**（persist + 入隊）→ 對比下更凸顯 ledger bridge 漏配。

### 修復方向（**未改，等指示**）
- 最小改動：`ledger-pos-bridge.ts` 兩處改成「persist + 入 `PRINT_JOB_CREATED` 隊列」。
- ⚠️ **唔可以直接 import `print-jobs.ts`**：`print-jobs.ts` 反過來 import 咗 `ledger-pos-bridge.ts`（`getBridgedPosOrder`）→ **循環相依**（同 `print-toggles` 當年拆檔同一個理由，見 `print-jobs.ts:56`）。正解：抽一個共用模組（例如 `@/lib/pos/print-job-enqueue.ts`）放 `appendPrintJobsWithSync` 邏輯，兩邊都 import 佢。
- ⚠️ 唔可以純 `appendPrintJobs`（一樣唔上雲）；`appendPrintJobsWithSync` 目前係**未 export** 嘅內部函式。

## 🔍 第十四輪 B：GitHub Desktop 認唔到 repo —— 已修復（2026-09-11 14:36–14:45）

**用戶**：「按了 check again 也是一樣」（GitHub Desktop 彈「Unable to locate the Git repository at C:\dev\macauPos\macauPosSystem」，有 Locate… / Check again 按鈕）。

### 真正根因
- GitHub Desktop 驗 repo 用**帶 `--branch` 嘅 status**：`git status --porcelain=v1 --branch`、`git status --short --branch`、`git rev-list --count origin/main` **全部 exit 128**（`error: Could not read 322231b…`）。但**無 `--branch` 嘅 `git status --porcelain` 照 exit 0** → 之前只驗呢個，所以睇唔出。
- 原因：`origin/main` 指向 `797c5c9`（其 parent `322231b` 缺失）→ 任何要遍歷 origin 側祖先嘅指令都爆。

### 重大發現：遠端一直有完整歷史（「重建歷史」原來係白做）
- `.git/FETCH_HEAD`（mtime **14:34** = 用戶撳 Check again 嗰刻 GitHub Desktop 自動 fetch 成功）寫住 `797c5c97… branch 'main' of https://github.com/homeu98-glitch/macauPosSystem` → **遠端 main = `797c5c9`，即事發前已成功 push**。
- 所以 09-01→09-11 之間嘅逐次 commit 記錄**其實一直喺 GitHub**，唔需要永久丟失。
- 當時 `git fetch` 拉唔到嘢（無報錯、無下載物件）＝ 本機 `pre-incident-20260911` 仍指向 `797c5c9` → 被當成 `have` 上報 → 伺服器判定「客戶端已有全套祖先」→ 一個物件都唔送。

### 已執行修復（全部成功）
1. 刪 `pre-incident-20260911`（SHA 已記錄）＋ 暫時把 `origin/main` 指返 `366260d`。
2. `git fetch --no-tags --negotiation-tip=366260d origin main` → **補齊全部缺失物件**（`322231b`、`6761815e` …）；`git fsck --connectivity-only` **零 missing**（只剩 dangling，無害）。
3. 重建被環境搬走嘅 `.git/refs/remotes/origin/`（`mkdir -p` + `head -c 40` / `printf`）。
4. `git reset --soft 797c5c9` → 把「重建 commit」`cb0b4b3` 換返**真實歷史 tip**，再 `git add -A` + `git commit` → 新 tip **`bff1fa0`**（parent = `797c5c9`），內容 = 6 個檔（`print-center.tsx` 預覽修正、`docs/113`、`docs/116`、`docs/117`、2 個 memory 檔）。
5. 清走 reflog 壞條目（`634aeca8`，各 2 行 × 2 個 log 檔）。
6. `git config --local gc.auto 0`（減少環境搬走 pack 嘅機會）。
7. `git push origin main` → **`797c5c9..bff1fa0 main -> main` 成功**；`git ls-remote origin refs/heads/main` 確認遠端 = `bff1fa0`。
8. push 後 `refs/remotes/origin/` 又被搬走 → 再次重建 → `git branch -vv` 顯示 `[origin/main]` 已同步、`status --branch` exit 0。

### 結果
- **`main` = `origin/main` = 遠端 = `bff1fa0`**，ahead 0 / behind 0，歷史鏈完整（`bff1fa0 → 797c5c9 → 0f71e3e → 322231b → …`）。
- 原本「失去 09-01→09-11 逐次 commit」嘅損失**已完全恢復**，唔再需要接受。
- GitHub Desktop 應該可以正常開到 repo（佢用嘅驗證指令已 exit 0）。
- 可清理：舊備份 `C:\dev\macauPos\macauPosSystem-git-damaged-20260911`（184MB）、dangling commit `cb0b4b3`（無害；**唔好喺沙箱跑 `git gc`**，會再被搬走）。
- ⚠️ 建議：**git 變更操作（fetch / push / gc）盡量喺用戶自己嘅終端或 GitHub Desktop 做**，唔好經沙箱 —— 環境嘅安全刪除層會搬走 `refs/remotes/**` 甚至 pack。
