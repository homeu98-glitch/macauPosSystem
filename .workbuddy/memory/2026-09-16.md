# 2026-09-16

## 雲端列印中繼「配對」判準修正（GET /pair 兩欄）

### 起因（用戶指出我口徑錯）
我之前用 `pair-status` 綠燈當「Android 已就緒」——**錯**。
`pair-status` 只證明 `pos_print_agents` 有行（APK POST 過 /pair）；
但 APK 仲要由 `GET /pair?agentId=` 拎到 **POS 專案** 嘅 `supabaseUrl` + `anonKey`
才訂得到 `pos_print_jobs`。**兩者可以一個綠一個紅。**

### 實際鏈路（APK 側，repo：`C:/dev/print-agent-android`）
- `app/build.gradle.kts:26` — `POS_URL = "https://macau-pos-system.vercel.app"`（v1.1.3 / versionCode 8）
- `RelayActivity.doPair()` → `RelayApi.selfPair()` → `POST /api/pos/print-agent/pair`
- `RelayService.tick():131` → `pollPair(BUILD.POS_URL, agentId)`（配對前每 tick 打）
- `RelayService:135-136` → `prefs.supabaseUrl / prefs.anonKey`（存 SharedPreferences）
- `RelayService.startRealtimeIfNeeded():173` → 兩者空 → note「欠 supabaseUrl / anonKey，用 30s 輪詢兜底」
- `RealtimeClient.sendJoin():179` → filter `store_id=eq.<storeId>`，訂 `public.pos_print_jobs`
- ⚠️ `sendJoin` 只認 `phx_reply` → **訂錯專案／表唔存在照樣回 `status:"ok"`** → 顯示「已連線（Realtime）」（靜默失效）
- `RelayPrefs.isPaired()` 只睇 agentId + agentToken + storeId → **唔會**因為缺 supabaseUrl 而當未配對

### 修正（macauPosSystem）
1. `src/lib/print-agent-server.ts` — 新增共用 `resolveRelayRealtimeConfig()`
   - 只讀 `SUPABASE_URL` / `SUPABASE_ANON_KEY`，**成對**才回值，否則 `null`
   - **刻意唔 fallback** `NEXT_PUBLIC_SUPABASE_URL`（= Ledger，冇任何 `pos_*` 表）
   - 理由：寧願缺值（APK 有可見 fallback + 30s 輪詢仍印得出）都唔可以回錯專案
2. `src/app/api/pos/print-agent/pair/route.ts` GET — 改用上述 helper
   - 缺值 → `supabaseUrl: null` / `anonKey: null`（唔再空字串）+ 一條 warn log
   - 唔回 error（否則 APK `pollPair` 見 status!=paired 會永遠配唔到）
3. `src/app/api/pos/print-agent/pair-status/route.ts` — 新增 `androidReady: boolean`
   - 同 `/pair` 用**同一個** helper，杜絕兩邊口徑漂移
4. `src/components/print-center.tsx` — 存 `androidReady` 並喺失敗面板加一句「Android 側未拎到雲端連線憑證」
   - `json.androidReady !== false`（舊部署未回此欄 → 當 true，唔好無故標紅）

### 驗證
- `tsc --noEmit` 全專案 clean
- eslint 三個改動檔 0 error 0 warn；`print-center.tsx` 16 error / 2 warn **全部係既有**（改動行 243-246 / 427-433 / 1778 無新問題）
- prod `GET /pair?agentId=__probe__` → `200 {"status":"pending"}`（正常，未配對）
- `pair-status` / `pos/state` → 401 鑑權閘生效 ✅

### ⚠️ 未確認（要店內實測）
- prod **實際** `SUPABASE_URL` / `SUPABASE_ANON_KEY` 值睇唔到（需 Vercel / Dashboard）
- 所以要拎**真** agentId 打一次 `GET /pair?agentId=<真>` 睇兩欄係唔係 POS 專案 host，先算證實
- 順帶記：`POST /pair` 仍然無鑑權閘（唯一一條，只由 APK 呼叫）
- ⚠️ 此 repo 目前 `git status` 報 `fatal: not a git repository`（.git 存在但壞）→ 唔可以用 git 做 baseline 比對
  → ✅ **同日已修復**（見下節）

---

## ✅ 修復 .git（第二次中同款損壞，2026-09-16 完成）

**症狀**：所有 git 指令報 `fatal: not a git repository`。

**實測損壞範圍**（比 09-11 那次更重）：
- `.git/refs/**` 整個目錄不見（git 的 `is_git_directory()` 要求 `refs/` + `objects/` 同時存在，缺 refs 直接當你唔係 repo）
- 4 個 `.idx` 索引在、`.pack` 本體全冇 → `fsck` 報 `failed to load pack in position 0/1`
- 131 個 loose object 目錄全部空的
- 但 `.git/objects/pack/pack-ad35001fee...pack`（182MB）其實**完好**，`cat-file -t 3d71dba0` 回 `commit`

**關鍵命脈**：`.git/FETCH_HEAD` 記著 `3d71dba0`，`ls-remote` 確認 `origin/main` 就係同一個 SHA
→ **本地冇未推嘅 commit，遠端完整，冇丟任何歷史**。

**修復步驟（實測有效）**：
1. 先備份破損 `.git` → `C:/dev/macauPos/_git_backup_macauPosSystem_20260916`
2. `git ls-remote https://github.com/homeu98-glitch/macauPosSystem.git refs/heads/main` 取遠端 SHA
   （**唔靠 `origin` 名**，remote 設定可能已爛）
3. **手動重建 `refs/` 目錄樹**：`refs/heads/main`、`refs/remotes/origin/main`、`refs/remotes/origin/HEAD`
   各寫入 SHA（尾加 `\n`）。此步一做完 `rev-parse --show-toplevel` **即刻通過**
4. `git -c gc.auto=0 fetch --no-tags <原始URL> main`（**必須丟背景**，見下面坑）
5. 刪除**孤立 `.idx`**（無對應 `.pack` 者）→ 消除 `failed to load pack in position N`
6. 刪 `tmp_pack_*`（中斷下載殘留）、`multi-pack-index`（會引用已刪 idx）
7. 清理 reflog 中含遺失 SHA 的行 → **要同時比對第 1 欄（old）同第 2 欄（new）**
   （只比對 new 會漏，`fsck` 會繼續報 `invalid reflog entry`）
8. `git config --local gc.auto 0`（預防，見 docs/113）

**驗收（全部須 exit 0）**：
- `fsck --connectivity-only` / `fsck --full`
- **`status --porcelain=v1 --branch`** ← **GitHub Desktop 判準**，唔可以只驗無 `--branch` 嘅
- `branch -vv` → `main 3d71dba [origin/main] up`
- `count-objects -v` → `in-pack: 7603, packs: 1, garbage: 0`
- `log --oneline` → **495 個 commit**（09-01 起歷史完整保留）

**本次新發現的坑（要補入 docs/113）**：
- ⚠️ **`--git-dir=<path>` 參數一樣無效**（`fatal: not a git repository: '...'`）——
  缺 `refs/` 時 git 直接拒絕，唔係認唔到路徑。**要修好 `refs/` 先有可能用任何指令。**
- ⚠️ 用 node `execFileSync` 跑長 fetch 會被 timeout **SIGTERM**，且**物件可能已部分寫入**
  （留低 `tmp_pack_*` 54MB 殘留）。改用 `run_in_background`。
- ⚠️ reflog 過濾必須**兩欄都比對**。

---

## pending-count 502 診斷（**未修**，待用戶決定）

**鏈路**：`app-sidebar.tsx:113`（30s）/ `members-page.tsx:109`（12s，tab=topup）/
`member-topup-panel.tsx:84`（12s）→ `pending-count-store.ts` → `POST /api/topup/pending-count`
→ 代理到 `top-up-automation.vercel.app/api/integration/sitea/pending-count`。

**三段式錯誤**：
1. POS 自己：`route.ts:70` shopId 非 8 位 → 400「無法取得充值店舖編號」
2. 上游非 2xx：`route.ts:90-98` → **一律包成 502**
3. 截圖嘅 `{ok:false,error:"找不到對應店鋪"}` = **上游 `payload.error` 原樣透傳**
   （POS repo 全文搜唔到呢串字，所以唔係 POS 文案）

**實測上游（GET，帶自簽 JWT）**：

| 送出 | 上游回應 |
|---|---|
| `Bearer test` | `400 登入 token 格式錯誤` |
| 假簽章 JWT | `400 登入 token 驗證失敗` |
| 真簽章 JWT | `400 找不到對應店鋪` ← 用戶個案 |

⇒ **JWT 格式同簽章都通過**（`TOPUP_SITEA_SSO_SECRET` 兩邊一致），
純粹係**該商家 8 位 shopId 冇登記在 topUp 系統嘅店鋪表**。屬**資料問題，非程式 bug**。

**shopId 解析優先序**（`fetch-shop-id.server.ts:54-81`）：
`owner_session` → `env_override` → `owner_profile` → `staff_session` → `none`

- ⚠️ `owner_profile` 查 `profiles` 表，**RLS 擋住會靜默跳過**（`L:39-40` `if (error || !data?.[0]) continue`）
  → 悄悄掉到 `staff_session`，用**店員自己號碼**冒充店鋪編號 → 上游必然「找不到對應店鋪」。
  **店主登入唔中，店員登入會中。**
- ⚠️ `.env.local` 不存在 → `TOPUP_SHOP_ID_OVERRIDES` 逃生門是空的。

**建議**（待批）：① 請 topUp 側補該店 8 位號碼（最正路）；② Vercel 設 `TOPUP_SHOP_ID_OVERRIDES`；
③ 修 `profiles` RLS；④ 前端對永久性 4xx 加退避/抑制，唔好每 30s 重打。

---

## 🔴 Sunmi 中繼機「配唔到對」根因定位（2026-09-16 · 唯讀實測）

> ⚠️ **本節結論已被同日下一節推翻** —— 用戶回報商米**當前顯示「配對失敗: POS雲端未設定」**，
> 證明 APK **活著而且連得上**。真正根因見下面「✅ 真根因（推翻上節）」。
> 保留本節的理由：「claimed_by 全 NULL ⇒ 中繼機從未認領」呢個判準**依然有效**，
> 但當時**誤解讀**成「機器死了」；正確解讀係「**機器活著但拎唔到 Realtime 憑證，只靠 30s 輪詢**
> 而輪詢路徑亦被 401 擋住」。

### 當時結論（❌ 已推翻）：**唔關 env 事、唔關代碼事 —— 係 Sunmi 上嘅 APK 死咗／被系統殺咗**（連續離線 ~10 小時）

### 證據鏈（全部由公開 anon key 唯讀探測 + 線上 bundle 還原得出）

| # | 取證項 | 實測 |
|---|---|---|
| 1 | 最後一次成功出紙 | **2026-09-15 14:07:47**（訂單16） |
| 2 | 24h 內有無任何 `claimed_by`/`finished_at` 有值嘅行 | **完全冇**（全部 NULL）⇒ 中繼機**從未認領**任何單 |
| 3 | 最新 job | 2026-09-16 00:07:19（`pending`，attempts=0）⇒ **收銀端正常落單，只係冇人印** |
| 4 | pending 最舊 | 22:16:35（等 151 分鐘，attempts=0） |
| 5 | 前端 bundle 有無 POS 專案 anon key | ✅ **有**（`ref=iyrywzormzisyppkokbi`，role=anon）⇒ env 已生效 |
| 6 | 前端 bundle 出現嘅 Supabase 專案 | POS 專案 ✅ + Ledger（`zymdemjflsckicwcinxl`，屬 A 段正常） |
| 7 | `/pair?agentId=` 線上 | `200 {"status":"pending"}`（正常回應，端點活著） |
| 8 | `/pair-status` 無憑證 | `400 {"paired":false}`；帶 storeId → `401`（鑑權閘正常）|
| 9 | POS Realtime WebSocket 端點 | `401`（服務在，正常）|

### 為何「website 顯示正常」

`pair-status` 回嘅 `paired` 只係「`pos_print_agents` 有無一行」——
**係一個歷史事實，唔係即時狀態**。APK 由 14:25 之後就冇再 heartbeat，
但**行仍然在** ⇒ `paired: true` 永遠為真 ⇒ **Web 綠燈同 App 已死可以同時成立**。
唯一會變紅嘅係 `lastSeenAt` 換算出嘅 `minutesAgo >= 5` → 但**只有 `/prints` 頁嗰條紅 banner 才顯示**，
且要先有「雲端未完成任務」才 render；**收銀台 `/` 完全睇唔到**。

### 排除項（逐項否定）

| 嫌疑 | 判決 | 依據 |
|---|---|---|
| 今日改嘅 env | ❌ 無關 | bundle 內 POS anon key **有正確 inline** → env 生效且正確 |
| `SUPABASE_URL` fallback 去 Ledger | ❌ 無關 | ① 改動未部署（prod 仍跑 22:04 版本）② 即使生效亦**唔會**阻配對，只會令 APK 退 30s 輪詢但**仍然印得出** |
| 代碼部署破壞 | ❌ 無關 | 故障 14:25 開始；首個相關 commit `2494cbb` 係 **22:04**（4.5 小時後） |
| DB / RLS | ❌ 無關 | Realtime 端點 401（在）、anon key 讀到 pos_print_jobs（RLS 正常） |
| `0042` migration 未跑 | ❌ 非本次主因 | 未跑只影響「分段式搶單窗」；本故障係**根本冇人 claim**，唔關窗口事 |
| 打印機硬件 | ❌ 非本次主因 | 若係硬件，應見 `attempts > 0` + `AGENT_FAILED`；實測 **attempts=0** = 完全冇碰過 |

### 最可能真因（按可能性排序）

1. **🔴 Sunmi 上嘅 APK 被 Android 系統殺咗**（電池優化 / Doze）——
   開頭「昨天下午 5:30 之後一直發生」符合「被殺後冇自動重啟」。
2. **Sunmi 部機離線 / 關機 / 冇電**。
3. **APK 前台服務 crash**（需要睇設備 logcat 才知）。
4. **有人喺 Sunmi 上按過「取消配對」** → `revoked_at` 有值 → `/pair` 永遠 pending。
   （⚠️ `pos_print_agents` anon 冇 SELECT 權限，**呢項由呢邊查唔到**，要用 SQL Editor 跑
   `tools/print-relay-revoke-stale-agents.sql` 或直接 `select * from pos_print_agents;`）

### 待做（下一步需要用戶配合）

1. **去 Sunmi 部機睇**：App 仲開唔開？狀態欄寫咩？（「POS 雲端未設定」/「已連線（Realtime）」/ 完全冇 App）
2. **SQL Editor 跑**（service_role 才睇得到）：
   ```sql
   select agent_id, store_id, name, revoked_at,
          last_seen_at at time zone 'Asia/Macau' as 最後心跳_澳門,
          round(extract(epoch from (now() - last_seen_at))/60) as 幾分鐘前
     from public.pos_print_agents order by last_seen_at desc;
   ```
3. **重啟 Sunmi APK** → 睇 `/prints` 紅 banner 有無由「N 分鐘前（疑似離線）」變回 < 5 分鐘。
4. ⚠️ **重啟前先確認冇陳舊 pending 會一次爆紙**：現時只剩 4 張 pending（最近 40–151 分鐘），
   數量可控；但 `ttl` 目前**只喺 insert 寫**，舊行 ttl 恆 NULL ⇒ **永不過期**。
5. **長期防護**（見 `docs/reviews/incident-2026-09-15-print-outage.md` 五節 C/D）：
   · 中繼機離線要提升到**收銀台 `/` 可見**（現時只有 `/prints` 見到）
   · APK 加前台服務 + 開機自啟 + 電池白名單 + 連續 N 分鐘無心跳主動通知

### 診斷手法（可重用）
公開 anon key 由**線上 bundle** 還原（`_next/static/*.js` 內 `eyJ…`，解 base64 睇 `ref` 欄），
再唯讀打 PostgREST `pos_print_jobs` —— 唔需要任何 secret 就查到心跳／積壓／attempts。
判「中繼機死活」的鐵證 = **有無 `claimed_by` 非 NULL 嘅行**（`attempts=0` + 全 NULL ⇒ 從來冇 claim）。

---

## ✅ 真根因（推翻上節 · 2026-09-16）

### 觸發事實：商米**當前**顯示「**配對失敗: POS雲端未設定**」

呢句話係**決定性反證** —— 若 APK 已死，機器上根本唔會 render 任何狀態。

### 🔴 最關鍵發現：呢句字串**唔喺 APK 源碼入面**

- 全 `C:/dev/print-agent-android` grep `POS\s*雲端未設定|雲端未設定|配對失敗` →
  **只中一條**：`RelayActivity.kt:261` 嘅 `"配對失敗，請檢查網絡或店舖 ID 是否正確"`（**Toast**，格式唔同）。
- APK 側**真正**會顯示嘅係（`RelayService.kt:174`）：
  `RelayState.note("欠 supabaseUrl / anonKey，用 30s 輪詢兜底")` → 出喺 RelayActivity
  「訊息：」一行 **＋ 通知欄**。
- `res/values/strings.xml` 只有 5 條，**冇**任何「雲端未設定」。
- ⇒ **「配對失敗: POS雲端未設定」唔係 APK 原生字串，而係 IPA（iPad 側）／或其他 web 端**嘅文案。
  **排查方向要轉去 iPad 部嘅 POS 網站，唔係商米部機。**

### 兩條端點嘅真身（實測）

| 端點 | 實測 | 判讀 |
|---|---|---|
| `GET /pair?agentId=<假>` | `200 {"status":"pending"}` | ⚠️ **不存在嘅 agentId 一律回 pending** → 呢個探測**證明唔到** env 有無值，唔可以做「env 正常」嘅證據 |
| `GET /pair-status`（缺 storeId） | `400 {"paired":false}` | early-return 喺 auth 閘之前，正常 |
| `GET /pair-status?storeId=test-store` | **`401 {"ok":false,"error":"未經授權：需要 POS 終端憑證，請重新登入 POS 帳號。"}`** | 🔴 **網站嗰個「配對失敗」= 呢個 401** |
| live bundle（20 chunk） | POS URL ✅ + Ledger URL ✅ + 1 條 JWT `role=anon ref=iyrywzormzisyppkokbi` ✅ | POS anon key 前端齊 → `SUPABASE_ANON_KEY` 大機會已設 |
| bundle 有冇 `androidReady` | **冇**（20 chunk 掃齊） | 線上**未部署** 09-16 改動 → 仍跑 09-15 22:04 版本 |

### 「配對失敗」字串真身：`relay-pairing-panel.tsx`

- 徽章：`StatusBadge` `state.kind === "failed"` → **「配對失敗」**（L449-455）
- 紅塊：`ResultMessage` `kind === "failed"` → 「配對失敗」+ `state.detail`（L498-505）
- `state = failed` 嘅**唯一**來源：`checkStatus()` 拎到 `!r.ok || data.error`（L154-164）
  → `detail = data.error ?? "伺服器回應異常（HTTP xxx）"`
- ⇒ **所以 iPad 上紅色細字應該係「未經授權：需要 POS 終端憑證，請重新登入 POS 帳號。」**
  —— 呢句可以直接分辨係 401、5xx 抑或網絡問題。

### 🔴 核心機制：加鑑權閘把「配對流程」自己都擋死

`pair-status` 09-15 加咗 `posRouteAuthGuard`（**放喺 503/400 early-return 之後**，位置正確）。
但 `relay-pairing-panel` 靠 `pair-status` 判斷配對狀態 → 401 令 panel 變 `failed` →
**panel 一紅，配對流程就停擺** → 商米永遠等唔到確認 → 兩邊同時紅。

時間線吻合：
- `POS_DEVICE_TOKEN_SECRET` 由 service-role fallback 改為獨立 secret → **所有簽發中 token 即刻失效**（`incident-2026-09-15-print-outage.md`）
- `.env.example` mtime = `2026-09-15T09:18:08Z` = **澳門 09-15 17:18** ← 正正喺用戶講嘅「下午 5:30」之前 12 分鐘
- `2494cbb`（改 `pair-status` 加 `androidReady`）Macau **09-15 22:04**

### 額外陷阱：徽章綠 ≠ 探測成功

`pairing`（localStorage 有冇記錄）同 `state`（啱啱探測結果）係**兩個獨立 state**。
徽章只睇 `paired`；`state.kind==="failed"` 另出紅塊。
⇒ **可以同時見到「已配對」綠徽章 ＋ 下面紅色「配對失敗」** —— 用戶講「網站顯示正常」可能就係睇咗徽章。

### 另一個卡死點：`macau-pos-relay-auto-pair-stopped`

`relay-pairing-panel.tsx:53,55-67,96-97` —— 撳過「解除配對」或「停止自動配對」後
`localStorage["macau-pos-relay-auto-pair-stopped"]="1"` → **reload 都唔會自動重配**，
只有手動撳「配對」才重啟 5s 循環。呢個好易令人以為「一路紅住」。

### 診斷報告
`docs/reviews/diagnosis-relay-pairing-failed-2026-09-16.md`（含修復步驟 Step 1-5 + 長期加固 A-F）

### 修復優先序（待用戶執行）
1. **iPad 重新登入 POS 帳號**（最可能一次搞定；同時解 401）
2. 去 `/settings` 睇紅色細字**完整**內容 → 分辨 401 / 5xx / 網絡
3. 若曾撳過解除配對 → 手動撳一次「配對」重啟循環
4. Vercel 確認 `SUPABASE_URL` + `SUPABASE_ANON_KEY` **成對**存在（缺一即商米報「POS 雲端未設定」）+ **redeploy**
5. 確認 `POS_DEVICE_TOKEN_SECRET` 係咪 09-15 後改過

### 長期加固（建議）
- **A**：`pair-status` 401 應有專屬 `kind:"unauthorized"` + 「重新登入」按鈕，唔好同「配對失敗」混一齊
- **B**：401 時**仍然繼續** 5s 輪詢（商米側可能已配對成功），唔好停循環
- **C**：`androidReady` banner 現時**只喺 `/prints`** 且要有未完成雲端任務才 render → 收銀台 `/` 完全睇唔到
- **D**：`resolveSupabaseUrl()` 嘅 `?? NEXT_PUBLIC_SUPABASE_URL` fallback 要加警告 log
- **E**：build 時檢查 `resolveRelayRealtimeConfig() === null` → 出 `console.error`
- **F**：`0042` migration（分段式 claim window）仍未跑

### 新增可重用技巧
**掃 live bundle 嘅特定字串判斷「線上實際部署版本」** —— 例如 bundle 冇 `androidReady`
即證明 CDN 送到瀏覽器嘅係舊版。比 `git log` 更直接（git 只反映 repo，bundle 反映 reality）。
配合 `x-vercel-id` + `age` header 分辨「真新部署」vs「CDN 舊快取」。

---

## 🔴🔴 收口：`POS_DEVICE_TOKEN_SECRET` / `POS_REQUIRE_DEVICE_AUTH` 都冇設（2026-09-16 01:04 用戶確認）

### 用戶提供嘅事實
- `POS_DEVICE_TOKEN_SECRET` —— **冇設**
- `POS_REQUIRE_DEVICE_AUTH` —— **冇設**
- 剛剛把 `SUPABASE_URL` / `SUPABASE_ANON_KEY` **重新再 save 一次**

### 推導（呢兩條 env 都冇設，會發生咩事）

`resolveSecret()`（`pos-device-token.ts:50-57`）三段 fallback：
```
POS_DEVICE_TOKEN_SECRET → ADMIN_SESSION_SECRET → SUPABASE_SERVICE_ROLE_KEY / _KEY
```
⇒ 只要 **`SUPABASE_SERVICE_ROLE_KEY` 有設**，簽名密鑰就**照樣拿得到**，token 照簽得出。**唔會因為冇 `POS_DEVICE_TOKEN_SECRET` 而壞。**

`isPosDeviceAuthRequired()`（`pos-device-token.ts:136-140`）：
```
raw === undefined || raw === "" → return true   // ← fail closed！
```
⇒ **冇設 `POS_REQUIRE_DEVICE_AUTH` ＝ 閘照樣開著**（唔係關）。**呢個直覺陷阱好易中** ——
「冇設」≠「關閉」，係「強制」。

### 🔴 決定性實測（線上）

| 端點 | 實測 | 判讀 |
|---|---|---|
| `POST /api/pos/device-token` 空 body | `400 {"error":"缺少 accessToken。"}` | 端點活著，rate-limit 通過 |
| `POST /api/pos/device-token` 假 token | `401 {"error":"Ledger 會話已失效，請重新登入。"}` | **證明唔係 503「系統未設定終端憑證密鑰」** ⇒ **secret 鏈有值，簽發能力正常！** |
| `GET /api/pos/state?storeId=test-store` | `401 「未經授權：讀取店舖資料需要 POS 終端憑證」` | 閘生效 |
| `GET /api/pos/print-jobs/status?storeId=…` | `401 「未經授權：需要 POS 終端憑證」` | 閘生效 |

**⇒ 關鍵結論：`SUPABASE_SERVICE_ROLE_KEY` 有設，簽發路徑健康。**
**所以 `POS_DEVICE_TOKEN_SECRET` 冇設 ≠ 問題根源。**
唯一後果＝簽名密鑰係「借」service role key，日後若換 service role key，所有 token 一齊失效（可接受）。

### ⇒ 真正的病因收窄到：**iPad 手上嘅 token 已經失效／過期**

`/api/pos/device-token` 回「Ledger 會話已失效，請重新登入。」係因為我送假 token；
但呢句**正好就係 iPad 會遇到嘅情況**：12h TTL 過期 / `ledgerAccessToken` 過期 →
`refreshPosDeviceTokenIfNeeded()` 續期失敗 → 冇 token → 打 `pair-status` 得 401 →
`relay-pairing-panel` 顯示「配對失敗」。

**修復＝iPad 重新登入 POS 帳號**（唔使改任何 env、唔使 redeploy）。

### `SUPABASE_URL` / `SUPABASE_ANON_KEY` 重 save 之後必須做嘅事
🔴 **Vercel 改 env 唔會自動套用到現有 deployment，一定要 Redeploy。**
建議去 Deployments 睇最新一次部署時間，確認係「重 save env 之後」才部署。

### 附帶要做（今次可順手做，已有代碼）
09-16 已實作但**線上未部署**嘅改動（bundle 掃唔到 `androidReady` 證實）：
`resolveRelayRealtimeConfig()` + `androidReady` + `print-center` 提示 + `pair-status` 註解。
**呢批改動正正就係為咗令呢類事故下次一眼睇得出**，建議連今次修復一齊 push + redeploy。

### 🔴 建議加嘅新 env（可選但強烈建議）
`POS_DEVICE_TOKEN_SECRET` = 隨機 32 bytes hex。理由：而家簽名密鑰係借 service role key，
兩者職責混在一起；日後換 service role key 會意外令全部 POS 終端即刻要重登。
設咗之後記得：**所有簽發中 token 即刻失效 → 全部 POS 終端 / Admin 要重新登入一次**（一次性代價）。

---

## 🔴🔴 真正原因找到了：`SUPABASE_ANON_KEY` 冇 fallback（2026-09-16 01:10）

### 線上版本（3d71dba）嘅 `GET /pair` 係咁寫嘅

```ts
// src/app/api/pos/print-agent/pair/route.ts (3d71dba)
const url = process.env.SUPABASE_URL ?? process.env.NEXT_PUBLIC_SUPABASE_URL ?? "";
const anonKey = process.env.SUPABASE_ANON_KEY ?? "";
return NextResponse.json({ status: "paired", storeId, storeName, supabaseUrl: url, anonKey });
```

🔴 **兩個 variabe 待遇唔對稱**：
- `url` 有 fallback（而且 fallback 去 **`NEXT_PUBLIC_SUPABASE_URL` = Ledger 專案！**）
- `anonKey` **完全冇 fallback**

⇒ 只要 `SUPABASE_ANON_KEY` 冇設：
- `anonKey = ""` → APK `RelayApi.pollPair` 嘅 `takeIf { it.isNotBlank() }` → 變 `null`
- → `RelayService.startRealtimeIfNeeded()` 判到缺值 → note「欠 supabaseUrl / anonKey，用 30s 輪詢兜底」
- → 商米顯示「POS 雲端未設定」

而 `url` 甚至可能靜默 fallback 去 **Ledger 專案**（冇 `pos_*` 表）→ 訂咗都冇用（靜默失效）。

**而我喺 653ea43（01:06 push）加嘅 `resolveRelayRealtimeConfig()` 要求兩個成對、
且刻意唔 fallback Ledger —— 正好根治呢個唔對稱。**

### 部署狀態
- `653ea43` 01:06:54 commit（含全部修改），**已 push**，working tree clean
- 01:14 實測：bundle **已含 `androidReady`** ⇒ **Vercel 已完成新部署**
- `resolveRelayRealtimeConfig` 唔喺 bundle 係正常（server-only）

---

## 📊 「雲端未認領」係點計出嚟（用戶提問）

### 完整鏈路（全部由雲端 DB 回讀，唔係本地推測）

1. POS 建單/結帳 → 本地 job `sent`
2. `appendPrintJobsWithSync()` → 上雲寫 `pos_print_jobs`（`status=pending`, `attempts=0`, `claimed_by=NULL`）
3. APK claim（`POST /claim`，用 `for update skip locked`）→ 標 `printing` + 寫 `claimed_by`
4. `/prints` 每輪 `syncCloudPrintOutcomes()` 打 `GET /api/pos/print-jobs/status?storeId=`
5. 該 route **第二段查詢**：`status IN ('pending','printing')` → `unfinished` 陣列（L110-116）
6. 前端 `print-center.tsx:1103-1114` 存成 `cloudUnfinished[id]`
7. 渲染 `print-center.tsx:1921-1932`：`pending` → 橙徽章「**雲端未認領**」；`printing` → 「已認領未回報（試 N 次）」

### 判準（ `print-job-failure.ts` `classifyPrintJobFailure()` ）
- `pending` 且 `created_at` **逾 6 分鐘** → `TIMEOUT_CLAIM`「一直冇中繼機認領」
- 6 分鐘內 → **正常排隊**，唔算異常（唔好自己嚇自己）
- `printing` 且 `claimed_at` 逾 90 秒 → `TIMEOUT_STALE`
- 有 `last_error` 原文 → `AGENT_FAILED`；`VOID_` 前綴 → `VOID_STALE`；`attempts>=5` → `ATTEMPTS_EXHAUSTED`

### 🔴 用手機（非 Sunmi）做中繼機測「能否連到」——**可行，而且係好嘅診斷法**

條鏈要拆開睇，得 vs 唔得嘅界線喺**第 5 步**，唔喺前面：

| 階段 | 手機做唔做到 | 原因 |
|---|---|---|
| ① 配對 `POST /pair` + `GET /pair` 輪詢 | ✅ | 純 HTTP |
| ② Realtime 訂閱 **或 30s 輪詢兜底** | ✅ | 🔴 **就算冇 `supabaseUrl`/`anonKey`，APK 仲有 30 秒輪詢兜底 ⇒ env 缺陷唔會令測試完全失效，只係慢 30s** |
| ③ Claim（`pending` → `printing`） | ✅ | 純 HTTP，與打印機硬件完全無關 |
| ④ Render bytes | ✅ | 純 CPU |
| ⑤ **Dispatch 出紙** | ❌（若要 Sunmi 內置） | `JobRunner.dispatch()`：`connectionType=="sunmi"` → `SunmiPrinter.ensureReady()` 需要 AIDL `woyou.aidlservice.jiuiv5`，普通電話冇 → failure「Sunmi 內置打印機未就緒（未綁定）」 |

**⇒ 「雲端未認領」係第 ③ 步未發生，同打印機冇關係。**
所以用手機測試，**「雲端未認領」係會消失嘅**（變「已認領未回報」→ 再變 failed）。

### 🔴 真正要觀察嘅「成功訊號」（用手機測）
```
pending「雲端未認領」
  → printing「已認領未回報」        ← ✅ 證明 配對 + 雲端連線 + claim + render 全通
  → failed + last_error「Sunmi 內置打印機未就緒（未綁定）」
                                    ← ✅ 呢個 failure 本身就係「前面全通」嘅證明（電話冇打印頭，預期之內）
```
只要見到 **failed 且 error 係「Sunmi 內置打印機未就緒」**，就代表雲端到 APK 成條路係好嘅，
問題只會剩落「部機本身」（冇打印頭 / Realtime 憑證 / 對外網絡）。

反之，若**一直停喺 pending「雲端未認領」** → claim 冇發生 → 查：
① APK 係咪真係配對咗（localStorage 有冇 pairing）② claim 係咪 401（`x-agent-token` sha256 要對 `token_hash`）
③ foreground service 有冇起

### 🔴 另一個高機率陷阱：**重裝 APK 會換 agentId**
`RelayPrefs.ensureAgentId()` 冇值就生成新嘅 `ag-<16 bytes hex>`。
- 舊行仲喺 `pos_print_agents`（舊 agentId）→ web `pair-status` 照樣回 `paired:true`（綠，但係**舊機**）
- 新 APK 用**新 agentId** 打 `/pair` → 永遠 `pending` → 配對唔到 → 唔會 claim → 「雲端未認領」

**⇒ 解法：喺新 APK 嘅中繼畫面自己撳「配對」+ 輸入 storeId**（`RelayActivity.doPair()` → `selfPair()` → POST /pair）。
storeId 可以喺 iPad `/settings` relay panel 底下嘅「**技術資料（店舖識別）**」`<details>` 度睇到
（`relay-pairing-panel.tsx:396-412`）。⚠️ 要填**真嘅 merchant UUID**，填 `macau-store-a` 呢類
會被 `isPlaceholderStoreId` 黑名單擋（POST /pair 400）。

---

## ✅ 01:40 續報：修復生效，中繼機開始認領；新卡點 = `dispatch failed`

### ① 雲端嗰截已經通咗（決定性證據）
- 之前：`claimed_by` / `claimed_at` **全 NULL**（從来冇人認領）
- 01:30 實測：`claimed_by = ag-a466746da8946cd1421916043eb643e1`、`claimed_at = 01:30:55`、
  `attempts` 真係加到 2/3/5，狀態走到 `pending → printing → failed`
  ⇒ 01:10 嗰個 `SUPABASE_ANON_KEY` 冇 fallback 嘅修復（`653ea43`）**生效**。

### ② 新失敗點
- 近 20 行有 8 張：`failed / attempts=5 / last_error="AGENT_FAILED: dispatch failed"`
- `result/route.ts:77` = `AGENT_FAILED: ${rawError}` ⇒ **`dispatch failed` 係 APK 自己 POST 上嚟嘅原文**
  ⇒ claim ✅、render 冇拋錯，**衰喺最後「送紙落打印機」嗰步**。

### ③ 🔴 結構性原因（最可疑）
每張 job 實測：
- `printer_name=廚房打印機`、`printer_id=printer-kitchen-1`
- **`printer`（jsonb 快照）= NULL**（server 從未寫過）
- **claim 回傳 `printers[]` = 硬編碼 `[]`**（`claim/route.ts:42`；`docs/98:452` 已記載係未修 TODO）

而三個版本 APK 嘅 `JobRunner.resolvePrinter()` 都係：
`row.printer` → `printers[]` → 本機同名設備 → **兜底**
兜底 = print-agent-android 嘅「Sunmi 內置打印機」／print-relay 嘅 `ipAddress=null`「LAN 打印機」
⇒ 前三步配唔到就**必然**跌落假打印機 → 100% dispatch 失敗。**同「全部同一句 error」完全吻合。**

### ④ ⚠️ `dispatch failed` 唔喺本機任何源碼／APK
- 全機 `C:/dev` 1389 個文字檔 grep → 0 命中
- 三個 Android repo 全部得中文文案（`"列印失敗（第 x/y 份）"` / `"打印機連線逾時…"` / `"Sunmi 內置打印機未就緒（…）"`）
- **解 zip → inflate `classes*.dex` → 掃 13 個 APK：`dispatch failed` 全部 0 命中**；
  `RelayState` / `列印失敗` / `printBytes` 有命中 ⇒ 方法有效
- ⇒ **機上個 build 源碼唔喺呢部機**。要用戶提供實裝 APK 先追得到。
- 🔴 **坑**：APK 嘅 dex 係 **DEFLATE 壓縮**，直接 `readFileSync(apk).includes(字串)` **一定掃唔到**
  （實測連 `RelayState` 都係 n）。要解 zip central directory + `zlib.inflateRawSync`。
  可重用工具：`tools/_apk-strings.cjs`（CLI：`node tools/_apk-strings.cjs "字串1" "字串2"`）。

### ⑤ 中繼機又靜咗
- 活躍窗口 **01:12 → 01:31**（約 18 分鐘），之後冇再認領
- 01:34:17 張新 job 到 01:40 仍 `pending`、`claimed_by=NULL`
- ⇒ 同 09-15 事故同一個病：退背景冇 foreground service 保住。

### ⑥ 建議（**未動手**，待用戶批）
1. job 補打印機快照（寫 `printer` jsonb 或令 `claim` 返真 `printers[]`）← 根治
2. 用戶提供機上實裝 APK
3. APK 補 foreground service + 開機自啟 + 電池白名單
4. 中繼離線要提升到收銀台 `/` 可見（現時只有 `/prints`）
5. 仍未跑 `0042` migration

### 診斷文檔
`docs/reviews/diagnosis-relay-pairing-failed-2026-09-16.md` §九（續）

---

## 🎯 07:40 真根因鎖定：**打印機唔喺同一網段**（唔關程式事）

### 用戶 push 後商米重新上線，但雲端仍然失敗 —— 卻夾雜咗一句決定性錯誤

任務 `01:13:47`（重試 5 次後）嘅 `last_error` 原文：

```
AGENT_FAILED: 兩種通道都失敗｜
  直連：failed to connect to /192.168.31.38 (port 9100)
        from /10.61.49.153 (port 41752) after 5000ms｜
  SDK：打印機連線失敗（lan 192.168.31.38,9100 → 失敗）
```

| 觀察 | 判讀 |
|---|---|
| `192.168.31.38:9100` | ✅ APK **正確解析到廚房打印機**（唔係兜底假機） |
| `10.61.49.153` | 🔴 **商米自己嘅 IP**，**唔同網段** |
| `兩種通道都失敗` | 呢句**只存在於 `print-relay`** ⇒ 商米跑 `com.macau.printhub` **v1.1.4 / versionCode 6** |

⇒ **`10.61.49.x` 打唔到 `192.168.31.x:9100`**。`192.168.31.x` = 家用路由器預設網段（打印機）；
`10.61.x.x` = 典型**手機熱點／流動網絡**網段 ⇒ 商米連咗**唔同一個網絡**。

### 修復：**唔使改一行代碼**
商米 Wi-Fi **改連店內同一部路由器**（要 `192.168.31.x`）+ **關掉流動數據**
（避免 cellular 做 default route）。驗證＝睇錯誤訊息 `from /<IP>` 有冇換段。

### 澄清 §9.3
「job 冇帶打印機連線資料」呢個結構缺陷**依然存在、依然要修**，
但**今次唔係主因**（`printer_id` 有值、`deviceConfigPrinters` 拉到配置、IP 解析正確）。
`printer` jsonb 恒 NULL 係因為 `/api/pos/sync` 只寫 `printer_name` / `printer_id`，**從來冇寫快照**。

### 🔴 新發現：下一粒定時炸彈
`print-relay` 嘅 `fetchDeviceConfig()`（`RelayApi.kt:221-252`）**完全冇帶憑證 header**，
但 `device-config` route（`route.ts:28`）**已經有** `posRouteAuthGuard`。
⇒ 一旦收緊到「有 storeId 就要驗身」，**中繼機即刻拉唔到路由配置 → 打印機 IP 變 NULL → 全店停印**。
**要盡快為中繼機補一條 agent-token 可讀 device-config 嘅通道。**

### `dispatch failed` 之謎（部分解）
商米上**前後裝過兩個唔同 App**：`ag-a4667…`（01:30，報 `dispatch failed`）
vs `ag-0590816d…`（07:38，報 `兩種通道都失敗`）。前者嘅 build 源碼唔喺呢部機。

---

## ✅ `dispatch failed` 完全解開 + `macau-ledger-merchant` 配對流程審查（源碼級）

### 用戶提供：`C:\Users\surface\Desktop\macau-ledger-merchant.zip`

**✅ 已驗證（Node zip 直讀 + `zlib.inflateRawSync`）：**
- zip 係**源碼包**（163 entries，**冇 `classes.dex`**）⇒ 讀唔到 `BuildConfig` 實際值
- 與 `C:\dev\_ref-macau-ledger-merchant` 逐檔比對：**85/85 `.kt/.kts/.xml/.pro/.json` 位元組完全一致**，0 差異 0 缺失
- ⇒ 本地行號**直接對應**商米上跑嘅 build

### 🔴 `dispatch failed` 真身找到了
**`posrelay/PosJobRunner.kt:247`** 硬編碼：
```kotlin
relayApi.report(agentId, agentToken, dto.id, "failed", "dispatch failed")
```
⇒ §9.4「全機 grep 唔到」之謎解開：**源碼一直唔喺本機**，唔係咩加密／混淆。

### 配對流程核心（`PosPairingManager.kt`）
`autoPair()`（`:13-44`）：`postPair` → `getPair` → 校驗 5 道 → `PosRelayClient.init(url, anonKey)` → `savePairing`。
五道防線：① `POS_URL` 空即停（`PosRelaySession.kt:201`）② 只認 `ok:true`（`RelayApi.kt:61`）
③ `storeId != merchantId` 拒絕（**防串店**）④ URL 唔可以等於 `BuildConfig.SUPABASE_URL`（**防打 Ledger**）
⑤ url/anonKey 必須成對非空。

### 🔴 新發現 ①：`POS_URL` 在三個實戰 env 檔全部缺失（潛在高危）
實測 key 清單：`.env.example`（13 key）**有** `POS_URL`；
`.env.local` / `.env.prod.local` / `.env.uat.local`（各 12 key）**全部冇**。
而 `loadProjectEnv()`（`build.gradle.kts:11-26`）**只讀第一個存在的檔，唔會合併** ⇒ `prod` flavor 讀 `.env.prod.local`（存在）⇒
**永遠唔會 fallback 去 `.env.local`** ⇒ `BuildConfig.POS_URL = ""` ⇒
`PosRelaySession.kt:201-204` 直接 `status="配對失敗：POS 雲端未設定"` 並 **return**（通道永不啟動、唔會自動恢復）。
⚠️ **但本次部署一定唔係空** —— 因為用戶截圖顯示「已認領未回報」，claim 成功過 ⇒ `POS_URL` 有值。
⇒ 列為**防禦性待辦**，唔係本次根因。修法：兩個 flavor env 各補 `POS_URL=https://macau-pos-system.vercel.app`。

### 🔴 新發現 ②：dispatch 失敗原因三層全被吞（今次查幾個鐘嘅結構原因）
```
LanTcpPrinter.kt:27-30    catch → log 本機 + 回 false（e.message 消失）
PosPrintDispatcher.kt:9-26  只回 Boolean
PosJobRunner.kt:247      上報硬編碼 "dispatch failed"
```
⇒ 雲端分唔清 `ECONNREFUSED` / `timeout` / `UnknownHost` / Sunmi 未綁定。
**對比 `print-relay` 嘅 `SdkPrinter.kt:182`**：`兩種通道都失敗｜直連：$rawErr｜SDK：…`
—— 就係嗰一句令 §10 一次過睇出跨網段。**`macau-ledger-merchant` 冇呢個能力。**
⇒ 建議 `LanTcpPrinter.send()` 改回 `SendResult(ok, error)`，分辨 `SocketTimeoutException`（逾時，多數跨網段）
vs `ConnectException`（被拒，多數機未開／port 唔開）。

### 新發現 ③④⑤（中優先）
- ③ `PosRelayPrefs.clearPairing()`（`:48-56`）**唔清 `KEY_ENABLED`** ⇒ 撳「解除配對」即刻自動重配（`PosRelaySession.kt:98-100` 刻意如此）。
  ⇒ 建議一併關開關，或按鈕改文案為「重新配對」。
- ④ `RelayApi.claim()`（`:130`）用 `optBoolean("ok", **true**)`，而 `postPair`（`:61`）用 `false` —— **兩處 fail 語意不一致**，建議統一 false。
  另 `:126` 靠 `error.contains("HTTP 401")` 判鑑權失敗，依賴 `post()` 格式，易靜默失效。
- ⑤ `RelayApi.post()`（`:229-241`）`catch (_: Exception) { null }` ⇒ DNS/TLS/timeout 全變泛化字串（「配對連線失敗」/「claim 連線失敗」）。

### ⚠️ 邊界重點（記住兩條）
- **「堂食測試印」（`PosRelaySession.kt:146-153`）完全繞過 runner**，直接 `sunmi.sendRaw()` ⇒
  **出紙 ≠ 配對正常**。店員易誤判。建議測試紙加印 `POS_URL` / 通道狀態。
- **`dedupKey` 已存在時直接報 `sent`**（`PosJobRunner.kt:228-231`）⇒「冇出紙但報成功」。重啟後舊 job 重 claim 會靜默收割。
- `printedKeys` 上限 400（`PrinterService.kt:241-243`）⇒ 繁忙店可能重印舊單。

### ✅ 結論：**唔係配對方法引起打印不成功**
1. 能 claim 就證明 `agentId`/`agentToken` 有效 + `storeId` 匹配 + POS 憑證已下發（§9.1 實測）
2. `192.168.31.38:9100` 解析正確 ⇒ `resolvePrinter()` 三段判斷正常運作
3. §2 命中 ⇒ `dispatchPrint()` 入 `PosPrintDispatcher.kt:18` → `LanTcpPrinter` catch → `false` ⇒ **純網絡不可達**
4. `macau-pos` 側 `/api/pos/print-agent/*` 四端點職責清晰，`resolveRelayRealtimeConfig()` 拒 fallback Ledger 是**正確設計**

**⇒ 根因仍係 §10：商米 `10.61.49.153` 與打印機 `192.168.31.38` 唔同網段。**
**最佳判別式**：截圖見「**已認領未回報（試 N 次）**」⇒ claim 成功過 ⇒ **`BuildConfig.POS_URL` 必然有值** ⇒ 新發現 ① 在此部署**唔成立**。

### 文檔
`docs/reviews/diagnosis-relay-pairing-failed-2026-09-16.md` **§十一**（11.1–11.12，含完整鏈路、五道防線、5 個問題、16 條邊界、與 print-relay 對比表）

---

## 🔴🔴 09:00 用戶新截圖：「配對失敗」＋「雲端已連線」同時出現 + 全部「雲端未認領」

### ① 「矛盾」係假象 —— 兩個狀態來自兩條**完全獨立**嘅通道

`PrinterService.kt:74-76` 把**兩個唔同來源**嘅狀態用一句文案接起：
```kotlin
combine(AppState.mqttStatus, PosRelayState.status) { mqtt, pos -> "外賣: $mqtt | 堂食POS: $pos" }
```
- `外賣: 已連線` = `AppState.mqttStatus`（**MQTT** broker，外賣通道）
- `堂食POS: 配對失敗：POS 雲端未設定` = `PosRelayState.status`（**relay** 通道）

⇒ **兩者可以同時為真**，完全唔矛盾。文案設計誤導（改法：`外賣通道: … ｜ 堂食POS通道: …`）。

### ② 🔴🔴 同一句 `配對失敗：POS 雲端未設定` 有**兩條路徑**，排查方向完全相反

全 `posrelay` 套件 grep `雲端未設定` → **只有 4 處**：

| 路徑 | 位置 | 觸發 | 有冇打過 HTTP | 修邊邊 |
|---|---|---|---|---|
| **路徑 1**（早退） | `PosRelaySession.kt:202` | `BuildConfig.POS_URL.isBlank()` → 直接 return | ❌ 完全冇 | APK 側 `.env`（要重 build） |
| **路徑 2**（握手失敗） | `PosPairingManager.kt:33` | `GET /pair` 回 `supabaseUrl`／`anonKey` 為空（＝Vercel `SUPABASE_URL`/`SUPABASE_ANON_KEY` 缺） | ✅ 有 | **Vercel 環境變數** |
| 路徑 2 變體 | `PosPairingManager.kt:36` | 回嘅 URL **等於** `BuildConfig.SUPABASE_URL`（＝Ledger） | ✅ 有 | Vercel env |
| 路徑 2 冒泡 | `PosRelaySession.kt:219` | `autoPair` 失敗後把 `exceptionOrNull().message` 當狀態顯示 | ✅ 有 | 同上 |

**⇒ 一句話判別（唔使拆 APK）：睇 `pos_print_jobs` 有冇任何 `claimed_by` 非 NULL 嘅行。**
- **有** ⇒ **路徑 2**（能 claim 證明曾配對成功 ⇒ `POS_URL` 一定有值）
- **冇**（全 NULL + `attempts=0`）⇒ **路徑 1**

✅ **本次實測有（08:30:35 / 07:35 / 01:30:57 三個窗口）⇒ 判為路徑 2。**
🔴 **⇒ 修正 §11.11／§11.4 嘅舊推論**：原本用「已認領未回報」推「`POS_URL` 必有值」方向**對**，
但錯在**把歷史窗口當成「現在仍成功」** —— 08:42／08:54 兩張新 job 係 `attempts=0`、`claimed_by=NULL`，
證明**目前這個實例**從未 claim 成功。

### ③ 🔴 路徑 2 **唔使重啟 APK** —— 15 秒自癒（推翻早期「永久卡死」講法）

`schedulePairRetry()` 用 `PAIR_RETRY_MS = 15_000L` 不斷重試，且路徑 2 **冇 401**
⇒ `unauthorizedStreak` 唔增 ⇒ **唔會入 `PAIR_COOLDOWN_MS = 300_000L` 嘅 5 分鐘冷卻**。
⇒ **Vercel 補回 env + Redeploy 後，最多 15 秒自動配對成功。**

### ④ 第 3 個獨立狀態：為何「能 claim」同「配對失敗」可同時為真

`restorePairing()`（`PosPairingManager.kt:49-77`）只有 `posStoreId != persistedMerchantId` 才清 prefs，
否則**保留舊 `agent_id`/`agent_token`** ⇒ 舊 token 仍能 claim（claim **唔需要** Supabase 憑證），
但 `PosRelayClient.init()` 從未成功 ⇒ `client == null` ⇒ `PosRealtimeSubscriber` 記
「POS Realtime client 未就緒」⇒ **Realtime 從不訂閱，只剩 60 秒對賬**。
（呢句 log 就係路徑 2 嘅獨立佐證，可直接喺 App 日誌睇到。）

### ⑤ 「已發送」＋「雲端未認領」同時出現係正常嘅

- 「已發送」= **本地樂觀值**（`RelayTransport.send()` 永遠回 `{ok:true}`，`print-center.tsx:463-467` 有註明）
- 「雲端未認領」= `status='pending'`（**雲端真相**），由 `syncCloudPrintOutcomes()` 寫入，**只向上覆寫**
⇒ 兩者係**唔同層**嘅事實，唔係矛盾。

### ⑥ 「雲端未認領」成因（按上游→下游，本次實測）

| # | 成因 | 證據 |
|---|---|---|
| **A** | **配對握手失敗（路徑 2）⇒ 堂食通道從未啟動** | 08:42／08:54 兩張 `attempts=0` |
| B | 中繼機離線／被系統殺（foreground service 冇保住） | 活躍窗口 01:12–01:31、07:35、08:30，之後全靜 |
| C | 跨網段（`10.61.49.153` → `192.168.31.38`） | 07:35 `兩種通道都失敗｜直連：… after 5000ms` |
| D | 出紙失敗原因被吞（`PosJobRunner.kt:247` 硬編碼 `dispatch failed`） | 今日 5/5 failed 全係呢句 |
| E | 下游 `status='printing'` 卡死行唔會被重排 | 3 張 07:38 行仍 `printing`、`updated_at` 停在 08:30 |

⚠️ **E 嘅細節**：07:38:17/:20/:35 三張 `claimed_at=08:30:35`、`last_error=AGENT_FAILED: dispatch failed`，
但 `status` 仍係 `printing` ⇒ **`result/route.ts` 寫 `failed` 時冇同時改 `status`**（狀態與 `last_error` 不一致）。

### ⑦ ⚠️ 收尾前必做：**清舊 job，否則一次爆 8 張紙**

未完成任務（本次實測，全部 ttl 未過期）：08:54:30(+0) / 08:42:28(+0) / 07:38×3(printing) /
07:32×2(+2,+3) / 01:34、01:27(+4,+4) / **09-15 21:19:30（`ttl=NULL` ⇒ 永不過期）**。
- `01:13:47` 嗰張 **安全**（`attempts=5` 令 `coalesce(attempts,0) < 5` 唔成立 ⇒ 永不 claim）✅
- 清法：先跑 SELECT 睇清單 → `select public.pos_void_stale_print_jobs('<storeId>');`
  （若 0042 未跑，退用 UPDATE 版，見 doc §12.7）

### ⑧ 🔴🔴 重大附帶發現：`POS_REQUIRE_DEVICE_AUTH` 已被**全局關閉**

匿名零憑證探測由 01:04 嘅 `401` 變成 09:00 嘅 **`200`**：
`/api/pos/state`、`/print-jobs/status`、`/device-config`、`/orders`、`/print-agent/pair-status` 全部 200。
⇒ `resolvePosRouteAuth()`（`pos-route-auth.ts:59`）第一條 `disabled` 分支直接放行。

- ✅ **意外解除了 iPad 側「配對失敗」401 迴圈**（`relay-pairing-panel` 唔再收 401）
- 🔴 **資安風險回到 09-15 之前**：`getSupabaseServerClient()` 優先 `SUPABASE_SERVICE_ROLE_KEY`
  ⇒ **繞過 RLS**，任何知 `storeId` 嘅匿名請求可讀寫該店資料。`storeId` 會出現在 URL／QR／log／localStorage。
- ⚠️ **唔可以喺未確認前改返 `1`** —— 若 iPad token 續期未通，改返會**即刻全站 401**。
  **正確次序**：先確認 `POST /api/pos/device-token` 回 200 + token ⇒ 再設 `=1` + Redeploy ⇒ 逐端點驗 401 恢復。

### 修復優先序（見 doc §12.9）
1. 🔴 **清 8 張爆紙舊 job**（修通道**之前**做）
2. 🔴 跑 `0042`（未跑就冇分段 claim 窗＋作廢端點）
3. 🔴 **Vercel 補 `SUPABASE_URL`/`SUPABASE_ANON_KEY` + Redeploy** ⇒ 15 秒自癒
4. 🔴 商米 Wi-Fi 改連店內路由器（`192.168.31.x`）+ 關流動數據
5. 🔴 查 `POS_REQUIRE_DEVICE_AUTH` 為何變 `0`，確認 iPad 能簽 token 後改返 `1`
6. 中：文案改「堂食POS通道」、`LanTcpPrinter` 回真錯誤、`result/route.ts` 同時改 `status`

---

## 🔴🔴🔴 09:10 Ledger 回覆後實測 —— **推翻「Vercel 憑證缺」**（本日最重要修正）

### 觸發：Ledger 回覆主張「新 App 重配時 `GET /pair` 沒拿到 supabaseUrl／anonKey」

他要我哋貼 `GET /pair` 的 url／key 是否空。**照做，結果兩欄完全唔空：**

```
GET /api/pos/print-agent/pair?agentId=ag-0590816d9f60e8d2f55a16cf721042dd
→ 200 {"status":"paired","storeId":"d564b932-0c91-45e9-86fd-0ec8e2711f13",
        "supabaseUrl":"https://iyrywzormzisyppkokbi.supabase.co",
        "anonKey":"eyJ…(role=anon, ref=iyrywzormzisyppkokbi)"}
```

⇒ **`PosPairingManager.kt:33`（兩欄空）同 `:36`（撞 Ledger）兩條都唔成立。**
⇒ §12.3 判「路徑 2 = Vercel 憑證缺」**係錯嘅**。

### 各 agentId 實測回應（`tools/_probe7-20260916.cjs`）

| agentId | `GET /pair` | 命運 |
|---|---|---|
| `ag-0590816d…`（08:30 活躍） | `paired` + 兩欄齊全 | ✅ 配得成 |
| `ag-f38b08c…`（07:35 活躍） | **`{"status":"pending"}`** | ❌ `restorePairing` false |
| `ag-a466746d…`（01:30 活躍） | **`{"status":"pending"}`** | ❌ `restorePairing` false |

### 🔴 真正關卡：`restorePairing()` 見 `pending` 就靜默 `return false`

`PosPairingManager.kt:65-68`：
```kotlin
val poll = relayApi.getPair(agentId)
if (poll.status != "paired" || poll.storeId != posStoreId) {
    return false          // ★ 冇 log、冇訊息 —— 查幾個鐘就係卡喺呢度
}
```
`pos_print_agents` 該行被撤銷 ⇒ `GET /pair` 永遠回 `pending` ⇒ `restorePairing` false。
之後 `autoPair()` 靠 **`AppSession.merchant.merchantId`**；
**店員未登入 Ledger／登入態過期 ⇒ merchantId 空 ⇒ `autoPair` 一樣失敗**
⇒ 最後由 `PosRelaySession.kt:219` 冒泡 `?: "配對失敗：POS 雲端未設定"`。

### 🔴 「配對失敗：POS 雲端未設定」= **垃圾桶文案**（三種無關原因同一句）
① `POS_URL` 空（`:202`）② `/pair` 兩欄空（`:33`）③ `autoPair` 任意失敗（`:219` fallback）。
**唔可以逐字當真、唔可以推論「雲端未設定」。**

### 修正 Ledger 說法（大部分對，一處錯）
| Ledger 講法 | 核實 |
|---|---|
| `print-job-failure.ts` 分類引用 | ✅ 完全準確 |
| 07:38 舊 App 有認領 → 舊版「可以」 | ✅ 準確 |
| 08:42／08:54 新單 `pending`、`attempts=0` | ✅ 準確 |
| 心跳停 34 分鐘 = claim/heartbeat 沒跑 | ✅ 準確 |
| **「不是 claim API 被改壞」** | ✅ **同意**（舊 agent 08:30 仍 claim 3 張） |
| **「`GET /pair` 沒拿到 url／anonKey」** | ❌ **錯 —— 實測兩欄有值** |
| 「舊版吃本機舊快取」 | ⚠️ 部分對：`restorePairing():69-70` 確實 fallback 本機舊值，**但仍要 `status=="paired"`（:66）先過得去** |

### 修正後次序
1. 對目前 agentId 打 `GET /pair` → `pending` ⇒ **App 內重新配對**（唔使改 env）
2. `paired` 但兩欄空 ⇒ 查 Vercel env
3. `paired` 兩欄有值 ⇒ 查 **Ledger 登入態**（`AppSession.merchant`）
4. 才輪到 `BuildConfig.POS_URL`
⇒ **唔使改 Vercel、唔使重裝 APK。** ⚠️ 但 `pending` 係伺服器側狀態，15 秒自動重試都救唔到，
要 App 重新 `POST /pair` 才變 `paired`。

### 給 Ledger 的回覆已寫入 doc §13.6（可直接用）

### 文檔
`docs/reviews/diagnosis-relay-pairing-failed-2026-09-16.md` **§十三**（13.1–13.6）

---

## ✅ 09:15 交付：即刻行動清單

用戶問「我現在要做什麼才能解決」。經 DB 實測後產出
`docs/reviews/action-now-print-fix-2026-09-16.md`（4 步）。

### 決定性數據（本次實測）
- **昨晚 09-15 那批舊單（17:30–20:25，43 張）已經被作廢**（`att=5` + `last_error` 寫「作廢：中繼機離線期間積壓…」）⇒ **安全，唔會爆紙**。
- **真正會爆紙只有 8 張**：09-15 21:19:30、09-16 01:27:13、01:34:17、07:32:07、07:32:27、07:38:35、08:42:28、08:54:30。
- 全部 55 張未完成行中，其餘 47 張因 `attempts=5` 而不會被 claim。
- 最近活躍 agent：`ag-0590816d…`（08:30:35）、`ag-f38b08c…`（07:35:56）；**兩者之中只有前者 `/pair` 回 `paired`**。
- **storeId = `d564b932-0c91-45e9-86fd-0ec8e2711f13`**（重新配對要填呢個）。

### 四步（次序不可顛倒）
1. 🔴 **先清 8 張**（SQL Editor；先跑 SELECT 睇清單 → `pos_void_stale_print_jobs()`；0042 未跑則用 UPDATE 退路）→ 覆核應回 0
2. **App 內重新配對**（填 storeId；**並確認已登入 Ledger**）→ 15 秒後狀態變「已連線」
3. **商米 Wi-Fi 改連店內路由器（`192.168.31.x`）+ 關流動數據**
4. 驗證：開測試單睇出紙

### 明確「唔使做」
改 Vercel env ❌、重裝/降級 APK ❌、改 `POS_URL` ❌。要改嘅只有「App 內撳一次配對」＋「Wi-Fi 設定」。

### ⚠️ 遺留隱患（已在清單註明）
`POS_REQUIRE_DEVICE_AUTH=0` ⇒ 鑑權閘全局關閉。**唔好即刻改返 `1`**，
要先確認 iPad 能簽 token（`POST /api/pos/device-token` 回 200），否則即刻全站 401。

---

## 09:35 — 🔴 推翻上面「重新配對」建議：真兇係 **runner 已死**

### 觸發
用戶喺店內（16/09 09:23）實測打印，仍然冇紙。截圖 `/prints` 顯示：
「有 2 張打印任務雲端仲未完成：未認領 2 張」+「中繼打印機：**最後心跳 1138 分鐘前**（疑似離線）」。

### 決定性實測（live probe，09:35）
```
GET /api/pos/print-agent/pair-status?storeId=d564b932-…
  → 200  paired=true  androidReady=true
         lastSeenAt = 2026-09-16T00:30:34.947Z  (= 澳門 08:30:34，56 分鐘前)
GET /api/pos/print-agent/pair?agentId=ag-0590816d…
  → 200  status=paired  supabaseUrl=有值  anonKey=有值      ← 憑證完全正常！

pos_print_jobs 最後一次 claim：
  08:30:35（同一批 07:38 單，att=4，err=AGENT_FAILED: dispatch failed）
  ⇒ 最後心跳 08:30:34 同 最後 claim 08:30:35 **同一秒**
```

### 結論（三改）
1. **配對鏈路完全正常** ⇒ §13 嘅「App 內重新配對」**都唔使做**（`paired` + 兩欄有值 + `androidReady:true`）。
2. **`PosJobRunner` 已於 08:30:35 死亡** —— `heartbeatJob`(30s) 同 `tickJob`(60s) 係**兩條獨立 coroutine**，
   同一秒一齊停 ⇒ 唔係網絡，係**整個 runner scope 被 cancel**（App 被系統殺／切走／電源優化）。
3. **截圖「1138 分鐘前」係舊快照、唔可信**：`print-center.tsx:1769-1772`
   `Math.round((Date.now()-lastSeenMs)/60000)` 純前端計算，頁面唔 reload 就**無限膨脹**。
   1138 分鐘反推 = 09-15 14:25，同真實 08:30 差成日。

### 改成「叫醒 App」四步（次序）
1. 清爆紙風險舊單（`pos_void_stale_print_jobs('d564b932-…')`）
2. **去商米把 App 切到前台**（唔係重新配對）→ 睇 `/prints` 心跳變「剛剛」
3. 唔動就**完全重啟 App** + 電池設「不限制」+ 開自啟動
4. 確認 Wi-Fi = `192.168.31.x` + 關流動數據

### 判死活口訣（已入 skill §❼）
**唔好信 `/prints` 嗰句「N 分鐘前」**；要 live 打 `pair-status` 讀 `lastSeenAt`，
再對照 `pos_print_jobs` 最後 `claimed_at`。**兩者同一秒停 ⇒ runner 死，唔係配對問題。**

---

## 09:40 — 🔴🔴 再推翻：`pos_void_stale_print_jobs` 回 0 正常；DB 有三個 store、三台機全停

### 觸發
用戶跑 `select public.pos_void_stale_print_jobs('d564b932-…');` → **回 0**，
但實時覆核 **11 張風險 job 一張都冇變**（唔係「清完」）。

### 更正 1：sweep 只掃 `ttl` 已過期（`0042:140-146`）
```sql
and ( ttl is not null and ttl <= (extract(epoch from now())*1000)::bigint )
```
11 張風險單實測分佈：**ttl=NULL 1 張、ttl 已過期 0 張、ttl 未過 10 張**
⇒ **回 0 完全正確。`ttl=NULL` 係 sweep 盲區**（`ttl` 只喺 insert 寫，舊行恆 NULL）。

### 更正 2：🔴 DB 裡有三個 store_id，三台中繼機**全部停咗**
| store_id | 未完成 | 風險 | agentId | 最後心跳 | 距今 |
|---|---|---|---|---|---|
| `d564b932-0c91-45e9-86fd-0ec8e2711f13` | 7 | 3 | `ag-0590816d…` | 09-16 **08:30:34** | 68 分 |
| `f6ec837a-03d9-48f0-ae05-f1fbc3483221` | 8 | 4 | `ag-4d013eda…` | 09-16 **01:31:06** | 488 分 |
| `8291f843-9def-4956-9d0b-1cfef2598306` | 43 | 4 | `ag-302b8281…` | 09-15 **14:24:37** | **1154 分** |

三個都 `paired:true` + `androidReady:true`（配對鏈路正常），但心跳全停、停喺唔同時刻。
按 `printer_name` 亦係三套設備（`printer` / `廚房打印機` / `小票機 · 通用 80mm`）。

### 更正 3：🔴 「1138 分鐘前」對應嘅**唔係**用戶嗰台機
`resolveStoreId()`（`sync-flush.ts:308-314`）＝**登入 session 嘅 `merchantId`**；
`print-center.tsx:419-424` 用佢叫 `pair-status?storeId=`。
- `8291f843…` = **1154 分** ✅ 對得上截圖「1138」（差 16 分 ≈ 截圖時差）
- `d564b932…` = 68 分 ❌、`f6ec837a…` = 488 分 ❌

⇒ **banner 顯示嘅係 `8291f843…` 停咗 19 個鐘嗰台**，唔係現場正用嗰台。

### 新行動次序
0. **先確認 iPad 登入邊個 merchant**（`localStorage` 嘅 auth session）—— 否則一直 debug 錯店
1. 針對**嗰間店**叫醒中繼機（切前台 → 重啟 → 電池不限制）
2. 另兩間店：停用就唔理；仍營運就要各自叫醒（獨立事件）
3. 清 job：sweep 對 `ttl=NULL` 無效 ⇒ 逐個 store 用 UPDATE 退路，**唔好一次過清三個**

### 教訓（已入 skill）
🔴 **查中繼打印問題，第一步應該「唔帶 store 過濾」掃 `pos_print_jobs` group by store_id** ——
「印唔出」有可能係「你查緊嘅 store 根本唔係 iPad 用緊嗰個」。


## 店員手機下單系統 — 方案設計（PWA）

### 需求
店員用手機喺**枱邊**幫客人現場點餐。訂單直接入後台並**自動出廚房單**。
手機**唔直連打印機**（沿用雲端派工鏈路）。

### 關鍵決策（用戶確認）
1. 技術形態：**PWA 網頁**（唔做 APK）
2. 主場景：**堂食枱邊點餐**（有枱號）
3. 打印：手機唔直連，走既有 `pos_print_jobs` 派工
4. 落點：**直接進後台 + 自動出紙**（唔經收銀台二次確認）

### 核心設計：新增 variant 分支，唔另起一套
- `useOrderingCore()` 已有 `variant: kiosk | scan`（`use-kiosk-order.ts:283`）
  → 擴充為 `kiosk | scan | staff`
- 新增路由 `/staff`，復用：菜單 bootstrap、`kiosk-cart.ts` 純函式、
  `computeOrderTotals()`（金額真源）、落單重試隊列、Realtime 售罄

### 🔴🔴 最重要風險：匿名通道白名單
`src/app/api/pos/sync/route.ts:398`：
```ts
const ANONYMOUS_ALLOWED_SOURCES = new Set([scan, kiosk]);
```
- 店員單 `source: staff` **必須走「已授權」分支**（帶 POS 憑證 → `authorized === true`）
  → 見 `route.ts:686` 嘅 `if (!authorized && ...)`，**天然繞過白名單，唔使改**
- ❌ **絕對唔可以**把 `staff` 加進白名單 —— 等於開放匿名偽造店員單
- ⚠️ 因為 `getSupabaseServerClient()` 優先 service_role **繞 RLS**，加錯＝裸奔 DB

### 與客人掃碼(scan)的差異
| 項目 | scan | staff |
|---|---|---|
| 身份 | 匿名 | 帶 POS 憑證 |
| 枱號 | URL 參數 | 店員主動選 |
| 店休閘 | 擋 | **唔擋**（同收銀台） |
| 單號 | 無 | **有**（店內序號） |
| 出紙 | 收銀台印 | 雲端派工自動印 |

### 建議新增角色
`waiter`（服務員）：只可落單／加單，**不可收款、退菜、免單**。
既有角色：`admin | manager | cashier`（`login-screen.tsx:57`）

### 交付
- 方案文件：`docs/staff-mobile-ordering-plan-2026-09-16.md`
- 含 6 階段實作步驟、風險清單、驗收標準

### 唔使踩嘅地雷（本專案既有）
- 建單後必須 `appendPrintJobsWithSync()`，淨 `savePrintJobs()` ＝ 零出紙
- 加單**必須重用同一 `order.id`**，否則重複出紙
- iOS Safari：PIN 欄必須 `type=text` + `inputMode=numeric`，不可用 `type=password`


## 店員手機版 — 介面確認稿（6 個畫面）

已繪製視覺確認稿（inline widget，非檔案）。六畫面：
1. 登入 — PIN 欄 `type=text` + `inputMode=numeric`（iOS 陷阱），12h 免登入
2. 選枱 — 樓面圖格狀（46×46px/格），**枱況三態**：空枱(綠)／用膳中(橙)／即將結帳(紅)
3. 點餐主畫面 — 頂欄枱號（標「加菜模式」）＋分類 chips＋2欄菜卡（88px高）＋底部結算條
4. 購物車 — 每行 `─ 1 ＋` 數量控制；小計／服務費／合計分列
5. 送出確認 — **二次確認對話框**（枱邊易誤觸、送出即出紙不可撤回）
6. 送出後回饋 — 顯示出紙三態「已排隊→列印中→已出紙」＋單號

### 設計關鍵
- **枱況三態由 `pos_orders` 未結單即時計算**（唔另建表）
  - 空枱 → 撳入去填人數 → 開新單
  - 用膳中 → 撳入去**載入現有單加菜**（重用同一 order.id）
  - 即將結帳 → 標金額，仍可加菜
- 手機端**不負責三件事**：不直連打印機、不收款結帳、不印客人小票
- 小票由收銀台結帳時出；手機只觸發廚房單

### 送出後台自動流程
`POST sync(帶憑證) → 寫 pos_orders(已授權分支) → 建 print_jobs(廚房單) → 中繼機 60s 內 claim 出紙`

### 待用戶確認後才實作
用戶要求「先畫圖給我看看」→ 已出稿，**未落任何代碼**。
下一步待批准：階段 1（新增 `/staff` 路由 + `"staff"` workbench 定義）。

## 店員手機版 — 高保真實機確認稿（HTML + 六張 PNG）

用戶要求「真實的實機圖片」→ 交付 docs/staff-mobile-ordering-mockup.html（六畫面高保真確認稿）
＋ docs/mockups/staff-mobile-0{1..6}-*.png（真實 Chromium 截圖）。

### 用戶最終確認的設計決定
1. **售罄菜灰化保留、唔隱藏**（店員可建議替代品）
2. **選枱只做格狀樓面圖**，唔要列表切換
3. 枱況三態：空枱(綠)／用膳中(橙)／即將結帳(紅帶金額)＋停用(灰)
4. 手機端唔直連打印、唔收款、唔印客人小票

### 確認稿做法（沿用專案 mockup 慣例）
- 配色對齊**真實 design token**（globals.css）：primary #4f46e5 / background #f3f5f9 /
  border #d6dbea；卡片圓角 18px、藥丸 999px（同 .menu-card / .status-pill 一致）
- 六個 iPhone 14 Pro 外框（393×852）並排，transform:scale(.872) 等比，**零 @media**
- 純靜態 HTML（冇 JS 切頁）⇒ dom-smoke-test.js 報「搵唔到 script」係**預期**，非缺陷

### 🔴 量度驗證（今次捉到 2 個真問題）
寫臨時 tools/_measure-staff-mobile.cjs（puppeteer-core + 本機 Chrome，
逐框量 getBoundingClientRect 溢出／裁切 + 逐框 elementHandle.screenshot()）：
1. **示範資料太少 → 大片空白**：原本 6 張枱、4 道菜，真實店有十幾枱、幾十道菜
   ⇒ 補到 13 枱（A/B 兩區）、10 道菜 + 底部漸層示意可捲
2. **購物車「送出訂單」按鈕被 grid row 拉伸**（應 52px → 變 110px）
   ⇒ .cartbar 加 grid-auto-rows:max-content；單按鈕頁加 .cartbar.solo{align-content:center}
- 最終：六畫面**全部零溢出零裁切** ✅

### 環境陷阱（再次確認）
- tail / grep / head 喺本機 git-bash **完全冇**（連 | grep 都 command not found）
  ⇒ 過濾輸出要用 node，唔可以用管道
- 相對路徑餵 puppeteer 會變 file:///docs/... ⇒ **一律用絕對路徑**
- Read 圖片有快取 ⇒ 每次重截**換新檔名／新目錄**（今次用 v1→v4）

## 訂單號出現 print-xxxxxxxx（用戶截圖 print-26cfbff2）

### 結論：呢個係 bug 症狀，唔係正常單號
訂單列表「訂單號」欄 = order.localOrderNo（local-orders-panel.tsx:579，直接 render 冇 fallback）。
正常值只有四種：訂單12 / 堂食… / 自取… / 外賣…（kiosk-order.ts:300-304）、
線上-xxxxxx / 自取-xxxxxx（ledger-pos-bridge.ts:358-367 resolveLocalOrderNo）、或掃碼單=台名。
完全冇任何路徑會產生 print- 開頭嘅 localOrderNo。

### 真兇：列印任務 id 洩漏成訂單號（兩個獨立入口）
print-jobs.ts:48-50 / print-job-enqueue.ts:46-48 / ledger-pos-bridge.ts:66 都有
uid(prefix) => `${prefix}-${crypto.randomUUID().slice(0,8)}`；
print prefix 用於 PrintJob.id。洩漏路徑：
1. pos-order-mapper.ts:73  localOrderNo: row.local_order_no ?? row.id
   —— DB local_order_no 為 NULL 時 fallback 用 row.id。
   sync/route.ts:65-70 text() 證實：空字串 / 非 string 一律回 null（if (!trimmed) return null）
   ⇒ 客戶端寫過空 localOrderNo 嘅單，上雲變 NULL，讀返就顯示 row.id。
2. 建單／自動接單時把 PrintJob.id 誤寫入訂單 id/localOrderNo
   （print / kiosk-resume 之類路徑共用 uid → 撞 prefix）。

### 配套症狀（截圖同證）
- 金額 MOP 0：同上雲失敗一致（total 亦冇寫入 / 被 money() 歸零）。
- 狀態 pending / 未結賬：status 冇上雲 ⇒ 讀返 ?? "draft"。
- print-26cfbff2 餐台=堂食、菜品=快餐（干扁四季豆…）—— 快餐單竟然有「堂食」台名 ⇒ 欄位錯位。
- 同一列表 訂單11/10/09 正常 ⇒ 只有個別單中招（間歇、非全量回歸）。

### 排查步驟
1. select id, local_order_no, total, status, created_at from public.pos_orders
   where id like 'print-%' or local_order_no like 'print-%' order by created_at desc;
2. 比對 id 同 local_order_no：若兩者相同且形如 print-xxxxxxxx ⇒ 路徑 2（建單時寫錯）。
3. 若 id 正常（uuid）但顯示 print-… ⇒ 唔可能（render 只讀 localOrderNo）⇒ 檢查 row.id。
4. 查該單本機 localStorage macau-pos-orders 有無同 id 記錄，確認係「上雲丟欄」定「本機已壞」。
5. 對照 pos_print_jobs 同 id 有無 print-<同8碼> 行 ⇒ 證明係 PrintJob.id 洩漏。

### 修正方向
- 讀取端改 row.local_order_no ?? 加 # 前綴 + row.id.slice(0,8)（同 admin/dashboard:494 一致），
  令異常值一眼可辨、唔會同真單號混淆。
- 建單路徑 audit：確認冇地方用 uid("print") 嘅值做 order id/localOrderNo。
- print-26cfbff2 語義＝「列印任務 26cfbff2」，屬 PrintJob.id，唔係訂單實體。

## 【結案】print-26cfbff2 / print-e4295217 真因 ＝ 孤兒單隔離區快照

### 關鍵突破：一張截圖推翻兩個假設
用戶第二張 SQL 截圖：pos_orders 掃 `id like 'print-%' or local_order_no like 'print-%'` ⇒ **0 rows**。
⇒ **DB 完全乾淨** ⇒ 先前「路徑 2（建單寫錯 id）＋ 路徑 1（讀取 fallback row.id）」**兩者全錯**。

### 真因：`sync-health-modal.tsx` 的 L4「已隔離訂單」面板
截圖是「同步健康檢查」彈窗，四個 print-* 全在 **「已隔離訂單 4 張」** 區塊下，
右側有「還原 / 永久刪除」；標註 **「自動隔離（手動更新）· 16/9/2026 下午1:33:03」**。

→ `row.reason === "auto-full-pull"` ⇒ 由 `pos-app.tsx:1409` 全量拉取後**自動隔離**（不是人手）。
→ 隔離＝`quarantineOrders()`（`sync-reconcile.ts:334`）：由 `loadOrders()` **移出**、寫入
  `QuarantinedOrderRow`（`storage.ts:989` `{order, quarantinedAt, reason}`）＝**完整 PosOrder 快照**。
→ 即係**本機 localStorage 的孤立記錄**，從來冇上雲 ⇒ 所以 SQL 一定查唔到（0 rows）。

### print-* 的真正來源：PosOrder.id（不是 fallback、不是寫錯）
`quarantineOrders()` 用 `o.id` 做 key（`sync-reconcile.ts:336-338`），面板顯示 `row.order.localOrderNo`。
四張單 `localOrderNo` 都是 `print-xxxxxxxx` ⇒ 這四張 **PosOrder 的 localOrderNo 本身就是 print-***。
來源＝**掃碼／自助單建單時 `uid("print")` 被誤當單號**（`print-jobs.ts:48` 同一 uid 家族）。
⚠️ 這些單從未上雲 ⇒ 前面只查 pos_orders 的 SQL 永遠查唔到，查詢方向本身就錯。

### 孤兒單三條件（`sync-reconcile.ts:260-264`）全部命中
1. 本機非終態（draft/sent_to_kitchen/paid/reopened）✓
2. 雲端全量 pull 冇呢個 id ✓（SQL 0 rows 已證）
3. outbox 冇 pending/failed 的 ORDER_* 事件支持 ✓
＋ 單齡 ≥ `ORPHAN_MIN_AGE_MS`（10 分鐘，`sync-reconcile.ts:274`）

### 鐵證：MOP 0.00 × 4 張（e4295217 / 26cfbff2 / 49e3f6a0 / 8683860f）
四張**全部 0 元**；而面板頂部「全部下載到本機（2 張）」是兩張**正常**的雲端單
（`A03 MOP 39.00` 待結單 ＋ `UU/ AUS MOP 39.00` 已完成 16/9 14:44:46）⇒ 有真單做對照。
⇒ 建單過程中途崩／`uid("print")` 被寫入 localOrderNo，金額欄從未賦值。

### 欄位錯位亦解釋得通
`A02` 表 / `堂食` / `外賣自取` / `A01` 台名 ＋ `MOP 0.00` ＝ 掃碼台單（tableName 正常）
但 localOrderNo 被塞了 print- id ⇒ 兩者不一致是同一個建單 bug 的兩個面向。
⇒ 上一輪我講「快餐單掛堂食台名 ⇒ 欄位錯位」是**誤判**：佢真係堂食台單，只係單號壞。

### 修正方向（比之前具體）
1. **建單路徑 audit**：搜 `localOrderNo` 賦值點，確認冇任何路徑可寫入 `uid("print")` 值。
   重點查掃碼 / kiosk 建單（`kiosk-order.ts` `buildKioskOrder` 是唯一決定 localOrderNo 的地方）。
2. 面板顯示建議加識別：`localOrderNo` 異常（唔符合 `訂單\d+`／台名 pattern）時顯示警示色。
3. 這 4 張已隔離單**唔應該「還原」**（還原＝放返 orders，雲端永遠冇 ⇒ 又變孤兒）
   ⇒ 應「永久刪除」（會寫 tombstone，`restoreQuarantinedOrder:365-366` 已處理 tombstone 分支）。

### 教訓（方法論）
- **先確定資料在哪一層**才查 SQL：本機 localStorage 的孤立記錄，查雲端 DB 必然 0 rows。
- 症狀面板本身就是最強線索：見到「已隔離 / 還原 / 永久刪除」字眼 ⇒ 係 quarantine 區，不是 DB。
## 【重要更正】print-* 孤兒單：排除 scan/kiosk，真因未定（待查本機快照）

### 用戶新事實（決定性）
**今天的單全部是線下商家自己落單**（source = "pos"），**唔係**掃碼／自助。
⇒ 先前「掃碼／kiosk 建單時 uid("print") 誤寫單號」的假設**被推翻**。

### 三張 SQL 結果（已確認）
1. `current_database()` = postgres、`pos_orders` 共 **245 筆** ⇒ 專案/表正確，0 rows 是真陰性。
2. 近 3 小時：零元單 **0**、草稿單 **0**、缺單號 **0** ⇒ 雲端乾淨。
3. `%26cfbff2%` ⇒ **0 筆 print-**，只回同時段 4 筆正常 `settled` 單：
   `order-e320865c`(訂單12/81)｜`ledger-3f8d6eac…`(007/39)｜`order-647a0ad2`(訂單11/53)｜`ledger-37074691…`(006/40)。
   ⇒ 同時段雲端正常運作 ⇒ 局部失敗，唔係全站回歸。

### 重要的側面證據：id 命名是語義化的
正常線下單 id = `order-e320865c`；線上單 = `ledger-3f8d6eac…`。
⇒ 同一套 `uid(prefix)` 家族；`print-` prefix 在 `src` 內**只用於 PrintJob.id**（已 grep 確認）。

### 已排查並排除的線下建單路徑
- `pos-app.tsx:2411-2431` `upsertCurrentOrder()` 建新單：
  `id: uid("order")` ＋ `localOrderNo: newLocalOrderNo ?? fallbackNo`（2413），`source:"pos"`（2428）。
  `fallbackNo` = `nextLocalDailyOrderNo(...)`（2345-2351）⇒ **不可能產生 print-**。
  ⇒ **主線下建單路徑已排除**。
- `kiosk-order.ts` `buildKioskOrder` ⇒ 已按用戶事實排除（今日無 scan/kiosk）。

### 仍未排除的兩個疑點（下一步方向）
1. **`upsertCurrentOrder` 的 `newLocalOrderNo` 參數**（2325）由 caller 傳入 ——
   需查全部 call site，確認有冇任何一處會傳入列印任務 id。
2. **`/api/pos/state` 讀取路徑 `mapOrderRow`（`pos-order-row.ts:50`）冇 fallback**
   （`localOrderNo: order.local_order_no`，直接可能為 null）——
   與 `pos-order-mapper.ts:73`（`?? row.id`）**兩份映射不一致**，係已知漂移點。
3. **雲端從未存在該 4 筆** ⇒ 它們必然是**本機產生**的 PosOrder；
   需直接讀本機 localStorage 隔離區快照，睇 `order.source` / `order.id` / `order.total` 實值。

### 下一步（唯一正確做法）
在該 iPad 的瀏覽器 console 執行：
```js
const rows = JSON.parse(localStorage.getItem('macau-pos-quarantined-orders') || '[]');
rows.forEach(r => console.log(r.order.id, '|', r.order.localOrderNo, '|', r.order.source,
  '|', r.order.total, '|', r.order.status, '|', r.reason, '|', r.quarantinedAt));
```
（key 名以 `storage.ts` 的 `STORE_SUFFIX.quarantinedOrders` 實際值為準，需先核對。）
睇 `order.source`：若係 `"pos"` ⇒ 線下建單鏈有漏；若係 `"scan"/"kiosk"` ⇒ 之前假設復活。

### 方法論教訓
- **唔好靠 `id` 前綴推斷來源**，要讀實際欄位值（`source`）。
- 用戶一句「今日全是線下單」比多條 SQL 更能收窄範圍 —— **先問業務事實，再查程式**。
## 店員手機下單（/staff）— 實作階段 1~4 完成

### 新增檔案
| 檔案 | 用途 |
|---|---|
| `src/lib/pos/staff-order.ts` | 店員單純函式 builder（`buildStaffOrder` / `applyStaffAddOn` / `newStaffOrderId`）|
| `src/lib/pos/spec-selection.ts` | 由 pos-app 抽出嘅規格換算（`priceWithSpecs` / `buildSelectedSpecs` / `menuItemDiscountRate`）|
| `src/lib/use-staff-order.ts` | 店員落單狀態核心 |
| `src/components/staff/staff-mobile-app.tsx` | 手機介面（選枱／點餐／購物車／確認／回饋）|
| `src/app/staff/page.tsx` | 路由（`AuthGuard` 包住）|

### 修改
- `module-catalog.ts`：`WorkbenchId` 加 `staff`；`WORKBENCHES` 加「店員手機」（group: counter、deviceRole: false、accent: sky、homePath: /staff）
- `apply-workbench.ts`：`loginModeOf()` 對 `staff` 回 **null**（唔准改店級掃碼模式）；營運模式落到 `dinein`（本機 localStorage，唔影響收銀台）

### 🔴🔴 關鍵決策：source: "pos"（唔加新 source）
`0015_pos_self_order.sql:19` 有 CHECK 約束：`source IN ('pos','kiosk','scan')`。
- 用 `kiosk` ⇒ 寫入被 DB 拒（500），而且收銀端 `isSelfOrder()` 會當自助單 → **重複出紙**
- 用 `pos` ⇒ 語意正確（員工落單）、**唔使加 migration**

### 🔴 出紙：手機自己建廚房單
`buildKitchenPrintJobs()` 讀 `loadDeviceConfig()`（zone 打印機）＋ `loadPosLocalSettings()`（廚房模板）。
手機冇參與過打印機設定 ⇒ 一定要先拉 `/api/pos/device-config?storeId=` 寫入本機，
否則 `zonePrinters.length === 0` ⇒ **回空陣列、零出紙、唔報錯**（最難查）。
出紙一律 `appendPrintJobsWithSync()`（淨 `savePrintJobs()` ＝ 零出紙）。

### 🔴 枱況：一定要拉雲端
只靠本機 `loadOrders()` ⇒ 新手機**所有枱都顯示空枱** ⇒ 店員會為已佔用嘅枱開重複單。
⇒ 拉 `/api/pos/state?storeId=&limit=200`（雲端為權威，用 `id` 去重，本機補漏）。
拉唔到（401／離線）⇒ `tableStateStale = true` ⇒ **顯示「枱況未確認」＋警示**，
唔可以照樣顯示「已連線」（會誤導）。

### 驗證（真瀏覽器，port 3017，唔需要後端）
- 流程：選枱 → 揀菜 → 購物車 → 送出確認，全部通過，**零 pageerror**
- 加菜：seed 一張 A02 未結單 → 顯示「用膳中 · 3 位 · $148」→ 撳入去「加菜模式」＋購物車預載
- 工作台頁有「店員手機」卡
- 觸控目標全部 ≥ 40px
- 截圖：`docs/mockups/staff-impl-0{1..6}-*.png`

### 捉到並修正嘅真問題
1. `cart.length` 係**行數**唔係件數 ⇒ 2×叉燒飯+1×牛腩麵會顯示「已選 2 項」（實為 3 件）⇒ 加 `cartCount`（quantity 加總），文案改「已選 N 件」
2. 冇 `capacity` 時顯示「- 位」⇒ 加 `capacityText()`，冇值就唔顯示
3. 枱況拉唔到時照顯示「已連線」⇒ 改「枱況未確認」＋警示橫幅

### 未做（下次）
- `waiter` 角色（只可落單、不可收款／退菜）—— 目前用既有 role，未加新角色
- 實機（真手機）測試 + 真店出紙端到端驗證
- 時價菜（`isMarketPrice`）目前**唔准加落車**（需要輸入價錢彈窗，未做）

### 環境（再次確認）
- `next dev` 生成嘅 `.next/dev/types/validator.ts` 會被截斷 ⇒ tsc 報 TS1005/TS1002 ⇒ 刪咗佢再跑 tsc
- 一定要用 `localhost` 唔可以用 `127.0.0.1`（Next 16 封鎖跨來源 dev 資源）

## 安全排查：疑似入侵（2 筆 kiosk 單）＋ 大量同步失敗

報告：`docs/reviews/security-incident-2026-09-16.md`。來源：`~/Downloads/macau-pos-system-log-export-2026-09-16T12-14-21.csv`。

### Vercel log 匯出嘅陷阱（必記）
- 🔴 **匯出 CSV 完全冇 IP 欄**（只有 TimeUTC/requestPath/status/UA/region/message…）⇒「來源 IP」問唔到。
  要 IP 就靠 `/api/pos/sync` 嗰句 `console.info(…ip=…)`（`sync/route.ts:375`），但佢**只在 `authEnforced && !authorized` 之下執行**
  ⇒ `POS_REQUIRE_DEVICE_AUTH=1` 一恢復，匿名 IP log 就跟住返嚟。`clientIp()` 讀 `x-forwarded-for`（`lib/pos/rate-limit.ts:43`）。
- 🔴 **一個請求會出多行**（每個 `console.log` 一行、同 requestId）。138 行其實只有 **50 個唯一請求**：睇落「33 次 sync 400」真相係 **1 個** 400 請求（帶 32 事件）。
- 🔴 用戶講「7:40–7:43」= **UTC−4（美東）**；澳門 19:40–19:43 = UTC 11:40–11:43。今次匯出只有 11:42:20–11:42:59（40 秒）
  ⇒ **啱啱錯過 19:41 兩筆單嘅建立**。**匯出時間 filter 之前先問清楚時區。**

### 訂單 id 前綴（判別「邊個程式建單」用）
| 產生者 | 前綴 | source |
|---|---|---|
| 收銀台／快餐收銀台 | `order-` | **寫死 `"pos"`**（`pos-app.tsx:2412,2428,3446`） |
| 店員手機 | `staff-` | `staff`（`pos/staff-order.ts:76,122`） |
| 自助機 /order、掃碼 | `kiosk-` | `isScanLink ? "scan" : "kiosk"`（`use-kiosk-order.ts:856`） |

⇒ UI「自助點餐機」徽章 = `source==="kiosk"`（`order-source-badge.tsx:10`）；**收銀台冇任何路徑**會出 kiosk 徽章。

### kiosk 落單入口
- `/order` 只需**手動輸入 storeId** 即綁店（`kiosk-order.ts:82-108`，存 localStorage `macau-pos-kiosk-device`）。
- 🔴 **storeId 係公開值**：枱 QR = `/menu?tableId=…&store=<merchantId>`（`sync/route.ts:351-353` 註解自認）。
- 伺服器**設計上**收匿名 `source ∈ {scan,kiosk}`（`sync/route.ts:398`）。

### 🔴 P0：`POS_REQUIRE_DEVICE_AUTH=0` 在生產生效（log 明文）
- `resolvePosRouteAuth()` 第 59 行 ⇒ 全部受保護端點對匿名回 200；`sync/route.ts:365-368` `authorized = !authEnforced || …` ⇒ **恆真**
  ⇒ 白名單（:686）、店內營業閘（:698）、售罄校驗（:460）全部失效；匿名者可自稱 `source:"pos"`、結帳、刪單、寫打印任務。
- 修復次序：`POST /api/pos/device-token` 驗 200+token → 設 `=1` → **Redeploy** → 逐端點驗 401。
  連帶風險：中繼機 `fetchDeviceConfig()` 冇憑證（`RelayApi.kt:221`）、kiosk 面板無店員 session ⇒ 都可能 401。

### 同步失敗（2 個獨立成因，同網絡無關）
1. 6 × `ORDER_CREATED`「payload 缺少訂單 id」= **永久**（payload 非 object 或 `id` 非字串；`sync/route.ts:618-620,673,1045`）。← 對應 UI 6 筆。
2. 25 × `拒絕覆寫訂單`（23 張 09-14/15/16 舊單）＝ LWW 正確拒收降級，`ack(ok:true, applied:false, reason:"downgrade")`（`route.ts:892-915`）。
- 🔴 **「按放棄又彈返」根因**：`discardFailedSyncEvent()` 只轉 `skipped`；但 `sync-flush.ts:558-560` 嘅 **legacy-heal 分支**（每次 reload `legacyHealed=false` 時行）
  **只排除 `failed`、唔排除 `skipped`** ⇒ 一 reload 就重新推 → 再 400 → attempts 5→6 → 轉返 `failed` ⇒ 又出現。
- 背景三條自動重試：flush 30s（`sync-flush.ts:69`）、failed 15min 退避（`:99,365`）、對賬守護 60s（`sync-reconcile-daemon.ts:62`）。
- 一個事件 `ok:false` 就會令**整批**回 400（`sync/route.ts:1317-1328`），但 `results[]` 仍逐條帶 ack，client 逐條處理。

### 關閉自助點餐機 = 擋唔住
`allowedModules` 只係 **UI 導覽授權**（`module-catalog.ts:130-138` `homePath:"/order"`；`select-workbench-screen.tsx:83-90`；`app-sidebar.tsx:44,117`）：
- `/order` 路由**冇**檢查模組授權；`/api/pos/sync` **完全唔知**模組授權；已設 kiosk mode 嘅機照樣開機入 `/order`。
- 由 Admin `PATCH /api/admin/merchants/modules` 寫入 ⇒ **每部終端要重新登入**先見到。

## 結案（20:50）— 用戶跑 SQL 覆核後嘅定論

### kiosk 單：確認係自助機客戶端建，非偽造
`kiosk-81ed2f69`(自取02,MOP43) / `kiosk-b0dc1cdd`(自取01,MOP17)，`store_id` 齊。
- 🔴 **UI 顯示嘅時間係「最後更新」唔係建立**：真正建立 **19:27:22 / 19:29:43（澳門）**，19:41:32/38 才 cancelled。
- 對應事件 `evt-2348dae2`/`evt-654b880d`，事件時間比 `created_at` **只遲 1–2 ms**＝`submitKioskOrder()` 簽名。
- 3 日內全平台**只有此 2 張** kiosk 單（其他店 pos/scan）⇒ **一次性**，唔似掃射。
- 追兇法：匯出 Vercel log **UTC 11:26–11:31**，睇有冇 `POST /api/pos/sequence` + 該請求嘅 `requestUserAgent`（iPad Mac UA vs iPhone vs Android）。

### 🔴 6 筆失敗事件真身 = **線上單橋接**（同 kiosk 完全無關）
`entity_id` 全部 `ledger-<uuid>`、`payload->>'source' = null`。
- 根因：`ledger-pos-bridge.ts:388-400 enqueueOrderEvent()` **兩種 type 都送 `{ order }`**，
  而 `sync/route.ts` 舊版**只喺 ORDER_UPDATED 拆 `.order`** ⇒ ORDER_CREATED 攞唔到 id ⇒ 永久 400。
- ⇒ 每次「線上單**首次**排位／採納」(`index<0`) 都產生一條永遠上唔雲嘅 ORDER_CREATED；
  該單喺 POS 雲端冇完整記錄（結帳時只由 `ORDER_SETTLED` 0 列 upsert 補最小記錄，**冇 items**）。
- 另一個後果：LWW **預取段**同一處亦冇拆 `.order` ⇒ 攞唔到現有 row ⇒ **LWW 守門靜默失效**。

### 已改（本地未部署）
- 新增 `src/lib/pos/sync-order-payload.ts`（拆解規則單一真源：`unwrapOrderEventPayload` / `addedItemsOfEventPayload`）+ `sync-order-payload.test.ts`（7 pass）。
- `sync/route.ts` 兩處（LWW 預取 + 主解析）都改用 helper ⇒ 兩種 type 收兩種形狀 ⇒ **已排隊 6 筆會自動補上**。
- `ledger-pos-bridge.ts` ORDER_CREATED 改送裸 order。
- 驗證：`tsc --noEmit` 0 error；`node --test <test file>` 7 pass。

## 21:25 — `POS_REQUIRE_DEVICE_AUTH=1` 已生效（實測驗證）

新增工具（可重用，只發 GET）：`tools/verify-pos-authgate.cjs`、`tools/audit-anon-endpoints.cjs`。

### 已擋
`state`/`shift`/`print-jobs/status`/`print-templates`/`note-presets`/`online-order-settings`/`device-config`/`print-agent/pair-status`/全部 `/api/admin/*` → 401 ✅。
仍匿名（設計需要）：`bootstrap`、`store-status`、`order-lookup`、`kiosk-settings`、`print-agent/pair` ✅。

### 🔴 掃 28 條 GET 後仲有 4 條漏網
| 端點 | 回傳 | 呼叫方 |
|---|---|---|
| `/api/pos/orders` **GET** | **269KB / 500 張單** | 倉內**冇** in-app GET（panel 只用 DELETE）；聲稱係主系統整合 API |
| `/api/backoffice/overview` | **全部店舖清單**（id+名） | `backoffice-client.ts:66` 冇憑證；`/backoffice` 有 `AuthGuard allowedRoles:["admin"]` |
| `/api/salon/state`、`/api/salon/bootstrap` | salon 資料（本店空） | `lib/salon/storage.ts:300-301` 冇憑證 |
| `/api/inventory/health` | `shop_users_count` | **冇任何呼叫方** |

### 🟠 中繼機 device-config（已查 APK 源碼，唔係即時停印）
- 兩個 App 都無憑證拉 `device-config`：`print-relay/RelayApi.kt:221`、`_ref-macau-ledger-merchant/.../RelayApi.kt:184` ⇒ 一定 401。
- 但：`fetchDeviceConfig()` 非 2xx → 回 `null`；`HubService.kt:217` `if (list != null)` **保留舊值**；
  `JobRunner.resolvePrinter()`（`JobRunner.kt:187-250`）四級 fallback：job.printer → deviceConfigPrinters → claim.printers → **本機 LAN 發現**。
- ⚠️ `RelayState.deviceConfigPrinters` **純記憶體**（`RelayState.kt:53` 初始 emptyList）⇒ **一重啟就失去權威路由** ⇒ 唔會停印但**可能印錯機**。
- ⇒ **今晚唔好重啟中繼機**；中期要 APK 帶 `agentId/agentToken`。

### 其他
- `GET /api/pos/store-status` → `isOpen:false`（店內暫停營業）⇒ 閘生效後**真正會擋**匿名掃碼／自助機單（以前繞過）。唔係刻意就記得開返。
- `print-agent/heartbeat` / `claim` 冇被擋 ⇒ 派工鏈路完好。

## 21:45 — 依用戶批准，收 5 條漏網端點

### 🔴 取證（改之前實測）
- `GET /api/salon/state` **唔帶 storeId** → **200、19.6 KB，含 `customerName` + `customerPhone`（顧客 PII）**
  （回嘅係 `mock-*` 種子資料，未涉真實客人，但代碼路徑同真店一樣）。
- `GET /api/salon/bootstrap` 唔帶 storeId → 回「最後更新嘅一間店」嘅 salon 主數據（跨店）。
- `GET /api/backoffice/overview` → 3.2 KB **全部店舖清單（id+名）**＝可列舉所有 storeId。
- `GET /api/pos/orders` → **269 KB / 500 張單**。

### 已改（本地未部署，7 個檔案）
| 檔案 | 改動 |
|---|---|
| `app/api/pos/orders/route.ts` | GET 補 `posRouteAuthGuard`（商家確認外部主系統冇用） |
| `app/api/salon/bootstrap/route.ts` | **必須 storeId**（移除「冇 storeId → 回最後更新嘅店」）＋ 加閘 |
| `app/api/salon/state/route.ts` | 同上；5 條 query 一律 `.eq("store_id")` |
| `app/api/inventory/health/route.ts` | 加「admin session 或 POS 憑證」閘 |
| `app/api/backoffice/overview/route.ts` | 同上 |
| `lib/salon/storage.ts` | hydrate 兩條 fetch 帶 `posDeviceAuthHeaders()` |
| `lib/backoffice-client.ts` | 先帶 admin token、冇就帶 POS 憑證；401 → fallback 本機 mock（安全） |

口徑：兩個「跨店視圖」端點（backoffice/inventory）**冇 storeId 參數** → 用
`!isPosDeviceAuthRequired() || adminClaims || deviceClaims`（同 kill switch 一致）；
POS 端點一律 `posRouteAuthGuard`，閘一律放喺「未配置 Supabase／缺 storeId」early-return **之後**。

### 驗證
- `tsc --noEmit` **0 error**；`eslint`（7 檔）**0 error**。
- **本機 dev server（`next dev -p 3999`）** 實測：`/api/backoffice/overview`、`/api/inventory/health` → **401** ✅。
- ⚠️ 本機冇 `.env.local` ⇒ Supabase 未配置 ⇒ `pos/orders`、`salon/*` 走 **mock early-return（200）**，
  即係**本機驗唔到**嗰三條閘，一定要**部署後**用 `tools/audit-anon-endpoints.cjs` 覆核。

### 中繼機方案（已出，寫入 `docs/integration/print-relay-hardening-brief.md` §8）
- Server：`device-config` GET 改「agent 憑證 OR posRouteAuthGuard」，用 `readAgentHeaders` + `verifyAgent`，
  **一定要驗 `agent.storeId === storeId`**（否則可讀別店配置）。
- APK：`print-relay/RelayApi.kt` + `_ref-macau-ledger-merchant/.../RelayApi.kt:184` 加 `x-agent-id`/`x-agent-token`
  （header 名同 `RelayApi.kt:259-260` 嘅 `post()` 一致）。
- 驗收 R1–R6（R3 跨店 401、R6 重啟後印對機）。過渡期：**唔好重啟中繼機**。

## 店員手機 — 終端級「落單專用」限制（A 方案，2026-09-16 晚）

### 用戶決定
- 行 **A**（UI 收縮，唔碰 migration），唔行 B（帳號級真角色）
- 「落單專用」**綁終端（手機）**，唔綁帳號

### 🔴🔴 先講清楚：呢個唔係安全邊界
旗標住 localStorage，**只擋前端路由**：
```
localStorage.removeItem("pos.orderOnlyTerminal.<storeId>")   // 一秒解除
curl -X POST /api/pos/sync -d '{"events":[{"type":"ORDER_SETTLED",...}]}'  // 直接結帳
```
解決嘅係**誤操作**（店員撳錯書籤／上一頁而誤結單），**唔係權限**。

### 實作
- 新增 `src/lib/pos/order-only-terminal.ts`：旗標 + 純函式白名單
  （`isPathAllowedOnOrderOnlyTerminal`）＋ `order-only-terminal.test.ts`（6 test）
- 白名單**只有三個**：`/staff`（落單）、`/login`（憑證過期）、`/select-workbench`（**逃生門**）
  ⇒ 冇逃生門，揀錯工作台部機就廢咗
- 段邊界比對（`/staff` 唔可以匹配 `/staffing`）
- `apply-workbench.ts`：`setOrderOnlyTerminal(merchantId, workbench === "staff")`
  ⇒ **兩個方向都寫**（同 kiosk 唔同：kiosk 刻意唔寫 false，但落單專用係跟隨工作台選擇）
- `auth-guard.tsx`：`orderOnlyBlocked` 攔截 → `replace("/staff")`；
  ⚠️ 要放喺 `roleBlocked` **之前**（呢個係「部機唔應該去呢度」，唔關帳號角色事）
- `/staff` 加「工作台」逃生入口

### 驗證（真瀏覽器 port 3018，四組對照）
- flag OFF：`/` `/orders` `/staff` 全部正常 ✅
- flag ON：`/` `/orders` `/reports` 全部導向 `/staff`；`/staff` `/select-workbench` 放行 ✅
- 逃生門：`/select-workbench` 揀「堂食收銀台」⇒ flag 變 `null`、成功入返 `/` ✅
- 零 pageerror；784 個單元測試全綠

### 🔴 測試陷阱（浪費咗一輪）
`page.evaluateOnNewDocument()` **每次新 document 都會重跑** ⇒ 用佢種「落單專用旗標」，
清咗之後一導航又被種返 "1" ⇒ 睇落好似「清唔到」，其實係**測試自己寫返落去**（假失敗）。
正解：去 `/login` → `page.evaluate` 寫 localStorage → 再 `goto` 目標頁。
**診斷法**：加臨時 `console.log` 確認函式有冇被叫、key 係咩 —— 一睇就知係「冇叫」定「叫咗但被覆蓋」。

### 未做（真權限，下次）
三個必要條件：① POS 端 waiter 真源（Ledger 只有 owner/staff，**冇 waiter**）
② 登入時簽入 POS 憑證 ③ `/api/pos/sync` 對 `ORDER_SETTLED` 驗角色。
🔴 **前置**：`POS_REQUIRE_DEVICE_AUTH` 要開返 —— `pos-route-auth.ts:59` 第一條分支
直接放行、**根本唔讀憑證** ⇒ 唔開返，角色檢查形同虛設。
次序（不可顛倒）：確認 device-token 回 200 → 設 1 → **Redeploy** → 逐端點驗 401。
