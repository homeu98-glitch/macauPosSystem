# 2026-09-15 工作記錄

## 修：報表「線上實收」vs 交班「線上線下合計」夾唔到（商家：交班 602、報表 0）

**診斷（兩個獨立口徑不對稱，一次過修）**

1. `src/components/shift-page.tsx` — `onlineLocalOrders`（本地線上投影單，`onlineOrderId` + `settled`）
   **漏咗日期過濾**。同一檔案其餘三個口徑（`todayLocalOrders` 線下 / `ledgerOnlyRows` Ledger 純線上 /
   `detailOrders` 明細）**全部**有 `orderMatchesReportRange(o, "today")`。
   ⇒ 往日嘅線上投影單一路加落「今日」線上實收 → 交班卡片大過報表（跨午夜 00:00 後最明顯）。
   ✅ 已加 `orderMatchesReportRange(o, "today")`。

2. `src/components/restaurant-daily-report.tsx` — `onlineDishSource` 以前**只 push `get_order_detail()` 成功嘅單**，
   而 `aggregate()` 嘅 Ledger 純線上單迴圈係**靠呢個 array 逐張計錢**（`ledgerOnlyPaidTotal`）。
   ⇒ 明細 RPC 一失敗，嗰筆錢喺 KPI 靜默消失（交班唔受影響，佢只讀 `list_merchant_orders`）。
   ✅ 而家 catch 都 `push({ order: o, items: [] })`：**金額照計**，只係菜品排行少嗰張；
   `onlineDetailInfo.ok` 改為 `collected.length - failed`，`status` 用 `failed === collected.length` 判 error。

**口徑鐵律（以後要守住）**：交班同報表嘅「線上實收」= 同一個 `list_merchant_orders` 篩選
（區間內 + 非取消 + `paymentStatus === "paid"`，見 `lib/ledger/paid-orders.ts`）**∪** 同區間內本地線上投影單
（按 Ledger order id 去重，**本地為準**）。一邊改範圍／判準，另一邊一定要跟。

**驗證**：`tsc --noEmit` exit 0、`eslint` 兩個檔 exit 0。
文件已補入 `docs/113-agent-gotchas.md`（新增一節，喺「Realtime 訂錯 Supabase 專案」之前）。

## ⚠️ 未 push / 未部署（重要）

`git status` 顯示**另一個 session 正在改同一個 repo**：`src/lib/shift-sync.ts` 同
`src/app/api/pos/shift/route.ts` 嘅 mtime 係 2 分鐘前（做「交班歷史雲端回填」，`fetchServerShiftHistory`），
`src/components/shift-page.tsx` 同時有我今次嘅改動 + 佢嗰個功能（`historyCloudCount` 等）。
⇒ 只 commit 我嗰部分會 build 失敗（缺 `fetchServerShiftHistory` export）。
**等另一 session commit 完，再一次過 push（Vercel 部署）**，本機改完唔部署＝零效果。

### ✅ 後續（00:24 收尾）

商家確認「今日應該係 0，所以報表係對」⇒ 證實係①（交班帶住往日線上投影單），診斷正確。

- 另一 session 於 `f9f8981`（09-15 00:22 +0800）一次過 commit 全部改動（**順手帶埋我今次兩個檔**），
  已 push 到 `homeu98-glitch/macauPosSystem` main（`HEAD == origin/main == f9f8981`）。
- Vercel Production 部署 **success @ 16:24:00Z**（ref=f9f8981）⇒ 修正已上線。
- 新增 `tools/probe-deploy.cjs`：一次過查 ① 網站 headers（age / x-vercel-cache）
  ② GitHub `commits/{sha}/status` 嘅 Vercel state（含 poll 到成功為止）③ `deployments` 時間軸。
  呢個 repo「push 咗但唔知有冇 deploy」出現過好多次，留低做工具。
- ⚠️ 教訓：**同一個 repo 有多個 session 同時改，commit 會互相夾帶** ——
  想只 push 自己嗰部分係做唔到嘅（`shift-page.tsx` 同時有兩邊改動，
  只 commit 一邊會缺 export → build 失敗）。要預咗一齊走。

## UI 確認稿：交班歷史工具列改版（未實作，等 J 拍板）

出稿：`docs/mockups/shift-history-toolbar-pagination-2026-09-15.html`
（固定畫布 1180、零 `@media`、可互動試「查看更多／收起／篩員工」）。

三項要求：① 篩選條件排成**同一行** ② 移除 Excel 導出、只留 CSV ③ 每頁最多 **10 天**＋「查看更多」。

實作要點（等批准後照做）：
- **篩選列獨立成一行**：標題＋副標題移上去自己一行，篩選收成一條 `.bar`
  （`flex` + `nowrap`：交班日期 由→至 ｜ 員工 ｜ 右邊導出 CSV）。
  舊版根因＝副標題太長搶寬度，逼到最後一粒掣換行、壓落表頭。
- 刪 `exportShiftHistoryExcel()` ＋ `exportingType` 嘅 `"excel"` 分支。
- 分頁：`filteredShiftHistory` 先按 `closedAt.slice(0,10)` 分組 → `loadedDays`（初始 10）決定顯示幾多日；
  底部「已顯示 X 天 · N 筆（合計 Y 天 · M 筆）」＋查看更多；展開後加「收起」。
- 表格欄寬重排（td 14px→12px、內距 12→10px）⇒ 金額單行、操作欄兩掣同一行、待同步改逐行顯示。
- 新增日分隔列（每天一行「日期（週X）· N 個班次」）。

驗證工具：`docs/mockups/_shots/verify-pagination.cjs`（真 Chromium 撳 查看更多/收起/篩選，斷言天數）；
`_shots/v3/*.png` = 最終版面。三個待 J 拍板問題見稿內註解（10 天定義／累加 vs 換頁／CSV 範圍）。

### ✅ 已實作並上線（commit `6e663bf`，Vercel Production 部署 success @ 00:48）

J 拍板：① 10 個日曆日、可以多過 10 行 ② **累加**再加 10 天，但**框尺寸不變** ③ CSV 導出**全部**。

改動（只在 `src/components/shift-page.tsx`）：
- 標題＋副標題搬上去自己一行；篩選收成一條 `.bar`（`flex` + `nowrap` + `overflow-x-auto`）。
- 刪 `exportShiftHistoryExcel()`，`exportingType` 收窄為 `"csv" | null`。
- `shiftHistoryDayGroups`（按 **`macauDateKey()`** 分日）＋ `historyLoadedDays`（初始 10）、
  `shiftHistoryVisibleGroups`；篩選一變就 reset 返第一頁。底部「已顯示 X 天 · N 筆（合計 Y 天 · M 筆）」
  ＋「查看更多（再載入 N 天）」＋（展開後）「收起」。
- 表格：`maxHeight 620px` 固定框、`table-fixed` + `min-w-[1130px]` + `<colgroup>` 百分比欄寬、
  表頭 11px、日分隔列、待同步／退款／應收實收現金改逐行顯示。
- 🔴 教訓：**12px 下「MOP 3,945」實測要 66–70px**，欄闊 58px 會斷成「MOP」/「3,945」。
  `scrollWidth > clientWidth` 捉唔到（係換行唔係溢出）—— 要用
  `Range.selectNodeContents(node).getBoundingClientRect().width` 量。我第一次就係被前者騙到。
- ⚠️ iPad 橫向（內容 ~1040px）下 12 欄仍需**橫向滾 ~90px**（固有問題）。想唔洗滾：
  把「MOP」搬去表頭（每格只顯示數字）→ 1130 → ~980。**已向 J 提出，等佢決定。**

驗證證據：`docs/mockups/shift-history-impl-verify-2026-09-15/`（10 天 / 20 天 / iPad 1180 三張截圖）。
tsc 0 error、eslint 0 error。
