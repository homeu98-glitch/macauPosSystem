# 2026-09-24 工作日誌

## 11:25–11:45 · egress 覆核（**營業中**窗口，補回一直缺嘅樣本）

商家提供 Supabase 用量截圖（period 已用 **0.06 GB / 250 GB Pro**）＋兩份 log
（`supabase_logs (16).csv` 1,000 行／10:29:41–11:22:16 MAC；Vercel 匯出 339 行 → **222 唯一請求**／10:54–11:21:40）。

### 結論
- ✅ **用量已回正常**：23 Sep 全日 ≈ 49 MB（PostgREST 37.9 ＋ Realtime 7.8 ＋ Storage 3.4 ＋ Auth 0.05），
  24 Sep 至 11:24 ≈ 10 MB。vs Pro 250 GB ⇒ 0.02%。舊大戶（`legacy=1`／`queue=300`／`limit=300`／stale 拒收）**全部 0 次**。
- 📌 兩店同時 `is_open:true`（`8291f843` 09:00 開、`d564b932` 亦開）⇒ 呢次係首次「營業中」覆核。
- ⚠️ **23 Sep 全日只有 23 張單**（21 Sep 23／22 Sep 37／24 Sep 至 11:15 只 4）⇒ 49 MB 係慢日數字，唔可當繁忙日基準。

### 新發現（未解，已寫入 DETAIL §G）
1. 🔴 **同一批事件每 30 秒重推 ≥51 分鐘**：`PATCH print_jobs print-11e37b9e` 27×、
   `PATCH pos_orders order-778dbb99` 25×、`ledger-2cc9593f` 22×，gap 全部 30.0s。
   後果：兩張單雲端 `updated_at` 被推到 11:26:35 而**內容零變化** ⇒ 報表用 `updated_at` 當事件時間
   ⇒ **跨日會漂移**。根因未定位（log 睇唔到 client），下一步＝Mac 開 DevTools 睇 `[pos-sync-flush]`。
2. ⚠️ 83 條逐筆 `DELETE pos_print_jobs`（4 秒內）＝打印中心清 83 張舊 job；1 個 invocation、payload≈0，可批次化（非緊急）。
3. ⚠️ 2 次 `23505 duplicate key pos_print_jobs_once_key_uniq`（`receipt:0`／`kitchen:normal:0:1ah9q6a`）＝同 onceKey 用新 randomUUID 重推。
4. ⚠️ Realtime WS 每 ~105 秒重連（09-23 係 11.8 分鐘）。
5. ✅ 打印鏈健康：最近 40 張 job 全部 `printed`、`attempts=0`、created→claimed 1–2 秒。

### 工具
新增 `tools/_probe-20260924.cjs`／`_probe2`／`_probe3`／`_vchead`／`_vcdetail`／
`_probe-daily-20260924.cjs`（按澳門日數訂單）／`_probe-pj-20260924.cjs`（打印 job 現況）／
`_probe-ord-20260924.cjs`（單張訂單現況）。全部唯讀。

### 記憶維護
MEMORY.md 由 **10,752 → 2,9xx 字元**（抽走敘述性內容到 `DETAIL-2026-09.md`，新增 §G／§H）。

報告：`docs/reviews/recheck-2026-09-24-egress-business-hours.md`

---

## 18:05 · 診斷：為何澳覓／mfood 單無法設定打印位置

用戶問「mfood 與澳覓訂單為何無法分別設定對應打印方式」——**純診斷，未改碼**。

### 根因（三個疊加）
1. **UI 冇呢個欄位**：`PosLocalSettings`（printZones / menuPrinterOverrides）＋`DeviceConfig`
   （只有 `shiftPrinterId`）都冇「平台訂單分區」。唯一「單據類型→機」binding 只有交班單。
2. **後端硬編**：`src/app/api/integration/grabber/orders/route.ts:157–166`
   `platformZone: null, defaultPrinterGroup: "kitchen"`，且唔傳 `menuItems`
   ⇒ `grabber-order.ts:483` `printerGroup: platformZone ?? menu?.printerGroup ?? defaultZone`
   ⇒ 平台單**全部品項**一律 `"kitchen"`。註釋已寫明「方案 A（平台訂單分區）屬 Phase 3」，未實作。
3. **平台菜名對唔到 POS 餐牌**（設計上係常態）⇒ `menu?.printerGroup` 永不存在。

### 路由機制（記住：唯一粒度係「菜品 → 分區」）
- `print-jobs.ts:239–296` `buildKitchenPrintJobs()`：取 `enabled && role==="zone"` 全部機，
  逐台 `matched = items.filter(it => !printer.zoneId || it.printerGroup === printer.zoneId)`，
  **`matched.length===0` ⇒ `continue`（冇 fallback）**。
- ⚠️ 對比 `ledger-pos-bridge.ts:209–358`：線上單**有 orphan catch-all**（孤兒品項塞入第一個 job）。
  兩條路唔對稱 —— 平台單冇呢層保護。

### 🔴 陷阱（要覆核一齊講清楚）
- 自己開「澳覓」「mfood」兩個分區再綁機 = **完全無效**（冇 item 帶呢兩個 group）→ 一張都唔印。
- 刪走／改名 `kitchen` 分區 ⇒ 平台單變 **0 張廚房單**（pos-app.tsx:4129–4137 會彈「分區對唔中打印機」）。
- **未搵到平台單嘅自動出紙路徑**：`isNewSelfOrder` 只認 `kiosk|scan`（`order-source.ts:29`），
  `online` 開關只管 ledger-pos-bridge ⇒ 平台單目前似只靠手動「打印廚房單／重打整單」。
  （此點屬推論，未在生產 log 驗證，下次要核。）
- `PosOrder["source"]` 型別聯集仍只有 `pos|kiosk|scan`，唔包 `aomi|mfood`
  （pos-app.tsx:5694–5701 已註明係刻意最小改動）。改型別要三處同步：`order-source.ts`＋badge＋呼叫端。

### 若要支援（建議方案）
- 擴充 `platformZone` 為 **per-source**（`{aomi?, mfood?}`），route 讀 per-store 設定傳入；
  加一格 admin UI（打印機綁定 → 平台訂單分區）。同時要決定「平台單會唔會自動出紙」。

---

## 20:17 · 診斷：廚房單重複打印 ＋ 收據卡「已發送」＋ 空單號 job（**純診斷，未改碼**）

商家回報三件事（截圖 ×2 ＋ Supabase log ＋ Vercel log）。兩個問題**同一病根家族**，
但**唔同層**：一個係 client 重複入隊，一個係 DB 唯一鍵設計錯。

### 🔴 收據卡「已發送」＝**DB `once_key` 冇 order 身分 ⇒ 自動收據被 23505 靜默吞掉**

- `pos_print_jobs_once_key_uniq` = `unique (store_id, once_key) where once_key is not null`（0045）。
- 但 sync route 寫入嘅係 **client 原始 `PrintJob.onceKey`**（`src/app/api/pos/sync/route.ts:1661-1662`），
  而自動收據嘅 onceKey = **`receipt:${reopenCount}`**（`print-jobs.ts:184`，呼叫端
  `pos-app.tsx:4663`／`print-jobs.ts:773` 都傳 `{once:true}`）⇒ **每張單都係 `receipt:0`**。
- ⇒ 全店**只可能有一行 `receipt:0`**；第二張自動收據 insert → 23505 → sync route 走
  `isUniqueViolationError` 分支 **`ack(true)`**（route.ts:1702-1708）→ client 剷走事件 →
  本地 job 永久停「已發送」、雲端冇行、**冇紅標冇紙**。
- **實證**：log `[pos/sync] 內容唯一鍵重複 → 略過重複出紙（job=print-ef087365 once_key=receipt:0）`
  ＋ DB `duplicate key value violates unique constraint "pos_print_jobs_once_key_uniq"`（3 次／43 分鐘）。
- **DB 真相（anon 直查，20:17）**：近 24h 48 行，`once_key` 非 NULL 嘅 **26 行全部係
  `kitchen:normal:*`，收據一張都冇**；6 張收據 `once_key` 全 NULL（＝人手補打）vs 22 張已結帳單。
- ⚠️ 設計文件（0045 檔頭）寫 `once_key = "<orderId>|<onceScope>|<printerId>"`，**實作從來冇砌過**；
  client 只喺 localStorage 帳本砌（`print-dedupe.printOnceKey()`），DB 收原始值 ⇒ 設計同行為唔一致。
- ⚠️ 09-24 早上已見過同一 23505，當時判為「同 onceKey 用新 randomUUID 重推」——**係誤判**（見上）。
- 副作用風險：兩張單菜品完全相同 ⇒ kitchen 簽名相同 ⇒ 一樣會撞（＝廚房靜默漏單）。

### 🔴 廚房單重複＝兩條入隊路徑、只有一條寫 onceKey

- 近 24h **7 個重複組**；其中 **5 張 `ticket_type=normal` + `printer_group=kitchen` 但
  `once_key IS NULL`** —— 按設計**唔應該出現**（自動廚房單一定要帶鍵）。
- NULL 嘅正常廚房單只可能來自**冇寫 onceKey** 嘅呼叫端：
  `pos-app.tsx:1601`（開機 backfill 自助單）／`:2113`（realtime 首次見自助單）／
  `pos-orders.ts:318`（confirmSelfOrder）。
- 帶鍵嘅係 `ledger-pos-bridge.ts:374/472`（接單／補印兜底）。
- ⇒ QR／掃碼單（Ledger 鏡像）**兩條路都會出紙**：兩個 guard（`hasPrintedLedgerOrder()` vs
  `loadPrintJobs().some(...)`）都係 **check-then-act ＋ 每 realm 一份 localStorage**。
  實證：`ledger-7e5579c8`（004）**13:22:31 同秒兩行**、`ledger-12f1e6a8`（005）**13:23:39 同秒兩行**
  ⇒ 典型同 tick race（唔係 stale guard）。
- **DB 索引攔唔到**：一條 NULL（永不衝突）＋一條 keyed ⇒ 兩張紙都印得出。

### ⚠️ 「空白、冇訂單號」嘅 job ＝ 雲端 `printing` 狀態 ＋ 缺 order 身分

- client `PrintJob.status` 聯集只有 `pending|sent|failed|printed`（`types.ts:1453`），
  **雲端 claim RPC 會寫 `'printing'`**（0020:96／0035:70／0042:83），
  `mapPosPrintJobRow` 只做型別 cast（`pos-order-mapper.ts:166`）⇒ 入到本地變成非法值 ⇒
  `normalizePrintJobStatus()`（`print-jobs.ts:78-87`）**一律標「失敗＋狀態欄位異常」**。
- 訂單號欄 render `{job.orderNo ?? job.orderId}`（`print-center.tsx:1852`）；雲端
  `order_no`／`order_id` 皆 NULL ⇒ 空白。截圖見到嘅「空白＋失敗」行＝雲端行缺 order 身分
  ＋ `printing` 狀態被當成壞資料。

### 待辦（未做）
1. `once_key` 必須改為 **含 orderId（＋printerId）** 先可根治；同時處理「已存在舊鍵」遷移。
2. 三個無鍵廚房單呼叫端要補 `onceKey`；兩個 guard 要收成一個共用判準（或改用 DB 唯一鍵做最終閘）。
3. `PrintJob["status"]` 加 `"printing"`（或 mapper 收窄），令「已認領未回報」顯示得正確。
4. 取證工具：`tools/_probe-dupkitchen-20260924.cjs`／`_dupkitchen2`／`_dupkitchen3`（唯讀）。
   註：`pos_device_configs` / `pos_print_agents` anon **42501 讀唔到**（0021 後已收窄）。

---

## 20:40 · 實施上述 5 項修復（用戶：「直接 1-5 吧，保證不會影響其他功能」）

### 改動（**只改行為，唔改資料格式、唔需要 migration**）
1. **DB 去重鍵補訂單身分**：`print-dedupe.ts` 新增 `printOnceDbKey()`（砌
   `orderId|onceScope|printerId`，砌唔到／過長 → `null`＝唔去重）＋ `printOnceScopeFromDbKey()`
   （反函式）。`sync/route.ts` 寫 `once_key` 改為經 `printOnceDbKey()`（server 統一砌 ⇒
   舊 bundle 一齊受保護）；`state/route.ts` backfill 前**還原**做原始 onceKey
   （否則本機 compose 兩次對唔上 ⇒ 跨終端去重失效）。舊行（不含 `|`）同新鍵唔衝突。
2. **三個漏網正常廚房單呼叫端補 `onceKey`**：`pos-app.tsx` ×2（開機 backfill ＋ realtime
   首見自助單）、`pos-orders.ts confirmSelfOrder` ⇒ 同 `ledger-pos-bridge` 同一條 scope。
3. **狀態詞彙表單一真源**：新 `src/lib/pos/print-job-status.ts`（零 import、5 個值 ＋
   `toPrintJobStatus` / `cloudRowToPrintJobStatus` / 標籤 / 顏色）。`types.ts` 加 `"printing"`、
   兩個 mapper 改經白名單、`print-center.tsx` 藥丸同明細改用詞彙表、
   `print-jobs.ts normalizePrintJobStatus()` 接受 printing、salon 補 label（**tsc 即刻捉到 salon 漏咗**）。
4. **23505 唔再假成功**：`sync/route.ts` 改 `ack(true, undefined, {applied:false, reason:"print-dedupe-skip"})`；
   `sync-flush.ts` 收到就把該 job 標紅（「雲端判定為重複、本機未出紙…請補打帳單」），
   **只喺未出紙狀態寫**（唔降級 printed / 已 failed）。
5. **止血／診斷 SQL**：`tools/2026-09-24-print-once-key-collision.sql`（逐段診斷 ＋
   舊鍵設 NULL 嘅 UPDATE **預設註咗解**）。

### 守衛（防第四次）
- `print-job-status.test.ts`：7 條功能 + **4 條原始碼守衛**（types.ts 有 printing／兩個 mapper
  經白名單／sync 用 `printOnceDbKey`／三條呼叫端有 onceKey）。
- `print-dedupe.test.ts` 加 7 條（含「兩張唔同單嘅收據唔可以撞同一條鍵」）。

### 驗證
- `node --test`：**1461 pass / 0 fail**（新兩支合共 31 條）。
- `tsc --noEmit`：**OK**（過程中捉到 `salon/prints-content.tsx` 兩個 status map 漏 printing）。
- `eslint`：改動檔案全部 clean；`print-center.tsx` / `pos-app.tsx` 嘅問題數同 HEAD **完全一樣**
  （16 errors/2 warnings、0 errors/9 warnings，全部係 pre-existing React Compiler 診斷）。
- `next build`：跑緊（背景）。

### ⚠️ 未做
- **冇 commit**：工作區本身已經有**別人／早前未 commit 嘅改動**（platform-order 方案 A：
  `device-settings.tsx`、`local-orders-panel.tsx`、`quick-*.tsx`、`mock-data.ts`、`storage.ts`、
  `platform-order*.ts` …），同我嘅改動**同檔交錯**（`pos-app.tsx`、`types.ts`、`print-jobs.ts`、
  `pos-orders.ts`）⇒ 要 J 確認嗰批係咪可以一齊入 commit 才動 git。
- 生產 DB 嘅舊 `receipt:0` 行**未清**（代碼修好後已唔會再攔，屬可選）。
- 未部署（未 push）⇒ Vercel 仲跑舊 bundle。

---

## 21:05 · 商家跑咗 §3 重複組查詢（7 日窗口）→ **證實推論 ＋ 揭出更緊急嘅事**

商家貼回 32 組重複（我早前唯讀探測只睇到近 24h 嘅 7 組，因為 anon RLS 只有 24 小時窗）。

### 🔴 最重要發現：`receipt:0` 阻塞行**由 2026-09-21 12:02 存在到現在**

- §3 見到 `ledger-931f55c2… / 002 / printer-22a790b1` 一對 `["receipt:0", null]`
  ⇒ 持有者 ＝ **`print-fb179f3f`**（09-21 12:02 建立）。
- ⇒ **嗰張就係最後一張成功插入嘅自動收據**，之後每次自動收據都撞 23505 被靜默丟棄
  （＝商家「收據一直卡住」嘅現行機械原因，維持咗 3 日）。
- ⚠️ **anon 24 小時窗口睇唔到佢** ⇒ 我早前兩次唯讀探測都漏咗 → 教訓：跨日阻塞行一定要用
  7 日窗口／service role 查，唔可以靠 anon 表查斷定「冇呢一行」。
- SQL §5b 已補：確認 `once_key='receipt:0'` 持有者 ＋ 逐 id delete（`print-fb179f3f`）。

### 32 組重複分類（人工核對，全部有 keys 可判）
| 類別 | 組數 | 是否 bug |
|---|---|---|
| 🔴 真·廚房重複（`normal` + kitchen，帶鍵＋無鍵並存） | **9** | 係（代碼已修） |
| 收據手動／歷史重複（receipt 機，全 NULL） | 9 | 唔係 bug，係「自動印唔出 ⇒ 人手補打」嘅**症狀** |
| `addon` / `void` | 10 | 設計上合法重複 |
| `kitchen:normal:0` ＋ `:1`（返結後重結） | 1 | ✅ 合法（`order-bbe795fa` 訂單08） |
| 0045 前歷史（全 NULL、09-18/19） | 3 | 歷史 |

- 🔴 **9 個真重複全部係 `ledger-*`（線上／掃碼單），冇任何一個係 `order-*` 本地單**
  ⇒ 完全證實「Ledger bridge（帶鍵）＋ POS realtime/backfill（無鍵）兩條路」推論，
  即係唔係「邊個亂印」。涉及：004／003／006／007／008／002×2／005×2。
- 🔴 **最新一個係 09-24 20:47**（`ledger-bf19ba82` / 008）⇒ **仲在發生**（因為未部署）。

### 用戶決定（2026-09-24 21:00）
1. **唔動 git**（唔 commit、唔 push）⇒ 修復未部署，症狀會繼續。
2. **自己跑 SQL 止血**（清 `print-fb179f3f`）。



## 20:36 「取餐碼 001（MOP 43、外賣自取、餘額扣點）線上訂單見到，報表／交班明細冇」

**取證（全部唯讀、anon 直查）**：`tools/_probe-order001-20260924.cjs`、`_probe-storegroup-20260924.cjs`、
`_probe-pj001-20260924.cjs`、`_probe-find001-20260924.cjs`、`_probe-missing-20260924.cjs`、
`_probe-reportcount-20260924.cjs`（同名前綴 `.out.txt` 留住做證據）。

- 表嫂（`8291f843-…`）09-24 雲端 `pos_orders` **32 張**（31 settled＋1 sent_to_kitchen），
  **完全冇 001**。今日有嘅線上投影單只有 002／003／004／005／006／007（全部係「取餐碼」被排到
  堂食真枱 A03–A08 ⇒ 經「排位」入咗 POS DB）。
- 🔴 **全店今日只有一張「有出紙但雲端冇訂單」**：
  `pos_print_jobs` 10:25:37 出過 `order_no=001`、`order_id=ledger-f74b4a98-c28c-4afb-9727-b8c86d84338c`、
  `table_name=自取`、`once_key=kitchen:normal:0:3sigr5`、票＝`kitchen/normal`（**冇 receipt 票**）。
  反查 `id=eq.ledger-f74b4a98-…` / `online_order_id=eq.…` ⇒ **雲端兩邊都搵唔到**。
  ⚠️ 注意：`local_order_no=eq.001` 只會搵到 **09-23** 嗰張（`ledger-cf100f81…`, MOP 48, A09）——唔好撞板。
- **報表「28 張」復算對得上「純雲端 POS 單」**：可計（settled/paid）＋`updated_at` ∈ 澳門 09-24
  共 31 張；剔除截圖當時（約 20:04–20:10）之後才結帳嘅 3 張 ⇒ **恰好 28**，而且逐行同截圖吻合
  （08/07/002/訂單01 …）。⇒ **Ledger 純線上單補入 = 0 張**（若生效會係 29 張）。
- **判讀**：① 001 只出過廚房紙、冇收據 ⇒ POS 端**從未採納／從未結帳**這張單；
  ② 報表（純雲端、永不 merge 本機）＋交班明細（以雲端為準）**同時**見唔到它；
  ③ 設計上應由「Ledger 純線上已付款單」補入（`sumPaidLedgerOrders` / `loadOnlineByHour`，
  條件：非 cancel ＋ `paymentStatus === "paid"` ＋ `createdAt ?? updatedAt` ∈ 澳門今日區間），
  但今日**冇生效** ⇒ 43 元營業額靜默少計。
- **待確認（未做）**：報表「尖峰時段」卡右上 tag（`POS+Ledger`＝抓成功／`僅 POS`＝抓失敗）
  或 `onlineFetchInfo`（fetched/counted/outOfRange/unpaid）／KPI「線下 X · 線上 Y」，
  以分辨係「RPC 抓唔到」定「該單唔符條件」。
- ⚠️ **Ledger（`zymdemjflsckicwcinxl`）anon 讀唔到**：`orders` 表 42501、RPC
  `list_merchant_orders` 42501（permission denied for function）。bundle 內 Ledger 公鑰
  ＝`sb_publishable_WpIr6L4VRjOaKEP8RgFExQ_MQZ6g8Dc`（**POS 嘅係那條 `eyJ…` JWT**）。
  ⇒ 要讀 Ledger 只可經 `/api/admin/ledger/orders`（需 admin session token）或商戶 console。

## 21:15 001 漏帳：P0／P1／P2 三層修復（已實作，未部署）

**🔴 P2 真因（邏輯排他，唔需要 Ledger 憑證）**：`RPC list_merchant_orders` 係按 **`updated_at` DESC**
排序，但報表／交班用 **`createdAt ?? updatedAt`** 判斷「係唔係已過區間起點」⇒ **過濾鍵 ≠ 排序鍵**。
一張「昨日落單、今日完成／今日預約取餐」嘅單（001 正是預約單）會令 `break outer` **提早中止翻頁**，
之後所有線上單一齊靜默消失。⇒ 排他理由：三個條件（非 cancel／paid／時間）只有「時間」可能唔符，
而 UI 顯示 13:11（= `orderEventISO` = updatedAt）證明 updatedAt 喺今日 ⇒ 一定係 `createdAt` 出事。

**改動（4 個檔）**
1. `src/components/restaurant-daily-report.tsx`：兩處（admin／非 admin 翻頁迴圈）改用
   `orderEventInstant()` / `orderEventISO()`；`byHour` 一齊統一（唔可以各自解讀）。
2. `src/lib/ledger/paid-orders.ts`（交班）：同上。
3. `src/app/api/admin/ledger/orders/route.ts`：server 端由 `created_at` 改 `updated_at`
   （連 `.order()` 一齊改），否則前端已改口徑而 server 仍按 created_at 篩 ⇒ admin 照樣漏。
4. 新增守衛 `src/lib/pos/ledger-online-range.test.ts`：**重現事故**（舊寫法得 `["007"]`、
   新寫法得全部）＋源碼掃描三個檔（禁 `createdAt ?? updatedAt` / SQL `created_at` 篩區間）。

**P0（可視化）**：新增零依賴 `src/lib/pos/online-reconcile.ts`（`reconcileOnlineOrders()` /
`onlineFetchWarning()` / `unadoptedNotice()`）＋ `src/components/online-reconcile-banner.tsx`。
報表（訂單明細上方）、交班（線上實收卡下方）各接一次。**冇事 render `null` ⇒ 佈局零改動。**
補上舊缺口：`skipped`（未登入 Ledger）以前**完全靜默**，`error` 只喺尖峰卡細字。

**P1（補建）**：`ledger-pos-bridge.ts` 加 `adoptCompletedLedgerOrderToLocal()`；
`upsertLedgerLocalOrder()` 新增 options `forceSettled` / `skipPrint` / `updatedAtOverride`（全部可選，向後兼容）。
安全閘＝只補 `paymentStatus === "paid"` ＋ `normalizeLedgerStatus() === "completed"`。
`id = ledger-<uuid>` upsert ⇒ 唔會重複；`skipPrint` ⇒ 唔會再出紙；`updatedAtOverride` 保留 Ledger 事件時間
（只接受過去值，防平台單未來時間）⇒ 補昨日單唔會計入今日。報表按鈕用**動態 import**（唔加初始 bundle）。
🔑 補建單自帶 `onlineOrderId` ⇒ 報表 `posOnlineIds` 會去重 ⇒ **唔會雙計**。

**驗證**：`tsc --noEmit` 0 error；`eslint` 改動檔 0 error（4 個 warning 全部既有）；
`node --test "src/**/*.test.ts"` ⇒ **1474 passed / 0 fail**。
