
## 時長統計重構為柱狀圖（2026-09-05）

- 用戶截圖回報「時長統計（堂食/外賣）」排版壞咗：原本兩欄並排 `StepRow`（flex justify-between + 固定 w-12 數字欄）喺窄 container 擠壓，文字重疊（「下單ed↔ 送廚」等）。
- 修復方案（`restaurant-daily-report.tsx`）：
  - 刪除 `StepRow` 組件，新增 `DurationBarChart`（水平柱狀圖：label 左、bar 中、avg 數值右；樣本 0 顯示灰 bar + —；hover title 有 med/P95/樣本）同 `DurationStatsTable`（單一統一表格：流程/類型/avg/med/P95/樣本，取代並排數字行）。
  - 兩個 chart 共用同一 `maxAvg` scale（堂食＋外賣全部步驟取最大），方便跨類別比較。
  - 顏色區分：堂食 indigo-400/500/600（分段淺、整體深）、快餐/外賣 amber-400/500/600；`isTotal` 步驟深色不透明。
  - 步驟來源：`dineInServing.{orderToKitchen,kitchenToSettle,total}`（3 bar）、`quickServing.{orderToKitchen,kitchenToServed,servedToSettled,total}`（4 bar）。
- 驗證：`npx tsc --noEmit` exit 0；ESLint 0 errors 0 warnings。

## 時長統計二次修訂：改垂直柱狀圖、只顯示 avg（2026-09-05 下午）

- 用戶回饋：水平 bar 唔夠似「柱狀圖」，而且只需要每個環節嘅平均時間（唔需要 med/P95）。
- 修訂（`restaurant-daily-report.tsx`）：
  - `DurationBarStep` 簡化為 `{ label, avgMin, count, colorClass }`（刪 medianMin/p95Min/isTotal）。
  - `DurationBarChart` 改為**垂直柱狀圖**：柱區固定 h-44，柱高 = `(avg/maxAvg)*85`%（上限 85% 預留數值標籤），柱頂顯示 avg 數值，X 軸下方 label（truncate + title）。
  - 單一 chart 7 根柱：堂食 3 根（indigo-500，整體 indigo-700）+ 外賣 4 根（amber-500，整體 amber-700），共用 maxAvg scale；卡內加圖例（深色=整體）。
  - **刪除 `DurationStatsTable`**（med/P95 明細表一併移除）；StepStats type 保留（其他地方用）。
- 注意：上一輪改咗 code 但用戶截圖仍係舊版 → dev server 舊 build / 瀏覽器快取，提醒硬刷新（Ctrl+Shift+R）。
- 驗證：tsc exit 0、ESLint 0/0。

## 隱藏純 website/PWA 嘅「桌面 Companion 代理」卡（2026-09-05）

- 需求：設置 → 打印機入面嘅「桌面 Companion 代理」卡，純 website / PWA 唔應該顯示（連唔到本機 loopback 127.0.0.1:9311，顯示紅燈只會誤導）。
- 實作（沿用 codebase 已有環境判斷慣例，**唔好**自己寫 userAgent sniff）：
  - `src/lib/print-bridge/companion.ts` 新增 `shouldShowCompanionUi()` = `shouldAutoDiscoverCompanion() || hasCompanionUrlParam()`
    = 原生殼（PC Electron / Android APK）OR localhost dev OR 帶 `?companion=` 參數。
    純 website / Vercel HTTPS / PWA standalone 一律 false。
  - 同 `shouldKeepCompanionAlive()`（輪詢 gate，唔包 localhost）嘅分別：UI 顯示要包埋 localhost，否則本機 dev 測唔到張卡。
  - `src/components/printer-companion-panel.tsx` 嘅 `CompanionStatusCard()` 加 mount-gated state
    `const [envVisible, setEnvVisible] = useState<boolean|null>(null)` + `useEffect` 設值，`if (!envVisible) return null;`
    —— 初始 null 令 SSR HTML 同 client 首次 render 一致，避免 hydration mismatch。
  - 一個 component gate 覆蓋晒兩個 call site：`device-settings.tsx:826`（打印機 tab）同 `salon/settings.tsx:1056`（PrinterCompanionPanel 內含同一張卡）。
- 順帶發現：`src/lib/ledger/reports.ts` 有前一次留下嘅 debug `console.table(label, record)` → TS2769 拖垮 `next build`（已由 user/linter 改做 console.log）。
- 驗證：tsc exit 0；ESLint 剩 2 個 pre-existing warning（reports.ts 多餘嘅 eslint-disable、companion.ts:660 `_networkOnline` unused），非本次改動引入。

## 報表 Ledger 欄位擴充：topup/deduct paid+gift 拆分 + 扣點 KPI（2026-09-05）

- 用戶對賬 Ledger 後台 8 格顯示（實際充值/贈送入帳/扣點/筆數/訂單數/訂單已收款/訂單餘額扣點/訂單到店付款），發現報表只用到 `topupMop` 同 `orderBalancePaidMop`，完全冇顯示 `deductMop` 同各 paid/gift 拆分。
- 已知口徑映射（落實）：
  - `topup_paid_avos` → 「實際充值」、`topup_gift_avos` → 「贈送入帳」、`topup_avos` = paid + gift
  - `deduct_paid_avos` / `deduct_gift_avos` → 扣點 paid/gift 子集、`deduct_avos` = paid + gift
  - 截圖 491 + 694 = 1185 證實 `order_paid_avos = order_balance_paid_avos + order_in_store_paid_avos`
- 改動（`src/lib/ledger/reports.ts`）：
  - `LedgerReportSummary` 加 `topupPaidMop`/`topupGiftMop`/`deductMop`/`deductPaidMop`/`deductGiftMop` + `rawAvos: Record<string, number>` 暴露原始 avos payload
  - `normalizeAvosPayload()` 過濾出**數值**欄位（RPC 返回可能帶 `merchant_id` 等非數字），減少噪音
  - **一次性 dump**：module-level `__reportSummaryDebugDumped` guard，每次 page load 只 console.log 一次 RPC payload，用嚟搵「筆數」(110) 對應 `topup_count` / `deduct_count` / `txn_count` 等。
- 改動（`restaurant-daily-report.tsx`）：
  - KPI 帶 6 格 → 7 格（`xl:grid-cols-7`），新增「會員扣點」KPI（顯示 `paid/gift` 拆分 subtitle），同步更新 skeleton 由 6 → 7 cells。
  - 「會員充值」KPI 加 subtitle：`實際 MOP · 贈送 MOP`。
  - 「會員充值 & 會員數」Card 加多粒 `會員扣點` Pill + 總金額下方細字顯示 `實際充值 / 贈送入帳` 拆分。
- 注意：KPI 帶原本 6 格硬性已寫喺 working-memory「報表模塊關鍵約定」；今次升 7 格要 update 該記憶段。
- 注意：`console.table(label, record)` TS2769（之前就已撞過，user 改做 console.log），今次 fallback 用 `console.log(label, rawAvos)` 唔再撞 type。
- 驗證：`tsc --noEmit` exit 0（唯一 TS2322 喺 pos-app.tsx:2277 嘅 pre-existing snapshot order construct，唔關今次事）。
- 待跟：等用戶 dump 一次 console payload 出嚟睇「筆數」RPC 字段名。

## 報表「會員充值 & 會員數」卡片 redesign + 中文化 + 說明移除（2026-09-05 下午）

- 需求四點：
  1. 落實前三項建議（加扣點 KPI、dump RPC payload、拆分 paid/gift 成獨立區塊）。
  2. 會員扣點內英文標籤改中文：`paid` → `已付`、`gift` → `贈送`。
  3. 重新設計「會員充值 & 會員數」卡片，減少空白、更緊湊充實。
  4. 完整移除菜品銷售排行同時長統計（堂食/外賣）嘅兩段長說明文字。
- 改動（`src/lib/ledger/reports.ts`）：
  - `LedgerReportSummary` 加可選 count 欄位：`topupCount` / `deductCount` / `newMemberCount`，由 `rawAvos` probe 多個可能 RPC 字段名（`topup_count`/`topup_txn_count`/...、`deduct_count`/`deduct_txn_count`/...、`new_member_count`/`member_new_count`/...），缺字段時 `undefined`。
- 改動（`restaurant-daily-report.tsx`）：
  - KPI 帶「會員扣點」subtitle 已改為 `已付 MOP · 贈送 MOP`。
  - 「會員充值 & 會員數」Card 由「單一大字 + pills」改為 **2×2 mini-block grid**：
    - 充值總額（indigo）+ 實際充值 / 贈送入帳 子項 + 充值筆數（如有）。
    - 會員總數（amber）+ 新增會員（如有）。
    - 會員扣點 + 已付扣點 / 贈送扣點 子項 + 扣點筆數（如有）。
    - 訂單餘額扣減 + 線上渠道佔比。
  - mini-block 視覺風格與其他卡片一致：rounded-xl bg-slate-50 p-3、text-slate-500 標籤、大字 bold。
  - 移除菜品銷售排行 Card 內嘅快照聚合長說明（保留 tag）。
  - 移除時長統計 Card 內嘅柱狀圖長說明。
- 驗證：`tsc --noEmit` exit 0；檔案內無殘留 `paid`/`gift` 英文標籤於會員區塊。

## 點餐菜品卡片 redesign：統一尺寸、移除 kitchen、售罄改位（2026-09-05 傍晚）

- 需求：用戶截圖回報點餐頁菜品卡片尺寸不一、顯示 `kitchen` 字眼、售罄時出現「不可加」而無法一眼辨識。
- 改動（`src/components/pos-app.tsx` 菜品網格卡片）：
  - 卡片高度固定 `h-36`（144px），改用 `flex flex-col justify-between`，所有卡片尺寸一致。
  - 菜品名由 `line-clamp-2` 改為 `truncate`（單行，過長顯示 `…`，hover title 顯示全名）。
  - 移除 `printerGroup` / `menuPrinterOverrides` 顯示嘅 `kitchen` 字眼。
  - 售罄時原本頂部嘅 `售罄` badge 移除，改為底部右側狀態標籤顯示 `售罄`（取代「不可加」），並用 `bg-amber-50 text-amber-700` 突出顯示。
  - 「只剩 X 份」badge 保留，但不再影響卡片高度（佔位由 `hasRemainingBadge` 條件 render）。
- 驗證：`tsc --noEmit` exit 0。

## POS 點餐介面 · 打印操作區塊搬移（2026-09-05 傍晚）

- 用戶要求：將「自動打印」「落單／結帳自動出單」「打印廚房單」「打印收據」四個控制項從「訂單明細」section 內搬到「收銀與支付」section 最底部（喺「最近訂單」註解之上、`</>` 之前），方便收銀員喺結帳區域直接操作打印。
- 做法（`src/components/pos-app.tsx`）：直接搬移整段 JSX（包含 `AutoAcceptPill` 開關 + 兩個 `printKitchenTicketNow` / `printReceiptNow` 掣 + 關閉提示框），兩處都保留原有 `onClick` / `disabled` / `aria-busy` 行為；原位留一行 JSDoc 註解標示已搬走，避免日後維護時找唔到。
- 留意：「收銀與支付」section 屬於右側 `<section className="flex h-full flex-col overflow-hidden ...">`，`flex-1 overflow-auto` 滾動區域內，所以打印區塊會跟住主體一起滾動，唔會浮喺底部固定 —— 同原本喺「訂單明細」section 內嘅滾動行為一致。
- 驗證：`tsc --noEmit` exit 0。
