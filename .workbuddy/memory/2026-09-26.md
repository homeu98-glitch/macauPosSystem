# 2026-09-26 工作日誌

## 庫存 UI「對齊確認稿」（J：設置頁要彈窗，其他都改，按照你的次序）

前一日 J 問「為什麼 UI 跟你畫的差那麼遠?」。覆核結論：主頁同收據 modal **其實忠實**，
落差集中喺「庫存・設置」同幾樣畫咗未做嘅細節。J 拍板：**設置頁保持彈窗**，其餘 4 項照做。

### 覆核方法（可重用）
`tools/mockup-canvas-fit.cjs docs/mockups/inventory-ux-5items-2026-09-25.html --shots --out …`
把確認稿逐個畫面重新 render 成 PNG（要 `NODE_PATH=<managed node workspace>/node_modules`），
再砌 `docs/mockups/inventory-ui-diff-2026-09-25.html`（**6 張圖 inline base64**）左確認稿右實作對照。

### 改咗乜

**macauPosSystem**
- 新 `src/lib/inventory-order.ts`（**零 import 純函式**：`reorderByStored`／`moveWithin`／`orderKeys`／`sanitizeKeyList`／`usageCount`）
- 新 `src/lib/inventory-order.test.ts`（13 條）
- 新 `src/app/api/inventory/master-usage/route.ts`（供應商「用過 N 次」，lazy、只拉 `merchant_id`、上限 1500）
- 重寫 `src/components/inventory/inventory-settings-panel.tsx`（4 chips 多選＋兩 panel 並排＋庫存品 panel 掛 InventoryTable＋支付方式唯讀 panel＋拖 ⠿ 排序＋用過 N 次；右上「完成」唔係「保存」）
- `src/components/inventory/inventory-table.tsx`（加 `embedded` ＋ `onMutated`）
- `src/components/inventory/inventory-view.tsx`（收據日期 chips／品項數量 stepper／付款方式 chips 分組＋展開器／底部 footnote 條／`patchLocalSettings` 統一入口／`productsVersion` key）
- `types.ts`／`mock-data.ts`／`storage.ts`（`invSupplierOrder`／`invCategoryOrder` ＋ **白名單**）
- 守衛測試 38 → **61 條**
- 新文件 `docs/152-inventory-ui-mockup-parity.md`
- 截圖 `docs/mockups/inventory-impl-2-2026-09-26/`（8 張）

### 實測數據（真瀏覽器，1440 寬）
- 設置 4 chips `aria-pressed` 正確、每個高 **44px**；兩個 panel 並排 `x=228/w=484`、`x=728/w=484`
- 供應商行 `58px`；⠿ 把手 **44×44、`touch-action:none`**；「用過 N 次」依 `invSupplierOrder` 排序正確
- **拖拽實測成功**：把手 0 拖到行 3 ⇒ localStorage `invSupplierOrder` 即時變
  `['黃記凍肉','澳門水產','聯興蔬菜','永發凍肉','大昌行','南光石油氣']`
- 收據日期 chips **48px**（今天／昨天／選日期…）；數量 stepper 按鈕 **44×48**、輸入框 62×48
- 主頁付款方式：收埋時 `全部（1）現金（1）＋3 個未用過`；展開後多 `月結（0）支票（0）銀行轉賬（0）`
- 底部 footnote 條 70px 高、1320px 闊，含「前往設置 →」
- 收據 modal 品類 chips 跟 `invCategoryOrder`（餐具／清潔用品／食材／飲料）

### 驗證
POS `tsc` 0 error｜`node --test` **1657 pass / 0 fail**（庫存 61＋排序 13）｜eslint 改動檔 0 error｜
`next build` ✓（`/api/inventory/master-usage` 以 `ƒ Dynamic` 出現）

### 🔴 過程中中招（已寫入 skill）
`page.evaluateOnNewDocument(seedFn)` 把函式 **source** 拎去 browser 執行 ⇒ **closure 變數全部唔存在**
⇒ `ReferenceError: MID is not defined`，但 page 冇 error banner、所有 query 都係空，
睇落好似「UI 改壞咗」。**只有 `page.on("pageerror")` 睇得到**。
✅ 正解：Node 端砌好 object 當**第二個參數**傳入。

### 仍然未做
- 兩個 repo 都未 commit／未 push。
- 主頁仍保留「庫存表」section（確認稿主頁冇畫）⇒ 同「設置 → 庫存品」兩個入口，
  已用 `productsVersion` 打通；如果要跟確認稿移除，要 J 拍板。
- 商家端「結帳顯示哪幾款」未收口到支付方式主檔。
- `receipt_items.user_id` 仍唔喺任何 SQL 檔（靠人手 ALTER）。
- expenseRecorder `npm i next@16.3.0` 未做（等 J 決定改 node_modules）。

### 補記：交付用對照頁（改動後）
`docs/mockups/inventory-ui-diff-2026-09-25.html` 貼嘅係**改動前**嘅實作截圖（落差仍在），
唔可以當成收口後嘅交付物。已另出 `docs/mockups/inventory-ui-diff-after-2026-09-26.html`
（10 張圖 inline base64，用 `inventory-mockup-ref-2026-09-25/` ＋ `inventory-impl-2-2026-09-26/` 砌）。
🔴 教訓：**報告「已修好」之前，先確認手上嘅對照圖係邊個版本嘅快照** ——
否則交出去等於自打嘴巴。
