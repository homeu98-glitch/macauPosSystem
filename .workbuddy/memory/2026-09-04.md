- 2026-09-04 11:48
  - 依據用戶提出的 5 項需求，完成專案探索並產出《實作計劃：列印記錄、報表與點餐提示優化》（implementation-plan-2026-09-04.md），供用戶審閱後再決定實作方向。
  - 重點釐清的現況：列印中心使用 localStorage 儲存 PrintJob、狀態篩選以 `status` 欄位為準；點餐介面列印失敗提示為左下角固定紅色按鈕；報表「當日人流」目前為 localStorage 手動輸入；訂單類型以 `tableId === "counter"` 區分堂食與快餐/外賣；出餐時間以 `sentToKitchenAt` 與 `servedAt` 計算。
- 2026-09-04 12:00
  - 完成 5 項需求實作（用戶確認「可以開工了」）：
    1. `src/lib/print-jobs.ts` 新增 `normalizePrintJobStatus`：將非標準 status 強制轉成 `"failed"`，確保 UI badge 與篩選條件永遠對齊。
    2. `src/components/print-center.tsx` 新增時間篩選（今天/昨天/7天/30天/全部，預設今天），與狀態篩選 AND 組合；徽章改為顯式 `=== "failed"` 比對，避免 catch-all 邏輯導致狀態異常字串顯示「失敗」但篩不到。
    3. `src/components/pos-app.tsx` 列印失敗 Toast 容器 `max-w-xs → max-w-[10rem]`，按鈕 `text-sm → text-[11px]`，並加入 3 秒自動關閉（依賴 `latestFailedJobId`，新失敗會再跳出）。
    4. `src/lib/restaurant-footfall.ts` 新增 `computeFootfallFromOrders(orders, range)`，依 `tableId === "counter"` 區分，堂食加總 `partySize`、快餐/外賣算單數；`src/components/restaurant-daily-report.tsx` 移除手動輸入框與 localStorage 讀寫，加入「堂食 N 人 / 快餐 M 單」子卡。
    5. `src/components/restaurant-daily-report.tsx` 新增 `StepStats/DineInServingBreakdown/QuickServingBreakdown` 型別與 `quickStepsForOrder/dineInStepsForOrder/summarizeSteps` 函式；堂食三段（下單→送廚、送廚→結帳、總計）、快餐/外賣四段（下單→送廚、送廚→出餐、出餐→完成、總計），每段皆列 avg/median/P95/樣本數/估算標記。
  - 驗證：`npx tsc --noEmit` 通過；ESLint 對所有變更檔案通過；`npx next build` 通過（先 Remove-Item .next 繞過沙箱 safe-delete 守護）。
- 2026-09-04 14:30
  - 收到 Ledger 團隊《線下報表對接建議》（pos-offline-report-proposal(1).md），否決 83/94「Ledger 直連 POS DB」、改為「POS 主動推線下日快照 HMAC upsert 進會員通」。
  - 產出回覆 `ledger-offline-report-reply-2026-09-04.md`：§6 九條全部「同意」方向，答 Q1–Q4。
  - 關鍵代碼事實（回覆依據）：store_id = Ledger merchants.id（UUID），見 `login/route.ts` 的 `storeIds:[staffRow.merchant_id]`；`macau-store-a` 是 mock 值、已列入 `store-id-guard.ts` 黑名單；退款單口徑 = 照加滿額 total 不扣退款（`pos_orders` 無 refunded_amount）；現有 `LEDGER_WEBHOOK_SECRET` 是 Ledger→POS 入站方向，新方案需另設 POS→Ledger 出站 HMAC；`party_size` 已上雲（docs/89）、人流已改自動計算（本日稍早）。
  - 待用戶拍板：Q2（第一版是否只 KPI）、Q3（HMAC 簽發方）、以及夜間 cron 基礎設施選型（Vercel Cron / Supabase scheduled / 獨立 worker）。
- 2026-09-04 17:00
  - 修復報表頁（`restaurant-daily-report.tsx`）換機/清 cache 後顯示空白嘅 root cause：
    - 根因：報表原本只讀 localStorage 訂單（`loadOrders()`），冇雲端補載。換機/清 cache/首次開啟時 localStorage 空 → 營業額、毛利（估）、菜品銷售排行、線上佔比全部空白；只有「會員充值」正常，因為佢直接讀 Ledger 雲端 `getMerchantReportSummary`，唔經 localStorage。
    - 修法：`restaurant-daily-report.tsx` 新增 mount 時雲端訂單補載 effect，用 `/api/pos/state` 分頁拉全量訂單（PAGE=1000 × MAX_PAGES=10 = 上限 10000 單，每頁 `ordersOnly=1`），用 `mergeOrderLists(loadOrders(), fetched)` 合併，只過濾本機 tombstone（`loadDeletedOrderIds`），唔套 `filterResurrectedOrders`（報表需要已結帳/退款單），亦唔 `saveOrders` 寫返 localStorage（避免污染收銀端 docs/52 工作清單語義）。
    - `src/app/api/pos/state/route.ts` 新增 `limit`（預設 200、上限 5000）、`offset`（0-based 分頁）、`ordersOnly=1`（跳過 queue/printJobs/deviceConfig，只拉訂單）三個 query param；並抽 `mapOrder()` helper 去重（ordersOnly 分支同主分支共用同一映射）。
    - 驗證：`npx tsc --noEmit` 通過（exit 0，`LayoutProps` 錯誤係早前刪 `.next` 令 generated types 唔見，build 重新生成後消除）；ESLint 對變更檔案通過（0 error、1 既有 warning）；`npx next build` 通過。
- 2026-09-04 18:00
  - 用戶回報：(a) Network 睇唔到 `/api/pos/state` call；(b) `merchants` 表不存在；(c) 有 service role key；(d) 報表 commit 後仍看不到內容。
  - 回應與處理：
    - Network 截圖全是 `prints?` RSC → 用戶當時在 `/print-center` 頁面，不在 `/reports`；切到 `/reports` 才會觸發報表補載。
    - POS DB 無 `merchants` 表；merchantId 來自 `staff_accounts.merchant_id`；建議改用 `select distinct store_id, count(*) from pos_orders group by store_id;` 或直接以 `store_id = '<UUID>'` 查詢。
    - 有 service role key 嘅情況下，RLS 14 天限制唔會影響 `/api/pos/state`。
    - 為協助現場排查，喺 `restaurant-daily-report.tsx` 加入「診斷面板」DevTools debug tool：顯示 merchantId、最後 HTTP 狀態、雲端拉回單數、本機單數、最終顯示單數、耗時、最後 URL、錯誤訊息，並提供「重新拉取訂單」按鈕。
- 2026-09-04 18:05
  - 用戶確認已部署，診斷面板顯示：merchantId 匹配、HTTP 200、雲端拉回 16 單、最終顯示 16 單，但營業額/訂單數/毛利全部仍為 0。
  - 判斷：問題從「冇拉雲端訂單」轉為「拉咗但 aggregate 後全部過濾掉」。
  - 升級診斷面板：新增「狀態分佈」、「可計入營業單數」、「匹配今天範圍」、前 5 筆訂單樣本，方便一睇就知道係 (a) 狀態唔係 settled/refunded/partially_refunded，定 (b) 日期唔喺今天範圍。
  - 驗證：`npx tsc --noEmit` 通過；ESLint 0 errors（1 個既有 warning）。
- 2026-09-04 18:25
  - 用戶貼出診斷數據：merchantId=`8291f843-...`、16 單（settled 11、sent_to_kitchen 3、cancelled 2）、全部 `createdAt` 喺澳門時間 9/1–9/3，今天 9/4 澳門時間範圍內**真係 0 單**。
  - 用戶確認三點：(Q1) 線上今天有訂單，線下冇訂單；(Q2) 今日口徑用 A（嚴格澳門時間 00:00-23:59）；(Q3) 大數需要包括 Ledger 線上單，並在大數下方加小字說明「線下/線上」拆解。
  - 設計：新增 `onlineOfflineSplit` useMemo，`線下 = POS DB 範圍內已結帳且 !onlineOrderId`，`線上 = Ledger RPC 總值（orderCount / orderPaidMop） − 線下`，涵蓋 POS 接單嘅線上單 + 其他渠道（kiosk / 外賣 / 小程序）。KPI 卡片改用 Ledger 總值作為大數，subtitle 顯示「線下 X · 線上 Y」；Ledger 連唔上時 fallback POS DB。
  - 改動：`restaurant-daily-report.tsx` 嘅 `Kpi` 組件加 `subtitle` prop；KPI 卡片（營業額 / 訂單數 / 客單價）改用 `onlineOfflineSplit.total*`；清理 unused `avgTicket` / `ticketMop`。
  - 驗證：`npx tsc --noEmit` exit 0；ESLint 0 errors（1 個 useMemo 嘅 `orders` redundant dep 警告，係合理誤報）。
- 2026-09-04 19:00+
  - 用戶要求「全面 review 報表模塊並修三個 bug」：(1) 時間過濾器錯誤，特別係昨日顯示 60000003 異常；(2) 多個區塊（菜品銷售排行/尖峰時段/時長統計/營運指標·同環比/當日人流/沽清菜品）完全無數據；(3) 切換店鋪後舊店數據殘留。
  - 全面修復：commit `4f14c8e`：
    - `/api/pos/state/route.ts` 加 `start` / `end` 查詢參數（用 `or(created_at/updated_at in [start,end])` 喺 SQL layer 過濾），`PosOrderDbRow` 加 `store_id?` 映射成 `storeId`。
    - `restaurant-daily-report.tsx` 嘅 backfillOrders：
      1. 從當前 `range` 計出 Macau 邊界，代入 `start` / `end` 查詢參數（SQL 過濾 + 前端 double-check `belongsToStore`）
      2. 加 `range` 入 useEffect deps — 揀 filter 即時重新拉
      3. PAGE 由 1000 → 2000、MAX_PAGES 10（上限 20000 單）
    - `orderMatchesReportRange`（`src/lib/ledger/report-period.ts`）統一用 `macau{...}Range()` 算 ISO 邊界（昨日/7d/30d），淘汰 `now.getTime() - 86400000` 嘅 UTC-naive 寫法，消除凌晨跨午夜嘅 off-by-one。
    - `useReportMerchantId()` hook 訂閱 `pos-auth-changed`，merchantId 即時更新；報表自帶 listener 觸發 `setOrders([]) + setBackfillSeq++`，雙重保險防 SPA 殘留。
    - 擴展 DevTools 診斷面板：新增 `currentRange` / `matchedCurrentRange / countedStatus` / `matchedYesterday` / `rangeStart` / `rangeEnd`，樣本樣本列印 `updatedAt` + `storeId`，方便快速辨識「揀錯時間 / 揀錯店」定「真無單」。
    - 移除 unused imports（`getLedgerMerchantId` / `fetchLedgerOrderMenu`），低庫存 endpoint 加 null 防護。
  - 驗證：`npx tsc --noEmit` exit 0；ESLint 0 errors 0 warnings（已修 useEffect exhaustive-deps、soldOut useMemo 嘅 unused `orders` dep）。
- 2026-09-04 20:00+
  - 用戶提出 4 項新需求，全部完成（commit 待用戶確認後提交）：
    1. **簡化菜品銷售排行 → 按大類聚合**：`restaurant-daily-report.tsx` 嘅 `DishRow` 由 `menuItemId`-based 改為 `categoryId`-based；新增 `MenuMeta` interface + `buildMenuMeta()` helper，從 `loadBootstrapCache()` 建立 `menuItemId → {categoryId, name}` 同 `categoryId → categoryName` Map。`aggregate()` 內每個 item 用 `meta.itemMap.get(it.menuItemId)` 解析大類；對照唔到（外店菜品／legacy 資料）fallback 用菜品 ID 當 key，UI 用「未匹配當前菜單」badge 標示。
    2. **修正訂單狀態篩選**：新增 `isSaleCountable(o)` 函式 — 線下 POS 只計 `settled`；帶 `onlineOrderId` 的單 `settled` 或 `paid` 都計；`refunded` / `partially_refunded` 一律唔計。`aggregate` / `onlineOfflineSplit` / `footfallBreakdown` 全部改用此 helper。
    3. **尖峰時段納入 Ledger 線上單**：新增 `useEffect loadOnlineByHour([merchantId, range])` 用 `listMerchantOrders` cursor-based pagination（PAGE=500、MAX_PAGES=8）撈齊範圍內線上單，篩掉 cancelled + unpaid，依「下單時間」createdAt 嘅 `macauHour()` 入 byHour。新增 `combinedByHour` useMemo 疊加 POS `agg.byHour` + `onlineByHour`，尖峰時段 Card 嘅 tag 顯示「POS+Ledger / 僅 POS / POS」來源；柱狀圖藍色疊加層表達線上單比例，tooltip 顯示「POS X + Ledger 線上 Y = 共 Z」。`onlineFetchInfo` state 收集 fetched/counted/outOfRange/cancelled/unpaid 計數 + 錯誤訊息。
    4. **營運指標方案 B**：保留舊嘅 POS 7 日均 `rev7dAvg` / `onlineShare7d` / `topup7dAvg`，並新增 `ledgerRev7dAvg = ledger.d7.orderPaidMop / 7` + `ledgerCount7dAvg = ledger.d7.orderCount / 7`，再加 `POS vs Ledger 差距` row（正數 = POS 漏計線上單）。
    5. **加強診斷面板排查 60000003**：新增 `scanStorageOrders()` helper 掃 localStorage 內 `macau-pos/stores/&#123;storeId&#125;/orders` 各分店單數 + `macau-pos/orders` legacy unscoped 殘留；`debugInfo` 加 `storeIdBreakdown` / `foreignStoreCount` / `matchedCategoryCount` / `unmatchedCategoryCount` / `storageOrdersByStore` / `legacyOrdersCount`。Category 命中用 `Set` 統計 distinct categoryId（避免引用下方 useMemo 嘅 `agg` 觸發 React hooks 順序錯誤）。
  - 留意：JSDoc 註解出現 ``macau-pos/stores/*/orders`` 會被 TypeScript parser 視為 comment 結尾 `*/`，導致下游 25+ 個 syntax errors；改用 `macau-pos/stores/&#123;storeId&#125;/orders`（HTML entity 形式）繞過。
  - 驗證：`npx tsc --noEmit` exit 0；ESLint 對 `restaurant-daily-report.tsx` 0 errors 1 warning（pre-existing `menuMeta` useMemo 嘅 redundant `merchantId` dep，由早前 commit 遺留）。
- 2026-09-04 21:30
  - 用戶貼出報表截圖：菜品銷售排行全部顯示「未匹配當前菜單」，診斷顯示 `matchedCategoryCount: 0 / unmatchedCategoryCount: 7`，但 storeId 無外店污染。
  - 判斷：訂單 item 嘅 `menuItemId`（`ledger-xxx`）對應唔到 bootstrap cache 入面嘅 menuItems，可能原因包括 (a) 未匯入 Ledger 餐牌、(b) bootstrap 內菜品 ID 冇 `ledger-` prefix、(c) 菜名輕微不同。
  - 改動（`restaurant-daily-report.tsx`）：
    1. `MenuMeta` 新增 `nameMap` / `normalizedNameMap` / `boot` 摘要；新增 `normalizeMenuName()` 同 `resolveMenuMetaItem()` helper，配對順序：ID → 菜名 → 正規化菜名。
    2. `aggregate()` 同診斷統計改用 `resolveMenuMetaItem()`，並記錄每種配對方式嘅次數。
    3. `DishRow` 加 `unmatched` flag；菜品排行 UI 改以 `d.unmatched` 決定 badge，並喺存在未匹配時顯示提示條（建議到「菜單管理」同步 Ledger 餐牌）。
    4. 診斷面板新增 `bootstrap cache 菜單摘要`（storeName、menuItemCount、categoryCount、lastUpdatedAt、ID 樣本）同 `菜品配對方式統計`（id/name/normalized/unmatched）。
  - 驗證：`npx tsc --noEmit` exit 0；ESLint 0 errors 1 warning（同上 pre-existing warning）。
- 2026-09-04 21:55
  - 用戶更新餐牌後再貼診斷：bootstrap cache 有 80 菜品 / 12 分類（`ledger-` 前綴，更新於 21:44），但 `dishMatchBreakdown` 全部 `unmatched`（連菜名都對唔上）→ 訂單用嘅係**舊餐牌版本**嘅菜品（Ledger 產品被刪後重開，UUID 變咗），舊單菜品變孤兒。
  - 判明「昨天出現舊數據」原因：`orderMatchesReportRange` 用 **updatedAt（結帳時間）** 口徑 — 4 張 08-31/09-01 落嘅 kiosk/pos 喺 09-03 18:19-18:20 一次過結帳，所以計入「昨天」。唔係外店數據（storeIdBreakdown 全部正確、foreignStoreCount=0）。今日排行空係正確：3 張單全部 cancelled。
  - 診斷面板再加：`unmatchedItemNames`（未匹配菜品名 × 數量，連 menuItemId 前綴）+ `sampleMenuItemNames`（當前餐牌菜品名前 12 個），用戶可直接肉眼對照新舊餐牌差異。
  - Console 500（`api/online-order-settings` / `api/pos/sync`）：屬 `pos_online_order_settings` 讀取同訂單 sync 寫入失敗，同報表讀取（`/api/pos/state` 200）無關；要查 server 回應 body / server log（可能係 0019 migration 未跑或 RLS）。
  - 待用戶決定：報表口徑維持「結帳時間」（updatedAt，現行）定改「下單時間」（createdAt）。
  - 驗證：`npx tsc --noEmit` exit 0；ESLint 0 errors 1 warning（pre-existing）。
