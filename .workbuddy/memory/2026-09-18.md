# 2026-09-18 工作日誌

## 交班「關店總掣」實作（完成）

**需求（J）**：結數交班後要一次性關閉該門市全部線上／線下接單通路。

**根因**：`closeShift()` 以前完全唔碰任何接單開關。三條互不相干軌道（班次／
線下 `pos_store_status.is_open`／線上 Ledger `merchant_enabled`）→ 設計現狀非 bug。

**四個取捨點（J 拍板）**：① 勾選預設勾 ② 線下成功+線上失敗→保留線下已關（唔回滾）
③ 做「未開工不可落單」延伸到客人端 ④ 加 `/pos` 殘留通道警示點。

### 新增檔案
| 檔案 | 內容 |
|---|---|
| `src/lib/pos/close-gate.ts` | 純決策 + 文案（**零 import**），14 test |
| `src/lib/pos/close-gate-run.ts` | 執行層 `runCloseGate()`（import 兩個 hook 模組）|
| `src/lib/pos/residual-channel.ts` | 殘留通道偵測（**零 import**），10 test |

### 改動
- `use-store-status.ts` / `use-merchant-order-config.ts`：**抽模組層函式**
  `applyStoreOpen()` / `applyMerchantEnabled()`（export），hook 內 setter 轉呼叫。
- `shift-page.tsx`：step2 勾選（預設勾）+ step3 回顯 + header 殘留警示條 +
  `closeShift()` 接總掣（位置：`forceSyncBeforeClose()` 之後、**兩個 early return 之前**）。
- `merchant-open-pill.tsx`：新增 `residual` / `residualHint`（label 前 8px 琥珀點）。
- `store-open-pill.tsx` / `online-open-pill.tsx`：傳 `residual`。
- `/api/pos/sync`：新增 **2.56) 班次閘**（匿名 + `pos_shifts` 冇 open row → 拒，
  `reason: "shift-closed"`）+ 拒單分支。
- `use-kiosk-order.ts`：`shiftClosed` state + i18n `shiftClosedTitle/Body`。
- `use-scan-order.ts` 轉發；`order/page.tsx` / `scan-order-page.tsx` 全屏頁。
- `docs/131-store-open-toggle.md`：新增 §8 關店總掣規格 + §6 更新。

### 🔴 本輪學到的關鍵陷阱

1. **`npm test` ＝ `node --test`，唔認 `@/` 別名、唔行 bundler。**
   可測模組必須**零 import**（現有 `store-status.ts`、`date-range.ts` 都係咁）。
   所以 `runCloseGate()`（要 import hook 模組）一定要拆去另一個檔，
   否則 `close-gate.test.ts` 直接 `ERR_MODULE_NOT_FOUND: Cannot find package '@/lib'`。
   ⇒ **寫新 lib 模組時：純邏輯同有 import 嘅執行層一定要分檔。**

2. **`closeShift()` 有兩個 early return**（`forceSyncBeforeClose` 失敗、
   `!isPrintContentEnabled("shift")`）——後者**會完成交班**。
   任何「交班後必做」嘅動作必須排喺呢兩個 return 之前，否則靜默漏做。

3. **非 React 呼叫端唔可以 call hook** → 要 module-level export 函式（A 方案）。
   兩個 hook 都係 `useSyncExternalStore` module singleton，
   多掛 hook **唔會**多開 Realtime channel（同一份 state、同一條 channel）。

4. **`pos_store_status` 同 `pos_shifts` 嘅 default 方向要相反**：
   - `pos_store_status` 冇 row ＝ **營業中**（店主冇主動暫停過）
   - `pos_shifts` 冇 open row ＝ **真係未開工**（班次係事實記錄，冇記錄就係冇開過）
   兩者 fail-open 條件都係「**查詢失敗**」放行（唔可以一斷網全店停單）。

5. **`shop-closed` 同 `shift-closed` 唔可以撈埋**：前者係店主主動關門
   （叫客人等可能等到今日都唔開）；後者係未開工／已收工（叫客人「稍後再試」係有意義嘅）。

### 遺留（未做，已記錄喺 docs/131 §6.4）
- `/api/pos/store-status` 仍有 `DEFAULT_STORE_ID = "macau-store-a"` 假店 fallback
  （GET 53 行 / POST 117-119 行），同 `/api/online-order-settings` 已移除 fallback 嘅
  做法唔一致。改動會影響 URL 回溯兼容性 → 要另開一輪確認。

### 驗證
`tsc --noEmit` 0 error ／ `node --test "src/**/*.test.ts"` 832/832 pass ／
eslint 改動檔 0 error。
