# macauPos 記憶 · 細節伴檔（2026-09）

MEMORY.md 為控制體積已抽走以下敘述性細節 —— **規則本身仍在 MEMORY.md，呢度係背景／實作清單**。
需要深挖時讀本檔；已成正式文件嘅內容一律以 `docs/` 為準。

## A. per-store token 實作清單（0051／0052／0053 已寫未部署）

- `0051`：Realtime 授權相關政策前置。
- `0052_pos_store_scoped_dual_claim.sql`：5 條政策改成
  `coalesce(auth.jwt() -> 'app_metadata' ->> 'store_id', auth.jwt() ->> 'store_id')`
  ⇒ 「Admin API 寫 app_metadata」與「舊式 top-level store_id」兩種機制都通，唔使再改 policy。
- `0053`：`pos_soldout` 加 `authenticated using (true)`（**必須** —— 冇佢 client 一升級就靜默讀唔到 soldout；
  呢張表要長期保留 anon，因為客人掃碼／kiosk 匿名讀，冇身份）。
- `POST /api/pos/realtime-bind`：身份**只由終端憑證（pv1）嚟**，只綁 `is_anonymous` 用戶，把 `store_id`
  寫入 `app_metadata`（只有 service_role 寫得入）。
- `realtime-auth.ts`：`signInAnonymously()` → bind → **refreshSession()** → `setAuth`；失敗冷卻 5 分鐘且永不 throw。
- `realtime-auth-claims.ts`：純決策模組，19 條單測。
- `supabase-client.ts`：改 `persistSession+autoRefreshToken: true`
  （否則每次 reload 建新匿名用戶 ＋ token 過期後**靜默失效**）。
- 4 個 realtime hook：一律喺 `.channel(` **之前** `await ensureRealtimeAuth`（Realtime 身份係**每條連線**，
  六條 channel 共用）；其中兩個係同步 module-level 函式 ⇒ 加 async 包裝 ＋ **世代守衛**防 channel 洩漏。
- 守衛檔 `src/lib/pos/print-and-order-realtime-guard.test.ts` 已擴至 **25 條**，含
  「`ensureRealtimeAuth` 必須出現喺 `.channel(` 之前」。
- ✅ 實測：匿名註冊 200、`role:authenticated`／`is_anonymous:true`／`app_metadata:{}`。
- 報告：`docs/reviews/per-store-token-assessment-2026-09-23.md`。

## B. egress 基準數據（2026-09-23 覆核，關店時段）

| 指標 | 基準 | 09-23 |
|---|---|---|
| `legacy=1`／`queue=300`／`limit=300` | 1,050／928／— | **全部 0 次** |
| `pos/state` 單次 bytes | 843 KB → 404 KB | **34,989 B** |
| 中繼機 claim 中位間隔 | 76s | **180s** |
| stale 拒收 log 行 | 924 行／45 秒、77 張單 | **79 行** |
| 待機請求 | — | **2.17/分（3,121/日）** |

- **79% 係「輪詢三角」**：`PATCH print_agents` ＋ `claim RPC` ＋ `GET device_configs` ＋ `GET print_agents`(verify)
  —— 每部機 60 次/小時，實際只需 1–2 個 round trip ⇒ 有合併空間。
- ⚠️ `pos/state` 喺 `incr=1`／`orders=0` 之下**仍回 35 KB 全量 config**，觸發源係 realtime 重連
  （每 11.8 分鐘一次）⇒ resubscribe 路徑應帶 `?fields=` 投影。
- ⚠️ 一部中繼機曾靜默 ≥2 小時（07:43 起）。
- ⚠️ **未喺「營業中開 POS」窗口覆核過**（09-22 至今未補）。
- 詳見 `docs/reviews/recheck-2026-09-23-egress-and-relay.md`。

## C. Egress 收口已落實項（背景）

RPC `pos_orders_page`(0046)、`?skipQueue=1`、投影＋日期下限＋每日上限、`poll-gate.ts`／`write-gate.ts`、
queue 身分簽名、APK `nextPollMs`、`[egress]` log。結果：904MB/日 → 估 10~20MB/日（09-22 收口）。

## D. 2026-09-22 P0b 迴歸時間線（背景）

`legacyThrottled` 回空骨架 → 舊 bundle 唔識 `incremental` 欄位 → 照跑孤兒對賬 →
**本機所有未結帳單被移入隔離區**（列表清空、每 3 秒重演）。修法：節流骨架改回未結帳單。
教訓：**伺服器唔可以用「partial payload ＋ 一個新欄位」去保護舊 client**。

## F. 生產取證工具（指令）

```bash
# 一次出齊：請求去重／路徑組成／limit= 指紋／逐 agent_id 節奏／[egress] 原文／postgres error
node tools/log-recheck.cjs --both <vercel.csv> <supabase.csv>

# 自動由 deployed bundle 抽 anon key，逐表探測曝光面
node tools/probe-anon-exposure.cjs
```

另有 `tools/_probe-*.cjs`（由 deployed bundle 抽 anon key 直讀 PostgREST，**唯讀**）、
`tools/_egress-*.cjs`（解析 Vercel `[egress]` 行）。

## E. 打印中心／queue base 事故時間線（背景）

`pos-app.tsx` 曾有自製 `enqueuePrintJobs()`（base 用 React state `queue`）⇒ **收據 job 從未上雲**。
實測：kitchen 35/35 `printed` vs receipt 只 3 張、`once_key` 全 NULL（＝全人手補打），
但打印中心仍顯示綠色「已發送」＝零出紙零紅標。2026-09-22 收口成單一路徑 `appendPrintJobsWithSync()`。
`appendPrintJobs()` 同名反轉語義（2026-09-11 `1e08343`）：新版**只寫本機**，唯一合法用法
＝`printKioskReceiptForOrder()`。

## G. 2026-09-24 egress 覆核（**營業中**窗口 —— 補回 §B 一直缺嘅樣本）

窗口：Supabase 10:29:41–11:22:16 MAC（52.6 分，1,000 行上限）／Vercel 10:54:00–11:21:40 MAC（27.7 分，
339 行 → **222 個唯一請求**，展開倍數 1.53）。兩店同時營業（`8291f843` 09:00 開；
`d564b932` 亦 `is_open:true`）。

**帳單面（GoTrue 用量頁）**：period 已用 **0.06 GB / 250 GB（Pro）**。
23 Sep 全日 ＝ PostgREST 37.86 MB ＋ Realtime 7.77 MB ＋ Storage 3.40 MB ＋ Auth 0.05 MB ≈ **49 MB**；
24 Sep 至 11:24 ≈ 10 MB。**23 Sep 全日只有 23 張單**（22 Sep 37、21 Sep 23）⇒ 49 MB 係「慢日」數字，
唔可以當繁忙日基準。

**訂單數（anon 72h 窗口直查 `pos_orders.created_at`，澳門日）**：
21 Sep 23（11:48 起，部分）／22 Sep 37／23 Sep 23（含 d564b932 1 張）／24 Sep 至 11:15 只 4。

**已確認達標**：`legacy=1`／`queue=300`／`limit=300` 全部 0 次；`legacyThrottled` 0 次；
stale 拒收 0；`pos/state` 增量 **34,989 B**、全量 bootstrap **416 KB**；`pos_queue_events` limit 指紋
`{0:19, ?:37}`（零個 300）；claim 單一中繼機 `ag-89639934` 中位 **60s**、P90 181s。

**未解／待查**：
1. 🔴 **同一批事件每 30 秒重推，歷時 ≥51 分鐘**（10:35:27→11:26:35）。證據：Supabase log 逐 id 去重
   `PATCH pos_print_jobs?id=eq.print-11e37b9e` **27 次**、`PATCH pos_orders?id=eq.order-778dbb99` **25 次**、
   `PATCH pos_orders?id=eq.ledger-2cc9593f…` **22 次**，gap 全部係 30.0s（＝`FLUSH_INTERVAL_MS`）。
   每 tick 固定 7 條 Supabase 查詢（`pos_store_status[is_open]`、`pos_shifts`、`pos_orders`、
   `PATCH pos_orders`×2、`PATCH pos_print_jobs`、`POST pos_queue_events`）＝ 14/分。
   ⇒ `flushPosSyncQueue` 永遠唔 idle（隊列非空）。**唔係帳單問題**（每次 1–2 KB），但係請求數噪音。
   🔴 **直接可觀測後果**：兩張單嘅雲端 `updated_at` 被推前到 11:26:35，而 **內容零變化**
   （`order-778dbb99` status=sent_to_kitchen、`client_updated_at`=10:35:27；`ledger-2cc9593f` status=settled、
   `client_updated_at`=10:58:11）。雲端冇 `original_settled_at` ⇒ **報表用 `updated_at` 當事件時間 ⇒ 跨日會漂移**。
   ⚠️ 未定位根因（log 睇唔到 client 端）。下一步＝喺 Mac 收銀台開 DevTools console 睇
   `[pos-sync-flush]`／`[queue-outbox]`／`[sync-reconcile]`；或喺 sync route 加「本批事件 id＋type」log。
2. ⚠️ **83 條逐筆 `DELETE pos_print_jobs?id=eq.print-…`**（10:59:34–37 四秒內）＝打印中心一次過清 83 張舊 job，
   每個 `PRINT_JOB_DELETED` 事件一條 DELETE。只 1 個 Vercel invocation、payload≈0 ⇒ 對帳單無影響，
   只係 Supabase 請求數。可批次化成 `id=in.(…)`（非緊急）。
3. ⚠️ **2 次 `23505 duplicate key "pos_print_jobs_once_key_uniq"`**（10:57:47 `receipt:0`、10:59:32
   `kitchen:normal:0:1ah9q6a`）＋1 次 409 ⇒ 同一 onceKey 用**新 randomUUID** 重推 ⇒ update-by-id 永遠 miss、
   insert 永遠撞唯一鍵。去重生效（冇重複出紙）但係 noise。
4. ⚠️ **Realtime WS 30 條 / 52.6 分 ≈ 每 105 秒重連**（09-23 記錄係 11.8 分鐘）⇒ 重連偏密；
   Realtime 佔當日 egress 15.8%。
5. ⚠️ `src=mount` 增量仍每次回 34,989 B 全量 config（10:56–11:05 八分鐘內 6 次 mount）。

**打印鏈健康**：`pos_print_jobs` 最近 40 張全部 `status=printed`、`attempts=0`、
`created_at → claimed_at` 1–2 秒 ⇒ **冇卡紙、冇積壓**。

## H. 下載入口／版本控制（2026-09-23，0050 已跑；全文 `docs/146`）

表 `pos_release_versions` ＋ bucket `macauposapk`（public，POS 專案）。`/login` 按 UA 出下載按鈕 →
公開 API `/api/release/versions/active`（免登入）。admin 頁 `/admin/versions` ＋
`/api/admin/release-versions`（GET/POST/PATCH/DELETE）。核心 `release-core.ts`（**零 import**）。
- 🔴 每平台最多一個 active ＝ **partial unique index**；切換走 RPC `pos_activate_release_version`
  （先落閘後上位），**唔可以**喺 route 分兩條 update。
- 🔴 base URL **唔可以** fallback 去 `NEXT_PUBLIC_SUPABASE_URL`（＝Ledger，冇 `macauposapk`）⇒ 404 死 link 零 log。
  次序：`RELEASE_DOWNLOAD_BASE_URL` → `SUPABASE_URL` → `NEXT_PUBLIC_POS_SUPABASE_URL` → `null`。砌路徑要逐段 encode。
- 🔴 DTO 分開 `downloadUrl`（解析後）／`explicitDownloadUrl`（DB 原值），admin 表單用後者。
- 裝置偵測要喺 `useEffect`（SSR 冇 navigator ⇒ hydration mismatch）；iPhone/iPad 歸 `desktop`；新版本預設唔 active。
- 🚫 **冇檔案上傳 UI ＝ 商家 09-23 明確決定**（只要「喺頁面管理 link」）。刪版本唔刪 Storage 檔案。
