# 2026-09-13

## 確認稿：登入流程改版（先登入 → 後選模組）

**需求（J）**：而家「模式」選擇器（快餐／堂食／美容／自助點餐機／後廚屏／出餐台屏）擺喺
**登入之前**；要改成 **先登入（帳號+PIN）→ 登入成功後先揀工作台／模組**，
並且 **admin panel 可逐個商戶開／關模組**，POS 只顯示已開通嘅。

**產出**：`docs/mockups/login-then-module-select-2026-09-13.html`
（單檔確認稿，4 個頁籤：① 新登入頁 ② 選擇工作台 ③ Admin 模組授權 ④ 流程對照）
已過 dom-smoke-test（全綠）+ measure-layout（4 畫面零重疊、零裁切）。

**現況座標（改動時要知）**
- 登入頁 = `src/components/login-screen.tsx`；`LoginMode = quick|dinein|salon|kiosk|kitchen|expo`
- 登入 API = `src/app/api/ledger/login/route.ts`（回 session，含 merchantId / posDeviceToken）
- 掃碼點餐模式**跟登入模式**：`src/lib/pos/scan-mode-from-login.ts`
  `scanModeForLoginMode()` → quick / dine_in / null（kiosk、salon、kitchen、expo 一律 null）
- 側欄模組清單 = `src/components/app-sidebar.tsx` → `baseNavItems`
  （/ 、/orders、/members、/prints、/reports、/soldout、/shift、/inventory、/settings）
- Admin 商戶/帳戶頁 = `src/app/backoffice/accounts/page.tsx` + `backoffice-accounts-page.tsx`
- 角色權限 = `refundOrder / voidItem / manageAccounts`（`src/lib/types.ts` UserPermissions）

**⚠️ 未拍板嘅連帶影響（已寫入確認稿 ③）**
登入頁冇咗模式選擇之後，`scanModeForLoginMode()` 就冇輸入 —— 要改成跟
**② 所選工作台**寫入（堂食收銀台 → dine_in；快餐收銀台 → quick），
並決定「堂食＋快餐兩個都開通時，邊一步寫入」。

---

## ✅ 已實作（同日）：先登入 → 後選工作台 + 商戶模組授權

J 拍板：未開通**灰住+🔒**（唔係隱藏）／要「記住呢部機」／側欄模組一齊做／**唔**掛角色。

**詳細設計文檔** → `docs/127-login-workbench-permission-plan.md`（改動前必讀）。
**Migration** → `supabase/migrations/0037_pos_merchant_modules.sql`（⚠️ J 要自己跑）。

### 新增檔案
- `src/lib/pos/module-catalog.ts` — **唯一真源**（工作台 7 個 + 側欄模組 8 個 + `normalizeMerchantGrants()`）
- `src/lib/pos/module-catalog.test.ts` — 15 個 case
- `src/lib/pos/merchant-modules-server.ts` — 讀寫（含「讀唔到 = 全開」不變量）
- `src/lib/pos/apply-workbench.ts` — 由 login-screen 搬出嘅工作台副作用（**只能有一份**）
- `src/lib/pos/workbench-preference.ts` — 上次使用 / 記住呢部機
- `src/app/select-workbench/page.tsx` + `src/components/select-workbench-screen.tsx`
- `src/app/api/admin/merchants/modules/route.ts` + `src/components/admin-merchant-modules-dialog.tsx`

### 改動檔案
`login-screen.tsx`（移除模式選擇器）、`app-sidebar.tsx`（按授權過濾 + 「工作台」逃生門）、
`admin/dashboard/page.tsx`（商家列表加「模組」按鈕）、`api/ledger/login/route.ts`（session 加 `allowedModules`）、
`storage.ts`（`AuthSession.allowedModules?` 選填）

### 驗證（已跑）
- `tsc --noEmit` exit 0
- `eslint` 改動檔案：0 error
- `node --test`：**462 pass / 0 fail**
- `next build`：待確認

### 🔴 最易中嘅坑（已寫入 docs/127 §5）
1. `allowedModules` 缺失 = **全部開通**，唔可以當「一個都冇」→ 否則升級後全線入唔到 POS。
   口徑要喺 4 個地方一致（server / AuthSession / login-screen / app-sidebar）。
2. `normalizeMerchantGrants()` 要**保留空陣列**（= Admin 明確全閂），唔可以變返全開。
3. 加新模組**一定要先改 `module-catalog.ts`**，只改一邊唔會 throw，只會靜靜冇咗個開關。
4. Admin PATCH 要**兩組一齊送**（server 用兩個值覆寫整行）。
5. `retail` 唔寫 `saveOperatingMode()`；終端行業每次都要明確寫（salon / restaurant）。

### 追加：Admin 預覽 + 共用卡片元件
- 新增 `src/components/workbench-picker.tsx`（`WorkbenchCardGroup`）—— **真·選擇頁同 Admin 預覽共用**
  （如果唔共用，好快會出現「預覽見到 5 個、實際見到 4 個」而且唔會 throw）。
- Admin 彈窗加「C. 預覽」：跟開關即時變（未儲存都變），深色框（卡片係深色底設計，塞入淺色卡會白底白字）。
- `columns` prop 要**明寫**（收銀組 4 項 → 2 欄 2×2；裝置角色 3 項 → 3 欄），唔可以自動推。

### ⚠️ 環境事故：同一個 repo 有**另一個 session 同時改嘢**
- 發現 `src/lib/escpos-template.ts`／`print-jobs.ts`／`types.ts`／`retail/retail-label-content.ts`
  喺我工作期間被改（retail 標籤打印），而且一路有 commit（`c71b491 up`）。
- 症狀：`tsc --noEmit` **偶發** `src/lib/print-jobs.ts(406,9): error TS1005` —— 係對方**寫檔中途**
  被讀到，重跑即刻變返 exit 0。**唔係我整壞**。
- 我啲檔全部完好（只係被對方 commit 順手掃咗入去，所以 `git status` 見到 ` M` 唔係 `??`）。
- 我自己嗰批：`tsc` exit 0、`eslint` exit 0、`node --test` **477 pass / 0 fail**。
- 未做：migration 0037（本機冇 `.env.local`、冇 supabase CLI → 要 J 自己喺 Supabase SQL Editor 跑）。
