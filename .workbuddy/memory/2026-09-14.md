# 2026-09-14

## 🔴🔴 走數實案：收銀台「會員扣款」全額用餘額時**完全冇扣**（MOP 85）

**J 原話**：「我先是下了單, 然後我成功扣了款, 然後我又在那張桌台下了單, 然後我要結帳,
結帳的時候我是選了會員扣款… 他說是『客人已支付，完成訂單』… 然後我按下去後,
在 ledger 那邊只扣了我 75（掃碼嗰筆）, **沒有扣到我新下單的那一筆**。」

截圖數據：小計 **MOP 160**、客人已支付 **MOP 75**（綠）、剩餘需收 **MOP 0**、
**會員扣款 MOP 85**、支付方式選中「會員餘額 可用 MOP 219 · 本次扣 MOP 85」。

### 根因（`pos-app.tsx`）—— 判斷條件太闊
```ts
// L1982
const paymentSummary = { ..., memberDeduction,
  total: Math.max(0, payableBeforeMember - memberDeduction), };   // ← total 已減計劃扣款
```
```tsx
// 撳鈕 onClick（L6449）
if (paymentSummary.total <= 0 && paymentSummary.prepaidAmount > 0) {
  completeOnlinePaidOrder();   // ← 直接完成，唔會 confirmPayment()、唔扣款
  return;
}
```

**逐步重演**：`payableBeforeMember` = 160 − 75 = 85；店員選會員餘額 → `memberDeduction` = 85
→ `total` = 85 − 85 = **0** → 條件成立 → `completeOnlinePaidOrder()` → **MOP 85 走數**。

⇒ **`total <= 0` 唔等於「已付齊」** —— 因為 `total` 已減咗 `memberDeduction`（**計劃**扣款）。

### 兩個問題嘅答案（我自己查，唔應該問 J）
1. **「客人已支付，完成訂單」原意** = `completeOnlinePaidOrder()`：客人喺 Ledger／掃碼端
   已經付**全款**（`paymentMethod: "線上已支付"`）→ 收銀台只需確認完成。**設計正確**，
   只係判斷條件太闊。
2. **收銀台「會員扣款」只有「去結帳」才會真扣** ✅ ——
   路徑 = `confirmPayment()` → `runCheckout()` → `executeLedgerMemberCheckout()`（`merchant_apply_pos_txn`）。
   而 `total <= 0 && prepaidAmount > 0` 時**永遠去唔到呢條路**。

### 修正
新增 `isOnlinePaidComplete`（`paymentSummary` 之後）：
```ts
const isOnlinePaidComplete =
  !(useMemberBalance && memberDeduction > 0) &&          // ← 關鍵：有計劃扣款就唔算已付齊
  paymentSummary.total <= 0 &&
  paymentSummary.prepaidAmount > 0;
```
三處改用它：`onClick` early return（`completeOnlinePaidOrder`）、掣文字、以及原本 `total<=0` 判斷。
⇒ `memberDeduction > 0` 時一定落 `confirmPayment("會員餘額")` → 真扣款 ✅

⚠️ 呢個係**既有 bug**（唔係今次會員登入改動引入）。只要「全額用會員餘額付」就會中招；
部分扣（例如應收 100 扣 50）因為 `total = 50 > 0` 反而正常 → 所以一直冇被發現。

### 驗證
`tsc --noEmit` **0 error**｜`eslint src/components/pos-app.tsx` **0 error / 0 warning**
（`isOnlinePaidComplete` 4 處；確認冇殘留舊寫法；誤刪嘅 `receivedAmount` 行已還原）

### ⚠️ 待 J
- 對帳：`select local_order_no,total,prepaid_amount,member_deduction_avos,member_deduct_txn_id from pos_orders where created_at >= now() - interval '1 day' order by created_at desc;`
  對比 Ledger 帳單筆數 → 搵出所有「聲稱扣會員餘額但冇 txn_id」嘅走數單
- 掃碼扣款（線 B）**已確認正常**（J 見到 Ledger 扣咗 75）

---

## 🔴🔴 第二個走數實案：`runCheckout` 靜默行非會員路徑（會員扣款完全冇執行）

**J 情境**（同一張 A03 掃碼單）：① 掃碼落單扣 75 ✅ → ② 商家幫加 1 個麵（桌台見到 2 項）
→ ③ 前台結帳選「會員餘額」（`memberDeduction` = 85、掣已變「去結帳」）→ ④ 撳落去 →
**Ledger 完全冇扣、冇任何 toast、餘額不變**；而且**收據只印 1 項 $75 但總金額寫 MOP 160**。

**J 確認嘅三點**（關鍵）：
- 桌台**有**兩個面（=`paymentBase` 正確）
- Ledger **冇扣**（只有掃碼嗰筆 75）
- 收據 items 來源 = **線上**（`order.items`）→ 只 1 項
- 撳完**冇 toast**、餘額**冇變**

### 根因：`runCheckout` 有兩條路（`pos-app.tsx`）
```ts
const runCheckout = async (targetOrder: PosOrder) => {
  if (memberLedgerOpsNeeded && merchantId && ledgerMember) {
    ... executeLedgerMemberCheckout(...) ...   // ← 真扣款
    return;
  }
  applyPaymentToOrder(targetOrder);            // ← 靜默、冇扣款、冇 toast（L4030）
};
```
**「冇 toast + 冇扣款」完全符合第二條路** ⇒ `memberLedgerOpsNeeded` / `merchantId` / `ledgerMember`
其中一個 falsy。而 `merchantId` 若 null 會有 toast（J 冇見到）→ 所以係
`ledgerMember` 或 `memberLedgerOpsNeeded` falsy。

⚠️ **確切係邊個仍未確定**（`useMemberBalance` 由客人 toggle 設，L1961 嘅 reset 只喺「號碼非 8 位」時發生）。

### 修正（防走數，唔係猜真因）
喺 `runCheckout` 第二條路之前加硬閘：
```ts
if (useMemberBalance && memberDeduction > 0) {
  setToast({ type:"info", message:"未能執行會員扣款（會員資料或商戶登入狀態不完整），已取消結帳。請重新輸入會員號碼查詢後再試。" });
  return;   // ← 寧願唔結帳，都唔可以標記已付款而冇扣錢
}
```
**理由**：走數（標記已付款 + 冇扣錢 + 冇提示）比「結唔到帳」嚴重得多；
而且呢個 toast 會**直接暴露真因**（下次一撳就知）。

### 收據 items 唔全（未修）
收據顯示 1 項但總額 160 ⇒ `order.items` 只有 1 項，但 `paymentBase` 有 2 項
⇒ **商家加菜可能只改咗結帳頁嘅計算基礎，冇 append 落 `order.items`**。**未查證、未修**。

### 🔴🔴 收據問題 —— 根因已收窄到「order 部分更新」（2026-09-14 補查）
J 確認：**訂單詳情顯示 1 項**（收據一致）。

**關鍵發現**：`paymentBase`（L1863-1868）**唔關 cart 事** —— 佢係由
`currentSettlementOrder`（`orders.find(...)` 嗰張單）自己嘅
`subtotal + serviceChargeAmount + taxAmount` 計出。

⇒ `paymentBase.total = 160` 代表 **`order.subtotal`/`total` 已經係 160**，
   但 `order.items` 只有 1 項 ⇒ **商家加菜時「金額欄有寫入、`items` 冇寫入」= 部分更新** 🔴
   （呢個正好解釋收據「1 項 $75 但總金額 MOP 160」自相矛盾）

**未完成**：
- `upsertCurrentOrder`（L2143）**有** `items: cartItems`（L2191 / L2210）⇒ 經佢加菜應該會寫 items
- 所以真兇可能係「商家加菜」**冇經 `upsertCurrentOrder`**，或者走咗另一條只寫金額嘅路
- **需要 J 講清楚「商家幫加一個面」嘅具體操作**（邊個畫面、撳咩掣）才能定位

### ⚠️ J 指示（2026-09-14）
**「你不需要幫我 push 和 commit 任何東西, 我會自己做」** ⇒ 之後只改 code，**唔好 commit / push**。

### 驗證
`tsc` 0 error ｜ `eslint pos-app.tsx` 0/0

---

## ✅ 加菜 items 未更新 —— 根因已確定並修正（2026-09-14 晚）

### 病症（J）
商家喺掃碼／線上堂食單加菜 → 桌台／點餐頁見到 N 項、**但「訂單詳情」同收據只印 1 項 $75，
總金額卻寫 MOP 160**（自相矛盾）。金額欄有更新、`items` 冇。

### 根因鏈（三層，缺一不可）
1. `pos-app.tsx` `upsertCurrentOrder()`（existing order 分支）**無條件寫
   `status: nextStatus`（＝`sent_to_kitchen`）** ⇒ 已收款單（`paid`）被降級。
2. `/api/pos/sync` 嘅「付款階段單向閘」（2026-09-12）見到 `paid → open` ⇒
   **拒收整條 `ORDER_UPDATED`**（`ack(applied:false, reason:"paid-downgrade")`）
   ⇒ **`items` 上唔到雲**。
3. `ORDER_SETTLED` 係**窄 patch**（只寫 status／payment_method／discount_amount／total／
   updated_at…），**按設計唔重寫 `items`**（docs/83:1279）⇒ 雲端 row 變成
   「`total` 160、`items` 1 項」。之後 realtime／backfill 經 `mergeOrderLists()`
   （**整張 LWW 覆蓋、冇字段級 merge**）把呢個矛盾版本 merge 返本機 ⇒
   連本機啱嘅 `items` 都被蓋走 ⇒ 收據／訂單詳情少一項。

⚠️ 為咩今日之前冇被發現：`paid` 舊口徑只認快餐 counter 單；2026-09-13 起
`assignLedgerOrderToTable()`（線上已付堂食「排位」）**也會**寫 `paid`，
但 `upsertCurrentOrder` 冇跟住更新 ⇒ 兩邊口徑脫節。

### 加菜路徑（(a)/(b) 已分別查清）
- **(a) 本地 pos 單**：點餐頁 → `sendToKitchen()` → `upsertCurrentOrder()` → 寫本地
  `orders`（items 有寫 ✅）→ 推 `ORDER_UPDATED` 上雲（**← 呢步被拒**）。
- **(b) 線上／Ledger 單投影**（`ledger-<id>`）：`buildLedgerPosOrder()` 用 Ledger `detail`
  重建 items、金額用 `detail.total ?? ledgerOrder.total`；POS **冇任何 API**
  可以把加菜推返 Ledger ⇒ 收據若走「線上單補打」就會長期停留喺原單 items。

### 收據 items 來源（已確認）
| 入口 | items 來源 | 金額來源 |
|---|---|---|
| 結帳收據 `printReceipt(updatedOrder)` | 本地 `order.items` | 同一張 order |
| 點餐頁「打印收據」`printReceiptNow()` | 現場由 **`cartItems`** 砌快照 | cart totals |
| 補打廚房單 `printKitchenTicketNow()` | `loadOrders()` 權威單 items | — |
| 線上單「補打帳單」`reprintReceiptForLedgerOrder()` | **Ledger `detail`（`mapDetailToOrderItems`）** | Ledger `detail.total` |
⇒ 收據本身冇第二個 items 來源：**items 同金額都係讀同一張 `PosOrder`**，所以一旦
「items 滯後、金額已更新」嘅單存在，收據就會如實印出自相矛盾嘅內容。

### 修正（已落 code，未 commit）
- `pos-app.tsx` `upsertCurrentOrder()`：加 `keepPaidStatus = existingOrder?.status === "paid"`
  → `status` 保留 `paid`；`fulfillmentStatus` 保留（唔可以順手清，線上單係 `preparing`）。
  🔴 打印行為**不變**（`isAddOnOrder` 一向只認 `sent_to_kitchen`，`paid` 單本來就出 normal 全單票）。
- `/api/pos/sync`：抽出 `incomingQty`/`existingQty`/`isAdditiveUpdate` 到
  `keepExistingStatus` 之前；新增 `paidAdditiveFromDevice`
  ＝（`ORDER_UPDATED` ＋ items 只變多 ＋ `authorized` ＋ DB `paid` ＋ incoming `open`）
  → **沿用 DB 現有 `status`/`fulfillment_status`**，唔再拒收（照顧未重建嘅 APK／
  desktop companion／舊網頁）。

### 驗證
`tsc --noEmit` **0 error**｜`eslint pos-app.tsx + sync/route.ts` **0 error**（9 個 warning 全部
係既有 unused var，喺改動範圍外）｜`node --test src/lib/pos-order-filters.test.ts` **32/32 pass**。

### ⚠️ 殘留（未修，待 J 決定）
- `ORDER_SETTLED` 仍然唔重寫 `items`（docs/83 設計）→ 若 `ORDER_UPDATED` 因任何原因
  上唔到雲（離線／其他守門），雲端會再出現「金額新、items 舊」。
  要根治 = 令結帳事件帶 items，或改為結帳時補一條 `ORDER_UPDATED`。
- 已經被污染嘅舊單（今日嗰批）要靠下一次加菜／結帳自癒；建議 SQL 對帳搵出
  `jsonb_array_length(items)` 對唔上 `subtotal` 嘅 row。
- 建議觀察：加菜後喺 Supabase 睇 `select status,total,jsonb_array_length(items) from pos_orders where id='ledger-…';`

### 文檔已更新
- `docs/113-agent-gotchas.md`：新增 §(3) 分支 **(3b) 加菜後 items 冇更新**（完整根因鏈＋修法），
  並**改正過時鐵律**「`paid` 只可能係快餐 counter 單、堂食 100% 唔受影響」
  （2026-09-13 排位單也係 `paid`）。
- `.workbuddy/memory/MEMORY.md` §一 加一條紅線。

---

## 🔴 結帳落錯枱（A03 實案，2026-09-14 01:15 後）

### 病症（J + 截圖）
對 A03 做「加菜 → 去結帳」，撳落去**結咗第二張枱嘅單**；A03 仍然「已付款、應收 95」。
桌台總覽同時出現：A03 **橙色（有單）但標籤寫「空閒」**。
店內線下訂單：A03 01:17 MOP 170 已付款、A03 00:41 MOP 160 已完成（兩張都係「訂單號 A03」，
因為掃碼堂食單用**台號做單號**）。

### 根因（兩層，互相放大）
1. **可結帳判準太窄**：`isSettleableOrder()` 第 2 條要求 `onlineOrderId`，
   但**掃碼堂食單冇呢個欄**（`buildKioskOrder()` 唔寫；`onlineOrderId` 係「排位」時
   由 `assignLedgerOrderToTable()` 先補上）。客人掃碼落單＋付款之後本地單係
   `paid` ＋ 真枱 ＋ `prepaidAmount > 0`，判準卻返 **false**。
   ⇒ 桌台卡「已結帳 / 待收尾」判準（`isOnlineDineInOrder()`）同樣中招 → 顯示「空閒」。
   ⚠️ 呢個同時係**上一輪 keepPaidStatus 修正嘅副作用**：以前加菜會打返
   `sent_to_kitchen`（一樣可結帳），而家保留 `paid` → 就撞上判準缺口。
2. **全店 fallback**：四個結帳入口最後一重係
   `orders.find((order) => isSettleableOrder(order))` → **全店任意揀一張單**。
   第 1 層判錯 → 第 2 層就揀錯枱。

### 修法
- `src/lib/pos/online-dinein-labels.ts`：新增 `isPaidDineInOrder(order)`
  ＝ `status === "paid" && hasTableAssigned(order)`（唔再要求 `onlineOrderId`）；
  `isSettleableOrder()` 第 2 條改為叫佢。快餐 counter 單靠 `hasTableAssigned()` 排除（口徑不變）。
- `src/components/pos-app.tsx`：新增 `resolveSettleTargetOrder(explicitId)` —— 三層、**永不跨枱**：
  ① 明確指定（含排除快餐 `__cart__` 哨兵）→ ② 當前工作台 `activeOrder` →
  ③ **只限 `activeTableId`** 嘅最新可結帳單 → 都冇 → `null`（toast「目前沒有待結帳訂單」）。
  四個入口共用：`openSettlementModal`／`confirmPayment`／`completeOnlinePaidOrder`／`confirmComp`。
- 桌台卡標籤改用同一份判準（`isPaidDineInOrder`）→ 掃碼 paid 單會顯示綠色「已結帳 / 待收尾」。
- import 由 `isOnlineDineInOrder` 換成 `isPaidDineInOrder`（`isOnlineDineInOrder` 仍留俾
  `online-dinein-fulfillment.ts` 推 Ledger 狀態，**唔可以**改闊）。

### 驗證
`tsc --noEmit` **0 error**｜`eslint pos-app.tsx + online-dinein-labels.ts(+test)` **0 error**
（9 個 warning 全部既存）｜`node --test online-dinein-labels.test.ts + pos-order-filters.test.ts`
**51/51 pass**（測試已更新：paid＋冇 onlineOrderId＋真枱 → **可結帳**）。

### ⚠️ 待 J（資料層）
- 搵返「被誤結」嘅單：
  `select id,local_order_no,table_name,status,total,payment_method,updated_at from pos_orders where store_id='<merchantId>' and status in ('settled','paid') and updated_at >= now() - interval '2 hours' order by updated_at desc;`
- 若當時有扣會員餘額，要查 `member_deduct_txn_id` 對應嘅 Ledger 帳單（可能「為第二張枱扣咗錢」）：
  `select id,local_order_no,table_name,member_deduct_txn_id,total,status from pos_orders where member_deduct_txn_id is not null and updated_at >= now() - interval '2 hours';`
- 修正後重測：A03（paid 掃碼單）撳「去結帳」應該**只**結 A03、顯示應收 95。

### 文檔
`docs/113-agent-gotchas.md` 新增「🔴 結帳目標唔可以喺『全店』揀」一節（含鐵律：
凡寫 status／收錢嘅操作，目標單一律明確指定或限定當前枱）。

---

## 🔍 分析（只讀）：排位 vs「待交付」狀態轉換（2026-09-14 11:20）

**問題**：訂單處於「待交付」時按「排位」，待交付會否改變？

**結論**：會 —— 而且係**跳過**：Ledger 由當前狀態一次爬梯直達 `completed`，
`ready`（＝「待交付」）只係 ladder 中間一級，UI 觀察唔到。

- 「待交付」有**兩個身份**：① `preparing` 狀態下的推狀態**按鈕**（`getPrimaryOnlineOrderAction()`
  `online-order-actions.ts:70-86` 只喺 `fulfillmentType==="merchant_delivery"` 叫「待交付」；
  `online-orders.tsx:1129-1137` 則係「非 pickup」一律「待交付」）；② Ledger `ready` 的**狀態值**
  （`order-mapper.ts:194-195`，takeaway→待取餐，其餘→待交付）。
- 爬梯：`assignLedgerOrderToTable()`（`ledger-pos-bridge.ts:624-645`）→ `syncOnlineDineInCompletion()`
  （`online-dinein-fulfillment.ts:44,64-81`）`DINEIN_LADDER = accepted→preparing→ready→completed`，
  `invalid transition / already` 就 `continue`，其餘錯誤即停。**冪等**。
- 排位鈕出現條件**唔限 raw status**（`online-orders.tsx:1066`：`!hasRequest && isOnlineDineIn`）
  ⇒ ready／completed 的線上堂食單都仍然出「排位 / 改枱」。
- 排位後該線上單**即刻由線上列表消失**（`transferredLedgerOrderIds()` `pos-order-filters.ts:33-39`
  → `withoutTransferredOrders()`），改由本地堂食單管理 ⇒ 「待交付」鈕自然冇。

**順手發現（未改）**：
1. ⚠️ `online-orders.tsx` `assignDineInTable()`（L912-925）**冇檢查** `result.ledgerProgress`，
   只彈「已排位」成功；`quick-online-orders-panel.tsx`（L647-654）有檢查並彈
   「排位已成功，但同步線上訂單狀態失敗」。⇒ 表格版若 Ledger 同步失敗會**靜默**，
   本地已排位（該單已從線上列表剔走）但 Ledger 停留舊狀態 → 雙狀態不一致。
2. ⚠️ 同一狀態兩處叫法唔同：堂食單 `preparing` 在 POS 主頁快捷面板叫「待取餐」
   （`online-order-actions.ts:76-78`），在訂單頁表格叫「待交付」（`online-orders.tsx:1136`）。
3. ⚠️ `ledgerStatusBadgeLabel()`（`online-order-actions.ts:29-31`）`ready` 三個分支都返回
   「待取餐」⇒ 快捷面板狀態膠囊永遠睇唔到「待交付」（copy-paste 遺留）。

### ✅ 11:25 已修正上述 3 項 ＋ 加「爬梯必須驗證」

| # | 問題 | 修法 |
|---|---|---|
| 1 | 表格版排位同步失敗**靜默** | `online-orders.tsx` `assignDineInTable()` 補檢查 `result.ledgerProgress`，失敗彈 error toast（文案同快捷面板一致） |
| 2 | 堂食 `preparing` 兩處叫法唔同 | `online-order-actions.ts` `getPrimaryOnlineOrderAction()`：`tabType === "pickup" ? "待取餐" : "待交付"`（只有自取係待取餐） |
| 3 | `ledgerStatusBadgeLabel()` ready 寫死待取餐 | 改 `fulfillmentType === "takeaway" ? "待取餐" : "待交付"`（同 `order-mapper.ledgerStatusLabel()` 唯一口徑） |
| 4 | 🔴 爬梯走完 ≠ 到 `completed`（無效轉換一律跳過 → 已取消單都報成功） | 梯頂成功即算成功（零額外請求）；梯頂被拒 → 讀一次真實狀態確認 |

**新檔**：
- `src/lib/pos/online-dinein-ladder.ts`（**零依賴**：`DINEIN_LADDER` / `DINEIN_FINAL_STATUS` /
  `isInvalidTransition` / `isLedgerStatusComplete`）＋ `.test.ts`（**8/8 pass**）。
- `src/lib/ledger/orders.ts` 新增 `getOrderStatus(orderId): Promise<string | null>`
  （`.from("orders").select("status").eq("id",…)`，Ledger 側表；`null` = 讀唔到＝無法確認）。

**🔴 關鍵認知（已寫入 docs/113 §(3b)(3c)）**：
- `mapRpcErrorMessage()` 會把 `invalid transition` **譯成中文「目前狀態不可執行此操作。」**
  ⇒ `isInvalidTransition()` 淨認英文會永遠認唔到，中文那條才是現行主力判定。
- 凡「本地成功 ＋ 遠端要跟住變」→ 唔可以用「RPC 冇拋錯」當成功；
  要嘛遠端親口回成功，要嘛讀返一次確認。驗證本身失敗（RLS／網絡）→ 當「無法確認」＋
  `console.warn`，**唔可以**製造假失敗。
- 驗證：`tsc --noEmit` **0 error**｜`eslint`（6 改動檔）**0 error / 0 warning**｜
  `node --test online-dinein-ladder.test.ts` **8/8 pass**。

---

## 🔍 排查：全單備註 / 單品備註「無法輸入文字」（iPad 實紙，2026-09-14 09:40）

### 結論：**元件層健康，問題喺「虛擬鍵盤／輸入法」層**（未改碼，等 J 決定）
代碼證據（`pos-app.tsx` 備註彈窗 L5615-5688）：
- `<textarea>`（L5679-5685）**冇** `disabled` / `readOnly` / `maxLength` / `inputMode`；
  `value={noteDraft}` ＋ `onChange`（L5681）綁定齊全 ⇒ **唔係「被禁用」亦唔係「缺事件綁定」**。
- 全 repo 掃過：`pos-app` 內**冇任何** `keydown`/`pointerdown` 全域 handler 會 `preventDefault`
  食按鍵（只有 audio unlock、network、bootstrap event）；`ResponsiveModal` overlay 有
  `stopPropagation`；flash 層係 `pointer-events-none`；`SelfOrderNoticeStack` ≤5 張卡時
  `pointer-events-none` ⇒ **冇透明層食 tap**。
- 照片反證：iOS 系統編輯選單（「貼上／自動填寫」）已彈出 ⇒ **焦點確實有落到 textarea**，
  所以係「鍵盤唔彈／按鍵入唔到」，唔係 focus 失敗。
- 鎖定無關：`openItemNoteEditor()`（L2521）同全單備註「編輯」掣（L5260）都會先擋；
  彈窗開得成 ⇒ 該單/該菜**未被鎖**（`orderNoteLocked` 只喺「保存」再守一次）。

### 已定位嘅 5 個 code 級風險因素（可修）
1. textarea 用 `text-sm`（14px）＋ `layout.tsx` `maximumScale:1 / userScalable:false`
   → iOS 唔可以放大去遷就焦點（慣例要 ≥16px）。
2. 備註 textarea **冇 `autoFocus`**（app 內其餘 5 個輸入框 6682/6943/7016/7085/7147 都有）。
3. `docs/109 §3.4 修正 A`（全局 `focusin → scrollIntoView({block:'center'})`）**從來冇實作**
   —— `src/lib/ios-focus-helper.ts` 唔存在，而 `body` 係 `overflow-hidden`。
4. 備註彈窗只有 `z-50`（同 SelfOrderNoticeStack 同級、低過 flash `z-[55]`／逾時 `z-[60]`）
   → 建議提到 `z-[70]`（同「免單備註」一致）。
5. 冇 `autoCorrect="off" autoCapitalize="off" spellCheck={false}` → 中文組字有被系統修正風險。

### 架構級根因（同 docs/69 §A 一致）
全 app 只有**數字**鍵盤基建（`numeric-keypad` / `fixed-number-pad` / `input-pad-modal`），
**完全冇自由文字鍵盤** ⇒「備註自由輸入」係唯一要打字嘅地方 ⇒ 只有呢兩處會爆。
⚠️ 同 2026-09-13「手機 PIN 撳極冇反應」係**同類**（欄位冇輸入機制），但今次唔同：
今次係真 `<textarea>`，唔係 `<button>` 假輸入框 ⇒ **唔可以照抄嗰個修法**。

### 參考
`docs/109-shift-sync-overtime-plan.md §3`（iPad 鍵盤問題已列過 3.2 成因 + 3.3 現場驗證 script，
但修正 A 未做）；`docs/69-touch-ux-audit.md §題③`（POS WebView 系統鍵盤不可靠）。

### 9:50 補充 —— J 答「鍵盤完全冇彈」＋ git 考古：**web code 冇回歸**
逐項查證（用 git 直接對版本，唔靠估）：
- 備註彈窗由手寫 `fixed` div → `ResponsiveModal` ＝ **2026-08-09（6580e42）**；textarea 實作 8 月初至今未變。
- `viewport maximumScale:1 / userScalable:false` ＋ `body{overflow:hidden}` ＝ **2026-08-04 首次提交** 就有。
- 全 repo 冇任何近期新增嘅全域 `keydown`／`touchstart` 攔截（`git log -S'preventDefault' -- src`）。
- `runtimeRefreshTick` 只做 effect 依賴，**冇**做 `key` → 唔會 remount 令焦點消失。
⇒ **唔係 web 改動造成「之前可以依家唔得」**，指向裝置／系統層。

### 9:50 已落嘅加固（tsc 0 error｜eslint 0 error / 9 個既存 warning）
- 🆕 `src/components/ios-focus-helper.tsx` —— **實作 docs/109 §3.4「修正 A」**（一直冇做過；
  之前 docs 寫「實現見 src/lib/ios-focus-helper.ts，optional」，但該檔根本唔存在）：
  `focusin` → `scrollIntoView({block:'center'})`（捲入最近可滾動祖先＝彈窗面板）＋
  `visualViewport` resize/scroll 補一次；**元素本來完整可見就唔動**（唔同 POS 自己 scroll 打架）。
- `src/app/layout.tsx`：`<IosFocusHelper />` 掛入 `ClientOnly`。
- `pos-app.tsx` 備註 textarea：`autoFocus`（app 內其餘 5 個輸入框都有，唯獨呢個漏咗）、
  `text-base`(16px，避 iOS focus 自動放大 vs `maximumScale:1` 衝突)、
  `autoCorrect/autoCapitalize/spellCheck` off、`enterKeyHint="done"`；彈窗 z-50 → **z-[70]**。
- `pos-app.tsx` 免單備註／折扣備註（同款 fixed 彈窗 free-text input）一齊補 16px + IME 屬性。

### 最可能成因（優先序）
1. **iPadOS 判定有實體鍵盤**（鍵盤保護套／藍牙鍵盤／HID 掃碼槍／藍牙讀卡機）→ 永不彈軟鍵盤。
   ⚠️ 呢個 POS 幾乎所有輸入都有自繪數字鍵盤，**只有「備註自由輸入」靠系統鍵盤**
   → 只有呢兩處會被發現 ⇒ 好易誤判成「只有備註壞咗」。
2. 引導式存取（Guided Access）選項入面嘅「鍵盤」被關（docs/116 §R4 建議用引導式存取）。
3. iOS 全屏 PWA + `body{overflow:hidden}` + fixed 彈窗（docs/109 §3.2-2）。
4. iPad 跑舊版 JS → 強制 reload。

### 分流測試（等 J 回覆）
① 同機「搜尋商品」欄打唔打得入 ② 撳「多飯／少冰」chip 有冇反應 ③ 設定→藍牙／引導式存取檢查。

### 文檔
`docs/113-agent-gotchas.md` 新增「🔴 備註欄『焦點有到、虛擬鍵盤唔彈』」一節（已排除清單＋git 考古結論＋
分流測試＋若裝置層無得救嘅兩條根治路：擴大常用備註 chip／做 App 內建文字輸入板）。

### 10:43 第三輪 —— 商家回報「點擊備註欄仍未彈鍵盤」
⚠️ **最直接解釋：我上次嘅改動仍未 deploy**（只改本機、未 commit／push；收銀機載 Vercel 部署
→ 未 push = 商家測嘅仍然係舊 build；iPad 分頁亦唔會自動換 JS，要**完全關分頁重開**）。
今次再加嘅保證（`pos-app.tsx` 備註 textarea）：
- `onPointerUp` 明確補 `focus()`（`document.activeElement !== el` 才叫）—— 防 iOS 首次 tap 被當成
  scroll／雙擊縮放而**冇 focus**（docs/109 §3.2-3「兩段式點擊」）；已聚焦時係 no-op。
- `style={{ touchAction:"manipulation", WebkitUserSelect:"text" }}`。
- 驗證：`tsc` 0 error｜`eslint pos-app.tsx` 0 error / 9 個既存 warning。
新加分流測試（已寫入 docs/113）：
① 搜尋商品欄 ② 彈窗 chip ③ 藍牙／引導式存取 ④ **用 Safari 普通分頁開（唔用主畫面圖示）**
⑤ 同彈窗嘅免單／折扣備註打唔打得入（打得入 ⇒ 收窄到備註欄本身）。
🔴 教訓：凡「改完商家話仲係唔得」→ 第一步確認佢跑緊嘅版本（有冇 push/deploy ＋ 強制重開分頁）。

### 10:46 第四輪 —— 商家實拍影片（7.03s，720×1280@30fps）逐幀分析 → **焦點正常，100% 系統鍵盤層**
本機冇 ffmpeg／cv2 → `pip install imageio-ffmpeg`（自帶 ffmpeg binary）＋ Pillow 抽幀分析：
- ✅ 撳落去**確實有聚焦**：textarea 邊框變橙（`focus:border-orange-400`）＋文字游標＋
  **iOS AutoFill 淡黃底色**（三者都只會在真聚焦時出現）。
- ✅ 系統層認得係可編輯欄位：彈出系統文字工具列「**貼上｜自動填寫**」並持續停留。
- ❌ **211 幀（7 秒）內虛擬鍵盤零次出現**；下半螢幕亮度均值只在 142–162 浮動（手／通知造成），
  冇「大面積淺色跳變」⇒ **唔存在「彈完即縮」**。
- 附註：影片見到 WeChat 通知蓋住 app、冇狀態列 ⇒ 平板上係**全屏/kiosk** 狀態。
⇒ **結論釘死：可編輯狀態、事件綁定、聚焦全部正常；再改 textarea focus 邏輯對「鍵盤唔彈」零作用。**
   成因只剩裝置側：① iPadOS 判定有實體鍵盤（藍牙／保護套／HID 讀卡機／掃碼槍）
   ② 引導式存取「鍵盤」開關 ③ **設定→一般→鍵盤→鍵盤清單只剩「表情符號」**
   ④ MDM／店鋪裝機／kiosk 管理工具統一停用軟鍵盤。⚠️ 軟鍵盤一旦被抑制，
   **手寫／語音／表情鍵盤一樣冇** —— 「叫商家用手寫」唔可行。
- 已寫入 `docs/113`（含影片證據段、新增第 5–7 條成因、第 6 個分流測試、
  以及三條唔靠系統鍵盤嘅兜底路：擴大 chip／**新增「最近用過」備註 chip 行**／App 內建輸入板）。

### 11:10 第五輪 —— **真正根因鎖定：viewport `user-scalable=no`**（Ledger 對照法）
J 提供決定性情報：「**Ledger 網頁上操作都正常，且冇任何藍芽裝置連接**」
（Ledger = `https://membership-uat.macau-tech.com`，同一部 iPad）⇒ 裝置層全部排除。

用 node fetch 兩站 HTML 比 `<head>`：
| | viewport |
|---|---|
| Ledger（正常） | `width=device-width, initial-scale=1, maximum-scale=1, viewport-fit=cover` |
| POS（唔彈） | 同上 **＋ `user-scalable=no`** |
+ POS 獨有 `body{overflow:hidden}` ＋ `h-full` 全屏非滾動外殼。

**機制**：① `user-scalable=no` 鎖死視口重繪 → iOS 彈鍵盤要做嘅 visual-viewport 重算失敗 → 唔彈
② 文檔唔可滾 → iOS「scroll 去聚焦元素」focus reveal 失敗（input 嵌喺 fixed+overflow:hidden 祖先會被截斷）。

**已落修正（tsc 0 error｜eslint 0 error / 9 既存 warning）**
- `src/app/layout.tsx`：viewport 刪 `maximumScale` + `userScalable`（同 Ledger 睇齊）＋長註解。
- `src/app/globals.css`：🆕 `@media (pointer: coarse) { input/textarea/select { font-size:16px } }`
  （唔禁縮放後，<16px 會被 iOS focus 自動放大 → 16px 完全避開；unlayered 蓋過 Tailwind `text-sm`）。
- 🆕 `src/lib/pos/ios-keyboard.ts`：`refocusForIosKeyboard()` —— iOS 只喺「焦點改變」才彈鍵盤，
  已聚焦（autoFocus 取過焦／上一下撳過）再撳同一欄位唔會彈 ⇒ 鍵盤明顯未開時（
  `visualViewport.height < innerHeight*0.75`）先 blur 再 focus，造出新鮮焦點改變。
- `pos-app.tsx`：備註 textarea ＋ 免單備註 ＋ 折扣備註 三個欄位加 `onPointerUp` 用上面函式。
- `ios-focus-helper.tsx`：改 `block:"nearest"`（唔用 center/end）＋**喺 focus 事件內同步呼叫**
  （放 setTimeout 會被 iOS 當非手勢上下文）＋ 260/420ms 補兩次。

**欄位盤點**（`src` 全掃）：45 檔、**269 個可輸入元素**（input 199 / textarea 11 / select 59）；
冇任何 `inputMode="none"`；只有 `printer-companion-panel.tsx` 一個 readOnly（顯示用，正常）。
⇒ 唔使逐個改，兩條全域規則已覆蓋全部。

⚠️ **仍未部署** ⇒ 商家側實際仍未修復。要 push + 叫商家**完全關分頁重開**再驗。
⚠️ 呢個係「差異對照 + 業界一致口徑」推導出嘅修法，**仍需實機驗證**（未 claim 已修好）。

### 11:30 第六輪 —— 第二段影片（登入頁）：**連登入頁都唔彈 → app-wide**
- 登入頁（`login-screen.tsx`，普通流式頁、**唔係 fixed 彈窗**）：帳號欄 8 位數字帶**藍色選取
  highlight**（焦點確定）、PIN 欄 focus ring —— 一樣零鍵盤。
  ⇒ 排除「只有 fixed 彈窗失敗」⇒ **app-wide**（同 viewport `user-scalable=no` +
  `body{overflow:hidden}` 全屏外殼呢兩個全域設定吻合：每頁都會中）。
- 新線索：畫面冇狀態列、冇瀏覽器地址列，右上角浮一粒 **「⋯」** ⇒ 唔係普通 Safari 分頁；
  最可能係**主畫面 PWA（standalone）／Stage Manager 浮窗／WeChat 內開**。
  ⚠️ 若係 WeChat 開：iPad WeChat WebView 對呢類全屏+禁縮放頁面嘅鍵盤問題係出名多 ——
  需要 J 確認開啟方式（主畫面 icon／Safari 網址／WeChat 連結）。
- 已寫入 docs/113；向 J 提出**唔使等部署嘅決定性測試**：同一部 iPad 用 Safari 直接開
  `https://macau-pos-system.vercel.app/login` 撳帳號欄 → 彈得出＝開啟方式問題；唔彈＝
  就係今輪修緊嘅 viewport/overflow-hidden（**仍未部署**）。

### 11:50 第七輪 —— ❗ **修正已部署，但商家仍然唔得**（重要轉折）
J 交代：**POS 係用 Safari「加到主畫面」嘅 PWA 方式開**；「還是不行」。
我用 node 直接 fetch 線上檔案核實（唔靠估）：
- `/login` 部署版 viewport＝`width=device-width, initial-scale=1, viewport-fit=cover`
  → **`user-scalable=no` 已消失 ⇒ 本輪 layout.tsx 修正已上線** ✅
- 部署 CSS 已含 `@media (pointer:coarse){…font-size:16px}` ✅（我 globals.css 那條）
- 部署 `<body>` 仍然係 `class="h-full overflow-hidden flex flex-col"`（全屏非滾動殼未改）
- meta：`mobile-web-app-capable=yes`、`apple-mobile-web-app-status-bar-style=black-translucent`
⇒ **「viewport + 16px」唔足以解決**（或商家未換版）→ 成因收窄到三個方向：
  ① **PWA standalone 執行環境本身**（Ledger 若用 Safari 分頁開就係唯一差異）
  ② **`body{overflow:hidden}` + 全屏 fixed 殻**（login 頁 `fixed inset-0 overflow-y-auto`、
     彈窗 `fixed inset-0` + panel `overflow-hidden`）→ iOS focus reveal 失敗
  ③ **客戶端未換版**（iOS 主畫面 web app 長期唔換版；SPA 切頁唔會重抓 HTML）

### 🔴 SW 快取審計（`public/sw.js`）
`CACHE_NAME = "macau-pos-v20-7-31"`（硬編，唔跟 build）＋ `APP_SHELL` 預快取 `/`、`/login`、manifest；
`fetch`：navigate＝network-first（好）／**其餘 GET＝cache-first 並即場 cache.put**（只排除 `/api/`，
⚠️ 連跨域 Supabase REST 都入快取 → 資料新鮮度風險，日後要查）。
⚠️ 另有 `min-h-screen` 之類 Tailwind utility 干擾過我 fetch CSS 嘅 grep（要睇清楚 match 位置，唔好誤判）。

### ⚠️ 本輪 J 明確要求：**先列完整成因清單（含判斷依據＋排查方式＋可能性排序），唔要修改方案**

### 11:55 第八輪 —— J 三個答案，改寫排序
1. **Ledger 係用 WeChat 打開嘅**（唔係 Safari）⇒ ① 排除裝置層（系統鍵盤本身可用）
   ② **排除 WeChat**（WeChat WebView 彈得出鍵盤）③ 唔再係「Ledger 冇 overflow:hidden」之別，
   因為對照組係**另一個 app × 另一個 runtime**。
2. POS 主畫面 icon **8 月尾就加上**；**剛剛（約 11:50）才 deploy** ⇒ 商家嗰次「還是不行」
   幾乎肯定係 **deploy 之前嘅 build**（兩者時間重疊）⇒「未換版」仍然高度可能：
   PWA 要**完全由多工掃走再重開**（甚至刪 icon 再加）才會載到新 HTML/JS。
3. 「登入介面睇落係新版本」＝弱證據（登入頁設計可能冇改過）→ 要睇**欄位字級係咪 16px** 才可靠。

### 🔴 新查到嘅 code 事實（縮窄範圍）
`grep navigator.standalone / display-mode / isNativeShell / isStandalone`：
全 app **只有兩處**用 standalone 判斷 —— `pwa-install-button.tsx`（standalone 時隱藏安裝鈕）
同 `login-screen.tsx`（`isNativeShell` 只控制顯示安裝鈕）。`isRunningInNativeShell()` 只認
`window.PosNative.printJob` / `window.companionShell`（Android/Electron 外殼），iPad PWA 一律 false。
⇒ **冇任何 standalone 專屬邏輯會影響輸入** ⇒ 排除「standalone 分支寫壞」呢個成因。

### `pwa-register.tsx` 審計
只 `register("/sw.js")`，**冇 `registration.update()`、冇更新提示 UI** ⇒ SW 更新靠瀏覽器默認
revalidate；`sw.js` 內容好少變 ⇒ 8 月尾預快取嘅 `/`、`/login` 有機會長期留存
（在線時 navigate 係 network-first 會刷新；離線先食舊快取）。

### 剩下兩個候選
**O1 standalone PWA 執行環境** vs **O3 `body{overflow:hidden}`＋全屏 fixed 殼**。
決定性實驗＝2×2：我哋 POS 喺 ①WeChat ②Safari 分頁 各試一次（Ledger 喺 WeChat 已知正常）。
①正常 ⇒ O1；①唔正常 ⇒ O3。

---

## 🔍 後續：J 截圖見到「目前沒有待結帳訂單。」（01:32）

### 證據鏈（結論：**跑緊嘅唔係修正後嘅 build**）
由截圖反推狀態：`桌號 A03` ＋ 訂單明細 2 項 ＋ 小計 170 ＋ **應收 95** ＋ 備註已鎖定
＋ 下單（唔係加單）：
- 應收 95 ⇒ `prepayment` 75 存在 ⇒ `currentSettlementOrder`／`activeOrder` 非 null；
- 備註已鎖定 ⇒ `status ∈ NOTE_LOCKED`（sent_to_kitchen／paid／settled／…）；
- 按鈕未被 `isReadOnlySettled` 禁用 ⇒ 唔係 `settled`；
- 下單而非加單 ⇒ `isAddOnOrder` false ⇒ 唔係 `sent_to_kitchen`；
⇒ **status = `paid`、activeOrder 非 null、枱號係真枱**。

而修正後 `isSettleableOrder(paid ＋ 真枱)` = **true** ⇒ `resolveSettleTargetOrder()`
第二層一定命中 ⇒ **唔可能彈嗰個 toast**。所以：J 測嘅頁面仍然係**舊 build**
（收銀機／desktop 載 Vercel 部署 → 本機改咗仲要 **deploy**；若係 localhost dev 就 reload 一次）。
⚠️ 一秒判斷法：**桌台總覽 A03 卡應該變綠色「已結帳 / 待收尾」**（唔再橙色＋空閒）。

### 本輪加固（唔靠 reload 都唔會錯 + 下次一睇就知）
- 目標解析規則本體抽出做**純函式** `src/lib/pos/settle-target.ts`
  （`resolveSettleTargetOrder()`，零依賴）→ 加 **`settle-target.test.ts`** 鎖死
  「**永不跨枱**」：空枱＋隔籬枱有可結帳單 → 必須 `null`；同枱多單 → 揀最新；
  快餐 counter paid 單唔入堂食結帳路。
- 加第四層 `workspaceOrder`（`activeOrderId` 實際載入嘅單）—— 點餐頁「去結帳」最直接嗰張。
- 失敗提示帶**本枱單狀態**：`目前沒有待結帳訂單（{枱}：{status…}）`
  （`describeTableOrderStates()`）→ 下次截圖一睇就知係「真係冇單」定「判準唔認」。
- **第三個狀態映射**（點餐頁頂部「狀態：…」，`pos-app` L4942）補 `paid` →「已結帳 / 待收尾」
  ＋ `reopened` →「待重結」。之前只有桌台卡同 `OrderStatusBadge` 兩處，點餐頁係漏咗嘅第三處。

### 驗證
`tsc` **0 error**｜`eslint`（4 個改動檔）**0 error**｜
`node --test settle-target + online-dinein-labels + online-dinein-fulfillment + pos-order-filters`
**65/65 pass**（新增 10 個跨枱 regression case）。

### 🧠 教訓（已寫入 docs/113 + MEMORY.md）
- 「改咗代碼但行為唔變」除咗「出紙程式冇 re-build」，仲有 **web 側：改完冇 deploy**
  （收銀機／desktop 載 Vercel 網址）。
- 收銀台可見狀態有**三份**映射（桌台卡／點餐頁／`OrderStatusBadge`）—— 改判準要三處齊改。

---

## ✅ 線上訂單新增「預約單 / 預約時間」（Ledger `scheduled_pickup_at`，2026-09-14）

需求（J）：線上訂單要有「預約時間」欄位 → 顯示「預約單」＋一個預約時間，對應 `orders.scheduled_pickup_at`
（參考左側「會員通」收據 `== 預約單 ==` / `預約時間：09/14 12:15`）。
**改動前全 repo 零使用**：`order-mapper.ts:14` 只宣告過 type，冇 map、冇任何顯示。

### 改動（顯示層 ＋ 收據／廚房單 ＋ 快到／逾時高亮，J 拍板範圍）
- 資料層：`LedgerOnlineOrder.scheduledPickupAt`（`mapLedgerOrderRow()` 讀 `scheduled_pickup_at`；空字串／空白當 null）。
- 新純函式 `src/lib/pos/scheduled-pickup.ts`（**零 import**、`node --test` 覆蓋）：`scheduledPickupKind()` → `scheduled|soon|overdue`
  （≤30 分鐘 = 快到；**剛好到點 = 逾時**，否則永遠唔轉紅）。
- 新元件 `src/components/scheduled-pickup-badge.tsx`：`ScheduledPickupChip` / `ScheduledPickupTimeText` / `hasScheduledPickup`
  —— 三處 UI 共用，唔准各自寫字串。
- UI：`online-orders.tsx` 列表（類型欄 chip ＋時間欄第二行）＋訂單詳情；`quick-online-orders-panel.tsx`（strip／card 兩個 layout ＋詳情）。
  兩邊都加 **30 秒 `nowTick`**（純時間函式，冇 realtime 事件都要自己轉色）。
- 打印：`PosOrder.scheduledPickupAt`（`buildLedgerPosOrder()` 帶入）＋ `ReceiptSectionId`／`KitchenSectionId` 加 `scheduled_pickup`
  ＋ `buildReceiptContent()`／`buildKitchenContent()` 出 `預約時間: MM/DD HH:MM`（新 `formatMacauMonthDayTime()`，同會員通收據同格式）。
  **靜態文字區塊 → 下游三端唔使改、唔使擰 versionCode**（`buildSnapshot()` 走 `ensureReceiptScheduledPickup()`／`ensureKitchenScheduledPickup()` 向前兼容舊 localStorage 模板）。
- 🔴 `storage.ts` `mergeTemplateOrder()` 修正：新區塊原本 append 到尾 ⇒ 會印喺 **footer 之後**；改為「跟 default 位置（搵最近一個仍存在嘅 id 做錨）」。
- `orders-hub.tsx` 線上單 CSV 加「預約時間」欄（非預約單留空）。

### 驗證
`tsc --noEmit` **0 error**｜`eslint`（13 個改動檔）**0 error**
（`print-center.tsx` 16 個 error 全部**既存**：`react-hooks/refs`／提前引用 `syncCloudPrintOutcomes`，與本次改動無關）｜
`node --test scheduled-pickup + order-notes + self-order-notice` **33/33 pass**。

### ⚠️ 待 J / 未驗證
- `list_merchant_orders` **首屏回應**有冇 `scheduled_pickup_at`（契約 §5.1 有列，但 RPC 白名單由 Ledger 側控制）→ 開收銀台 console 睇
  `[ledger→pos] Realtime orders 表列欄位` 診斷 log 即知。若冇：列表首屏冇預約時間，要等 realtime update 才補上。
- **未 commit／push**（J 指示：佢自己做）。
- 商家實測：列表／快餐卡片／詳情見到「預約單」；補打帳單收據印「預約時間: 09/14 12:15」。

---

## 🔴 修正：已完成／已結帳嘅預約單仍然標「已逾時 159 分鐘」（2026-09-14 14:54）

**J 截圖**：線上訂單列表一張 `取餐碼 001`、`外賣自取`、`14/09/2026 10:37`、`MOP 56`、
狀態**已完成**、支付**已支付（餘額扣點）**，但時間欄第二行寫
**「預約 09/14 12:15 · 已逾時 159 分鐘」**（紅色）＋類型欄「**預約單 · 已逾時**」。

### 根因：逾時判定只比較時間，**完全冇睇訂單狀態**
`scheduledPickupKind(value, nowMs, soonMinutes)` 只有三個維度 —— 有冇值、而家幾點、快到門檻。
`overdue` ＝ `minutes <= 0`，同 `status` 零關係 ⇒ 任何已完結單（`completed`／`cancelled`／本地 `settled`）
只要預約時間過咗就永遠標紅。

**時間戳冇問題**：`scheduled_pickup_at` = 04:15Z（澳門 12:15），截圖時間 14:54 → 差 159 分鐘，
計得完全正確。**亦唔關流程／設定事** —— 純粹係顯示層缺一個維度。

### 修法（狀態維度壓過時間維度）
- `src/lib/pos/scheduled-pickup.ts`：`ScheduledPickupKind` 加 **`closed`**；
  `scheduledPickupKind(value, nowMs, soonMinutes, closed = false)` —— `closed` 時**一律**回 `closed`
  （連「快到」都唔出）。chip 文案「預約單」、配色 `slate-100/500`；時間文字 `slate-400`。
- `src/components/scheduled-pickup-badge.tsx`：新增並 export `isClosedScheduledOrder(order)`
  ＝ `normalizeLedgerStatus(status)` ∈ {`completed`,`cancelled`}（另認本地 `settled`/`refunded`/`partially_refunded`）；
  `ScheduledPickupChip`／`ScheduledPickupTimeText` 內部自動帶埋 `closed`，而且 `closed` 時**唔出相對時間**
  （所以再冇「已逾時 N 分鐘」）。⇒ **三處 UI 自動同步，caller 唔使改**。
- 行為定案：**已完結單只留中性「預約單」標籤 ＋ 預約時間**（保留紀錄），未完成單照舊
  琥珀（仲有排）／深琥珀（≤30 分鐘）／紅（已過）。

### 驗證
`tsc --noEmit` **0 error**｜`eslint`（3 個檔）**0 error / 0 warning**｜
`node --test scheduled-pickup.test.ts` **9/9 pass**（新增 2 個 closed 案例：覆蓋「已逾時」被降級、
「未到都算 closed」、以及 closed 但冇有效時間仍然 `null`）。

### 排查同類問題嘅方法（記住）
1. 先分清楚係**時間戳問題**定**判定邏輯問題**：對一次帳
   `select id, local_order_no, status, scheduled_pickup_at, created_at, updated_at from orders where id='<orderId>';`
   —— 若 `scheduled_pickup_at` 同單上顯示一致，就係判定邏輯（唔係資料）。
2. 再問：**呢個警示對「當前狀態」仲有冇意義？** 凡「時間型警示」（逾時／快到／停留過久）
   都一定要先過狀態維度，否則已完結單會永久洗版。
3. `docs/113` 已寫入紅線：`closed` 參數**必傳**，狀態維度壓過時間維度。
