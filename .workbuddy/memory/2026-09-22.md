# 2026-09-22 工作記錄

## 00:15 — Supabase Dashboard 對帳（21 Sep = 904 MB）

**J 提供截圖**：Usage → Egress。

```
Included in Free Plan  5 GB  /  Used in period  6.89 GB（超額）
21 Sep 單日：PostgREST 904.333 MB (98.6%) ＋ Realtime 12.789 MB (1.4%)
```

### 對帳結果：兩個數字都對得上實測
- **904 MB** ÷ 846 KB（舊 bundle 單次全量）≈ **1 070 次**；÷ 12.8 次/分鐘（實測活躍段）
  ≈ **84 分鐘全速循環**。完全落喺「650 MB/小時」之內（分幾個 burst 跑）。
- **Realtime 12.8 MB（1.4%）** 同早前實測 0.7~1.2% 一致 ⇒ 再次確認
  **唔可以為省流量關 Realtime**。

### 歷史對照（同一張圖）
| 日期 | Egress | 解讀 |
|---|---:|---|
| 17–19 Sep | ≈150 MB/日 | 優化前嘅「正常日」基線 |
| **21 Sep** | **904 MB** | 🔴 ＋750 MB ＝ **舊分頁循環**（基線 ×6）|

⇒ **兩件事唔可以混為一談**：優化處理嘅係「150 MB 基線」；
21 Sep 爆到 904 MB 係「一個新出現嘅循環」。

### 部署時間軸（解讀關鍵）
| 澳門時間 | Commit | 內容 |
|---|---|---|
| 17:18 | `c5db7a0` | `skipQueue=1`、`pos_orders_page` RPC、`[egress]` log |
| 19:30 | `d16c644` | `queue` 身分守衛、退款欄探測、single-flight |
| **23:54** | **`f25af2f`** | **輪詢閘、寫入閘、G2／G3、`x-pos-state-src`、舊版 bundle 偵測** |

⇒ 21 Sep 嗰 904 MB **絕大部分係 17:18 之前嘅舊 bundle** 造成
（舊分頁連 `skipQueue` 都唔識傳：846 KB vs 424 KB）。

### 下一步（要一份新 log）
23:56 之後嘅 Vercel function log 應該見到兩樣**以前冇**嘅嘢：
1. `[egress] pos/state … src=queue-dep|mount|resubscribe|manual`
2. 仲有舊分頁 → `[pos/state] 🔴 偵測到疑似舊版 bundle …` ＋ egress log `legacy=1`

⇒ **收口後預期 ≈ 17 MB/日**（按 0.45 MB / 38.8 min 實測推算）。

### 交付
- 審計報告加 **附錄 C**（Dashboard 對帳 ＋ 部署時間軸 ＋ 驗證方法）：
  `docs/reviews/errwarn-and-call-audit-2026-09-21.md`。

> ⚠️ 承接 09-21：`f25af2f` 已 push；`LEDGER_SUPABASE_SERVICE_ROLE_KEY` 未設
> （配對驗真維持 fail-open，唔影響運作）；**嗰部 Mac 嘅 POS 分頁要閂咗重開**
> —— 如果未做，22 Sep 會繼續燒。

---

## 00:20 — PWA 更新機制（J 打算叫商家「開一次網頁 + 重新安裝 PWA」）

### 結論：可行，但**唔一定要重新安裝** —— 完全閂掉再開就夠
理由（查 `public/sw.js`）：
- **導航請求係 network-first**（`request.mode === "navigate"` 分支）⇒ 一開 PWA 就會拎**最新 HTML**。
- **chunk 係內容雜湊命名**（`/_next/static/immutable/chunks/<hash>.js`）
  ⇒ 新 deploy ＝ 新檔名 ⇒ SW 嘅 cache-first（`caches.match` miss）**照樣會去 fetch**
  ⇒ 新 JS 一定拎得到。舊 chunk 留喺 cache 但冇人引用。
⇒ **一個普通 reload 已經足夠**；重新安裝只係「更徹底」（順手清走舊 cache），成本低所以做都冇壞。

### 🔴 但有三個陷阱（要同商家講清楚）
1. **一定要「完全閂掉」，唔可以只切去背景**：切走再切返，個 window 從未銷毀
   ⇒ **舊 JS 一直留喺記憶體**，呢個正是嗰部 Mac 燒咗 904 MB 嘅原因。
2. **要閂埋 Safari / Chrome 嘅分頁**（唔止 PWA 圖示）—— 因為可能同時有普通分頁開住 POS。
3. **重新安裝唔會自動清 in-memory JS** —— 已開住嗰個 window 仍然係舊碼，要閂咗先。
   （SW 更新有 `skipWaiting` + `clients.claim`，但**換 SW 唔會令已執行嘅 JS 變新**。）

### 驗證（唔使靠估）
開完之後睇 Vercel log：
- 冇再出 `[pos/state] 🔴 偵測到疑似舊版 bundle …`，egress log 亦唔再見 `legacy=1` ＝ **收口**。
- 亦應該見到 `[egress] pos/state … src=` 全部係 `mount` / `manual`（唔係每 4.5 秒一次）。

### ⏸️ 未做（已記錄為中期項）
`public/sw.js` 嘅 `CACHE_NAME = "macau-pos-v20-7-31"` 係**寫死**字串。
目前安全（因為 chunk 內容雜湊命名），但建議改成**建置注入**，免得日後有人以為 bump 咗其實冇。
⚠️ 注意 `public/` 內嘅檔**唔會**經 Next 建置處理 ⇒ 要注入就要加 prebuild script 或者改用 route 派 `sw.js`。

---

## 00:45 — 設置頁「版本號」顯示（已實作）

### 目標
J 要由介面直接睇到「當前運行嘅版本」，以便**確認各商家實際用邊個版本**。

### 核心設計決定：版本號一定要**內聯入 JS bundle**
🔴 如果由 server 提供（例如開 `/api/version`），一個**跑住舊 JS 嘅分頁**一樣會顯示「最新」
⇒ 完全失去作用。內聯 ⇒ 跑舊 JS 就顯示舊版本 ✓

### 機制
- `next.config.ts` 加 `env: { NEXT_PUBLIC_BUILD_ID / _TIME / _ENV }`，
  由 Vercel **自動**系統變數（`VERCEL_GIT_COMMIT_SHA` / `VERCEL_ENV`）算出
  ⇒ **J 完全唔需要設任何 env**。
- ✅ 已查證 Next 16 **仍然支援** `next.config` `env`
  （官方 doc：`next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/env.md`）。
- 🔴 **兩個官方限制**：① `env` 值**一律入 bundle** ⇒ 唔可以放機密
  ② **唔可以 destructure `process.env`**（DefinePlugin 只做字面替換）⇒ 要逐個字面寫。

### 顯示（決策）
- **位置**：設置頁 `/settings` 頂部、`status` 條**下面**、**跨 tab 常駐**。
- **格式**：`版本 1a2b3c4（2026-09-21 23:54 · 正式）` ＋ 右邊細字 `線上最新：xxxxxxx`
  ＋ 兩者唔一致時黃色警示「請完全閂掉此視窗再重新打開」。
- 時間一律**澳門 +8**（`formatMacauStamp` 自寫，唔用 `toLocaleString` 以保確定性）。
- 對照：`1a2b3c4` = `VERCEL_GIT_COMMIT_SHA` 前 7 位 ⇒ 可直接喺 GitHub commit／
  Vercel Deployments 對上；冇 sha 就用 `VERCEL_DEPLOYMENT_ID`（12 位）；本機 → `dev`。

### 實作落點
| 檔案 | 內容 |
|---|---|
| `next.config.ts` | `env` 注入（Vercel 自動變數）|
| 🆕 `src/lib/build-info.ts` | 零 import 純模組（`buildLabel` / `formatMacauStamp` / `describeBuildMismatch` ＋ 觀測伺服器版本 store）|
| 🆕 `src/components/build-version-row.tsx` | 顯示元件（自成一體，唔撐大 4100 行嘅 `device-settings.tsx`）|
| ✅ `device-settings.tsx` | `status` 條下面 render `<BuildVersionRow />` |
| ✅ `app/api/pos/state/route.ts` | 回應加 **`x-pos-build`** 標頭（**零額外請求**）|
| ✅ `pos-app.tsx` | 由 state 回應讀標頭 → 寫入 module store |
| 🆕 `src/lib/build-info.test.ts` | 18 條單測 |

### 保守決定
1. **任一未知 → 唔提示**（本機 `dev` / 標頭讀唔到）—— 唔可以喺開發環境嘈。
2. **只有提示、冇自動 reload** —— 收銀結帳中途 reload 會出事（本專案明確禁用）。

### 🔑 同 `CACHE_NAME` 加固嘅分別（J 問）
**唔係同一概念**：今次係**應用層「門牌」**（觀測：睇得到係邊次建置，**零行為影響**）；
`CACHE_NAME` 係**Service Worker 層「清倉」**（快取正確性，**有行為影響**：首次載入重新下載）。
結論：**今次做完門牌；倉存清理仍然未做**（而目前未做都安全）。

### 驗證
- `tsc` 0 error；`build-info.test.ts` **18/18**；全量 **1055 pass / 0 fail**。
- 🔬 **內聯機制已證實**：用 `VERCEL_GIT_COMMIT_SHA=deadbeefcafe1234 VERCEL_ENV=production`
  跑 `next build`，喺 **client chunk**（`.next/static/chunks/*.js`）同 **SSR chunk**
  （`.next/server/chunks/ssr/*.js`）**兩邊都搵到**：
  `let e="deadbee", r="2026-09-21T16:34:20.668Z", n="production"`
  ⇒ ✅ Next 16 + Turbopack **支援 `next.config` `env` 內聯**；
  ⇒ ✅ SSR 同 client 值一致 ⇒ **冇 hydration mismatch**。
- 🔬 **真瀏覽器實測**（`/settings`）：見到「版本」＋ `dev（2026-09-22 00:36 · 本機）` ✓
  （本機冇 Vercel 變數 ⇒ `dev` / `本機`，符合預期；澳門 +8 時間亦正確）。
- 17 條路由 **17/17、0 問題、0 pageError**。
- ⚠️ 注意：`/settings` 係 `○ (Static)` 且外層有 auth guard ⇒ **預渲染 HTML 唔含頁面內容**
  （只有 14 KB），版本列係 **client 渲染** ⇒ 唔可以靠掃 HTML 驗，要真瀏覽器。

---

## 10:30 — 早上複核（商家已重啟網頁 ＋ Sunmi 已優化）

**樣本**：`supabase_logs (9).csv`（`01:25:49Z→02:25:34Z` ＝ 澳門 **09:25→10:25**，403 行）
＋ Vercel `…02-25-27.csv`（實際內容 `00:24:04Z→00:53:35Z` ＝ 澳門 **08:24→08:53**，
⚠️ **檔名時間又落後 ~2 小時**，同之前一樣）。

### ✅ 兩項確認成功
**① Sunmi APK 已優化（P1＋P2 都做咗）**：
`PATCH pos_print_agents` 22 次、**間隔中位 180.5 秒**（180.3/180.8/181.1/181.5/181.6 極穩定）；
`rpc/pos_claim_print_jobs` 21 次、間隔中位 **180.9 秒**。
⇒ claim 讀到 `nextPollMs`（**P2 生效**）；而且**每 180 秒只有一個來源**
（若心跳 30 秒仍在會夾雜 30 秒 PATCH）⇒ **獨立心跳已刪**（**P1 生效**）。
**3.0 → 0.33 次/分鐘（−89%）達成。** 更新時間大約喺 09:00–09:30 之間
（Vercel 08:24–08:53 仍見 30 秒心跳）。

**② POS 網頁側 94/94 請求全部係 APK**：
`/api/pos/state`、`/api/pos/sync`、`pos/shift`、`store-status`、`topup` **全部 0 次**
⇒ 瀏覽器側乾淨（亦因此冇 `[egress]` 行，屬預期）。全部 200、零 error/warning。

### 🔴 仍未處理：一次 **2.7 分鐘全量拉取循環**（澳門 10:15–10:20）
`_fullstate-bursts`：40 次，其中 **38 次連續 162 秒、13.7/min、間隔中位 4.28 秒**；
六張表（RPC ＋ device_configs ＋ templates ＋ note_presets ＋ print_jobs ＋ queue_events）
**時間戳對齊** ⇒ 仍然係 `loadRuntimeState()`（唔係 ordersOnly）。
- **4.28 秒 ＝ 舊版 bundle 指紋**（新版有 `queue` 身分守衛 ＋ 輪詢閘）；
- **其餘 57 分鐘係 0 次** ⇒ 唔係 24 小時持續，係「開頁就爆幾分鐘」；
- 成本：16 MB（新版 424 KB/次）～33 MB（舊版 846 KB/次）⇒ 若每小時一次 ⇒ 0.4–0.8 GB/日。
⇒ **最可能係某部裝置／分頁仍跑舊 bundle**。**要靠 Vercel `src=` / `legacy=1` 確認**
（要 UTC 02:10–02:30 ＝ 澳門 10:10–10:30 嘅 Vercel log）。

### 其他殘留（分類）
| 現象 | 判斷 |
|---|---|
| `error 23505 pos_print_jobs_once_key_uniq` ＋ `warning 409`（同一件事，差 44ms）| **我方系統、預期行為** —— 內容唯一鍵成功攔重複出紙（`sync/route.ts:1600-1606` **刻意** `ack(true)`）。**功能正確**，只係 log 級別過高。可選優化：`upsert(..., { onConflict:"once_key", ignoreDuplicates:true })`（代價：失去「略過重複出紙」嘅 info log，因嗰個 log 對追重複出紙事故有用，**建議保留現狀**）|
| `DELETE pos_print_jobs` 10 次、間隔中位 0.03s | **商家操作**（打印中心清除）⇒ 正常，唔使處理 |
| `GET pos_shifts` 16 次但最大間隔 **2942 秒** | 正常（180s tick ＋ 開頁/focus）；49 分鐘空檔 ＝ 無人用（輪詢閘生效）|
| `websocket` 101 ×13 | 正常（每次開頁／返前景重訂）|
| `00000` ×25 | **可忽略**（Postgres checkpoint）|
| 退款 `42703` | **0 次** ⇒ ✅ 修復保持生效（修前每 30 秒 1 條）|

### 交付
- 審計報告新增 **附錄 E**（時間線、逐項分類、下一步）。
- ⏭️ 唯一待做：攞 UTC 02:10–02:30 嘅 Vercel log（睇 `src=` / `legacy=1`）。

---

## 10:40 — 🎯 唔需要 Vercel log 都判到「邊部機跑舊 bundle」

J 再畀嘅 `…T02-33-23.csv` **同上一份逐位元相同**（`sha256 22bd76d99bf5c810`, 44,593 B）
⇒ 又係舊窗口（連續三次都係同一個），冇新資訊。

### ⭐ 新診斷技巧（已寫入 skill）：`limit=300` vs `limit=0` 指紋
`skipQueue` 係 **client** 傳嘅 param，而 `state/route.ts` 收唔到時查 `limit=300`、
收到時查 `limit=0` ⇒ **Supabase log 嘅 `pos_queue_events` URL 就係 bundle 版本指紋**：
- `limit=300` ＝ **舊 bundle**（846 KB/次）
- `limit=0` ＝ **新 bundle**（424 KB/次）

工具：**新檔 `tools/analyze-sb-limit-fingerprint.cjs`**（一條命令出表 + 自動標「迴圈指紋」）。

### 實測（09:25–10:25 窗口）
```
🔴 舊 bundle（limit=300）34 次  10:16:03 → 10:20:49  跨度 286s  間隔中位 4.49s  ≈27.4 MB
✅ 新 bundle（limit=0）    1 次  10:15:57           之後完全冇再拉        ≈0.4 MB
```
### ⇒ 結論：**係兩部唔同裝置（或同一部機兩個分頁）**
- **新 bundle 嗰部：拉一次就停** ⇒ ✅ **我哋嘅修正完全生效**
- **舊 bundle 嗰部：一開頁就循環 4.8 分鐘（4.49 秒間隔）** ⇒ 🔴 **仍然跑舊 JS**
⇒ **商家今早重啟嘅係「一部」；仲有另一部未重啟。**

### 搵係邊部機
1. ⭐ 用啱啱做好嘅**設置頁「版本號」**（附錄 D）：叫商家逐部開設置頁 ⇒
   舊版嗰部**會自動出黃色警示**，唔需要 J 逐個對
2. Vercel log（**要新窗口**）見 `[pos/state] 🔴 偵測到疑似舊版 bundle … ip=…`

### 唔建議做（已記錄理由）
- 收緊 `/api/pos/state` rate limit 令舊 bundle 撞 429 ⇒ 舊 bundle 只係 13/min，
  要設到 ~10/min 才咬得到，而 NAT 後多部終端會被誤殺 ⇒ **POS 收到 429 比多打幾個請求嚴重**
- 自動 reload ⇒ 收銀結帳中途 reload 會出事（明確禁用）
⇒ 唯一安全做法：**搵到嗰部機、完全閂掉視窗再開**。
（可選：把過期警示由設置頁擴展到收銀台 `/pos` 頂部 —— 要 J 拍板。）

---

## 11:10 — 收銀台「版本過期」橫幅（已實作）＋「按版本硬擋」評估

### 實作
- 🆕 `src/components/build-stale-banner.tsx`：`/pos` **頁面最頂**（in-flow，**推低內容、唔蓋控制項**）。
  只喺「本機版本 ≠ 線上最新」出現，其餘 `return null`。
- ✅ **有加按鈕**：`立即重新載入`（`min-h-[40px]`）。
  🔴 **確認保護**：冇未完成工作 → 直接 reload；**有購物車／結帳畫面 → 先確認**，
  並講明「會清空 X」＋「已落單／未上雲嘅紀錄**會保留**」（語氣紀律：唔可以講「失去資料」）。
- 🆕 `describeReloadRisk()`（`build-info.ts`，純函式可測）。
- 🔴 **配套排版改動（唔改會爛）**：外層 `flex` → `flex-col`；
  兩個分支嘅根 `h-[100dvh]` → `min-h-0 flex-1`（唔改 ⇒ 底部被 `overflow-hidden` 裁切）。

### 驗證
`tsc` 0；**1067 tests / 0 fail**（`build-info.test.ts` 24 條 ＋
`pos-app-stale-banner.test.ts` 6 條）；真瀏覽器**臨時強制顯示**橫幅 →
`/` 文字 424→512、見到「⚠ 此裝置運行舊版本（」、**17/17、0 問題、0 pageError**、
**截圖確認底部無裁切** ⇒ 臨時改動已還原（`grep TEMP_FORCE` = 0）。
⚠️ 本機 dev **好難自然觸發**（client／server 都係同一個 dev server 產生嘅值）⇒ 要臨時強制。

### ❌「版本唔正確就 block 請求」——**唔建議做**（6 個理由）
1. **分唔到「舊 client」同「唔係我哋嘅 client」**：舊 bundle 根本唔送 build id ⇒
   唯一做法係「冇送就當舊」⇒ **一改就令所有舊裝置即刻死**（kiosk／店員手機／admin 頁）。
2. **版本 ≠ 相容性**：build id 每次 deploy 都變，但多數改動同 API 契約無關 ⇒
   **將每個 deploy 當 breaking change** ⇒ 逼商家每次部署都要即刻重啟全部裝置，否則停業。
3. **被擋嘅正正係你想救嘅畫面**：收銀台 `loadRuntimeState` 一失敗 ⇒
   訂單／枱況／打印全停 ⇒ **冇單比多打幾個請求嚴重**。
4. **部署／快取過渡期**：新舊 instance 並存、CDN／304 ⇒ 會「啱啱開頁就被擋」。
5. **唔係安全機制**：改個 header 就過。
6. **運維成本轉嫁客人**（長期負擔）。
⇒ 一句話：**用停業風險換 egress，唔值。**

### ✅ 建議路徑（L1→L3）
- **L1** 設置頁版本顯示（已做）／**L2** 收銀台橫幅＋一鍵重載（已做）
- **L3（建議下一步）**：**偵測同一部機開咗多個 POS 視窗**（localStorage／BroadcastChannel 心跳）
  → 提示「此裝置有另一個視窗，可能係舊版本」（**唔擋**）。
  🔑 **真正根源唔係「舊版本存在」，而係「同一終端同時開住新舊兩個視窗」**
  （今次實測：10:15:57 新 bundle 拉一次、10:16:03 舊 bundle 開始循環）⇒ L3 直接針對根源，零停業風險。
- **L4** 只讀降級（拒寫入）／**L5** 硬擋 ⇒ **唔建議**。

### 交付
- 審計報告新增 **附錄 F**（實作清單 ＋ 排版陷阱 ＋ 6 個理由 ＋ L1~L5 建議）。

---

## 11:25 — 🎉 最終複核：**全清**（商家完全閂掉 Safari 之後）

**樣本**：`…T03-17-44.csv`（新檔 sha `6701e77a0f915468`；內容 **02:45:24Z→03:13:31Z** ＝
澳門 **10:45→11:13**，28.1 分鐘）＋ `supabase_logs (11).csv`（**10:50→11:16**）。

### Vercel：**20 個請求 / 28.1 分鐘 ＝ 0.7 次/分鐘（原 15.2 ⇒ −95%）**，全部 200、零 error/warning
逐條核對 ⇒ 只有：
- `POST print-agent/claim` **10 次、每 180 秒**（202.45:25→03:13:31）✅
- `GET pos/device-config` **5 次、每 360 秒** ✅
- `/login` ×3（02:46:09–12，一瞬間，有人開登入頁）＋ `/icon` ＋ `/apple-icon`
- 🔴 `heartbeat` = **0 次** ✅✅（心跳完全消失）
- 🔴 `/api/pos/state` = **0 次**、`/api/pos/sync` = **0 次** ✅

### Supabase：**零網頁流量、零 POS 相關錯誤**
`PATCH pos_print_agents` 9 次（中位 **181.29s**）／`rpc/pos_claim_print_jobs` 9 次（**180.97s**）／
`GET pos_print_agents` 5 次（361.9s）／`GET pos_device_configs` 5 次（361.96s）。
**完全 0 次**：`pos_queue_events`（新舊都 0）、`pos_orders_page`、templates、note_presets、
print_jobs、orders、shifts、store_status、online_order_settings ⇒ 瀏覽器側真係停咗 ✓
亦**零 409、零 once_key 重複**（附錄 E.3 嗰條都冇再出現）。

🔥 **最有力嘅比例**：`PATCH pos_print_agents` 9 : `claim` 9 ＝ **1:1**
⇒ 每個蓋章都係 claim 造成 ⇒ **獨立心跳真係冇咗**（若仍在，會係 52:9）。

### 兩點仍要注意
1. ⚠️ **未能確認「POS 頁面開住時」嘅行為**：呢個窗口 `/login` 3 次但 POS 主頁 0 次
   ⇒ **嗰段時間根本冇開 POS 頁** ⇒ 「零」同時代表「冇循環」同「冇人開頁」，兩者未分開。
   最後確認＝要一個**營業中、POS 開住**嘅窗口，連續幾個鐘**冇** `limit=300`（舊 bundle 指紋）出現。
2. 🔴 `error 23505 schema_migrations_pkey` @ **11:15:48**（澳門）
   ＝ **有人重複執行已套用嘅 migration**。若係手動跑 ⇒ 無害；若係自動化重跑 ⇒ 要查。
   **唔關 POS**，係獨立事件。

### 追溯：三件事各自嘅貢獻
| 改動 | 效果 |
|---|---|
| APK 刪心跳 ＋ claim 180s | 3.0 → 0.33 次/分鐘 |
| 輪詢閘 `poll-gate.ts` ＋ 手勢喚醒 | 網頁週期拉取 → 0 |
| **商家完全閂掉舊 Safari 分頁** | **循環 → 0（最後一塊）** |

⇒ **程式改動處理咗「系統自己產生嘅流量」；最後嗰 27 MB/小時 係一部跑住舊 JS 嘅分頁，
只有「閂掉」先解決得到** —— 同附錄 B／D／F 嘅結論完全一致。
21 Sep **904 MB/日** → 現在估計 **10~20 MB/日（−98%）**。

### 交付
- 審計報告新增 **附錄 G**（最終複核 ＋ 逐條請求 ＋ 兩點仍要注意 ＋ 貢獻追溯）。

---

## Admin · POS 工作階段總覽（設計確認稿）

### 交付
- **`docs/mockups/admin-pos-sessions-2026-09-22.html`**（單檔確認稿，5 個畫面：
  ① 按店家分組總覽 ② 平鋪清單＋管理操作紀錄 ③ 強制關閉確認框 ④ Session 詳情抽屜 ⑤ 全部正常）。
- 版面沿用 `AdminShell`（nav 加第 3 項）+ 既有 slate/orange；KPI 固定 5 格。

### 關鍵設計決定（實作要跟）
1. **欄位**：店家（分組標題）／員工+角色+裝置／**版本號（客戶端 build_id + 落後標籤）**／
   開啟時間／**狀態·最後上報**／來源 IP／操作（強制關閉、清除）。
   分組標題加 `N 個工作階段`＋`⚠ 多開`＋`N 個版本落後`；多開組有「關閉多餘（保留最新）」批次鍵。
2. **狀態門檻 ≥ 6 分鐘**（唔可以照抄打印機 30 秒）—— 輪詢閘在 Realtime 通時最長 5 分鐘唔出聲。
3. **版本欄零新基建**：客戶端 `NEXT_PUBLIC_BUILD_ID`（內聯）vs `readServerBuildId()`（`x-pos-build` 同口徑）。
4. 🔴 **session 識別碼必須 `sessionStorage`** —— `localStorage` 同源共用，兩個分頁會撞成同一個 ⇒ 永遠測唔到多開。
5. 🔴 **續期唔加新請求**：掛既有**寫入**請求順手更新 `last_seen_at`＋60 秒節流；**GET 唔寫 DB**
   （沿用 `loadPairedAgent(recordActivity)` 教訓）。
6. 🔴 **強制關閉是軟踢**：唔拒收結帳、唔自動 reload ⇒ 分頁下次請求收到訊號後顯示橫幅 + 停輪詢 + 要求重新登入。
   確認框要分開列「會發生／唔會發生」（唔會刪單、購物車保留、唔影響其他 session 與打印中繼）。
7. 註冊點＝`/api/ledger/login`（server 端唯一權威知道 `merchantId` 嘅地方），零新增 credential 交換。
8. 與今日較早嘅 **L3 建議**（偵測同機多開視窗）係同一問題嘅兩面：L3 係本機提示，本頁係
   **跨店、管理員側**可見版本 + 可下達關閉。

### 確認稿驗收（全部綠）
- `dom-smoke-test.js`：5/5 頁籤、冇 undefined、冇 `@media`、div 配對 124 對。
- **`check-canvas-fit.js`（本次新增到 skill）**：5 個畫面零滾動／零截斷／零超出畫布。
  修過嘅真問題：總覽 originally 918 vs 可用 848 ⇒ 最底卡片下溢 44px（加高畫布至 1200×980 ＋ 收行高解決）。
  另外捉到「平鋪表表頭同列用兩組唔同欄寬」（工具捉唔到，靠截圖睇出）＋ 分組列表原本冇表頭／冇明文狀態欄。
- 新增 skill 腳本：`pixel-stable-ui-mockup/scripts/check-canvas-fit.js`（已寫入 SKILL.md 檢查清單）。
- 環境：`puppeteer-core` 已裝在 `~/.workbuddy/binaries/node/workspace`（npm 要經 `npm-cli.js` 跑）。

---

## 11:00–12:00 — POS 工作階段功能（設計→實作完成）

### 交付（全部已跑綠：tsc 0 error、`node --test` 1127/1127、真 Chrome 驗證）
| 檔案 | 內容 |
|---|---|
| `supabase/migrations/0047_pos_sessions.sql` | **⚠️ 未跑**（要人手貼 Supabase SQL Editor） |
| `src/lib/pos/session-record.ts` + `.test.ts` | 零 import 純邏輯：門檻／分類／分組／KPI／相對時間（54 單測） |
| `src/lib/pos/session-key.ts` + `.test.ts` | 分頁識別碼（`sessionStorage`）＋ `rotatePosSessionKey()` |
| `src/lib/pos/session-revoked.ts` + `.test.ts` | 「已被管理員關閉」模組 store |
| `src/lib/pos/session-registry-server.ts` | register／touch／list／revoke／clear（**永遠唔 throw**） |
| `src/app/api/admin/sessions/route.ts` | GET 列表（＋店名）／PATCH `revoke` \| `clear` |
| `src/app/admin/sessions/page.tsx` | 頁面：KPI 5 格／篩選／分組＋平鋪／詳情抽屜／確認框／操作紀錄 |
| `src/components/pos-session-revoked-banner.tsx` | 收銀台橫幅（in-flow、**唔自動 reload**） |
| 改動 | `write-gate.ts`（新 reason `session-closed`）、`sync-flush.ts`（廣播）、`pos-app.tsx`（偵測標頭＋駁旗標）、`poll-gate-client.ts`（撤銷＝冇 session ⇒ 停輪詢）、`pos-sync-auth.ts`（帶兩個標頭）、`login-screen.tsx`（輪替 key＋註冊）、`admin-shell.tsx`（第 3 項導覽）、3 條 POS route |

### 🔴 實作學到嘅（唔可以重蹈）
1. **`sessionStorage` 係命門**：用 `localStorage` 同源共用 ⇒ 兩個分頁撞成同一行 ⇒
   「多開」永遠偵測唔到（靜默失效）。同一分頁 reload 要**保留** key（唔可以當新 session）。
2. **門檻 ≥ 6 分鐘**：輪詢閘 Realtime 通時最長 5 分鐘唔出聲，照抄打印機 30 秒 = 全店假離線。
   已加守衛測試釘住「live 門檻 ≥ `POLL_INTERVAL_PUSHED_MS` + 60s」。
3. **零新請求**：註冊搭 login、續期搭既有請求（POST 60s／GET 5min 節流）；
   **GET 只續期唔建立**（保持「讀取唔創造狀態」）。已撤銷嘅 row **唔再續期**
   （否則 admin 頁永遠「使用中」，管理員會重複撳）。
4. **軟踢唔硬斷**：`x-pos-session-closed: 1` ⇒ 橫幅＋停輪詢；`write-gate` 只擋新生意
   （結帳／退款／刪單／出紙／`ledger-` 鏡像照放行 —— 客人走唔到更嚴重）。
5. **復原＝重新登入**（`clearAuthSession()` + 回 `/login`）：單純 reload 冇用（身分仍然被撤銷）。
6. 三個「唔可以」：唔可以喺 GET 建立 row；唔可以令 registry 失敗阻擋落單（全部 try/catch）；
   唔可以因為「版本未知」就標「落後」（舊 client 冇傳 = 唔嘈）。
7. 🔴 **真瀏覽器驗證：唔可以自己砌 POS API mock**（實測爆 `localFloors is not iterable`）
   ⇒ 要**代理真實 dev server 回應再加標頭**。已寫入 skill `pos-ui-live-verify` 第三之四／五節。
8. 實作時捉到嘅小問題：角色顯示英文枚舉（改中文）、`dev` 建置 id 誤觸版本警告（已排除）、
   開關冇旋鈕、撤銷時間換行。

### 交付截圖
`docs/mockups/admin-sessions-impl-verify-2026-09-22/`（列表／確認框／詳情抽屜／POS 橫幅）。

---

## 12:00 — 打印投訴排查（印唔出 ＋ 非常慢）→ `docs/reviews/print-out-failure-and-latency-2026-09-22.md`

**兩個獨立問題，唔可以當同一個查。**

### 🔴 A. 部分單據永遠印唔出（迴歸，2026-09-11 起，持續 11 日）
- 根因：`1e08343`（2026-09-11 15:07）把 `appendPrintJobs()` **同名反轉語義** ——
  舊版＝落本機＋推 `PRINT_JOB_CREATED` 上雲；新版＝**只寫本機**（同步版改名 `appendPrintJobsWithSync`）。
  5 個呼叫端沿用同名 ⇒ 靜默由「印得出」變「永遠印唔出」，tsc／lint／測試全綠。
- 受影響（全部**早於**該 commit 已存在，`git show 1e08343^` 可證）：
  ① `pos-orders.ts:248` **返結單** ② `pos-orders.ts:314` **確認自助單**（kiosk／掃碼 → 廚房單＋標籤單＋顧客小票）
  ③ `pos-app.tsx:1905` realtime 新自助單 ④ `pos-app.tsx:1949` 自助單加單 ⑤ `pos-app.tsx:1488` backfill 補建。
  ✅ 唯一合法：`print-jobs.ts:686` `printKioskReceiptForOrder()`（kiosk 本機小票，docs/87 §3.1）。
- **零症狀機制**：打印中心「未上雲」徽章靠 queue 有未 synced 事件；呢條路**連事件都冇** ⇒ 冇徽章；
  `syncCloudPrintOutcomes()` 只按 job.id 配對 ⇒ 雲端冇行 ⇒ 永遠「已發送」。
- 修法：5 處改 `appendPrintJobsWithSync`；另建議加源碼掃描守衛測試 ＋ 自助單補 `onceKey`（防兩條路各出一張）。

### 🔴 B. 出紙非常慢（egress 優化副作用）
- `SUGGESTED_CLAIM_MS = 180_000`（`claim/route.ts`，`f25af2f` 09-21 23:54）⇒ APK 認領**每 3 分鐘一次**
  （生產實測 median 180.5s）；舊值約 30–60 秒。**APK 今日 09:00–09:30 生效，同商家投訴時間吻合。**
- 放大：APK `claim` **一次最多 5 張**（`p_limit`）⇒ 高峰約 1.7 張/分鐘吞吐。
- UI 遲滯：`print-center.tsx:448` 結果回填 8s → **30s**。
- 叫醒路徑（`RealtimeClient.kt` 訂 `pos_print_jobs` INSERT）通＝1–3 秒；唔通＝等足 180 秒。
  ⇒ 建議 **B1 自適應**：`jobs.length >= limit` 就回 `3_000`，否則維持 `180_000`（唔需出 APK、閒時唔反彈）。
- ⚠️ 現役 APK 源碼本機 **冇**（5 個副本全部無 `nextPollMs` 實作，但生產 log 證明有讀）。

---

## 13:30 — P0 ＋ B1 **已實作**（打印修復）

### 改動
| 檔案 | 內容 |
|---|---|
| `src/lib/pos-orders.ts` | 返結單（`reopenPosOrder`）＋ 確認自助單（`confirmSelfOrder`）改 `appendPrintJobsWithSync`；移除 `appendPrintJobs` import |
| `src/components/pos-app.tsx` | backfill 補建／realtime 新自助單／自助單加菜 3 處同上；`appendPrintJobsWithSync` 由 `@/lib/pos/print-job-enqueue` 匯入 |
| `claim/route.ts` | `SUGGESTED_CLAIM_MS` 180_000 → **30_000**；新增 `CLAIM_BACKLOG_MS = 5_000`；`nextPollMs = jobs.length >= limit ? 5_000 : 30_000` |
| `heartbeat-contract.test.ts` | 守衛升級：掃**所有** `*_MS` 常數值域 5_000~180_000 ＋ 釘住雙檔判斷式 |
| `src/lib/pos/print-enqueue-callsites.test.ts` | **新增**守衛：掃全 `src/` 嘅 `appendPrintJobs(` 呼叫端，白名單只准 `lib/print-jobs.ts`（2 處） |

### 🔴 實作學到嘅（唔可以重蹈）
1. **APK 對 `nextPollMs` 有硬性有效範圍 `5_000..180_000`**（`resp.optInt(...).takeIf { it in 5_000..180_000 }`）
   ⇒ 我最初想用 `3_000` 追趕，會**靜默 fallback 30 秒**（以為調快咗其實冇）。任何節奏值都唔可以細過 5 秒。
2. **寫「源碼掃描型」守衛測試時，掃描器會捉到自己**：regex literal `/appendPrintJobs\(/` 本身就係一次匹配。
   ✅ 解法＝用字串拼接砌 needle：`new RegExp(\`${"append" + "PrintJobs"}\\s*\\(\`)`，
   令原始碼唔會出現「函數名＋(`」呢個連續字串（同檔嘅字串訊息亦要避免寫成 `` `fname(` ``）。

### 驗證
`tsc --noEmit` 0 error／`eslint` 改動檔 0 error（9 warning 全部係 pos-app 既有）／
`node --test` **1130 passed, 0 fail**（原 1127 ＋ 新增 3）。

### 前後差異（已寫入報告 §7）
- P0：5 條路徑由「永遠冇紙」→ 正常；**只影響修復之後新發生嘅單**（舊殘留唔會自動補印）。
- B1：單張最壞 180s → 30s；10 張 6 分鐘 → ~35 秒；請求 0.33 → 2 次/分鐘（**仍 < 優化前 3.0**）；
  ＋約 1 MB/日。⚠️ 若 Realtime 叫醒正常，本身已係 1–3 秒 ⇒ B1 只係收窄最壞情況。
- 未做：B3（UI 回填 30s）、P2（未上雲重推掣）、B2（APK 批次 5→20，要出 APK）。

---

## 13:40 — Egress 覆核（商家畀兩份 log）＋「本地優先」重新設計方案

樣本：`supabase_logs (12).csv`（**⚠️ 匯出上限 1000 行**，所以窗口只有 12.3 分鐘）＋
Vercel `…T05-32-31.csv`（304 行 / 150 requestId）。
腳本：`tools/_egress-recheck-20260922.cjs`、`_egress-deep-20260922.cjs`、`_egress-byip-20260922.cjs`。
報告：`docs/reviews/egress-redesign-local-first-2026-09-22.md`。

### 🔴 結論：同步邏輯「細份咗但冇少做」
- Supabase 側 **81 次/分鐘**；六表全量拉取 **71 次 / 12.3 分鐘 = 5.76 次/分，中位間隔 4.64 秒**。
- Vercel `[egress]` 實測：窗口 7.4 分鐘 **19.1 MB**，其中 **ip=60.246.53.111 佔 17.6 MB（92%）**，
  全部 **903 KB／次 × 20 次**，`legacy=1 skipQueue=0 queue=300`，集中喺 13:24:15–13:25:47（92 秒）
  ⇒ **11.5 MB/分鐘 ≈ 690 MB/小時**。
- 新版（`skipQueue=1`）每次 **412 KB**（已 −54%），但**拉取次數冇收斂** ⇒ 一部舊分頁就足以打爆配額。
- 商家講「~500 MB/日」≈ **一部舊 bundle 分頁開 ~45 分鐘**；長開一日 = **5.5 GB/日**，5 GB 免費額 2–3 日用完。
- ⭐ **最有用嘅取證指紋**：Vercel log 嘅 `[egress]` 行自帶 `bytes= / mode= / orders= / queue= /
  printJobs= / skipQueue= / legacy= / ip= / src=` ⇒ **唔需要 Supabase log 就分得出「邊部機、幾大、幾密」**。

### 次大來源（同窗口）
`pos_print_jobs` `limit=200 select=*` **222 次/12.3 分（18 次/分）≈ 1,621 KB/分**；
`pos_queue_events limit=300` 96 次（舊版才有）；`pos_sessions` 每 7.3 秒（應搭既有寫入請求）；
`pos_void_stale_print_jobs` 每 60 秒；`pos_shifts`/`pos_store_status`/`online_order_settings` 20–28 秒輪詢。
`refund_records` migration 未跑（5 次 error）；`pos_print_agents` **7 部未撤銷**並存。

### 提出方案（local-first）
本地 = 真源；雲端只做「出紙中繼 ＋ 跨裝置交換 ＋ 異地備份」。
上傳觸發：① 要出紙 ② 線上單 ③ 收錢 ④ 交班 ⑤ **定時增量備份（必須保留 —— 防 iPad 壞機）**。
下載觸發：① 開頁只拉設定（~25 KB）② Realtime 單筆 ③ 第二台 iPad `since=` 差量 ④ 換機全量（明確動作）。
核心 API：`/api/pos/state` 加 `since` / `scope=bootstrap|changes|full`。
估算：1.9 GB/日 → **15–20 MB/日（−98~99%）**；5 GB 由「唔夠 1 間店」變 **可容 8–16 間店**。
止血（唔改架構）：server 見到 `legacy=1` ⇒ 回縮水 payload ⇒ 690 MB/小時 → <30 MB/小時。

---

## 14:00 — Egress P0–P4 **全部實作** ＋ Admin「雲端用量」頁

報告：`docs/reviews/egress-p0-p4-and-traffic-dashboard-2026-09-22.md`

### 交付（全部已跑綠：tsc 0 error、eslint 0 error、`node --test` **1169 passed 0 fail**）
| 項目 | 內容 | 效果 |
|---|---|---|
| **P0** | `state/route.ts`：legacy（非 ordersOnly 且冇 skipQueue）**強制 `queue=0`** ＋ `legacyQueueOff` log ＋ `x-pos-legacy-pull` 標頭 | 903 KB → **422 KB／次（−53%）** |
| **P1** | `since` 增量：新純模組 `src/lib/pos/state-sync-watermark.ts`（12 單測）＋ 執行層 `state-sync-client.ts`；開頁／重連／訂單頁全部走增量 | 412 KB → **~30 KB（−93%）** |
| **P2** | `print-jobs/status`：sweep 60 秒 → **5 分鐘**；兩條查詢 limit 200 → **120**；改用 `jsonWithEgressLog` | 每分鐘 1 RPC → 每 5 分鐘 |
| **P3** | `local-orders-panel.tsx` 亦用同一套水位 | 296 KB → **~3 KB（−99%）** |
| **P4** | 確認 `use-store-status` / `use-merchant-order-config` / 輪詢閘**本身冇 polling**（event-driven + Realtime）；只有 sweep 同 topup 按時間 | — |
| **計量** | migration `0048_pos_egress_daily.sql` ＋ `egress-meter-server.ts`（記憶體緩衝、60 秒/25 次/512 KB 節流 flush、`after()` 後寫、**冇 migration 自動停用**） | — |
| **Admin 頁** | `/admin/traffic`（＋ nav 第 4 項）：KPI 5 格 ＋ 逐店表 ＋ 14 日走勢 ＋ 頭幾條路徑；純邏輯 `egress-usage.ts`（14 單測） | 逐店用量一眼睇到 |

### 🔴 實作學到嘅（唔可以重蹈）
1. **增量 payload 唔可以當全集用** —— `pos-app.tsx` 孤兒單對賬判準係「雲端冇呢張單」，
   增量之下照跑會**把全店未變更過嘅單一次過隔離**（收銀枱面清空）。已加 `if (!payload.incremental)`
   ＋寫成 source 掃描守衛（`state/state-incremental-contract.test.ts`，13 條）。
2. **舊版止血只可以「少行數」** —— 唔可以回 4xx／空 body／改欄位名（舊分頁會完全拉唔到）。守衛明文禁止。
3. **手動「更新」必須 `forceFull` 而且繞過 single-flight** —— 否則會被 in-flight 增量吞掉，
   撳完「乜都冇變」而孤兒單對賬永遠唔再跑。
4. **`truncated`（撞 limit／查詢失敗）一定要清水位 ＋ 補拉全量** —— 唔可以靜默截斷（漏單永久化）。
5. **水位只可以喺成功回應之後更新**（失敗／401 唔可以推進）。
6. ⚠️ 源碼掃描型測試嘅**相對路徑要數清楚層數**：`src/app/api/pos/state/…test.ts` 去 `src/`
   要退 **4** 層（我 첫 次寫 2 層，readFileSync 直接 throw，測試檔連載入都失敗）。
7. ⚠️ 新 helper 改名／加參數會撞既有 source 掃描測試（`pos-app-queue-identity.test.ts` 咬死
   `runLoadRuntimeState(src: string)` 同 `loadRuntimeState("manual")`）⇒ 改簽名要一齊更新守衛。
8. ⚠️ 本機 **冇 `sleep`**（coreutils 缺失）⇒ 要等 dev server 就緒要自己寫 Node 輪詢
   （見 `tools/_smoke-pages-20260922.cjs`，亦會 `taskkill /T` 收尾）。

### 部署（未做）
① Supabase SQL Editor 手動貼 **0048**（唔跑都得，admin 頁顯示「計量尚未啟用」）
② commit ＋ push ＋ Vercel Redeploy（P0/P1/P2 係 server 端，一 deploy 即生效；客戶端要 reload）。
