# 2026-09-21 工作日誌

## 排查：訂單 001 一次過出 4 張收據（重複出紙）

**取證（雲端真源 `pos_print_jobs`，store 8291f843 / 表嫂美食）**
- 同一張單 `order_id = ledger-e26fa77b-df1d-4e67-978d-2358c2574c67`（= POS `001`，A01，settled，線上已支付 44）
  近 3 小時共 **7 張 job**：
  - kitchen ×3 → printer-861f2f6e（10:42:37 ×1、10:56:33 ×2 同秒）
  - receipt ×4 → printer-22a790b1（10:58:00.373 / 10:58:01.324 / 10:58:01.340 / 10:58:06.711）
- 4 張收據之中，**2 張 `table_name = 堂食`（未排位投影口徑）、2 張 = `A01`（已排位本地單）**
  ⇒ 證明係**唔止一條路徑**各自建 job，而且建 job 時手上嘅訂單版本唔同。
- 對照：同日 `訂單02`（本地單 order-9677ab4d）＝ **kitchen 1 + receipt 1，完全正常**
  ⇒ 重複只發生喺**線上單（Ledger 鏡像）**。
- DB `kind` 欄**永遠 NULL**（`/api/pos/sync` 的 `contentPatch` 冇寫 kind）→ 內容鍵去重唔可以用 kind。

**根因（結構性）**
- 出紙＝客戶端各自 `uid("print")` 建 job → 上雲 → 中繼機逐行 claim。唯一去重鍵係 `job.id`；
  **冇內容鍵**（`mergePrintJobs` 只按 id），所以「同一單／同類單／同一機」可以無限次建新 job。
- 收據三條獨立入口：
  1. Ledger Realtime echo（status→completed 且已付）→ `printReceiptForLedgerOrderOnce`
     ⚠️ 只有 **60 秒 in-memory Set**（`print-jobs.ts` `rememberOnce`），**per 分頁**，reload 即失效。
     ⚠️ **「排位」會間接出收據**：`assignLedgerOrderToTable` → `syncOnlineDineInCompletion` 爬梯到
        `completed` → echo 觸發已完成收據。
  2. POS 主介面結帳／免單／「客人已支付，完成訂單」→ `pos-app.tsx:4037 printReceipt()` —— **零去重**。
  3. 手動「補打帳單」→ 每次點擊新一張（設計如此）。
- 「60 秒 once-guard 係 per realm」+ 兩張堂食收據相隔 0.97s ⇒ 當時極可能**兩個 POS 視窗／終端**同時開住
  （另外兩部機共用同一 `printer_id` 嘅可能：`/api/pos/device-config` GET 冇 store filter，會讀到別台機嘅 printers）。

**未做（等商家拍板）**
- 建議：DB 對 `pos_print_jobs` 加內容唯一索引 `(store_id, order_id, printer_id, printer_group, ticket_type)`
  + route 改 ON CONFLICT DO NOTHING；`printReceipt()` 一併行「已出過紙」帳本；收窄 echo 收據為「只補漏」。

**工具**：`tools/_probe-dup-receipt-20260921.cjs`（近 3 小時 job + 按 order_id 分組）、
`_probe-dup-receipt2-20260921.cjs`（單張 job 完整欄位 + pos_orders + 單機 24h）、
`_probe-dup-receipt4-20260921.cjs`（近 7 日每單出紙張數統計）。全部唯讀。

---

## ✅ 已落實：內容唯一鍵去重（商家拍板方案 1）

**商家確認**：「有在打開兩個視窗」⇒ 證實 60 秒 once-guard 係 per realm 嘅判斷。

**新增／改動（11 改 + 4 新；tsc 0 error、866 test 全綠）**
| 檔案 | 內容 |
|---|---|
| `src/lib/pos/print-dedupe.ts`（新，**零 import**） | 鍵 `orderId\|onceKey\|printerId`；`dedupeOnceJobs` / `mergeOnceKeys` / `printOnceContentSignature`（FNV-1a） |
| `src/lib/pos/print-dedupe.test.ts`（新，14 test） | 4 張→1 張、世代唔同要再出、手動唔可攔、改單簽名要變 |
| `src/lib/types.ts` | `PrintJob.onceKey?` |
| `src/lib/storage.ts` | `printedOnceKeys` 帳本（上限 600，跨視窗共用） |
| `src/lib/pos/print-job-enqueue.ts` | `claimOncePrintJobs()`；`appendPrintJobsWithSync` 內去重 |
| `src/lib/print-jobs.ts` | 收據 `receipt:${reopenCount}`（`opts.once`）；廚房 `onceKey+內容簽名`；`findPosOrderForLedger` 枱號改以本地鏡像單為準 |
| `src/lib/ledger/ledger-pos-bridge.ts` | 接單／採納／補印兜底一律 `kitchen:normal:${reopenCount}` |
| `src/components/pos-app.tsx` | `printReceipt` 帶 once；`enqueuePrintJobs` ＋落單路徑各自 `claimOncePrintJobs` |
| `src/lib/use-staff-order.ts` | 店員手機新單帶 onceKey（加菜唔帶） |
| `src/lib/supabase-errors.ts` | `isUniqueViolationError`（23505） |
| `src/app/api/pos/sync/route.ts` | 寫 `once_key`；23505 → 略過但 `ack(true)`；42703 → 拔欄重寫 |
| `src/app/api/pos/state/route.ts` | printJobs 補 `printerId` + `onceKey`（令本機 backfill 都認得出） |
| `supabase/migrations/0045_pos_print_jobs_once_key.sql`（新） | `once_key` 欄 + partial unique index（**未跑**） |
| `docs/113-agent-gotchas.md` | 新增該節（鐵律 + 三條 entry + 枱號快照） |

**未做／待跟進**
- 🔴 **migration 0045 未跑**（code 已做 42703 降級，未跑都唔會壞）。
- ⚠️ 順帶發現：Kiosk 自助單嘅廚房單路徑（`pos-app.tsx` 約 1204 / 1620）用**本機限定**
  `appendPrintJobs()`（唔上雲）⇒ 中繼通道下可能零出紙；未改（超出今次範圍，等商家決定）。
- ⚠️ 殘留：標籤單刻意唔去重；加菜/退菜/返結亦唔去重（合法重複）。

