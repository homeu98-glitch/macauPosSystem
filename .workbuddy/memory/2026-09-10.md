# 工作日誌 2026-09-10

## 訂單頁：線上/線下訂單由卡片改為列表（2026-09-10）
- 需求：訂單頁（`orders-hub`）線上＋線下訂單由卡片改為與報表訂單明細一致嘅**列表**。欄位沿用、每單一行、操作釘最右、欄位對齊易掃描。
- **版面**：`orders-hub.tsx` 由 `grid-cols-1 lg:grid-cols-2`（左右分欄）改為 `grid-rows-2`（上下分區、各佔全寬）。原因：8 欄塞半欄會逼爆；全寬先做到固定欄寬。header 文案改「上：會員通線上訂單 · 下：店內線下訂單」。
- **列表**：`local-orders-panel.tsx` 同 `online-orders.tsx` 各加 `<table className="w-full min-w-[1080px] table-fixed">`，共用常數 `TH_CELL`（sticky top-0 bg-slate-50）/ `TD_CELL`（px-3 py-2 align-middle）。
  - 線下 8 欄：訂單號118 · 餐台104 · 時間116 · 菜品(彈性) · 金額150(右) · 狀態108 · 來源96 · 操作232(右)。
  - 線上 8 欄：訂單號118 · 類型·客戶150 · 時間116 · 菜品(彈性) · 金額150(右) · 狀態108 · 支付132 · 操作224(右)。
  - 金額欄 `tabular-nums` 右對齊；折扣資訊（已優惠/原價 line-through）收喺金額下方小字，唔另開欄。
  - 狀態藥丸顏色/文字沿用原本，縮到 `text-xs` + `h-2 w-2` dot；`changeRequestLabel` 由 `renderOrderActions` 帶入操作欄（冇遺漏）。
  - 行 `border-t border-slate-100 even:bg-slate-50/60` 斑馬紋；窄屏表格自己橫向滾動（`min-w-[1080px]`）。
- **踩過嘅坑**：線上訂單表格初版冇包空狀態條件 → 空列表會渲染「只有表頭嘅空表格」。已改為 `filteredOrders.length > 0 ? (<div><table>…) : null`（線下版本來用 ternary 冇事）。
- 驗證：tsc ✓ / eslint 0 error（1 條既有 exhaustive-deps warning）/ next build ✓。

## 打印記錄（/prints）由卡片改為列表（2026-09-10，跟訂單頁一致做法）
- 範圍：只改「打印記錄」tab（`print-center.tsx` `activeTab === "records"`）嘅任務卡片 → `<table>`。模板設計 tabs（收據/標籤/廚房/自助點餐機）係表單+預覽，**唔改**。只改 UI，零邏輯改動（所有 onClick／status 分支原樣搬入表格）。
- 8 欄（沿用原本卡片欄位）：訂單號124 · 餐台104 · 打印機132 · 票種112 · 時間140 · 失敗原因(彈性) · 狀態112 · 操作196(右)。`min-w-[1080px] table-fixed`。
- 原本卡片將「餐台 · 打印機 · 票種」擠一行 → 列表拆成 3 個獨立欄（列表要欄位對齊掃描）。
- 「失敗原因」原本係卡片內紅色塊（`whitespace-pre-wrap`）→ 改為獨立彈性欄，`line-clamp-2` + `title` 顯示全文（modal 冇顯示 lastError，所以列表要保留）。
- 狀態藥丸加 `h-2 w-2` dot（同訂單頁一致）；操作欄 `rounded-xl text-xs`：查看 = `bg-slate-900`（主掣同訂單頁一致）、重試打印 = `bg-orange-500`、重打整單 = 白底 ring（原本「查看」樣式讓位）。
- 常數 `TH_CELL`/`TD_CELL` 加喺 L224（`print-center.tsx` 頂部 helper 區）。
- ⚠️ 注意：`print-center.tsx` 有 8 個**既有** lint 問題（5 error + 3 warning：L232 offlineMode unused、L369 react-hooks/immutability、L377 exhaustive-deps、L418 usingSampleOrder unused、L856 react-hooks/refs ×4），全部喺改動範圍外；行號正好 = 原本行號 +4（我加嘅常數偏移），可據此快速判斷係既有而非新引入。eslint error 唔阻 `next build`。

## 訂單明細（共用組件）改回列表 + 補「狀態」表頭（2026-09-10）
- 背景：`order-detail-list.tsx` 曾被 commit `6520e1f`（2026-09-09 20:32，訊息 "update"）由原本 `<table>`（餐台/應收/實收/收款類型/收銀員/結賬時間 6 欄）改成「每單一張豎排卡片 + label–value」，同時**新增**「訂單號」同「取餐碼」兩個欄位。呢個唔係我今次對話做嘅改動。
- 現已改回 **一條 row 對應一條 record** 嘅 `<table>`，8 欄：訂單號118 · 狀態120 · 餐台(彈性) · 收款類型128 · 收銀員132 · 結賬時間168 · 應收130(右) · 實收130(右,emerald)。`min-w-[1080px] table-fixed`；表頭 sticky、行 `border-t border-slate-100 even:bg-slate-50/60`、金額 `tabular-nums`。
- **「狀態」欄**：原本「取餐碼」badge 同訂單號擠同一格、**冇自己嘅表頭**（用戶指出表頭同資料對唔齊）→ 而家獨立成欄並補上「狀態」表頭（`TH_CELL w-[120px]`）；線上單顯示 `取餐碼 XXX` 橙色藥丸，線下單顯示「—」保持對齊。
- **合計列改為 `<tfoot>`**（原本係表格外嘅獨立 div），`colSpan={6}` + 應收/實收兩格，同上方欄位天然對齊。
- ⚠️ **邊框/滾動容器由呼叫方提供**（同「支付方式分項」一致）：組件只出 `<table>`，**唔自己加 border/rounded**。兩個呼叫方（`shift-page.tsx` L1218、`restaurant-daily-report.tsx` L2349）本來就有 `max-h-[420px] overflow-auto rounded-xl border border-slate-200`（已補 `bg-white`）。若組件自帶邊框會變雙重邊框；若自帶 `overflow-x-auto` 會令表頭 sticky 綁錯滾動容器。
- 驗證：tsc ✓ / eslint 0 error / next build ✓。

## 「改咗但畫面唔變」根因：PWA service worker 快取（2026-09-10）
- 症狀：用戶報「交班紀錄／報表嘅訂單明細仍然唔係 table」。**代碼同線上部署其實都已經係新版**——`curl` 生產站 `macau-pos-system.vercel.app` 抽 `/_next/static/immutable/chunks/*.js`，`0h9z09ptysxnl.js`（/reports）同 `41m3aomzywbqe.js`（/shift）都揾到 `children:"狀態"` + `tfoot` `合計（`，舊卡片字串 `children:"應收金額"` 完全冇。
- **成因 ①（即時）**：長時間開住嘅 session（POS tab / iPad PWA）一直用部署前已載入嘅舊 JS；App Router **client-side 換頁唔會重新下載 bundle** → 要整個頁面 reload 先換到代碼。
- **成因 ②（系統性，已修）**：`public/sw.js` 舊版對**所有非導航 GET 一律 cache-first 且永不 revalidate**，包括 Next.js App Router 嘅 `?_rsc=` RSC payload 同 `/_next/static/**`；而 `CACHE_NAME` 寫死 `"macau-pos-v20-7-31"`（7 月 31 日起冇 bump）→ 舊 RSC payload 會一直指向舊 chunk 圖，**部署咗都會渲染舊組件**。另 `cache.addAll` 一條 404 就整個 install 失敗，舊 SW 永遠唔退場。
- **修法**（`public/sw.js` 已重寫）：`CACHE_VERSION = "2026-09-10"`（**每次部署前要 bump**）；導航 = network-first；`?_rsc=` = network-first + cache fallback；`/_next/static/**` = cache-first（內容雜湊、不可變，安全）；其他同源靜態 = stale-while-revalidate；跨域／`/api/` 一律 bypass；install 改逐條 `cache.add().catch()` 容忍失敗。
- **教訓**：呢個 repo「改咗 UI 但用戶仲見舊樣」第一件事唔係查代碼，而係**直接 curl 生產站抽 chunk 驗證實際派咩版本**（`grep -o 'src="/_next/static/immutable/chunks/[^"]*\.js"'` 再逐個下載 grep 特徵字串）。驗證手法可重用。

## 交班結算單「唔似 template」根因：job 冇 template 快照 → 通道 fallback 硬編廚房渲染（2026-09-10）
- 症狀（真紙）：抬頭印 `Macau POS` + `【廚房單】`、多印一行 `打印機: …`、**每一行尾都帶 `  x1`**、分節標題變純文字行 `— 店內（今日）— x1`、無字型/粗體/對齊/模板分格線。
- **根因（已端到端核實）**：
  1. `src/components/shift-page.tsx:167 shiftDetailToLines()`（交班）同 `:462 buildShiftPrintLines()`（重打）把整張結算單壓成**一串純文字**，再 `items: lines.map(l => ({name:l, quantity:1}))`（L576 / L859）——**無 `content`、無 `template`**，且刻意塞 `quantity:1`。
  2. `sync/route.ts:560-580` contentPatch 寫 `template: null` / `content: null`，**亦從不寫 `kind` 欄**（0020 欄位歷史遺漏，見 docs/103）。
  3. Hub `relay/JobRunner.kt:123-158`：`kind = row.optString("kind")…?: job.template?.kind ?: "kitchen"` → template 為 null、kind 為空 → **`renderKitchenTicket`**（`EscPosRenderer.kt:202-233`）。該 fallback `buf.line(storeName ?: "Macau POS")` + `ticketTypeLabel` → `【廚房單】`，逐行 `${item.name}  x$qty` —— **同出紙逐字吻合**。HubHttpServer.kt:170-173、SdkPrinter.kt:123-126 同一分支。
- 本 repo 預覽同樣退化：`print-center.tsx:1362-1371` 冇 template → `KitchenTicketPreview`（`kitchen-ticket-preview.tsx:28` 用 DEFAULT_KITCHEN_TEMPLATE）→ 預覽同出紙一致地錯，同「模板設計」唔一致。
- **附帶不一致**：`shiftDetailToLines` vs `buildShiftPrintLines` 兩份近似但**內容唔同**（前者有「線上訂單 N 張／餘額扣點／到店貨到付款／支付方式分項（線下 POS）」；後者只有「已付線上營業額／線上線下合計」）→ 交班即印 同 歷史重打 出紙唔同。
- **設計缺口**：`printTemplates` 只有 receipt/label/kitchen/kiosk **四個槽位，冇「交班單」槽位** → 交班單從設計上就冇模板可用，只能硬編行文。
- 待用戶回覆：期望樣張來源（新增 shift 槽位 / 沿用 receipt 槽位）、本次出紙通道（hub APK / desktop-companion / native），以及雲端 `pos_print_jobs` 該行 SQL 佐證。

## 新增「交班模板」功能（商家可自行編輯嘅交班結算單排版）（2026-09-10）
- 需求：喺現有專案新增供商家自行編輯嘅模板，命名「交班模板」；要能**新增 / 編輯 / 刪除 / 套用**，同既有模板系統整合方式一致，並有驗證與錯誤處理。方案 = **走上面 §根因 嘅第二選項**：新增第五個槽位（唔借用 receipt 槽位，因為交班單欄位同收據本質唔同）。
- **資料結構**（`src/lib/types.ts`）：`ShiftTemplate { blocks, order, headerText, footerText, sectionTitles }`（對齊 `KitchenTemplate` 結構 → 直接沿用既有設計介面 / buildSnapshot / 雲端同步 / normalize 機制）。`ShiftSectionId` **29 個區塊**（基本資料 6 + 值區塊 18 + `section_*` 分節標題 5）。**刻意無 `divider`**——三個渲染器只喺 `items` 前後產生分格線，交班冇 items → 加咗係撳完冇反應嘅死開關。`ShiftTemplateVariant { id, name, template }` = 範本庫；`PrintTemplates` 加 `shift`；`PrintTemplateKind`/`PrintKind` 都加 `"shift"`；`PosLocalSettings` 加 `shiftTemplatePresets` / `activeShiftTemplateId`；`ShiftHistoryRecord` 加 `detail?: ShiftSettlementSnapshot`（歷史重打百分百還原當日數字）。
- **`ShiftSettlementSnapshot` 由 `shift-page.tsx` 搬去 `types.ts`**：`escpos-template.ts` 嘅 `buildShiftContent()` 要讀佢，lib 唔應該反向 import component（circular dependency）。
- **刪咗兩份分歧 builder** `shiftDetailToLines()` + `buildShiftPrintLines()`，收斂成 `buildShiftContent()` 單一真源（同時餵設計頁預覽 `SHIFT_PREVIEW_SAMPLE` / 交班預覽 / `PrintJob.content`）。新增 `buildShiftPrintJobs()`（`print-jobs.ts`）→ job 帶 `template` 快照 + `content`。`shift-page.tsx` 交班即印（`closeShift`）同歷史重打（`reprintShiftRecord`）**兩條路**都改用佢。舊記錄冇完整快照時 `shiftRowToSettlement()` 由扁平欄位還原。
- **print-center**：新增分頁「交班模板」（tab `shift-template`）+ 範本面板（套用/覆蓋/改名/刪除/儲存為新範本）。驗證：名稱非空、≤20 字（`SHIFT_PRESET_NAME_MAX`）、唔准重名（改名時排除自己）、庫 ≤20 個（`SHIFT_PRESET_LIMIT`，防 localStorage 塞爆）。刪除有 `window.confirm` 並明文講「唔影響生效中排版」。id 用 `crypto.randomUUID()`（唔可以用 count，刪完再新增會撞 id）。`section_*` 區塊可自訂標題（存 `sectionTitles`）。
- **雲端同步**：migration `0030_pos_print_templates_shift.sql` 加 `shift` + `shift_presets` 兩個 jsonb（`not null default '{}'` → 舊 row 唔使 backfill）；`/api/pos/print-templates` GET/POST 帶呢兩欄；`/api/pos/state` 帶 `shiftPresets`。`normalizePosLocalSettings` 白名單 + pos-app **明確** merge 分支（靠 default 傳播會靜靜剷走本機範本）。
- **部署事故兜底**：`isMissingColumnError()` 偵測 Postgres `42703` → 0030 未跑時只讀寫四個舊欄，否則**整條模板同步（連收據 / 標籤 / 廚房）一齊壞**；response 帶 `legacyColumns` 做診斷。
- **踩過嘅坑（值得記低）**：
  - `print-templates/route.ts` 一度把 select 欄位抽成 `const TEMPLATE_SELECT = (columns: string) => supabase.from(...).select(columns)…` → 參數寫 `string` 令 supabase-js `select<Query extends string>` 型別推導失效，`data` 變 `GenericStringError` → 12 個 TS2339。**兩個 select 必須各自 inline**。另 `LooseTemplateRow` 欄位係 `unknown`，唔可以直接餵 `normalizePrintTemplateSet()` → 加 `rowToTemplates()` 集中做斷言。
  - `patchBlock` 函數簽名同行體被誤併成一行、`reprintShiftRecord` 被插入咗兩份（`old_string` 撞到第一個匹配）→ 都要讀返行號範圍再修。
  - **真 bug（順手修）**：`applyShiftPreset` 初版 `updateShiftPresets(...)` 之後再 `applyTemplate(...)`，兩者都由**同一個 stale `localSettings` closure** 砌新 state → 第二次 `setLocalSettings` 覆蓋第一次，「使用中」標籤永遠唔更新 + push 兩格撤銷歷史。已改為一次過原子寫入。同時把 `updateLocalTemplate` 嘅 `options?: {recordHistory?}` 收斂成 simple boolean（實測全部 caller 都傳 false）。
- **跨 repo：零改動（已逐個核實原始碼，唔係假設）**：`kind="shift"` 喺 POS `escpos-render.ts` TITLE、`companion-server.mjs:443`、`print hub`/`print-relay` `EscPosRenderer.kt:303` 三處都 fall through 空字串 → 標題靠 `header` 區塊帶（商家可自改，且三個 repo 唔使動）。其餘區塊一律行 `content[b.id]` 分支（`EscPosRenderer.kt:399`、`companion-server.mjs:536`），key 就係 `ShiftSectionId`。`divider`/`qr_code` 分支唔會被觸發（交班模板冇 items、冇 qr_code）。Hub/relay 兩個入口都係 `job.template != null -> renderTemplateTicket`，快照存在就唔會再 fallback 去廚房渲染器。
- 驗證：`npx tsc --noEmit` ✓ 0 error；`npx next build` ✓（5m55s，全部 route 正常）；eslint 新增代碼 0 error。⚠️ `print-center.tsx` **既有** React Compiler `react-hooks/refs` 誤報由基線 5 error 增至 16（改動前基線已用 `git show HEAD:… | eslint` 核實：offlineMode unused / immutability / exhaustive-deps / usingSampleOrder unused / refs×4）。新嘅 2 個 refs 呼叫點（`commitRenameShiftPreset` / `applyShiftPreset`）係**同一類誤報**（ref 讀取只喺事件處理器發生），**唔阻 build**（Next 16 已唔喺 build 時跑 ESLint）。曾試過把 options 收斂成 boolean 睇下會唔會靜 → 冇用，已如實改寫註釋（唔留錯誤說明）。要徹底消音需把範本面板抽成子組件，屬獨立重構。
- 文檔：`docs/shift-template-2026-09-10.md`（用途/適用情境、資料結構、範本管理 CRUD、整合方式、驗證與錯誤處理、部署步驟、已知事項）。
- 待跟進：**未喺真機印過紙**——已靜態核實三個渲染器分支，建議驗收時用 hub APK 同 desktop-companion 各印一次。

## 【只做咗調查，未落手改】預覽改「固定版面＋靜態範例資料」方案（2026-09-10）
- 需求（用戶，明確要求**先畀方案、唔好鬱手**）：5 個列印模板嘅預覽唔好再用真實最後一張單，改為固定版面＋靜態範例資料；所有欄位一律顯示（唔好因為資料空被隱藏）；列出改動檔案/位置/原因；確保預覽==出紙，並畀驗證方式 + 畫出每個 template 最終樣貌。
- **根因 A（預覽用真單）**：`print-center.tsx:245` `const [orders]=useState(()=>loadOrders())`；`:454-455` `usingSampleOrder = orders.length===0`（**unused，冇任何 UI 提示**）+ `sampleOrder = orders[0] ?? SYNTHETIC_SAMPLE_ORDER`（`:105-145`）→ 有單就用最果張真單，每個商家設計頁睇到嘅嘢都唔同。
- **根因 B（欄位被隱藏 = 唯一咽喉點）**：`escpos-render.ts:210-211` `const text=content?.[b.id]; if(!text) continue;`。**三個 repo 同步呢個行為**（companion-server.mjs:537 `if(!text) continue`、`EscPosRenderer.kt:399 `?: continue`）。所以**改呢度會同時改埋出紙**，唔可以隨便改成無條件顯示。配合 content builder 回 `""` 就隱形：
  - receipt：`store_tel`/`order_time`/`server`/`service_charge_amount`/`tax_amount`/`rounding_amount`/`discount_amount`/`cash_tendered`/`change_amount`/`order_note`/`discount_breakdown`（`buildReceiptContent` 全部係 `>0 ? ... : ""`）
  - label：`temperature`/`cup_type`/`sugar`/`ice`/`sugar_tag`/`ice_tag`/`addons`/`specs`/`item_note`（靠 keyword 由 specs/note 抽，抽唔到就空）
  - kitchen：`server`、`customer_count` —— `buildKitchenContent` 回**硬編 `""`**，而且 `print-jobs.ts:248 buildKitchenPrintJobs` **從來冇傳過 server/headcount** → 呢兩個區塊喺**真實出紙都係死嘅**（PosOrder 有 `partySize`，可作為 customer_count 來源）
  - shift：`online=null` 成組消失、`purchase=null`、`note=""`、`cash.actual/diff` optional；`SHIFT_PREVIEW_SAMPLE` 本身 `prepaid/refund/purchase/note` 都係 0 → 範本自己都隱形
- **根因 C（預設熄咗，就算有 data 都唔顯示）**：`visible:false` 預設 —— receipt 有 8 個（`checkout_time`/`server`/`service_charge_amount`/`tax_amount`/`rounding_amount`/`discount_amount`/`cash_tendered`/`change_amount`）、kitchen 有 2 個（`server`/`customer_count`）。→ 要「全部顯示」必須**預覽層 force visible**，唔可以改預設（改預設會影響現有店嘅真實出紙）。
- **已發現嘅預覽≠出紙（重要，寫入方案第四部分）**：① card 排版 sub-line **順序唔同** —— 真實出紙（companion mjs:470-499、hub EscPosRenderer.kt:346-370）係 `主行 → 分格線 → 規格 → 備註 → 折扣行`，但 `escpos-preview.tsx` 係 `主行 → 折扣行 → 分格線 → 規格 → 備註`。② receipt items 右欄：真實出紙用 `twoColumn()` 帶點線 leader（companion/APK 都有），POS 網頁預覽得 flex `justify-between`，**冇點線**。③ 紙寬：預覽硬編 80mm（`print-center.tsx:1411/1739`）、label 62mm；`DevicePrinterConfig.paperSize`（58/80mm）只喺 device-settings / printer-wizard **顯示**，從來冇餵入預覽。
- **改造方向（已向用戶提出的方案）**：新增 `src/lib/preview-fixtures.ts` 做唯一靜態範例資料源（4 份：receipt / label / kitchen / shift），預覽路徑「用 fixture 餵**同一個** `buildXxxContent` + forceVisible 嘅 snapshot → 照 call 原有 `renderEscPosLines`」→ 三個 repo 零改動、預覽結構同 companion/APK 完全一致；加 dev-only coverage guard（assert 每個 SECTION_META id 喺 preview 都有非空值）防止日後加區塊漏 Fixture。唔改 `if(!text) continue`（會同時改埋出紙）、唔改 `visible` 預設（唔影響現有店真實出紙）。
- 一致性結論：設計頁預覽 = 版面樣本（靜態資料），出紙 = 真實資料，兩者只差 `content`/`items` 嘅 payload；靠「共用 builder + 共用 renderer + 共用紙寬解算」三者同源對齊，另建議加「真單校對模式」（選一張歷史單用真資料渲染，驗證是次排版）。

## admin 營業報表頁右上角「重新載入」按鈕（2026-09-10）
- 需求：admin `/admin/reports` 右上角加 refresh 掣，點一下全頁數據（報表/圖表/KPI）重新載入；進行中 loading + 防重複點擊；完成後同步刷新；失敗要出錯提示。
- **做法 = key remount**（唔係逐個 effect 加 refreshToken）：`RestaurantDailyReport` 係 3000 行、有 4 條獨立載入 effect（backfill POS 訂單 / 線上單 byHour / Ledger 彙總+低庫存 / 線上單明細）+ 大量 useMemo。逐條加 dep 好易漏一個源；換 `key`（`${selected}-${refreshSeq}`）令 React 由零重建 instance → state/effect/memo 全部重跑，最徹底。
- **remount 會重置 `range`（今日/7天/30天…）** → 新增兩個 prop：`initialRange?` + `onRangeChange?`，上一層 `reportRange` 記住用戶選擇、remount 後還原（`useState(props.initialRange ?? "today")`，POS `/reports` 唔傳 → 行為同舊版一致）。
- **busy 由子層回報**：子層 `loadBusy = !backfillDone || !ledgerDone || onlineFetchInfo.status==="loading" || onlineDetailInfo.status==="loading"`，經 `onBusyChange` 上去（用 ref + state 雙軌，畀 polling 讀最新值）。初始兩個 done flag 都 false → mount 即 busy=true（上一層按佢 disable 掣）。
- **完成偵測用 polling 而唔係單純 transition effect**：極端情況請求快到同一個 render batch 內完成，effect 捕捉唔到 busy true→false 跳變 → 掣會永久卡 loading。改為 `refreshing` 期間每 300ms 讀 `busyRef.current`，false 且已過 600ms 下限（避免秒回閃爍）就收工。
- **防卡死**：`reportMounted = selected==="all" || Boolean(selectedMerchant)`，子層冇掛載（例如 URL merchantId 未喺名單）時要把 busyRef/reportBusy 清 false，否則掣永久 disabled。
- **錯誤統一**：子層 `onLoadError` 彙總 debugInfo/onlineFetchInfo/onlineDetailInfo/ledgerError；注意 **admin allStores 模式嘅 POS 訂單錯誤已經由 `adminOrderFetcher` 直接 setOrderFetchError**，所以要 `&& !adminOrderFetcher` 排除，否則同一原因彈兩行。上一層把 orderFetchError + loadError 合併成一張琥珀提示卡（去重字串），排查方向加「④ 點右上角重新載入」。
- 商家列表都抽成 `loadMerchants` useCallback（刷新時一齊重拉），原本係 inline effect + cancelled flag。
- 樣式跟足 admin 既有慣例（`dashboard/page.tsx` 嘅「重新載入」）：`rounded-lg border border-slate-300 px-3 py-1.5 text-xs font-semibold text-slate-600 hover:bg-slate-50` + disabled 灰化；icon 用 inline SVG（repo 冇圖標庫，只有 `components/icons.tsx` 得 Check/X），`animate-spin`。
- 驗證：`tsc --noEmit` 0 error；eslint 兩個檔 0 error（剩既有的 PosLocalSettings/debugOpen/Pill unused warning）；`next dev` 200 且無 compile error。SSR 只出「正在載入頁面…」（ClientOnly/AuthGuard 關係），掣要喺瀏覽器入面先睇到 → **未做 UI 實測**。

## 掃碼點餐系統全面審查（2026-09-10）
- 產出：`docs/reviews/qr-self-order-audit-2026-09-10.md`（P0×4 / P1×6 / P2×6 / P3×7，每項附 `檔案:行` 證據 + 建議 + 原因 + 效益）。
- 範圍：`/menu`（手機掃碼）、`/order`（kiosk 平板）、`lib/use-kiosk-order.ts`（共用 hook）、`lib/kiosk-order.ts`、`api/pos/{sync,state,sequence,bootstrap,kiosk-settings}`、POS 端 realtime 接單。
- **P0（新發現，未修）**：
  1. `/menu` 落單失敗**完全靜默** —— `menu/page.tsx:409-418` 未 await 就 `setCartOpen(false)`，而 `error` 只喺已關閉嘅 sheet 內渲染（`:407`）。
  2. **重複掃碼後「加單」死鎖** —— `use-kiosk-order.ts:426` `addToOrder()` 只讀 `tableOrder`，但 resume 路徑（`:244-261`）只 `setResumedOrder`、從不 `setTableOrder`；`activeTableOrder = resumedOrder ?? tableOrder`（`:278`）→ 畫面出得嚟但撳掣 no-op，按完成再開始點餐仍回同一畫面。
  3. `/api/pos/sync`（`:152`）**無鑑權**但用 service_role 寫入，支援 `ORDER_DELETED` 刪單；QR 內容已公開 `store=<merchantId>`（`kiosk-qr-panel.tsx:125`），全 repo 無 middleware。
  4. `/api/pos/state?storeId=`（`:20-24,145-226`）**無鑑權**回傳 200 單（limit 可 5000）+300 queue+200 printJobs+模板/設定；客人手機 resume 就打呢支（`kiosk-order.ts:367`）→ 知道 storeId 即可拉全店訂單。
- **P1**：①`storeId = binding?.storeId ?? scanStoreId`（`:156`）同 menu fetch 用 `scanStoreId ?? binding`（`:203`）**優先級倒置** → 曾綁店嘅瀏覽器掃別店 QR 會「睇 B 店餐牌、落單入 A 店」（binding 喺全機共用 localStorage，`kiosk-order.ts:14`）；②售罄只用 realtime 增量、無初始快照（`:189-198`，MenuItem/PosBootstrap 都冇 soldOut 欄）→ 已售罄菜照可落單；③客人端「總計」= subtotal，但 `buildKioskOrder:161-163` 會加 tax/service → 報價≠實收；`isMarketPrice` 只喺 `pos-app.tsx:2121` 處理，`/menu`/`/order` 完全冇（時價菜 price 可為 0 → 0 元落單）；④落單無 outbox/重試（專案已有 `queue-outbox` 但 kiosk 未用），序號失敗 fallback timestamp 與店內日序號唔同源；⑤`/api/pos/bootstrap:32-34` 未知店回 `mockBootstrap`（示範店菜式）→ 新店客人見到 demo 餐牌並可落單；⑥客人端 `saveBootstrapCache` 無 store scope（`storage.ts:96-101` 無 session 時退化成全局 key `macau-pos/bootstrap`）→ 離線換店掃碼會用上一店菜牌。
- **P2 要點**：售罄 UI 係死碼（`visibleItems:283-289` 已 filter 售罄項，但兩頁仍寫 `sold` 分支）；客人加單會用舊快照覆蓋商家 `sent_to_kitchen→preparing`（server 只擋終態降級）；購物車行 `MOP {line.price*qty}` 無 toFixed（`menu:377`/`order:349`）；`/order` 嘅 `quickType/setQuickType/setLanguage/persistLanguage` 全部無 UI 使用（外賣分支死碼）。
- **功能缺口**：**完全無訂單狀態追蹤**（`/menu` 只有靜態成功頁+本枱明細）；支付只有「到收銀付款」（`prepaidAmount:0`、`paymentMethod` 不設），docs/110 會員登入+點數抵扣未開工。
- 亦記錄：`package.json` 零測試基建；所有端點都無 rate limit；建議實施順序喺報告尾段。

## 掃碼點餐審查「全批次落地」（23/23 已修）（2026-09-10）
- 用戶指令「幫我把所有批次全部改」→ 依報告四批次一次過落地，P0×4 / P1×6 / P2×6 / P3×7 全數完成。
- **驗證**：`npm run typecheck`（= `tsc --noEmit`）exit 0；`npm run build` exit 0（全路由表產出）；`npm run test`（= `node --test`）10 tests / 10 pass。`package.json` 已加 `typecheck` + `test` 兩個 script。
- **⚠️ node --test 唔使指路徑**：Node 22.18+ 預設 strip types，`node --test` 會自動探索 `**/*.test.ts`（排除 node_modules），唔使寫 glob（Windows cmd 下 glob 亦唔會展開）。呢個 repo 用 `node --test` 就啱。
- **新增檔案 11 個**（詳見報告附錄 A）：`pos/pos-device-token.ts`、`pos/rate-limit.ts`、`pos/pos-sync-auth.ts`、`pos/soldout.ts`、`pos/kiosk-outbox.ts`、`kiosk-cart.ts`、`kiosk-cart.test.ts`、`components/kiosk/order-summary-card.tsx`、`components/kiosk/spec-sheet.tsx`、`api/pos/order-lookup/route.ts`、`api/pos/device-token/route.ts`。
- **授權架構（重要，屬專案長期約定）**：
  - 因掃碼客本質匿名 + repo 冇 `middleware.ts`，採**分通道**而唔係一刀切：已授權（POS device token 或 admin token）→ 放行全部事件；匿名 → **只准** `ORDER_CREATED`/`ORDER_UPDATED` 且 `source ∈ {scan,kiosk}`。
  - 憑證 = HMAC-SHA256 無狀態 token（`pos-device-token.ts`，仿既有 `admin-session-token.ts`），TTL 12h；密鑰 `POS_DEVICE_TOKEN_SECRET` → `ADMIN_SESSION_SECRET` → `SUPABASE_SERVICE_ROLE_KEY`。
  - **fail-closed**：`isPosDeviceAuthRequired()` 預設要求憑證；`POS_REQUIRE_DEVICE_AUTH=0` 係應急回滾開關（部署灰度用）。
  - 12h TTL 避免全店斷線：`/api/pos/device-token` 用 Ledger access token 換發 + client `refreshPosDeviceTokenIfNeeded()`（解 `exp`，剩 <10min 續期，`inflight` promise 去重，永不 throw）。
  - `/api/ledger/login` 登入成功時回傳 `session.posDeviceToken`；`AuthSession.posDeviceToken` **必須**經 `normalizeAuthSession` 帶返（漏咗會被 reload 剷走）。
  - 所有打 `/api/pos/state`、`/api/pos/sync` 嘅 client 呼叫點都要 `await refreshPosDeviceTokenIfNeeded()` 後帶 `posDeviceAuthHeaders()`。
- **Kiosk 離線隊列唔可以重用 `pos/queue-outbox`**：後者 flush 靠 `resolveStoreId()`，掃碼客冇 → 事件會被分類成「無主」永遠推唔出。故另建 `pos/kiosk-outbox.ts`（store-scoped key `macau-pos/kiosk-pending-orders/{storeId}`，上限 20）。
- **錯誤分類**：`submitKioskOrder` 加 3 次指數退避；`KioskOrderRejectedError`（4xx/業務拒絕，唔重試、直接報錯）vs `KioskOrderTransientError`（網絡/5xx，入本地隊列顯示「同步中」）。
- **`order-lookup` 端點**：替代客人 resume「拉全店 state」，只回一張單 + 欄位白名單 + 精確 UUID 匹配 + per-IP 60/min。⚠️ supabase-js 對**字串拼接 select** 推唔到型別 → 要 `data as unknown as PosOrderDbRow`。
- **售罄**：`pos_soldout` 只有 realtime 增量，**必須**另拉初始集合（`fetchStoreSoldoutIds`）；`soldoutRealtimeRef` 防「初始快照回來時蓋掉期間收到的新變更」。server 端落單校驗**刻意 fail-open**（查 DB 失敗放行），免得 DB 抖動令客人落唔到單。
- **單一真源**：`lib/kiosk-cart.ts` 抽出 `lineSignature`/`mergeCartLine`/`changeCartQty`/`computeOrderTotals`（純函式、無 `next/navigation` 依賴才可單測）。`computeOrderTotals` 刻意**唔四捨五入**（浮點噪音由顯示層 `money2()`/`toFixed(2)` 處理）；單測斷言要用 `toFixed(2)` 比字串，唔好直接 `===`（`36.3*3 = 108.89999999999999`）。
- **React 19 `react-hooks/refs`**：唔可以在 render 期間寫 ref → 要搬入 `useEffect` 同步（`order/page.tsx` 曾中招）。
- **未完成**：`quickType`/`KioskQuickType`/`delivery` i18n key 仍留在 hook 內（已無 UI 觸達）；圖片只補 `width/height/decoding`，未接 CDN resize；**未喺真機/瀏覽器實測**（只做 tsc + build + 單測）。
- 報告：`docs/reviews/qr-self-order-audit-2026-09-10.md` 已加頂部修復狀態 banner + 各項標題 ✅ + **附錄 A · 修復落地摘要**（含新增檔案表、逐項落地、已知未完成、部署前必讀）。

## 🔥 事故：掃碼「加單」完全冇反應（2026-09-10，當日修復）
- 症狀（用戶真機）：「掃碼落單成功 → 按加單 → **完全冇反應**：冇新單、冇打印、冇介面更新」；但手機顯示「落單成功」+ 琥珀「訂單已收到，正在同步…」。POS 端只有第一次嘅單（訂單01），手機卻顯示 訂單03 含 3 個菜。
- **根因 1（致命，既有缺陷，非今次引入）**：`ORDER_UPDATED` payload 形狀唔夾。
  - 收銀台 `pos-app.tsx submitOrder()` 送 `payload: { order, addedItems }`；
  - kiosk/掃碼 `kiosk-order.ts submitKioskOrder()` 送 **裸 `order`**；
  - 但 `sync/route.ts` 對 ORDER_UPDATED 讀 `eventPayload.order` → `undefined` → `orderId` 空 → `ack(false, "事件 payload 缺少訂單 id")`。
  - **同資料無關、必然發生** → 所以「第一次落單正常、之後每次加單都死」。已用 `git show HEAD~1` 核實三個檔喺改動前都一樣 = 既有 bug。
- **根因 2（令失敗變靜默）**：`/api/pos/sync` 舊版**任何 `ack(false)` 都回 HTTP 500**（已喺生產實測：POST 未知事件類型 → `HTTP 500` + `{"ok":false}`）。而 client 規矩係「4xx（非 429）= 永久，其餘 = 可重試」→ 500 被當網絡抖動 → 重試 3 次 → 入 `kiosk-outbox` → `placeOrder()` 照樣 `setSubmittedOrder` + `orderSyncPending=true` → **假成功**。
- **教訓（最重要）**：本次審查 P1-4 新增嘅「失敗入本地隊列 + 顯示已收到」**把一個睇得見嘅失敗變成睇唔見嘅失敗**。分類錯誤比冇兜底更危險 —— 兜底之前一定要先令「永久拒絕」同「可重試失敗」分清。
- **修復**：①`sync/route.ts` payload 形狀相容（`nestedOrder ?? eventPayload`）＋ 錯誤分類（業務拒絕 → 400/401 + `retryable:false`；基建 → 500 + `retryable:true`；`queue_events` 失敗降級 warning 唔再令已成功嘅單回 500）；②售罄校驗只驗新增菜品（舊版驗整張單 → 舊菜賣完會永久鎖死加單）；③**加菜豁免**（項目數變多時唔受時間戳 LWW 判 stale —— 收銀機時鐘快過客人手機時會靜默 skip）；④rate limit 分流（已授權按 storeId 600/min、匿名按 IP 300/min；舊版一律按 IP → 場內共用 NAT IP 會自我 DoS）；⑤client `kiosk-order.ts` 送 `{order, addedItems}`、4xx 永久唔入隊、429 唔硬重試、`applied:false` 嘅加單唔當成功；⑥`use-kiosk-order.ts` 用 `diffAddedItems()` 算新增菜品；⑦`kiosk-outbox.ts` 存/傳 `addedItems`、永久拒絕即移出隊列；⑧**`pos-app.tsx` 新增「加單補印廚房單」**（自助單更新且菜品變多 → `ticketType:"addon"` + `itemsOverride` 只印新增，簽名去重）；⑨新增純函式 `src/lib/pos/order-item-diff.ts` + 8 單測。
- **加單應該觸發嘅流程**（設計口徑）：`pos_orders` 同 id 更新（狀態沿用收銀端）→ `pos_queue_events` 審計 → Realtime `onOrderUpsert` → 合併本機 orders（介面即時更新）＋ 出「加單」廚房／標籤單（只含新增菜品）→ 打印通道出紙；**唔重印**顧客小票。設計依據：`print-toggles.ts` 明寫「加單」屬自動流程、受 kitchen/label 開關管。
- **排除咗售罄**：`pos_soldout` 喺兩個 Supabase 專案都查唔到（PGRST205），且**全 repo 冇任何寫入點**（POS 沽清 = 本機 localStorage + `/api/inventory/soldout` TODO stub）→ server 售罄校驗永遠 fail-open。**附帶結論：客人端「售罄」架構上未接通**（`fetchStoreSoldoutIds()` 永遠回 null → 客人菜單唔會灰化售罄菜）。要接通需新增店級售罄上雲（獨立工作項）。
- **⚠️ 未確認基建疑點**：由部署包抽到 `NEXT_PUBLIC_SUPABASE_URL` = `zymdemjflsckicwcinxl`（**Ledger 專案**，有 `merchants`/`orders`/`transactions`，**冇任何 `pos_*` 表**），但 `getPosSupabaseClient()`（瀏覽器端 Realtime + 售罄快照）用嘅正是佢 → 若屬實，**瀏覽器端所有 POS Realtime 訂閱都訂錯專案、永遠收唔到推送**（POS 只見 mount/重連時嘅 `/api/pos/state` backfill）。已在報告附錄 B.6 寫明驗證命令；修法要另開 `NEXT_PUBLIC_POS_SUPABASE_URL`，**未動**（牽涉環境變數 + RLS 政策）。
- **node --test 陷阱**：測試檔**一定要用相對路徑 + `.ts` 副檔名**（`"./order-item-diff.ts"`），Node 內建 type-stripping **唔識** tsconfig 嘅 `@/` alias，會 `ERR_MODULE_NOT_FOUND`（而且只會顯示成「整個測試檔 not ok」，唔會逐條報）。
- 驗證：`tsc --noEmit` ✅ exit 0；`npm run test` ✅ **18 tests / 18 pass**；eslint 改動檔案 0 error（`pos-app.tsx` 只有既有 unused-var warning）；`npm run build` ✅ exit 0（4m32s，完整路由表）。
- **未做真機驗收**：需先配置 zone 打印機 + 打印通道，否則所有單只會入隊，會誤判成「加單冇出紙」（驗收步驟見報告附錄 B.4）。

## 【已落地】預覽改「固定版面＋靜態範例資料」＋紙寬/標籤尺寸（2026-09-10）
- 用戶拍板：① 預設 `visible` **唔改**；② 廚房 `server`/`customer_count` 死區塊**剷走**；③ 紙寬**手動切** 58/80mm；④ 標籤加業界常用尺寸選項。
- **新增 `src/lib/preview-fixtures.ts`**（唯一靜態源）：`PREVIEW_RECEIPT_ORDER`/`PREVIEW_KITCHEN_ORDER`/`PREVIEW_LABEL_ITEM`/`SHIFT_PREVIEW_SAMPLE`（由 escpos-template 搬嚟）+ store/tel/server/qr 常數。fixture 淨係普通 `PosOrder`，照餵原有 `buildXxxContent` → 格式同源。⚠️ 收據金額手砌要對得住數：`142+14+7−1−14===148`（對唔到 `resolveTotalDiscount` dev 會噴 warning）。
- **`print-center.tsx`**：刪 `SYNTHETIC_SAMPLE_ORDER`/`usingSampleOrder`/`sampleOrder`/「需要最少一個菜品」分支。預覽用 **force-visible snapshot clone**（`divider` 例外，尊重商家熄線）→ 三個 repo 零改動。加 dev-only `assertPreviewCoverage()`（缺值即報，防日後加區塊漏 fixture）。**唔可以改 `if(!text) continue`**（三邊都有，改咗會改埋出紙）。
- **共用 `toPrintItemLines()`**（`escpos-render.ts`）：以前 `print-jobs.ts`（出紙）同 `print-center.tsx`（預覽）各抄一份 → 同交班兩份 builder 分歧同一死法，已收斂。
- **廚房死區塊剷走**：`types.ts KitchenSectionId`/`KITCHEN_SECTION_META`/`KITCHEN_BLOCK_DEFAULTS`/`buildKitchenContent`/`mock-data.ts`。`mergeTemplateOrder()` 本身 filter 未知 id → 舊雲端 record 殘留自動清。下游只 loop `snapshot.blocks` → 唔使改。
- **62mm 查證結論（重要）**：**唔係**熱感標籤業界標準闊度。只見於收銀熱敏紙卷闊度列表（37/50/57/58/60/**62**/70/80mm）同 Brother DK 62×100 niche；歐洲「58×62×12」= 闊58/**直徑**62（收銀卷）→ 大概率當年由紙卷規格誤植。降為選填保留項（怕踢爛真有 62mm 卷嘅商戶）。
- **標籤尺寸 7 個**（`LABEL_PAPER_PRESETS`，columns=floor((W−8)/1.5)）：40x30=21 / 50x30=28 / 58x40=32 / 60x40=34 / 70x50=41 / 80x50=48 / 100x75=61，+ 62mm=36 保留。主流餐飲係 **50×30（出現率最高）** 同 **60×40（台灣飲品杯貼/成份表）**。
- ⚠️ `LabelTemplate.paperSize` **唔使 migration**（jsonb），但 `normalizePosLocalSettings()` 逐欄重建 label → **必須手動白名單**（同當年 `qrUrl` 坑）。
- **預覽紙闊由字數反推**：`EscPosPreview` prop `paperWidthMm` → `columns`；`paperPx = columns*6.6+16`。以前 `mm*3.2` 令 80mm 得 ~36 字位但實機 48 字 → 預覽提早換行。分格線改 `"-".repeat(cols)`。
- **`EscPosTemplateSnapshot.cols`（統一四個 repo）**：POS `buildSnapshot(kind,tpl,cols?)` 計一次；companion 以前**完全冇 58 判斷**（永遠 48、分格線寫死 32 dash），hub/relay/print-agent 自己判 `paperSize` → 同一張單三種闊度。而家全部 `template.cols ?: paperColumns(printer)`。出紙逐**打印機**計（snapshot 搬入 loop）。
- 驗證：`tsc` 0 error；eslint 無新增（refs 誤報 3 個位置行號 +68 一致，同基線）；`next build` ✓ 54s。文檔 `docs/preview-static-fixture-2026-09-10.md`。
- 待跟進：**未喺真機印過紙**（58/80 各印一次驗證）；未加「真單校對模式」；CJK 換行位仍有 ~17% 誤差（等寬字 CJK/ASCII 比例 1.67 vs 實機 2）。

## 設計：訂單狀態上雲「自動閉環」（2026-09-10，未落地）
- 需求：iPad 本地已 settled，後台仍 sent_to_kitchen（表嫂美食反覆發生，KPI「未結帳 2 張」）。設計一套**商家零操作**嘅自動同步機制。
- 產出：`docs/112-order-sync-closed-loop-plan.md`（含分層設計、失敗偵測/重試表、四層驗證、分期、回滾、邊界）。
- **七個 root-cause 機制（附行號，全部已核對）**：
  - M1 `ORDER_SETTLED` 屬必須憑證事件（`sync/route.ts:258,386`），但 `syncNow()` **冇**先續期（`pos-app.tsx:2543`），只有 `doFlush` 有（`sync-flush.ts:362`）→ 憑證 TTL 12h 過期即全體結帳事件被拒。
  - M2 `MAX_SYNC_ATTEMPTS=5` 後 `failed` = **永久放棄**（`sync-flush.ts:65,335`，註解自認）。
  - M3 server 對 stale/終態降級回 **`ack(true)`**（`sync/route.ts:493-502`）→ client 剷走事件 = 假成功。
  - M4 LWW 用 **client 牆鐘** `client_updated_at`（`sync/route.ts:443,482-488,547`）→ NTP 回撥／時鐘偏移會令結帳事件被判 stale → 併入 M3，**完全冇網絡問題都會發生**。
  - M5 `computeReconcileDrift`/`pushOrderSnapshotForReconcile` 只喺手動開 Sync Health Modal 先跑（`sync-health-modal.tsx:134`）→ 零自動驗證。
  - M6 終態只存單機 localStorage（Safari ITP 會清）。
  - M7 `syncNow([...queue, paymentEvent])` 用 stale state 且新事件未 `withStoreScope()` stamp → 被 `filterEventsForCurrentStore` 剔走，真正推上去嘅係 flush worker（即「即時同步」名不副實）。
- **設計核心五層**：L0 全量快照 + **單調 `clientRev`**（migration `0031`，`pos_orders.client_rev`；rev 優先、時間為輔、terminalRank 守門）→ L1 回執升級 `{ok, applied, rev, reason}`（`applied:false` 唔准剷）→ **L2 常駐對賬守護 `sync-reconcile-daemon.ts`**（每 60s/每次 flush/online/結帳後 20s；`POST /api/pos/orders/verify` 比對，分叉即自動補推，3 次失敗 → `blocked`）→ L3 `syncAck` 帳本（可驗證式 `acked.rev >= clientRev && acked.status === status`）→ L4 IndexedDB 持久化 + 心跳 `pos_device_heartbeats`（0032）+ 雲端巡檢 SQL（`status in (draft,sent_to_kitchen,paid) AND updated_at = created_at`）。
- **邊界（重要）**：自動化**只推商家已做嘅事**，唔會自動把雲端 open 單改成 settled（付款事實只能由收銀端確認）；衝突（兩邊都終態但唔一致）只標 `conflict` 交人。
- 建議 P0（唔改 schema）：L5 加固 + `applied/reason` 欄位 + 對賬守護常駐化（先用 `/api/pos/state?ordersOnly=1` 做校驗通道）。

## 預覽折扣行去黃（2026-09-10 15:15）
- 用戶報收據/自助點餐機模板預覽出現黃色底。源頭：`escpos-preview.tsx` 折扣 sub-line 用 `bg-amber-100 text-amber-800`（當年刻意設計：螢幕標示「呢行係折扣」，因熱敏紙印唔到色）。
- 修正：改為 `bg-slate-900 text-white`（黑底白字反白），同真實出紙 `ESC { 1` inverse（companion-server.mjs `textLine(..., inverse=true)`、APK 同款）一比一對應 → 螢幕所見 == 熱敏紙所見。
- `EscPosPreview` 係收據/自助點餐機/廚房/交班/標籤五個預覽共用元件，一次過覆蓋；商用 amber 提示卡（print-center/pos-app 等）屬唔同畫面，冇掂。tsc + eslint 乾淨。

## 落地：訂單同步閉環 P0（2026-09-10 下午）
- 用戶授權：「如果你覺得 M1, M2, M7 就已經保底, 做吧, P0, 如果你需要的, 也可以做.」→ 直接進入實作，**唔改 schema**（避開 migration 0031），目標係「唔使教商家手動同步」。
- **新檔 `src/lib/pos/sync-acks.ts`**（docs/112 L3 上傳帳本 + 健康燈）：
  - 可驗證式 `ack.status === order.status && ack.orderUpdatedAt === order.updatedAt`（`isOrderAcked`）。導出 `shouldTrackAck`/`putSyncAcks`/`buildAckIndex`/`isOrderAcked`/`listUnackedTerminalOrders`；blocked 管理 `loadBlockedRows`/`markOrderBlocked`/`clearOrderBlocked`/`clearAllBlocked`/`isOrderBlocked`。
  - `computeSyncHealth(networkOnline)` level 序：`blocked` > (`failed`/`pending`/`waitingAck` → 在線 `pending`／離線 `offline`) > `ok`。**關鍵設計**：離線但冇嘢要傳 **唔示警**（唔好畀商家見到無意義紅燈）。窗口 `SYNC_HEALTH_WINDOW_MS = 7 天`。
  - **刻意唔 import `sync-flush`**：pending/failed 直接由 queue status 統計 → 避免 `sync-acks ↔ sync-flush` 循環依賴（一 import 就會爆 TDZ）。
  - `useSyncHealth()` 聽 `pos-sync-queue-changed` / `SYNC_HEALTH_EVENT` / `pos-orders-changed` / `pos-network-status-changed` / `pos-auth-changed` + 15s 兜底輪詢。
- **新檔 `src/lib/pos/sync-reconcile-daemon.ts`**（L2 常駐對賬守護，**方案核心**）：取代「手動開 Sync Health Modal 才對賬」。
  - 觸發：每 `SCAN_INTERVAL_MS=60s` + flush 後 `8s`（`POST_FLUSH_DELAY_MS`）+ 訂單變更後 `20s` + `online`/`visibilitychange` + mount 後 3s。
  - 一輪 `runReconcileRound(reason)`：①本地篩（**零網絡請求**，冇待確認就 return）②續期憑證 ③`fetchServerOrders(storeId, null)` ④逐單比對：一致 → 寫 ack 結案；兩邊都終態但唔同 → `conflict` 標 blocked；雲端冇且本地 >`AUTO_PUSH_MAX_AGE_MS=24h` → 標 blocked missing；其餘 → `pushOrderSnapshotForReconcile(order)` 自動補推 ⑤`notifyQueueChanged()` 令 flush worker 立即推。
  - 退避 `BACKOFF_STEPS_MS=[15s,60s,5m,15m,1h]`、`BLOCK_AFTER_ATTEMPTS=3`、`BLOCKED_RECHECK_MS=30min`、每輪上限 `MAX_ORDERS_PER_ROUND=100`。`notePushAttempt(order, reason, serverStatus=null)` 統一記帳。
  - **掛載點 = `pos-sync-flush-worker.tsx`**（`installSyncReconcileDaemon()` / `uninstallSyncReconcileDaemon()`），**唔係** pos-app —— worker 係常駐 layout 層，切頁唔會被卸。
- **`sync-flush.ts` 核心修正**：
  - `isRetryableEvent(event)` 取代 `attempts < MAX_SYNC_ATTEMPTS` 嘅**永久放棄**（M2）：attempts 到頂後等 `FAILED_RETRY_BACKOFF_MS = 15 * 60 * 1000` 仍可慢速重試。
  - 新增 `applyEventResults({allQueue, flippable, perEvent, failedAt, fallbackError})`：**只有 `ok && applied !== false` 才剷走**；`ok && !applied` → `status:"skipped", skipReason:"server-newer"`（**唔剷、唔燒 attempts**）→ 根治 M3 假成功。
  - 失敗路徑：`perEvent?.some(r => r.reason === "unauthorized")` → `refreshPosDeviceTokenIfNeeded(true)`（M1）。
  - HTTP 200 路徑也讀 body `results[]` 判 applied（唔再單靠 status code）。
- **`src/app/api/pos/sync/route.ts`**：`EventAck = {id, ok, applied?, reason?, error?}`；`ack(ok, error?, extra?)` 帶 `applied: extra?.applied ?? ok`。
  - **終態升級豁免（M4 根治，最關鍵一行）**：`isTerminalUpgrade = TERMINAL_ORDER_STATUSES.has(incomingStatus) && !TERMINAL_ORDER_STATUSES.has(existingStatus) && existingStatus !== "reopened"`；`if ((isStale && !isTerminalUpgrade) || isDowngrade) → ack(true, ..., {applied:false, reason:"stale"|"downgrade"})`。即係**結帳事件永遠唔會因為 client 牆鐘比 server 舊而被丟**（NTP 回撥、時鐘偏移都免疫）。
  - reopened-guard 回 `{applied:false, reason:"reopened-guard"}`；unauthorized 回 `{reason:"unauthorized"}`。
- **`pos-app.tsx`（M7）**：`syncNow()` 由「第二條推送路徑」收斂為**單一路徑** —— 未存過嘅事件先 `withStoreScope()` 補入隊，然後一律走 `flushPosSyncQueue({silent})`。以前 `syncNow([...staleQueue, ev])` 直接推，新事件未 stamp → 被 `filterEventsForCurrentStore` 剔走，實際上推上去嘅係 flush worker（「即時同步」名不副實）。
- **`storage.ts` / `types.ts`**：store-scope key 加 `sync-acks` / `sync-blocked`；`SyncAckRow{orderId,orderUpdatedAt,status,ackedAt,via:"push"|"verify"}`、`SyncBlockedRow{orderId,localOrderNo,localStatus,serverStatus,attempts,lastError,blockedAt,lastTriedAt}`；`QueueSkipReason` 加 `"server-newer"`。
- **`app-sidebar.tsx`**：在線/離線徽章下方加「同步健康燈」button（`useSyncHealth()` + `retryReconcileNow()`），文案 同步正常／N 張待傳／同步受阻／離線待傳；`level==="ok"` 時 disabled。
- **驗證**：`npx tsc --noEmit` ✓ 0 error；`npx eslint`（8 個改動檔）0 error；`npm run test` ✓ 10/10；`npm run build` ✓ 成功（54 條路由，無 warning 級新問題）。`isTerminalOrderStatus` 未使用 warning 屬 **git HEAD 既有**，非本次引入。
- **未做（真機/瀏覽器實測）**：P0 只過靜態檢查，仍未在 iPad/瀏覽器跑過一次「結帳 → 後台見到 settled」嘅端到端流程。建議驗證步驟：①iPad 開單結帳 ②側欄健康燈變綠 ③後台刷新見 `settled` ④斷網結帳後恢復網絡，觀察 daemon 自動補推。
- **P1/P2 未做**：P1 = 單調 `clientRev` + `pos_orders.client_rev`（migration 0031）+ 全量快照 + `/api/pos/orders/verify` + rev 守門；P2 = IndexedDB 持久化、`/api/pos/sync-heartbeat`（0032）、後台「設備同步健康」頁、雲端巡檢 job + `pos_sync_alerts`。
- 坑：本輪 `Edit` 工具多次被 CRLF/空行不一致搞到 `String to replace not found`（sync-flush.ts、app-sidebar.tsx、pos-app.tsx）→ 對策 = 用 `node -e` 做 indexOf/slice 精準替換，或者 Read 之後用更短嘅 `old_string` 錨定。

## 產品問答 + UI 收斂：同步健康燈併入「在線」徽章（2026-09-10 16:00）
- 用戶問三件事：①按鈕係乜 ②舊 iPad 未上傳嘅單要唔要返去按 ③手動定自動。答完之後佢指出按鈕佔位過大（正常用家唔會撳）→ 要求整合入「在線」。
- **答覆要點（之後再問同樣問題唔使重新推導）**：
  - 按鈕第一身份係**狀態燈**（`computeSyncHealth()` 純讀 localStorage 出 4 級），第二身份才是「立即重試」（`retryReconcileNow()` = 清冷卻 + 清 blocked + 跑一輪）。
  - **隊列係 per-device localStorage**：在 iPad A 撳，對 iPad B **完全無效**。但亦**唔需要**特登去 iPad B 撳 —— 只要該機開得到、有網、本地資料仲在，mount 後 3s / 每 60s / 恢復連線 / 回到前景都會自己補推。撳掣只係加速器，唔係鑰匙。
  - **三個救唔到嘅例外**（要老實同商家講）：①該機已洗機／換機／iOS ITP 清過 localStorage（7 日無互動）→ 本地單消失，任何掣都救唔返（= M6 未解，要 P1 IndexedDB）②本機單 >24h 而雲端完全冇記錄 → 守護**刻意唔補推**，標紅交人（防亂造單）③憑證過期／已登出 → 要重新登入。
  - 實務建議：**舊 iPad 唔好即刻退役**，開一次連網放前台 1–2 分鐘等守護跑完，再去後台核對。
- **實作（用戶選方案 B：整合入「在線」按鈕，最壞優先）**：
  - `app-sidebar.tsx`：獨立全寬按鈕**刪除**，「在線」徽章由 `<div>` 改 `<button>`，一個徽章同時表達兩件事。
  - 底色 `syncBadgeClass` 最壞優先：`blocked`→`bg-red-600` / `pending`→`bg-amber-500` / `offline`→`bg-amber-500/80` / `ok`→`networkOnline ? emerald : amber`。
  - 文案 `syncBadgeLabel`：一切正常時仍係「在線／離線」（商家熟悉講法唔變）；有嘢未上雲則變「同步受阻」「N 張待傳」「離線待傳」→ **避免出現「紅底寫住在線」嘅自相矛盾觀感**。
  - `level === "ok"` 時 `disabled` + `cursor-default` = 純狀態徽章（唔會誤導人撳）；異常時才可按，`syncRetrying` state 顯示「重試中…」（解決舊版「撳完冇反應」）。
  - 影響：側欄底部由 4 行縮回 3 行（省 ~36px）；`retryReconcileNow()` 只係加速，自動同步完全不受影響。
  - 驗證：`tsc` 0 error ✓；`eslint src/components/app-sidebar.tsx` 0 error ✓；`npm run test` 10/10 ✓；`npm run build` ✓ 成功。
- **設計取捨記錄**：曾考慮方案 A（徽章加右上角狀態點 + 點擊開 `SyncHealthModal`）與方案 C（完全移除、只留設定頁入口）。A 較保守但「綠點」太細，且商家要自己去別的頁面睇詳情；C 回歸 M5 病根（商家根本唔知要撳邊個掣，正係 2026-09-09 十三張單個案）。用戶選 B，理由是零額外高度。

## 診斷：iPad 已結帳但後台仍顯示未結帳（2026-09-10 17:45）
- 用戶報：iPad 1（店員那台）訂單都已在結帳狀態、持續開單，但 admin 仍顯示之前那批「未結帳」。問成因 + 檢查項 + 排查步驟。
- **查核後確認嘅事實（重要，唔使再查）**：
  - 部署狀態：P0 核心（`sync-acks.ts` / `sync-reconcile-daemon.ts` / `sync-flush.ts` / `pos-sync-flush-worker.tsx`）喺 **commit 650a979（15:45）**；側欄整合喺 **38d82c6（16:17）**；`main` 已推上 `origin/main`。**即係代碼已入 repo 並 push，但 iPad 上開住嘅分頁唔會自動換 JS** —— 分頁停留喺載入那一刻嘅 build。
  - admin 報表嘅「未結帳」KPI 讀 `/api/pos/state?ordersOnly=1` → `pos_orders`，**同同步寫入同一個表** → 可以排除「兩邊讀唔同表」。
  - localStorage key：`macau-pos/auth-session`（`merchantId`）；每店 `macau-pos/stores/{storeId}/{orders|sync-queue|sync-acks|sync-blocked|quarantined-orders}`（注意 queue 嘅 suffix 係 **`sync-queue`** 唔係 `queue`）。
  - `resolveStoreId()` = `authSession.merchantId` → fallback `kioskDeviceBinding.storeId`；但 `loadOrders()` 冇傳 merchantId 時用 `getActiveMerchantId()`（**只讀 authSession**）→ 兩者在「純 kiosk 未登入」情況下會分歧（邊緣情況）。
  - `readNetworkOnline()` = `navigator.onLine`（iPad Safari 上偏向誤報 true，即多數唔會阻住守護）。
  - backfill 會 `mergeOrderLists(loadOrders(), current, payload.orders!)` + `saveOrders(cleaned)`（pos-app.tsx:952/970）→ 雲端 open 單**會**寫入本機 localStorage，所以收銀機通常揸住本店 open 單副本。
  - 診斷入口（唔使 DevTools）：POS 設定頁已有「同步健康」掣（pos-app.tsx:3910）→ `SyncHealthModal` 會跑 `computeReconcileDrift` / `computeMissingLocalOrders` / `computeOrphanLocalOrders` / `loadFailedEvents`，五個分區：「同步失敗事件」「已結帳但雲端未同步」「雲端有但本機缺」「孤兒單」「已隔離訂單」。
- **⚠️ 新發現嘅真缺口（未修）：回執帳本 `syncAcks` 冇有效期 → 死鎖。**
  - `runReconcileRound` 第一步 `listUnackedTerminalOrders(); if (all.length === 0) return 0;`；而 `listUnackedTerminalOrders` = `loadOrders().filter(o => shouldTrackAck(o) && !isOrderAcked(o, index))`。
  - `isOrderAcked` = `ack.status === order.status && ack.orderUpdatedAt === order.updatedAt` → **一旦寫過回執，除非本地訂單再有改動，永遠唔會再被 verify**。
  - 後果：雲端若之後被另一台機 / 部署前嘅舊 snapshot 回水成 `sent_to_kitchen`，本機毫不知情 → **健康燈仍然綠、後台仍然錯**。呢個正正解釋「綠燈 + 後台唔對」。
  - 建議修法（不改 schema、不動 UI）：`isOrderAcked` 加可選 `maxAckAgeMs`；守護用 `RECONCILE_ACK_TTL_MS = 10 分鐘`（回執過期即重新納入工作集 → 每 10 分鐘至少重新核實一次 7 日內終態單，核實一致就重寫回執，天然節流）；健康燈用預設（永久有效）令燈唔會閃。
- **三個最可能成因（按可能性排序）**：①iPad 分頁仍跑舊 build（15:45 前載入 = 完全冇對賬守護）②回執死鎖（上面）③該批單嘅本地副本唔喺 iPad 1（對賬只能由揸住副本嗰台做）。
- **其餘要查**：店 ID 是否一致（iPad `auth-session.merchantId` vs admin 選中嘅店）；憑證續期失敗會令 `fetchServerOrders` 回 401 而守護只 `console.warn`（靜默）；本機終態單 >7 日不入窗口（`SYNC_HEALTH_WINDOW_MS`）；雲端完全冇記錄且本機終態 >24h → 刻意唔補推並標紅（`AUTO_PUSH_MAX_AGE_MS`）。

## 第三輪：掃碼下單同 Kiosk「完全分離」（2026-09-10 18:00）
- 用戶三點：①掃 QR 要由 DB 讀該台現有訂單（唔可以空白/當新單）＋確認落單即時寫入 DB；②掃碼**移除單號機制**、以**台號**做查詢/呈現依據、客人端睇唔到單號、兩套流程邏輯同介面完全拆分；③已落單頁只顯示狀態＋內容，**移除「完成」**。
- **落地**（詳見 `docs/reviews/qr-self-order-audit-2026-09-10.md` 附錄 C）：
  - `api/pos/order-lookup` 加 **`?tableId=` 模式**（回該台所有非終態 `source="scan"` 單，升序上限 20；回應加 `orders[]`，`order` = 最新一張，向後兼容）。
  - `kiosk-order.ts`：`orderNoSource:"sequence"|"table"`；`fetchScanTableOrder()`（台號查）＋導出 `fetchScanOrderById()`；`fetchUnsettledKioskOrder()` 改成「orderId 快路 → 台號查 DB」兩段。
  - `use-kiosk-order.ts`：內核抽成 `useOrderingCore(variant)`，尾加 `useKioskOrder()` = `core("kiosk")`；**`isScanLink` 加 `variant==="scan"`**；resume 命中即 `setStarted(true)`；`placeOrder` 掃碼跳過序號；落單 ack 成功後**回讀 DB**做真源。
  - **新檔** `use-scan-order.ts`（`useScanOrder()` = `core("scan")`，刻意唔暴露 `returnToHome`）；**新檔** `pos/order-status-label.ts`（客人端狀態文案純函式 + 6 個 test）。
  - `order-summary-card.tsx` 加 `statusLabel` / `hideOrderNo` props（`/order` 唔傳 = 不變）；`/menu/page.tsx` 改用 `useScanOrder()`、兩個重複分支合併成一頁、去單號、去「完成」。
  - `/order/page.tsx` **一行都冇改**（`useKioskOrder()` 簽名同回傳不變）。
- **關鍵取捨**：台號查詢**只認 `source="scan"`**。因為收銀端「自助單確認/拒絕」同「**加單補印廚房單**」全部靠 `isSelfOrder(order)`（`source ∈ {kiosk,scan}`）分流 —— 客人改咗 `source="pos"` 嘅單，收銀端唔會補印 → **廚房靜默漏單**（同附錄 B 同一類病）；server 亦會把 `source`/`local_order_no` 改成掃碼值令報表走樣。要放寬須先改收銀端加單補印閘（獨立工作項）。
- **掃碼單 `localOrderNo` 寫「台名」**（唔留空）：`pos_orders.local_order_no` 雖然 nullable 冇 unique，但 `PosOrder.localOrderNo` 係必填 string、收銀端模板要印，而 server `text("")` 會變 NULL → 收銀端顯示 `#null`。寫台名 = 唔產生新號碼、唔燒序號資源、收銀端用枱號對單，客人端完全唔顯示。
- 「拆分」= 拆**介面入口**（`useScanOrder()` vs `useKioskOrder()`）＋ core 用 `variant` 分流所有單號導向行為；**唔抄兩份**（抄兩份 = 修 bug 要改兩個地方，正係附錄 B 事故模式）。
- 驗證：`tsc` 0 error ✓；`npm run test` **24/24**（18 原有 + 6 新）✓；`eslint` 8 個改動檔 0 error ✓；未真機實測。

## 診斷：admin「未結帳 3 張」但現實只有 1 張（2026-09-10 18:05）
- 用戶重開 APP 後，admin 見「未結帳訂單 3 張 · MOP 465 · 已送廚房（未結帳）3 張」，但店內實際只有 1 張未結帳。
- **已查證嘅報表口徑（可以排除「顯示層重複計數」）**：
  - `restaurant-daily-report.tsx` `pendingSplit` = `orders`（**雲端為唯一真源**，`final = fetched.filter(...)`，雲端空+成功 = 空狀態，唔 merge 本機）中「非 `isSaleCountable` 且非 cancelled/refunded/partially_refunded」＝ 包含 `draft` / `sent_to_kitchen` / `reopened` / 無 `onlineOrderId` 嘅 `paid`。
  - 去重已做兩層：server `fetchOrdersInRange` 兩腿（created_at 腿 / updated_at 腿）按 `id` 用 `Map` 去重（`src/lib/pos-orders-range.ts:92-96`）；client 只 concat 分頁（PAGE=2000）。→ **顯示 3 就真係 DB 有 3 條**，唔係前端 bug。
  - `orderMatchesReportRange` 用 `updatedAt || createdAt`（Macau 邊界）→ 跨日單若 updated_at 落喺今日就會計入今日。
- **成因排序**：①**最可能**＝3 張中有 2 張其實本地已結帳、雲端仍 `sent_to_kitchen`（即原症狀嘅「數量表現」，KPI 口徑下就係「雲端記錄停留在非終態」）②雲端真有重複 row（同一實體單兩條 id：重複落單 / 掃碼單 + POS 單 / 離線重推）③本機已取消或已刪但 ORDER_UPDATED(cancelled) / ORDER_DELETED 未上雲 ④跨日單計入今日。
- **交付用戶嘅 SQL**（`pos_orders` 真實欄位：`id, store_id, local_order_no, table_id, table_name, status, fulfillment_status, sent_to_kitchen_at, served_at, subtotal, tax_amount, service_charge_amount, discount_amount, total, prepaid_amount, online_order_id, source, party_size, payment_method, created_at, updated_at`；另有 `client_updated_at` 只喺 sync route 用到）：一條列非終態 row + `created_at = updated_at`（從未被改過 = 最可疑）＋一條按 `local_order_no`/`table_id` group by 找重複。
- 注意：log 上面「第三輪：掃碼下單同 Kiosk」同本輪無關（另一個 session 嘅 QR 自助點餐工作，改動 `api/pos/order-lookup` + `lib/kiosk-order.ts`）。
