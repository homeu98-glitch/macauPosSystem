# 2026-09-12 工作日誌

## 快餐模式訂單狀態修復 + 介面完善（用戶需求 3 點）

**用戶報告**：剛剛下單並結帳，卡片應顯示「已結帳」與「製作中」兩個狀態，但
① 撳「製作中」卡上嘅「可取餐」後狀態零變化（toast 有出，狀態無變）；
② 訂單彈窗缺「可取餐／完成」；
③ 訂單頁彈窗樣式要同點餐頁彈窗對齊。

**根因**：UI 用「`status === "paid"` 且 `fulfillmentStatus === "ready"`」推導「待取餐」，
但快餐單有兩條合法路徑停在 `sent_to_kitchen`（收銀台未收款先出餐 docs/87 §6.3、
自助單先出餐後付款）→ ready 明明寫入（本機＋雲端都寫，`updatedAt` 有更新）但分組唔認。
連帶：pos-app「訂單詳情」彈窗快餐分支 `inWaiting = isBothDone || (paid && ready)`
兩個都唔中 → **跌落堂食分支**出「取消結帳／去結帳」，完全冇可取餐掣。

**改動（4 檔，未 commit）**：

- `src/lib/pos-order-filters.ts`：新增 `isQuickOrderReady()`（出餐階段唯一真源）、
  `getPaymentBadge()`（已結帳／未結帳）；`localOrderStatusLabel()` / `getOrderStatusBadge()` /
  `matchesLocalOrderPanelTab()` 一律改按 ready 分，並加「只認 draft/sent_to_kitchen/paid」
  終態守門（避免殘留 ready 蓋過取消／退款／返結）。
- `src/components/quick-local-orders-strip.tsx`：卡片加付款標籤；按鈕改為單自己嘅 ready
  （未 ready→可取餐；ready→只剩完成），唔再靠父層 `mode`。
- `src/components/pos-app.tsx`：`quickPreparingOrders/quickWaitingOrders` 改按 ready 分組；
  `updateQuickFulfillment()` 加冪等＋唔准時嘅 toast（唔再靜默 return）；
  訂單詳情彈窗快餐分支改 `isOpen && !ready → 可取餐` / `isOpen && ready → 完成`；
  header 加快餐付款標籤；桌台總覽「自取／掃碼訂單」面板改用 `getOrderStatusBadge` +
  付款標籤，ready 後出「完成」（原「標記可取」）。
- `src/components/local-orders-panel.tsx`：狀態欄改雙標籤（出餐＋付款）；
  `QuickOrderActions` 抽出成列表／彈窗共用（variant row|modal）；「查看」彈窗整體重寫成
  同點餐頁「訂單詳情」一致（header＝訂單詳情＋單號·枱別＋兩粒標籤、品項卡、總計框、
  actions 一行：關閉／補打帳單／可取餐→完成／接受·拒絕）。

**堂食隔離**（用戶特別要求「確定不要影響到堂食」）：全部改動都收喺
`isQuickCounterOrder()`（`!onlineOrderId && tableId === "counter"`）分支內；非快餐路徑
逐行比對過 diff，只係原封不動。堂食單有真 `tableId` → 行唔到任何快餐分支；
訂單頁「查看」對未結堂食單本來就係 `router.push('/?tableId=…')`（唔開呢個彈窗），
已結堂食單行「收據預覽」彈窗（未改）。快餐模式揀「堂食」類型嘅單 `tableId` 係 `counter`，
同枱面真堂食單唔會混淆。

**驗證**：`npm run typecheck` 綠、`npm run test` 160/160 pass、eslint 0 error（9 個都係
pos-app 既有 warning）。未跑 `next build`、未 commit／未部署。

**文件**：docs/113 新增 §「快餐（counter）出餐狀態：撳『可取餐』睇落零變化」；
`.workbuddy/memory/MEMORY.md` 由 8196 字元壓到 ~2.4k（超 3k 注入上限）。
（備份 `archive/MEMORY-2026-09-12-full.md` 已入 commit `19a7914`，但之後被環境嘅
「安全刪除」層搬走 → 磁碟上冇咗。要拎返：`git show 19a7914:.workbuddy/memory/archive/MEMORY-2026-09-12-full.md`。
MEMORY.md 已改為只指向 docs/113，唔再依賴呢個檔。）

## 第二輪：付款狀態閃爍 + 「取消結帳」補回（同日 · 用戶需求 2 點）

**用戶報告**：① 訂單明明已結帳，入到線下訂單頁「已結帳」**閃一下又彈返「未結帳」**；
② **「取消結帳」掣必須保留**（客人落單後約 2 秒內仍可能反悔），冇咗會令訂單卡死。

### 閃爍根因（三條路，全部要守）

1. **客戶端合併用錯鐘域**（主因）：`mergeOrderLists()` 以前用 `orderTimestamp()`
   ＝`updatedAt`。但本機寫入嘅 `updatedAt` 係 **iPad 鐘**；雲端返嚟（`/api/pos/state`
   backfill、realtime push）嘅 `updatedAt` 係 `mapOrderRow()`/`mapPosOrderRow()` 映射嘅
   **`pos_orders.updated_at` = server 蓋章**。而 server 端 LWW 一直用 `client_updated_at`
   （client 鐘）→ 兩邊唔同尺，舊狀態 snapshot 只要 server 收件時間較新就「扮新」贏出。
2. **`paid` 冇守門**：快餐 counter 結帳寫 `paid`（唔係 terminal 嘅 `settled`，要等「完成」），
   所以 `TERMINAL_ORDER_STATUSES` 同「終態優先」都擋唔到佢被 `draft`/`sent_to_kitchen` 降級。
3. **收銀台「下單」會把已收款單打返 `sent_to_kitchen`**：流程 落單 → 結帳(paid) →
   喺 strip 撳返張卡載入點餐頁 → 再撳「下單」。`resolveExistingOrderForUpsert()` 嘅
   `activeOrderId` 分支會回饋嗰張 paid 單，`upsertCurrentOrder()` 就寫 `sent_to_kitchen`。

### 改動（未 commit）

- `src/lib/types.ts`：`PosOrder.clientUpdatedAt?: string`（LWW 專用 client 鐘）。
- `src/lib/pos-order-row.ts` + `src/lib/pos/pos-order-mapper.ts`：帶出 `client_updated_at`
  （兩個 mapper 都要改 —— 一個服務 `/api/pos/state`、一個服務 realtime）。
- `src/lib/pos-order-filters.ts`：新增 `mergeTimestamp()`（clientUpdatedAt 優先，同 server
  同鐘域）、`isPaidOrderStatus()`；`mergeOrderLists()` 加**付款階段單向閘**（paid 唔可被
  open 降級；open 唔可拖返 paid）＋ LWW 改用 `mergeTimestamp()`。保留 `orderTimestamp()`
  做「顯示／陳舊度」判斷。
- `src/app/api/pos/sync/route.ts`：新增 `PAID_ORDER_STATUSES` / `OPEN_ORDER_STATUSES`，
  `isPaidDowngrade` → `ack(true,…,{applied:false, reason:"paid-downgrade"})`。
  ⚠️ 判斷用 **`writeStatus`**（實際會寫入嘅狀態）而**唔係** raw `incomingStatus` ——
  匿名通道（kiosk／掃碼）上面已強制 `writeStatus = existing.status`，用 raw 值會令
  「kiosk 向已收款單加菜」成條事件被拒 → items 上唔到雲。合法例外：終態（cancelled/refunded
  /settled）同 reopened 照通（所以「取消結帳」＝`cancelled` 唔會卡住）。
- `src/components/pos-app.tsx`：`resolveExistingOrderForUpsert()` —— 快餐 counter 已 `paid`
  嘅單**唔可以做 upsert 目標**（`return null` 開新單；⚠️ 一定要 `return null`，唔可以放行落
  下面「可並存多張已收款單」分支，否則加菜會落錯隔籬張單）。
- **「取消結帳」補回**：`src/lib/pos-orders.ts` 新增 `cancelLocalOrder(orderId, reason?)`
  （同 `rejectSelfOrder()` 同一模式：本機寫 `cancelled` → `enqueueEvents` → `notifyQueueChanged`
  → 廣播 `pos-orders-changed`）；`local-orders-panel.tsx` 列表 + 「查看」彈窗加入口 +
  取消原因彈窗（datalist 用 `cancelSettings.cancelNotePresets`）；`pos-app.tsx` 訂單詳情彈窗
  兩個快餐分支（`isQuick` / self-`showSplit`）都補返掣。只喺 draft/sent_to_kitchen 出
  （paid/settled 已收錢 → 走返結／退款），draft 自助單唔出（已有「拒絕」）。
- `src/lib/pos/quick-labels.ts`（新）：把 `quickCompletionLabel` / `quickCompleteLabel`
  抽成**零依賴**純模組（`quick-order-fulfillment` 原處 re-export，call site 不變），
  令 `pos-order-filters.ts` 唔使拖 client 依賴鏈 → 可以 `node --test` 直接載入。
  內部用**相對＋顯式 `.ts`** import（`./pos/quick-labels.ts`；repo 已有先例：`src/lib/kds/*`）。

### 堂食隔離

`paid` 全 repo 只有三個寫入點（`confirmPayment` / `settleCompOrder` / 線上已支付），
一律 `quickPaidFlow ? "paid" : "settled"`，而 `quickPaidFlow = isQuickMode && tableId === "counter"`
→ 上述所有守門**只影響快餐**，堂食 100% 唔變。

### 驗證

`npm run typecheck` 綠；`npm run test` **177/177 pass**（160 → 177，新增
`src/lib/pos-order-filters.test.ts` 17 個回歸測試鎖住付款階段守門 + 同鐘域 LWW）；
eslint 0 error。未跑 `next build`、未 commit／未部署。

**文件**：docs/113 新增兩節 —— §「快餐（counter）付款狀態：撳完結帳『已結帳』閃一下變返
『未結帳』」（三條根因 + 鐵律）同 §「『取消結帳』掣唔可以喺重構中消失」。MEMORY.md 更新至 2993 字元。

## 第三輪：線上訂單「顧客已付款、廚房冇出單」審計（同日 · 只診斷，未改代碼）

**用戶報告**：顧客線上落單並付款（截圖：取餐號 001 / 堂食 / 11:03 / 已支付餘額扣點 / 已完成），
廚房完全冇收到廚房單。打印中心（今天）只有一張 **小票機** job（訂單 001、11:55、**打印成功**）。

**關鍵推論（可複用）**：小票機 job 顯示「打印成功」＝雲端 `pos_print_jobs` 有行、
中繼 APK 已 claim 出紙 → **整條出紙通道正常**；所以故障唔在 relay/APK/雲端權限，
而在**上游「廚房 PrintJob 由頭到尾冇被建立」**。呢個推理鏈以後可直接套用。

**發現（詳見 `docs/reviews/online-order-kitchen-print-audit-2026-09-12.md`）**：

- 🔴 三個「靜默回空」閘門都收喺 `ledger-pos-bridge.ts buildPrintJobsForItems()`：
  `:186` `online` 開關（2026-09-11 新掣，商家可能為咗避免 Sunmi 重複出紙刻意熄）、
  `:190-192` kitchen+label 都熄；而 `:204-213` 冇 zone/label 機時係 **throw**（本地路徑
  `print-jobs.ts:204` 則靜默 `[]`）。
- 🔴 **真架構缺口**：`quick-online-orders-panel.tsx`（快餐模式 POS 內嵌面板）
  `handleInsert/handleUpdate` **冇** `ensureKitchenPrintForAccepted` 兜底（`online-orders.tsx:357/:411`
  兩邊都有）→ 「接單唔係由呢部機做」時快餐機永遠唔會補印廚房單。
- 🔴 `ensureKitchenPrintForAccepted` 只認 `accepted`/`preparing`（`online-orders.tsx:229-230`）
  → POS 離線/Realtime 斷線期間單由 pending 直跳 completed 就永遠唔補印。
  （現場旁證：訂單 11:03、收據 11:55，差 52 分鐘 → 疑似 POS 當時唔在 realtime 上。）
- 🔴 `use-ledger-orders-realtime.ts:48-56`：Ledger session 攞唔到 token 只靜默重試
  （`WAITING_FOR_SESSION`），**UI 零警示** → 收銀台以為連住。
- 🟠 `quick-online-orders-panel.tsx:322-328` auto-accept（`silent:true`）連 print 拋錯都吞 →
  只彈「已自動接單」＝**假成功**（`online-orders.tsx:470-489` 已修，快餐面板未跟）。
- 🟠 `ledger-pos-bridge.ts:216-228` 嘅廚房 job **冇 `content` / `template` 快照**
  （`print-jobs.ts:232-247` 有）→ 靠 APK fallback 渲染，模板/欄寬（58/80mm）唔跟設定。
- 🟡 `companion.ts:665` vs `hub.ts:63` 兩份 `resolveJobPrinter()` 唔一致（前者未合併 kiosk 機）。

**交付物**：
1. `docs/reviews/online-order-kitchen-print-audit-2026-09-12.md`（完整鏈路 + 斷點清單 + 3 分鐘自查 + 修復建議）
2. `tools/2026-09-12-diagnose-kitchen-print.js`（貼 POS 瀏覽器 Console 嘅**唯讀**腳本：
   列 toggles / printer roles / 今日 job 分佈 / 未上雲事件數）

**未做**：任何代碼改動（用戶偏好先批准方案再開發）。

## 第三輪（續）：自動接單路徑專項複查（同日）

**用戶補充**：自動接單成功 → 冇打印動作、打印佇列／紀錄**零 job**；「之前版本印得到，現在唔得」。

**追查結論（已寫入 docs/113 §「線上單廚房單：唯一可以『靜默由有變冇』嘅閘門」）**：

- POS **主介面兩處**（快餐快捷 bar `quick-mode-orders-bar.tsx:89`、堂食快捷操作 `pos-app.tsx:4589`）
  都用 `quick-online-orders-panel.tsx` 做自動接單 —— 佢**冇** `ensureKitchenPrintForAccepted` 兜底。
- **回歸定位法**：`git log -S '<閘門>' --oneline -- <file>`。
  實測 `buildPrintJobsForItems()` 內除咗 `isPrintContentEnabled("online")`（只有 `1e08343`，2026-09-11 15:07
  引入）之外，其餘由 `f1fd4d4` 之後**從未改動** → 「有 job 變冇 job 且零錯誤」**唯一**可能來源就係
  `printContentToggles.online === false`（`readToggle` 只有顯式 false 才關，一定係人手熄過）。
  ✅ 順手核實：一鍵全關（`pos-app.tsx:3269-3278`）只寫 kitchen/label/receipt，**冇**寫 online（docs 警告被遵守）。
- **四層靜默**（缺一都唔會咁鬼）：① `ledger-pos-bridge.ts:186` 設計性靜默；② 自動接單傳 `silent:true`
  → `catch` 吞拋錯（`quick-online-orders-panel.tsx:324-328`）；③ 成功 toast 在 `autoStartPreparing` 分支
  **寫死**「已接單並開始製作」、**完全忽略 `kitchenJobCount`**（`:337-347`）；④ `pos-app.tsx:4594-4599`
  把 `error` 降級成 `info`。
- **獨立成因**：接單若由外部／另一部機做，本機 auto-accept（只認 pending）唔會跑，加上快餐面板缺兜底
  → 永久零 job 零提示。
- **可重用判斷法**：**人手**接一張新 pending 線上單（非 silent）→ 由 toast 文案一次分辨
  「開關熄（回空）／冇 zone-label 機（throw）／正常送廚」。

**文件**：docs/113 新增上述 gotcha（含「睇到收據打印成功＝通道正常，唔好查通道」嘅口訣）；
`docs/reviews/online-order-kitchen-print-audit-2026-09-12.md` 加「專項複查：自動接單路徑」一節
（行邊段代碼 / 四層靜默 / 回歸定位 / 7 項待確認 / 兩層修復建議）。**仍未改任何生產代碼。**

## 第四輪：現場實測三症狀定位（同日 14:03 · 用戶自測堂食 002 + 外賣自取 003）

**症狀 → 結論（全部有代碼實證，詳見 docs/113 同審計報告「現場實測複查」一節）**：

1. **堂食線上單（002）零 print job** ＝ `online-orders.tsx:517-521`（**訂單頁**嘅自動接單）
   **明文排除 `tabType !== "dine_in"`**；而 `quick-online-orders-panel.tsx:363-368`（POS 主介面）冇排除
   → 有冇自動接單取決於**當時開住邊一頁**。
   🔑 判別法（免查 log）：**「拒單」只喺 `pending` 出現**（`online-order-actions.ts:113-118`），
   `accepted` 之後冇任何取消掣 → 單若人手變 `cancelled`，就一定**從未被接單**。
   唔可亂改：`acceptLedgerOrder()` 唔收桌台參數（`pos-app.tsx:4582-4584`）。
2. **外賣自取（003）job 卡「已發送」** ＝ `relay-transport.ts:25-34` 樂觀 `return {ok:true}`
   （`flushPosSyncQueue` 失敗都當成功）＋ `print-center.tsx:966-981` 只向上覆寫
   → 本地有、雲端冇 = **永遠卡「已發送」零紅標唔會自我修正**。
   候選：網絡（image 5 實拍 `TypeError: Load failed`）／queue `skipped`（`queue-outbox.ts:111-124`）／
   `posDeviceToken` 12h 401。⚠️ 旁證：線上單廚房 job **冇 `content`/`template` 快照**，同所有印得出嘅 job
   唯一分別 → 若 APK 已模板驅動可能渲染唔到。
3. **重打整單彈「資料已不在本機快取」＝必然 bug**：`bridgedOrders` 只有
   `resolveLedgerPosOrderForReceipt()` / `bridgeLedgerOrderToPos()` 會寫，
   **`printKitchenForLedgerOrder()` 冇寫**（但快餐面板／自動接單正正叫佢）＋線上單唔 mirror 入本機
   （契約 M3/M8）→ 打印中心「重打整單」**必定**失敗，reload 更清空。
4. **「自動打印」按完、手動更新後消失**：桌台總覽「手動更新」＝ `pos-app.tsx:1275-1341`，
   最後**強制 `window.location.reload()`**（`:1336`）；reload 前 `loadRuntimeState()` 寫
   `savePosLocalSettings(merged)`，而 merged **以 server `device_configs.local_settings` 為底**
   ＋白名單 override（`:1224-1258`，printContentToggles 有份）。
   `setAutoPrint()` **唔檢查 `savePosLocalSettings()` 回傳值**（`writeJson` 失敗只 console.error）→
   「撳完似開咗、reload 打回原形」；而「自動打印」係 `kitchen && label && receipt` 嘅**衍生值**。
   與前兩症同屬「靜默失敗」家族；⚠️ 共同嫌疑：localStorage 寫入失敗（quota／私隱模式）會**同時**
   令設定寫唔入同 jobs/queue 寫唔入。

**建議決定性檢查（10 分鐘）**：① 而家落一張本地單睇印唔印（分辨「通道整體壞」vs「只係線上單 job 冇上雲」）
② Supabase `pos_print_jobs` 有冇 003 行、lastError 係乜 ③ 打印中心「待補傳」／同步健康睇 skipped
④ 打印開關設置三行實際值 ⑤ 貼 console 腳本 ⑥ 訂單頁 vs 點餐介面各落一張堂食單對照。

**修復優先序**：P0 `printKitchenForLedgerOrder()` 補寫 `bridgedOrders`（或落 store-scope 持久 cache）；
P0「已發送」唔可以係樂觀值（睇 flush 結果，未上雲標「待上雲」+ 紅標）；P1 線上單廚房 job 補 `content`/`template`；
P1 堂食自動接單（需業務決定）；P1 設定寫入失敗要出聲。**仍然未改任何生產代碼（等用戶批准）。**

## 第五輪：線上堂食單「排位」功能設計方案（同日 · 只出方案，未實作）

**需求**：①「排位」掣 × 4 入口（快捷操作列表＋查看彈窗／訂單頁線上訂單列表＋查看彈窗）
② 因為線上單可能已付款，堂食介面要有綠色「已結帳」狀態而唔破壞現有 logic
③ 自動接單後未 assign 座位 → 顯示「待安排座位」
④ 用戶明確：**快餐模式**收到 `dine_in` 單 → 當平常快餐 POS 單（出餐口自取、唔排位）。

**方案**：`docs/online-dinein-table-assign-plan-2026-09-12.md`

**核心發現（可行性大增）—— 一半腳手架已存在**：

- `isLocalOrTransferredDineIn()`（`pos-order-filters.ts:11-19`）**已經定義**「線上堂食單已轉枱 → 當本地單管理」，
  `LocalOrdersPanel` 亦已用佢做 filter（`local-orders-panel.tsx:149`）→ 只要寫入 `orders` 即自動入「店內線下訂單」。
- 桌台佔用 = `openOrders`（含 `paid`）→ `tableOrderMap`（`pos-app.tsx:1744-1754, 1836-1842`）
  → 線上單以 `paid` + 真枱號寫入即**自動佔用枱面**，零改動。
- 「排位」彈窗 UI 已存在（quick panel `:738-770`；online-orders `:1071-1098`，文案已寫「並在收銀台建立堂食單」）。
- 雲端 `pos_orders.online_order_id` 欄已存在（`api/pos/sync/route.ts:751`），報表**已有去重邏輯**
  （`restaurant-daily-report.tsx:1698-1701,1846-1859`）→ 即 L2「寫入 orders」係系統已預期嘅模式。
- ⚠️ 4 個缺陷令佢今日行唔通：① 兩處 POS 都傳 `skipTableAssignment` → 掣唔 render；
  ② 枱號只入 in-memory `bridgeLedgerOrders`，**冇持久化**；③ `assignDineInTable = runAcceptAndBridge`
  **綁死接單**（已接單後無法排位，且對 accepted 單會再打 `update_order_status('accepted')`）；
  ④ `pos-app` 兩處 render `QuickOnlineOrdersPanel` **冇傳 `tables`**（彈窗會話「尚未設定桌台」）。

**關鍵建議**：枱號走 **L2＝寫入 `orders`（本機＋`pos_orders`，id=`ledger-<id>`、帶 `onlineOrderId`、`status:"paid"`）**，
唔靠新增 status 值，改用**派生標籤**（已結帳 / 待安排座位），判斷收喺零依賴純函式模組（跟 `quick-labels.ts` 先例）。
「排位」要係**獨立於接單**嘅動作（`assignOnlineOrderTable()`），支援接單後補排／改枱／取消排位。
快餐模式唔出排位掣；但注意 `isQuickCounterOrder()` 要求 `isLocalPosOrder`（`!onlineOrderId`），
所以快餐「可取餐→完成」今日撈唔到線上單（`pos-app.tsx:1781,1801` 亦明文排除）→ 要唔要 support 由用戶定。

**待拍板（已用 AskUserQuestion 問）**：D1 枱號存邊（L1 本機／L2 寫 orders／L3 Ledger後端）、
D2 入枱後可否加菜（唔准／另開加菜單／全單轉本地）、快餐模式要唔要可行取餐完成、枱衝突處理、返結准唔准。

**用戶已拍板（同日）**：
- **D1 = L2**：寫入 `orders`（本機＋`pos_orders`），id = `ledger-<id>`、帶 `onlineOrderId` + 枱號 → 要明文修訂契約 M3/M8。
- **D2 = (c) 全單轉本地管理**（可加菜／結帳／返結）。
- **快餐模式 = 要行「可取餐→完成」**（當 counter 單）。

**定案 v2 新增發現（重要，令實作大減）**：
- 🔑 **`prepaidAmount` 已全鏈路接通** → 「全單轉本地管理」**唔需要改結帳引擎**：
  結帳應收 `pos-app.tsx:1908` `max(0, total − discount − prepaid)`；彈窗 `:6237-6247`；枱卡 `:4466`；
  已結帳判斷 `:5934`；交班單 `shift-page.tsx:85` 有獨立 `prepaid` 欄；mapper/sync route 齊；
  收入認列同一張單只認一次（`restaurant-daily-report.tsx:341-346`）。入枱寫 `status:"paid"` + `prepaidAmount: total`。
- 快餐 counter 化**只需改 3 處**：`pos-order-filters.ts:22-23` `isQuickCounterOrder()` 放寬（連帶 8 個使用點自動正確）、
  `pos-app.tsx:1781 actionBarLocalOrders`、`:1801 counterKioskOrders`（移除 `!order.onlineOrderId`）。
  **唔使改**：`updateQuickFulfillmentInStore()`（只查 tableId + status，冇 onlineOrderId 守門）、
  `pos-app.tsx:2142` upsert 合併目標、`:2896` 退桌、`api/pos/orders` 清除線下單（已 `.is("online_order_id", null)`）。
- ⚠️ **未解風險**：`reopenOrder()`（`pos-orders.ts:150-209`）**冇 `onlineOrderId` 守門** → 返結會試圖沖正
  「其實喺 Ledger 已收」嘅錢（POS 冇對應 RPC）→ 建議一期**唔准返結**（待用戶拍板甲/乙/丙）。

**文件**：`docs/online-dinein-table-assign-plan-2026-09-12.md`（§8 定案 v2、§8.4 落地模組清單、§9 返結風險）。

## 第六輪：實作（同日）— 排位 + 打印修正

**商家最後定案**：返結＝**(乙)** 准返結但 `prepaidAmount` 鎖死 + 單據標明；
枱衝突＝**已佔用嘅枱唔可以揀**（彈窗介面）；快餐模式＝要行可取餐→完成。

**已落地（typecheck ✅ / test 189/189 ✅ / eslint 0 新問題）**：

1. 新 `src/lib/pos/online-dinein-labels.ts`（零依賴）+ `.test.ts`（12 個新測試）：
   `needsTableAssignment()` / `onlineTableBadge()` / `onlinePaymentBadge()` / `onlineTableAssignLabel()` / `isTableSelectable()`。
2. 新 `src/components/table-assign-modal.tsx`：樓層分組、已佔用枱 disable +「使用中」、觸控 ≥40px。
3. `ledger-pos-bridge.ts`：新 **`assignLedgerOrderToTable()`**（upsert 本地單 + `saveOrders` + `ORDER_CREATED/UPDATED`
   上雲 + 補印帶枱名廚房單）；`registerLedgerProjection()` 統一寫 in-memory **＋ 持久投影快取**（修好「重打整單」必失敗）；
   廚房 job 補 **`content` + `template` 快照**（修好線上任一 job 冇快照、靠 APK fallback 渲染）。
4. `storage.ts`：新 `ledgerOrderCache` key + `loadLedgerOrderCache()` / `cacheLedgerPosOrder()`（上限 50 LRU）。
5. `print-jobs.ts`：`findPosOrderForLedger()` 加持久快取層；`KitchenPrintOpts.orderNoteOverride`；
   返結單自動加「線上已付 … 不會沖正」。
6. `quick-online-orders-panel.tsx`：四入口中嘅兩個（卡片 strip/stack + 查看彈窗）加「排位／改枱」；
   雙標籤（已結帳綠 / 待安排座位）；**接單唔再被安排桌台攔住**；自動接單 catch 加 console.error；
   成功 toast 帶「有冇真係送咗廚」。
7. `online-orders.tsx`：列表＋查看彈窗加「排位」；「接單並安排桌台」→「接單」；排位走新 bridge；
   ⚠️ 呢個檔自己有 `loadOrders`（async Ledger loader）→ 本機訂單要 alias 做 `loadLocalOrders`。
8. `pos-app.tsx`：傳 `tables`/`tableAssign`；`assignableTables`（剔除返結 temp 枱）；
   `onToast` 唔再把 error 降級；返結彈窗加紅字警告。
9. `print-center.tsx`：狀態欄加「**未上雲**」紅標（sent + queue 仍有未 synced PRINT_JOB_CREATED）。

**未做（下一批，已寫入計劃 §10.2）**：快餐 counter 化 —— `isQuickCounterOrder()` 放寬、
`pos-app:1781/1801` 移除 `!onlineOrderId`、快餐自動 adopt 成 counter 單，**關鍵未決**係本地
「可取餐／完成」要唔要回寫 Ledger（否則雙狀態機）；已核實 `updateQuickFulfillmentInStore()` /
`markQuickOrderCompletedInStore()` 冇 onlineOrderId 守門亦冇印收據 → 只差回寫 Ledger。

**docs**：`docs/113` 新增「線上單排位 + 未上雲紅標」一節；計劃文件加 §10 實作記錄。

## 第七輪：快餐 counter 化（同日完成 · 用戶答「要」）

**內容**（typecheck ✅ / test **195/195** ✅（+6）/ eslint 0 新問題）：

1. 🔴 `pos-order-filters.ts` **`isQuickCounterOrder()` 移除 `isLocalPosOrder()` 條件** → 改為
   `tableId === "counter"`。連帶 8 個使用點（strip / 標籤 / 分頁 / 詳情彈窗 / showSplitActions）
   全部自動正確；本地／線上分工仍由 `isLocalOrTransferredDineIn()` 把關。
2. `pos-app.tsx` `actionBarLocalOrders` / `counterKioskOrders` 移除 `!order.onlineOrderId`。
3. `ledger-pos-bridge.ts`：新 **`adoptLedgerOrderAsQuickCounter()`**；upsert 抽成共用
   `upsertLedgerLocalOrder()`（排位／採納同一入口 → 永遠只有一張本地單，收入唔會雙計）。
4. 新 `src/lib/pos/online-quick-fulfillment.ts`：**`syncOnlineQuickFulfillment()`** 爬梯回寫 Ledger
   （preparing→ready→completed；Ledger 只接受逐級轉換而 POS 冇存當前狀態；冪等）。
   四個「可取餐／完成」寫入點（`quick-order-fulfillment.ts` ×2、`pos-app.tsx` ×2）全部掛背景回寫 + toast。
5. `quick-online-orders-panel.tsx`：新 **`quickCounter`** prop（快餐模式採納）。
   🔴 **自查捉到 bug**：枱位標籤原本用 `skipTableAssignment` 當 `quickMode`，但**堂食模式一樣傳 true**
   → 會令堂食單標籤變成「出餐口自取」。已改用獨立旗標（所以「skipTableAssignment ≠ 快餐模式」要記住）。
6. `quick-mode-orders-bar.tsx` 傳 `quickCounter`；`online-orders.tsx` 按 `loadOperatingMode() === "quick"`
   一樣採納（避免喺訂單頁接單時漏採納）。
7. `pos-order-filters.test.ts` +6 個回歸測試。

**刻意留低嘅限制**：快餐 strip 對非自助單唔出「結帳」掣（`showSplitActions` 要求 `isSelfOrder()`；
`source` 只可以係 pos/kiosk/scan，加新值要 migration）→ 到店付款未收錢嘅線上快餐單要喺結帳區收錢。
反向同步（Ledger completed → 本地 settled）同取消／退款（必須走 `merchant_resolve_order_change`）未做。

## 第八輪：用戶實測回饋 4 個問題（同日 · 全部修好）

**(1) 排位後線上／線下兩邊同時出現** → 新 `transferredLedgerOrderIds(localOrders)`
（`pos-order-filters.ts`）：有 `onlineOrderId` + 真枱號（≠ counter）＝已轉枱 → 兩個線上面板
（`online-orders.tsx` / `quick-online-orders-panel.tsx`）一律剔除 + 訂閱 `pos-orders-changed` 即時重算。
⚠️ 快餐採納嘅 counter 單**唔剔**（喺快餐 strip 管理）。+4 個測試。

**(2)(3) 快捷操作欄超屏 + 點餐頁三欄被推歪** → 真因：該欄只有 `lg:280px`（卡片淨 ~190px），
我加嘅「未結帳（到店付款）」藥丸（9 字）＋大尺寸按鈕逼爆卡片 → 橫向滾動。
修：藥丸文案縮到 **≤4 個中文字**（「未結帳」）、stack 卡片按鈕改 compact
（`onlineOrderActionButtonClass(tone, true)`）、卡片 `min-w-0 overflow-hidden`、
藥丸列 `flex-wrap`、容器/欄 `min-w-0`。

**(4) 線上單「已發送」但印唔出 —— 真實原因（DB 層死鎖）**：
`pos_claim_print_jobs`（0020）只揀 `status in ('pending','failed')`，但 claim 成功會寫
`status='printing'` → **一旦 APK claim 咗冇回報（render 拋錯／中途死／result POST 失敗），
嗰行永遠停留 `printing`**：冇任何機可以再 claim（0020 寫嘅「60 秒重排」係**死代碼**）、
attempts 唔再增加、唔會報錯。而 `GET /api/pos/print-jobs/status` 只回 printed/failed
→ POS 端零訊息 → 本地永遠「已發送」。**4 張 job 全部咁 = 用戶症狀**。
- ✅ 新 migration **`0035_print_job_stale_claim_requeue.sql`**：claim 條件加入 `printing`
  + 60 秒 stale 判斷（仍受 attempts<5 / ttl 保護）。⚠️ 要人手貼 Supabase SQL Editor。
- ✅ 可見性：status route 新增 `unfinished[]`（pending/printing + attempts + claimedAt）；
  `pair-status` 新增 `lastSeenAt`；打印中心出紅框「未認領 N / 已認領未回報 N」＋代理最後心跳，
  行內亦有「雲端未認領／已認領未回報（試 N 次）」藥丸。
- 一次性補救 SQL（清 attempts≥5 嘅 stale printing 打返 pending）已寫入 migration 檔尾。

**教訓**：「已發送」＋**冇**「未上雲」紅標 ＝ 事件已上雲但雲端冇人印得出 —— 唔係同步問題，
係 claim/APK 問題。呢個判別法已寫入 docs/113。
