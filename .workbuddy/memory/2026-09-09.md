# 2026-09-09

## 修復:新機打印機預設 4 台幽靈打印機
- `src/lib/mock-data.ts`:`defaultDeviceConfig.printers` 改 `[]`(舊 4 台 mock 打印機刪除,連帶修好 10+ 個 `loadDeviceConfig() ?? defaultDeviceConfig` fallback 點)。
- `src/components/device-settings.tsx`:
  - 刪除首次開頁 `saveDeviceConfig(defaultDeviceConfig)` pre-seed;
  - 初始化 effect 改為 deviceConfig + localSettings 一齊「先問 DB」(帶 storeId),DB 空/離線 → printers 留空,localSettings 仍 fallback default(維持 09-08 決定);
  - `loadRemoteConfig` fetch 補 storeId(以前冇帶 → route 返 null,同步通道係死嘅)。
- tsc + eslint 通過(2 個 pre-existing warning 非本次引入)。backoffice `dbConfigured:false` mock 路徑依家會顯示 0 台打印機(合理)。

## 修復:桌台無法刪除(00:20)
- 根因 ①:設置頁桌台 tab 完全冇刪除按鈕(只有新增/改名/座位數)。
- 根因 ②:saveTablesLocal merge「bootstrap 獨有枱保留」會令被刪枱由 bootstrap cache/server 復活。
- 修復(device-settings.tsx):
  - 每張桌加「刪除」掣 → removeTable();
  - removeTable:有進行中訂單(draft/sent_to_kitchen/paid/reopened,同 pos-app openOrders 口徑)→ 阻刪+提示;歷史單唔阻;
  - session 級 tombstone `deletedTableIds` state,saveTablesLocal merge 扣走已刪 id → 保存時同步刪本地 cache + server pos_bootstrap_config(POST 全量 tables upsert by store_id)。
- 已知限制:多終端情境,其他 terminal 本地 localSettings 仍存已刪枱,佢哋撳「保存」會重新推上 server(多主同步固有問題,記錄待議)。
- ⚠️ git 物件庫損壞(bad object HEAD,stash 失敗)——另開 session 處理。
- ⚠️ 00:21-00:22 有另一 session 同時編輯 online-orders/ledger 檔案,tsc 嗰批 changeRequestStatus error 同本次無關。

## 修復:線上單「客人取消/改單」審核三錯配(00:30,依 Ledger 團隊交接契約)
- Ledger `orders` **無** `change_request_status` 欄位;待確認申請只看 `change_request_type`('cancel'|'modify'|null),拒絕/同意/撤回都係清返 null(Realtime UPDATE 推)。
- order-mapper.ts:`LedgerOrderRow` 剷走 `change_request_status`、加 `change_request_at`/`change_request_payload`;`hasPendingCancelRequest` 改為 type==='cancel'(唔再要求 status);新增 `hasPendingChangeRequest`(cancel|modify);`changeRequestLabel` 支援「客人申請修改」。
- order-actions.ts:`respondToCancelRequest`(call 錯 `update_change_request_status`)→ `resolveOrderChange(orderId,'approve'|'reject')` 打 **`merchant_resolve_order_change`**(p_order_id+p_action);mapRpcErrorMessage 加 4 條友善文案(no pending change request/not authorized/order not found/invalid action)。成功回傳 `{status,resolved,print_kind}`。
- online-orders.tsx + quick-online-orders-panel.tsx:
  - `handleUpdate` 加分支:becameCancelRequest/becameModifyRequest → 播音+toast;requestCleared 且非 cancelled → toast 收橫幅(斷線 merge 後橫幅都會出,可能冇播音,可接受);
  - 「同意取消」由 `update_order_status('cancelled')`(商戶自取消,**唔會沖正**,錯路)改打 RPC approve;approve 後自行 `printVoidForLedgerOrderOnce`(直連 RPC 唔觸發 Ledger 作廢單 MQTT;realtime echo 冪等唔重印);approve 改單 → getOrderDetail+補印廚房單;
  - 待審時隱藏一般接單/推進狀態鈕(online-orders renderOrderActions 逐個 gate;quick panel 經 getPrimaryOnlineOrderAction 返 null);action key confirm_cancel/decline_cancel → approve_change/reject_change;getCancelRequestActions → getChangeRequestActions(支援 modify);
  - 音效:cancel_request 沿用 cancel-order.mp3、modify_request 用 new-order.mp3(未加新音效檔)。
- pending 直接取消(status→cancelled)路徑不變(作廢單+播音)。無加 MQTT/輪詢/setInterval,無打 Ledger HTTP。tsc+eslint 過(3 warnings 全 pre-existing)。UAT 清單見 Ledger 交接文件(本日 quote):開/關自動接單兩條取消路徑、雙端搶先、撤回、餘額沖正。

## git repo 損壞與修復(00:30)
- 現象:GHD 顯示 Can't find macauPosSystem;git 全部指令 bad object HEAD。
- 原因:00:10 commit+push(5ea010f「up」,含打印機修復)後,**loose objects 被外部刪走**(00:10-00:14 之間;非本 session——我只有 diff/status/log/失敗的 stash/cat-file/fsck 等讀取指令)。refs 指向的 commit 物件缺失 → git 全癱。
- 修復:備份 .git → ../macauPosSystem-git-broken-20260909;遠端 main=5ea010f8 完好 → `git fetch origin` 補回物件,repo 完全恢復。
- 之前 2026-09-01 都試過一次(外層有 .pos-git-broken-20260901 資料夾)→ 懷疑防毒/磁碟清理工具刪 .git/objects,建議排除。
- 未提交變更:桌台刪除修復(device-settings.tsx)+ 另一 session 嘅 ledger/online-orders 改動。
